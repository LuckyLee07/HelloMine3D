# 岩台地表过渡 terrain v15

当前 r31 的三 seed 隐藏实机原图显示：土层横跨整个坡面，沙/岩边界过于整齐；本批承接用户
最高优先级的地貌多样化，在真实生成路径补足中尺度地表组织。原图保留于本地
`build/visual-upgrade-20260917/terrain-transitions-r32/visual-before-r1/`。

- 新建世界追加 v15；v1–v14 原生成和存档不迁移。save v12、v14 地标与奖励规则保持。
- 沿用 v13/v14 高度及生态身份，仅在干燥岩台及其边缘修改真实表层材料：连续岩带逐渐分成
  裸岩斑块，泥土成为间断坡积斑，沙沟边缘有宽窄变化。保留岩台主体、沙沟和可接近的台阶。
- 纯世界坐标场控制数米至数十米斑块，不用逐格随机撒点，不加载邻区块，不新增缓存。
  近水地面与湿地保持；不改变高度、洞穴/矿脉生成规则、材料 ID 或碰撞规则。
- 先验证八 seed 宏观、区块边界和 signed 极值：v14/v15 高度及生态完全一致；实际表层与规划
  一致、加载顺序无关，改块保存重开不恢复。旧版样本/区块与已冻结的基线逐字节一致。
- 新表层须保持主体裸岩和连片性，变化格至少 90% 有同材质正交邻居，避免单格椒盐噪声。
  原土带的大块连续覆盖须明显减少；分布统计不能替代实机艺术检查。
- 三 seed 同机位原图、近地面与区域接缝连续镜头，补夜间和兼容材质。保持真实材料辨识，
  小地图与地面一致。双配置客户端、聚焦/完整世界回归、必要短 Q3；同场景常驻/流送各三对
  性能，P95/P99 中位数比不超过旧 v14 的 1.10，记录几何与驻留成本。
- 正常新建/保存重开核对 v15；持续键鼠体验依用户要求暂缓。工程、诊断、正常输入分别记账。

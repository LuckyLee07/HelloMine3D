# HUD 交互执行记录

## 当前恢复状态

- 用户完整 HUD Goal 的本轮功能交付与最终复核已完成。源码仍为 `ee42d30`，B1/B2/B3/B4 分别见下文；解锁后 B5 的普通路径与测试物品来源分开记录，见[最终复核](hud-interaction-final-input-2026-09-22.md)和[交付记录](hud-interaction-delivery-2026-09-22.md)。不恢复此前已经完成的地形 Goal。
- 当前没有构建、性能采样或本轮客户端进程运行。所有 exec session 已完成；本轮工作目录 `build/hud-interaction-20260922/`，最终包 `HelloMine3D-HUD.app` 约 51 MiB，工作包和正常测试世界保留。新交付包与历史用户包不再原位刷新或操作。
- 用户已解锁，最终任务卡命中、关闭、地图旋转／缩放／选点、切世界、普通菜单重启和空槽清理已通过。工具／食物以隔离测试库存完成真实鼠标触发及移出检查，明确不是正常采集证据；右键持续拖动与持续 WASD 等未声明范围保持。
- B5 两次运行均正常保存退出，最终 `pids=[]; windows=[]`；临时物品世界已移出普通列表。无待继续的构建或验证进程，不重复全量构建／性能。
- 用户客户端 `build/adventure-terrain-20260921/HelloMine3D-Adventure.app`、历史 `Diagnostic.app`、旧工作包及其存档受保护，两个历史二进制哈希已复核未变；本轮正常存档 metadata 未被诊断改变。
- 分发 119 项已校验；最后 Release 菜单和 Debug 地图后台渲染通过；本轮清理 2,009,154,118 B 重复二进制／Debug 缓存／临时包，原始失败和当前证据保留。

## B1：指针与物品详情

- 已完成 Tab／未占用旧别名、暂停模拟、输入所有权交接防穿透、真实材料／工具／食物详情和点击选槽。查看模式隐藏不可用的合成／战斗提示。
- macOS x86_64 Release 客户端构建 r4 通过；输入纯测试 Debug、Release 各 33/33；P11A Release 99/99；资源 Release 114/114。命令及原始日志在上述工作目录。首次 P11A 失败保留：双语词条从 489 增至 529，原精确数量断言未同步；更新数量及新词条断言后 r2 通过，未放宽检查。
- 独立 `Working.app`（bundle `local.hellomine3d.hud-work`）经正常菜单创建 `HUD-Normal-v19`、seed 42、普通难度，Computer Use 实际完成 Tab、第三槽点击、空槽提示、Esc 直接恢复、再次 Esc 暂停及保存退出。未操作历史用户客户端。此次包二进制 SHA-256 `28055b44f13b37b9e20a6c7c9d697fd2015093277d1ab583209242213df237ed`；最后的提示隐藏调整只完成 r4 编译，后续页面验收使用新包复核。
- 工具／食物已使用注册表真实值，实际有物品悬停与窄窗口留至整体界面验收。纯输入检查证明旧按键释放门；正常窗口离散点击不等同持续按键验收。Computer Use 首次应用选择异常等待约 5 小时，之后交互恢复正常，不把等待计入游戏表现或验证进展。
- B2 任务日志、B3 地图、双配置完整世界及最终包尚未完成。

## B2：任务日志与追踪

- 任务卡点击进入双栏日志；窄版改为上下布局，列表和详情分别滚动。支持全部／可进行／已完成过滤、真实前置条件、主线／可选标识、数量进度和临时追踪。完成或切换世界后恢复自动追踪；不改变存档或目标权威。
- World → AlphaJourney → ObjectiveSystem 按需复制可见任务；默认 HUD 未追踪时不构造完整日志。隐藏目标不泄露，可选目标不计入主线数量。新增 6 项检查覆盖状态、快照隔离、完成解锁、保存重载及可见可选任务。
- Release 客户端构建通过；P11C 47/47、资源 114/114。首次 P11C 的唯一失败是旧快捷提示精确文本断言仍期待 Esc，已同步为 Tab 后重跑通过；原失败保留。双语各 544 词条。
- 同一独立工作包二进制 `939b3a9562e6e40cc4dc17964b4c2fe55d3cc622c809254c684eb113807a1060`，正常存档恢复后 Computer Use 完成任务卡点击、追踪、锁定任务前置详情、已完成空列表、关闭按钮、Esc、正常设置切换中文及再次打开日志。中文与英文实机画面通过；原图 `b2-journal-prerequisite-r2.png`、`b2-journal-zh.png`。首次窗口采集因同进程辅助窗口而拒绝模糊选择，指定查询得到的主窗口 ID 29076 后成功。最后正常保存退出。
- B3 开始前尚需：3D 地图全部实现，最小窗口／大字号、有物品浮层、双配置完整回归与性能、最终独立包。此处不提前声明整体完成。

## B3：立体地图（实施中）

- 已接入小地图点击、真实地表高程／材料的正交立体网格、旋转／缩放／平移／朝北／回到玩家、前后遮挡排序和地表拾取、当前玩家与已发现地标。未知列无几何，未知邻居不生成虚假悬崖；缓存重建仅由样本或角度变化触发，沿用 65²／195 列／30 Hz 预算。
- 纯检查双配置各输入 33 项、地图 26 项通过，包含最终释放位置驱动快速拖动、抖动点击不旋转、平移不重复累计、有限参数／高差／正反面／未知区／深度拾取。65² 几何构建和排序 60 次均值 Release 0.362 ms、Debug 5.064 ms（纯 CPU 检查，非游戏帧耗时）。
- macOS Release 完整世界 1716/1716、资源 114/114；Debug 完整世界 1716/1716（资源运行中或待读取）。Release r3 首次增加诊断页输出漏 iostream，编译失败已保留，补包含后 r4 成功。最终 Debug 客户端增量 r2 正在构建。
- 正常旧测试存档的 Computer Use 已实际完成地图展开、滚轮 1.0→1.7、朝北、平移按钮和地表点选。第一版快速拖动无响应，保留 `b3-map-drag-no-change-r1.png`；改为从按下原点到释放位置计算后，实际拖动改变 yaw／俯仰且罗盘同步，`b3-map-rotated-zoom-r2.png`。该图片采集首次自动审批审核超时，按工具允许重试一次后成功。
- 随后 Computer Use 报告 Mac 已锁屏、自动解锁不可用。停止实机输入操作，继续不依赖桌面的构建／回归与隐藏诊断；不绕过锁屏。当前工作窗口只停留在只读地图，最近已正常保存的测试世界保留。后续工具／食物浮层正常悬停、右键持续拖动和最终客户端正常启动尚未全部实机检查。
- 下一步：完成诊断页最小 640×480／最大 1.25 字号及 0.85 比例；验证地图和日志控制可达；正常物品浮层或明确区分的诊断证据；HUD 关闭配对性能和打开地图成本；最终客户端／体积整理／中文提交。B3 未提交，Goal 仍 active。

### 性能协议（执行前固定）

同机后台隐藏、x86_64 Release、2560×1440 帧缓冲、1280×720 窗口、seed 42、位置 36.3 92 56.5、旋转 0 0 0、时间 6000、视距 8、FOV 90、标准材质、关闭阴影与后处理。预热 5 秒、记录 30 秒且关闭图像读回。默认 HUD 比较本 Goal 前的 v19 交付二进制（SHA `3ffe41d4d7f925d3da1e9e349c62c126edc0b04d2ceea59b30f29b3c316dd315`）与本批最终 Release，三轮交错，P95/P99 中位比 ≤1.10。另用本批 pointer／map 页三轮配对记录查看页面成本；两者均暂停模拟，不与运行中的世界帧耗时混为纯地图成本。几何／采样有界独立记录。测试前等构建与完整回归结束，不并行运行重负载任务；保留所有轮次，失败不删。

B3 后台验收补充：锁屏后台窗口实际 backing 为 1×。首次 640×480 诊断错误期待 1280×960，工具如实 FAIL，改为实际 1×重新采集。八场中英布局／物品内容完成采集；开发者看图发现操作提示前景层遮挡浮层、任务卡自动高度被覆盖按钮布局反馈不断撑高。已修为指针详情时隐藏前景提示／手持表现，任务卡通过窗口命中、不新增撑高布局项；修复后双配置客户端和三场双帧复核进行中。正常点击路由此前已实机验证，最终命中方式调整仍需解锁后复核，不以诊断替代。

**性能执行前环境修订**：根据上述实际 backing 证据，本轮配对采用同一锁屏后台 1280×720 点／1280×720 像素、1×；原定 2×前台不适用当前桌面。其他坐标、预热、时长、三轮配对和 1.10 门槛保持不变；不会把本轮结果外推为 2×性能。尚未开始任何性能采样。

B3 实现验证结束：最终候选 Release r6／Debug r5 编译成功；地图／输入纯测试双配置各 26／33 PASS；世界双配置 1716、资源双配置 114 PASS。三场浮层修复后的 6 秒／10 秒原图位于 `b3-tool-small-zh-r2`、`b3-food-small-en-r2`、`b3-block-wide-zh-r2`；工具 56/56 耐久、5.0 伤害／3.5 m／0.4 s，面包 +6／1.0 s，方块堆叠／用途均可读，提示条不再遮挡，任务卡保持紧凑。它们是 `--panel pointer --hud-fixture --inspect-slot` 的明确诊断，无正常采集／悬停声明。新增诊断页仅在 capture／performance 已启用且参数合法时接受，普通启动不打开页面；正常输入状态由真实客户端早先实际操作与纯释放门分别证明。

中英文最小 640×480、1.25 字号任务日志与地图，以及 1280×720／0.85 地图均完成看图；小窗口日志列表与详情独立滚动、关闭固定可达；地图紧凑栏优先保留地表详情。首轮问题图片和失败日志均未删除。当前实现准备小批次提交，性能与最终独立包仍未完成；Mac 解锁后的最终输入复核请求已异步发出，后台工作不等待。

## B4：收尾数据修正与性能

- 数据核对发现 Water 没有库存 Material，原地图详情会被转换成 None。新增地表名称入口，对水面使用真实双语名称，其余材料保持原映射；双语词条 564。最终 Debug／Release 客户端构建通过，名称语义与输入相关 P11A 双配置各 99/99 PASS，额外资源词条检查 114/114。此前完整世界双配置各 1716、资源各 114 的数据／状态路径未再变化；没有为名称修正重复全量世界回归。
- 冻结渲染实现 `cd43625`（Release `a43b7566eb4ad5df8ff367fafc7be61cf17cd15024c61a69f02915d400566c3b`）完成 12 次、95693 帧配对采样：关闭 HUD 页面时 P95/P99 中位比 0.9568／0.963406，均小于 1.10；驻留地形缓冲中位数新旧均 39,531,184 B。完整结果见 [性能数据](hud-interaction-performance-2026-09-22.json)。最终候选只增加上述选点水面命名修正，被测默认 HUD 和无选点地图路径未变化；性能附件保留实测二进制身份，不冒充新哈希重跑。
- 指针页／地图页 P95 中位数 5.193／6.120 ms，P99 6.182／7.116 ms；采样期间模拟 tick 均为 0、世界时间保持。它们是页面整体成本：诊断在进入世界 3 秒后暂停，不同轮次冻结时的驻留网格数量不同，不能把两个值之差当作隔离的地图组件成本。全部为锁屏后台 1×，不外推到 2×前台。
- 正常操作说明已同步 README，说明 Tab 查看时暂停世界以及任务／地图入口，移除旧“释放鼠标但不暂停”的说明。

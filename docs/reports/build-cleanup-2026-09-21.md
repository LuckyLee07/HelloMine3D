# build 产物清理记录 · 2026-09-21

用户明确要求清理 `build` 中可删除的历史产物，包括 BMP 测试截图；不再要求长期保存这些原始媒体。本次未修改游戏运行时代码。

| 项目 | 清理前 | 清理后 |
| --- | ---: | ---: |
| `build` 占用 | 107,046,464 KiB（102.09 GiB） | 59,716 KiB（58.32 MiB） |
| 项目目录占用 | 约 103.1 GiB | 约 1.06 GiB |
| `build` 中 BMP | 7,795 张 | 0 张 |
| `build` 中应用包 | 414 份 | 1 份 |

`build` 目录占用减少约 102.03 GiB。以上为 `du` 统计，不等同于 APFS 卷可用空间的精确变化。清理后卷可用空间约 136 GiB。

## 已删除

- 历史截图、录像、性能采样、日志及验收 ZIP。
- 旧客户端、冻结测试副本、临时测试世界、编译缓存、目标文件及生成工程。
- `visual-upgrade-20260917/checkout` 和 `round2-checkout` 两份旧 Git 工作副本，移除前已保存其未提交代码及新增文件。

两个工作副本通过 `git worktree remove` 移除，其余 816 个产物路径清理完成，无删除错误。仅清理本项目 `build` 范围；根目录源码、正式素材、美术源文件、Git 历史和 `bin` 未清理。

## 保留与核验

- 用户正在运行的 `build/visual-upgrade-20260917/round2-compact-layout-recheck-r3/Diagnostic.app` 保留原路径，没有关闭、移动或重新打包。115 个受保护客户端文件的 SHA-256 均与清理前相同；31 份原有存档元数据全部仍在，进程清理后仍运行。
- 旧应用中的 19 个普通存档目录及相关配置保存在 `build/_preserved-20260921/old-play-saves.tar.gz`，209 个文件逐一校验通过，压缩后 76,292 字节。当前应用的存档原地保留。
- 两份旧工作副本的未提交文件、补丁、基准提交记录保存在 `build/_preserved-20260921/checkout-recovery.tar.gz`，102 个文件逐一校验通过，压缩后 7,155,870 字节。
- 清理清单、校验结果和清理前任务恢复记录保存在同一个本地保留目录。上述本地文件不提交到 Git。

## 历史证据与后续构建

本次清理没有重新执行游戏验收，也没有改变历史报告记录的结果；但旧报告中指向已删除 `build` 产物的原始附件现已不可用，不能再声称可从这些路径复核截图、逐帧数据或历史包。需要复核时，应按相应提交重新构建和采集，并区分新运行与历史运行。

后续构建可使用仓库既有生成脚本；清理后的首次构建需要重新生成工程和编译缓存。原始媒体用于当前迭代和问题定位，完成批次后按 [工作流](../current/agent-workflow.md) 及时整理，不再无限累积测试副本。

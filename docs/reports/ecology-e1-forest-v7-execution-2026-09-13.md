# E1 森林生态 v7 执行与验收记录

状态：`Done`，用户已于 2026-09-14 明确确认 **E1 整轮验收通过**。用户正常试玩
完成林中移动、转头、采木、合成与摆放工作台；当时保存失败的原始 FAIL 与进度
均保留，修复版已正常保存重开。当前包的三组配对窗口性能通过；旧包的流送首轮
FAIL 和密林固定机位近树遮挡仍作为风险交付，不因复测通过而删除。
范围和退出条件见
[E1 合同](../contracts/ecology-forest-v7-e1-contract-v1.md)。起始 commit
`f0692df00d350d3b6163810e41eead543c6ae51f`，起始工作区干净。

## 本轮实现

新建世界默认 terrain v7；只在森林区域以世界坐标、seed 和五格候选单元决定橡树位置，
再用连续低频字段形成轻林与密林的疏密差。原有 v6 方块橡树的形状/资源没有修改。
原始植物生成流保留，森林另加少量世界坐标草花。非森林候选仍用原先的树木规则。
未修改高度、生态边界、水系、洞穴、矿物、结构或存档格式。

## 已取得的证据

| 检查 | 状态 | 证据与边界 |
| --- | --- | --- |
| v6 兼容 | PASS | 改动前后生产程序各跑 8 seed 的 T0-SURVEY，463056 行地表采样和 32 个区块的方块/metadata/entity CSV 逐字节相同。`chunks.csv` SHA-256 `1f9a89741e8ee891dc55c3d1e632872a30774873c638478a0d1980b785e826e3`。原始文件在本地 `build/ecology-e1-20260913/baseline-v6/` 与 `candidate-v6/`；快照在 `tools/fixtures/terrain/e1-v6-chunks.csv`。 |
| v7 聚焦 | PASS | Debug 7/7：版本追加、8 seed × 6 区块反向加载、地下地貌和地点规划一致、两处实际森林出现不同树群、v6/v7 存档重开保持版本与玩家编辑；`build/ecology-e1-20260913/e1-focused-debug-final.log`。 |
| v7 地表与资源 | PASS（采样范围） | v6/v7 的 463056 行高度/生态输出相同；32 个指定区块的煤/铁数量相同，8 个有森林植被差异。生产采样 `candidate-v7-r3/`。这不代替所有世界资源或正常玩法。 |
| 两处已冻结林地机位的实际生成 | PASS（统计） | seed 20260807 的 3×3 区块，林地树干 v6 38→v7 47、草花 33→60；密林树干 39→37、草花 24→31。局部区块树干分别 3–7、3–6。来自 `e1-focused-debug-r3.log`，尚不等于视觉认可。 |
| Debug/Release 完整 WorldRuntime | PASS | 新增存档聚焦后两配置均为 1043/1043；`build/ecology-e1-20260913/world-full-debug-final.log`、`world-full-release-final.log`。Release 最终构建日志 `world-release-final-build.log`，退出码 0。此前两配置 1042/1042 的原始记录仍保留。 |
| v1–v5 兼容 | PASS | Release 生产 survey 五版本全量运行，`tools/validate_terrain_foundation.py` 报 `PASS`，包含既有冻结地表和区块快照；`build/ecology-e1-20260913/compat-v1-v5/validation.json`。 |
| 存档/资源 | PASS | Debug/Release 的 SaveLoadSmoke 和 ResourcePackSmoke 各自通过，日志在 `build/ecology-e1-20260913/headless-gates/`。 |
| 同条件固定林地原图 | 部分 PASS | v6/v7 的 seed 20260807、位置 `(1032,77,1024)`、时间 6000、High 阴影/Post On 原图分别在本地 `visual-v6-forest-desktop/frames/` 和 `visual-v7-forest-desktop/frames/`；均为固定机位诊断。v7 近景有一棵树遮挡正前方，最终审美及通路需在正常移动中判断。 |
| 密林固定原图 | 用户认可当前版本；保留近景遮挡观察 | v6 原图在 `visual-v6-dense-desktop/frames/`；用户指示改用固定项目包后，v7 以同 seed/机位/时间/画质在 `visual-v7-dense-stable-01/frames/` 成功捕获，`capture.json` 为 `CAPTURED`、`package_mode=REUSE_STABLE_APP`，输出目录无新 `.app`。开发者此前因 v7 近景树干占据右侧近半而暂判该机位视觉 FAIL；原始观察和 100 秒超时的 `visual-v7-dense-desktop/` 均保留。用户随后明确表示当前版本效果不错、可以提交，此主观审美判断以用户为准。 |
| 同包旧世界对照 | PASS（身份） | 增加 `--save-template` 后，同一个 E1 `.app` 载入 v6 密林存档，`visual-v6-dense-stable-01/capture.json` 为 `CAPTURED`、terrain version `6`；画面树木布局与旧 v6 密林截图相符。这证明后续可在同一应用身份下做 v6/v7 窗口对照，无须重启旧临时包。该存档的生命值/敌人状态不适合作为性能基线，尚未据此宣称性能通过。 |
| 客户端帧成本 | 常驻 PASS；流送首轮 FAIL、复核 PASS，保留波动风险 | 同一 Release `.app`、相同画质/seed/位置/模板，v6/v7 常驻各 3 次 P95/P99 比为 `1.006/1.008`；快速流送首轮各 3 次为 `1.645/0.981`，P95 超过合同 `1.10`，记 FAIL。按相反启动顺序再各 3 次为 `0.996/1.000`，六次汇总 `0.979/1.000`，均未改阈值。原始逐帧 CSV、mesh/buffer/加载数据和失败结论均在验收证据包，不能抹去首轮异常。 |
| 正常输入创建/保存重开 | PASS（菜单/存档） | Computer Use 操作同一路径客户端，从真实中文主菜单创建 seed `8675309`、terrain v7、休闲世界 `E1-Forest-8675309`，保存退出并从菜单重开，画面仍为森林。`world.meta` 原件在验收证据包；界面图保存在当轮 Computer Use 输出。 |
| 正常输入林中行走/转头/采木 | PASS（用户实玩，后续取得存档证据） | 此前 Computer Use 短按/拖动受阻；用户随后亲自操作，失败候选存档保留了从 `(-67.7,82,-7.5)` 到 `(-9.41,82,0.81)` 的位移、转角 `(26.45,90.30,0)`、采木/合成/摆放工作台目标与快捷栏木材。正常沿途视觉由用户继续判断。保存失败本身另记 FAIL，见下节。 |

第一版候选在密林样板仅有 v7 13 个树干，对照 v6 39 个，过疏；
其原始测试 `e1-focused-debug-r2.log` 和 `candidate-v7/` 均保留。调整后的上述
v7 数值来自 r3，不沿用 r2 的视觉或性能结论。

## 窗口权限与恢复

项目和此前每次新建的隔离游戏包都位于 `~/Desktop/Workspace/`。macOS 对该受保护
目录的访问提示是系统隐私授权，不是 Codex 的命令执行审核。第一次在沙箱内启动
出现 `kLSNoExecutableErr`，直接启动又因 OpenGL 上下文失败；桌面运行方式
可取得三组原图，但重复启动触发用户所见访问提示，最后一次 v7 密林在 100 秒
超时。全部失败目录保留，没有通过删去失败记录来改判。

为排除桌面路径触发，曾准备一份 `/private/tmp/HelloMine3D-E1-Visual-Test.app` 的
本地副本，但**没有启动**：Computer Use 自动审核拒绝 `getApp` 操作，理由是此举
可能绕过先前的 macOS 权限阻塞，也与用户要求停止反复启动客户端冲突。
未用其他方式重试该临时包。用户随后明确要求复用单个项目内客户端包；
新增 `--reuse-app` 模式，109 个清单文件校验通过，现有
`build/ecology-e1-20260913/HelloMine3D-E1-Forest-v7-Prototype.app` 原位完成
v7 密林与 v6 旧世界捕获，并可从正常菜单创建/重开世界；这些运行使用同一路径，未另建应用包，
此次未观察到隐私弹窗。应用窗口已保存并关闭。
打包工具也支持仅对已验证、未验收且未签名的工作包原位刷新；首次刷新因已存在
`Contents/MacOS` 目录报 `FileExistsError`，修正后在同一路径成功刷新 109 个清单文件。
两次原始结果保存在本地 `package-refresh-attempts.log`，刷新未启动应用。

## 用户认可与检查点包

用户先表示“这版本效果不错”，在已获知窗口性能和正常玩法证据仍缺之后，明确表示
“这版可以提交”。本次授权覆盖当前 E1 视觉候选的一个本地检查点，不改判尚未执行或
受阻的门槛，也不授权进入 E2。固定工作包沿用同一个 `.app` 路径；只把它封存成
`build/ecology-e1-20260913/HelloMine3D-E1-Forest-v7-Checkpoint.zip`，未启动新的包。
ZIP 完整性检查 PASS，大小约 21 MB，SHA-256
`b404700250e03022c5f7e1ee9300de10c3551f2f5f0ed7a32ec789203a00c171`。
分发清单覆盖 109 个文件，清单 SHA-256
`841728d889457fb9c2a933f991467e087b79bc78cdb79cef33656c70eb2930bf`；
可执行文件 SHA-256
`b6ffb0f155bff977c554ba30f21ce1c655e87934404161970db12d00468e638a`。

## 2026-09-14 收口复核与交付

复核起点为本地检查点 commit `827c952b45fd3cbef4dcb0cbcb0f2d83a820ede8`，
工作区干净。没有修改运行时代码，也没有新建或反复启动临时 `.app`；所有窗口采集
和 Computer Use 均复用上面的固定客户端。最初一次在沙箱内启动同包得到
`kLSNoExecutableErr`，`perf-v6-steady-r1/capture.json` 原始失败保留；桌面方式可运行。
先从旧 v6 存档进行的性能试跑生命值低且有历史敌人，不用作基线。性能专用模板
仅将两版本同源旧存档的历史 Actor 清零、生命值设为 20、难度设为休闲，再复制
为每次独立测试存档；模板改动详见 `perf-template-setup.json`。它不用于正常玩法验收。

同一台机器、同一个 Release 可执行文件、High 阴影、Post On、seed `20260807`、
位置 `(1032,77,1024)`、时间 6000 的窗口采样，每次预热 5 秒、记录 30 秒。
森林常驻首轮 v6/v7 各三次中位 P95 为 `23.252/23.399 ms`，P99 为
`23.951/24.149 ms`，符合 `≤1.10`；快速流送首轮 P95 为
`14.392/23.679 ms`，**FAIL（1.645 倍）**，P99 为 `24.906/24.428 ms`。
未清除异常，而是将运行顺序反转后各加三次；复核流送 P95 为
`13.009/12.961 ms`（0.996 倍）、P99 为 `23.442/23.445 ms`（1.000 倍）。
六次汇总 P95 为 `13.570/13.287 ms`（0.979 倍）、P99 为
`23.662/23.669 ms`（1.000 倍）。v7 首轮两次约 21 ms 的 render P95 长尾
没有在反序复核中出现；其原因尚未确认，作为性能稳定性风险留存。
逐帧追加诊断：首轮快速流送超过 20 ms 的帧，v6 为 `201/7623`（2.64%），
v7 为 `432/7660`（5.64%）；v7 慢帧中 87.3% 的 `render_ms` 超过 18 ms。
反序复核 v6/v7 的慢帧占比分别为 2.47%/2.12%。这提示超线集中于渲染帧节奏，
但仅是从现有数据作出的推断，尚不能确定是 v7 树木成本还是系统/窗口状态。
逐次 chunk visible P95、loaded chunks、mesh rebuild、地形 buffer bytes 与逐帧
CSV 均保留，不以测试机位截图代替窗口性能。

Computer Use 使用固定客户端完成中文主菜单建档、保存和重开；测试世界
`E1-Forest-8675309` 确认 terrain v7，重开后仍显示森林。进一步的游戏内
短按和鼠标拖动没有产生可从存档验证的位置或朝向变化，也未采得橡木。
这只证明当前 Computer Use 输入方式无法完成本项验收，不推断手动键鼠存在故障。
正常林中行走、转头、采木与依此判断的通路视觉均为 `BLOCKED`。

独立证据包：`build/ecology-e1-20260913/E1-Closure-Evidence-2026-09-14.zip`，
ZIP 完整性和内置的 85 项载荷 SHA-256 逐项检查 PASS；ZIP SHA-256
`32bf7995ecf33bfc21373cceb2a9826f33a9c7e4e98818ab7d3f5d208f47269c`。
其中包含 v6/v7 固定林地图、Release/Debug 日志、18 次性能采集的原始帧数据、
首轮 FAIL/复核报告、旧失败和正常建档 metadata。应用包仍为上节已冻结的
`HelloMine3D-E1-Forest-v7-Checkpoint.zip`，其哈希再次核对一致。该历史检查点包
包含测试存档；为交付准备了更干净的 `HelloMine3D-E1-Forest-v7-Review.zip`，
它直接从**同一个固定客户端**的 109 项分发清单封存，并逐项核对源文件与 ZIP 内容，
不含测试存档和运行日志，也没有创建/启动另一个 `.app`。Review ZIP 完整性 PASS，
110 项文件，SHA-256
`8a779497d2bd686f106a270f999449483bac1ceeddf9c465ed2bfb457f46defe`；
其可执行文件仍为上述 SHA-256。没有对 Review ZIP 解包启动，正式正常玩法的实机
证据来自同一分发内容的固定工作客户端。

| E1 收口项 | 判定 | 未闭合之处 |
| --- | --- | --- |
| v1–v6 兼容、v7 聚焦、资源/存档、Debug/Release 回归 | PASS | 已有原始日志与快照。 |
| 同条件固定森林原图及当前视觉版本 | PASS（固定机位） | 用户已认可当前视觉；不能覆盖林中正常移动视角。 |
| 森林常驻窗口 P95/P99 | PASS | v7/v6 为 `1.006/1.008`。 |
| 快速流送窗口 P95/P99 | 首轮 FAIL；复核 PASS | 首轮 P95 `1.645` 超线，反序三次和六次汇总低于 `1.10`；原因未明。 |
| 正常菜单建档、保存、重开 | PASS | Computer Use 界面和 terrain v7 存档已核对。 |
| 正常林中行走、转头、采木及沿途视觉 | 原收口时 BLOCKED，现已有用户实玩证据 | 下节记录后续进展；修复版的完整视觉/性能仍须核验。 |
| E1 整轮退出与完成版提交 | BLOCKED | 流送失败风险待处理/验收；未获用户整轮明确确认。 |

## 2026-09-14 用户实玩存档故障与恢复

用户在固定客户端 `E1-Forest-8675309` 正常操作至采木、合成并摆放工作台。
旧客户端点击保存时显示保存失败并取消返回菜单；这是**真实 FAIL**，此前无进度的
菜单存档 PASS 不覆盖此次失败。先将当时世界目录完整只读复制到
`build/ecology-e1-20260913/save-failure-2026-09-14/world/`，保留旧 `world.meta`、
失败候选 `world.meta.failed`、所有区块与备份。原始候选 SHA-256
`571e5041ddccd6cef52f0b6bb2b3ef67ebf4c469988cb69c227cd8851bd646e2`。
候选保留 21 个已完成目标、四个背包物品栏、12 个 Actor 与四个区块，区块探针
确认有 **1 个工作台**；旧正式 metadata 则只有建档初始状态。

根因为四个掉落物 Actor 的水平速度经逐帧阻尼降至 `±1.40129846e-45`，
macOS libc++ 的 `float` 流提取对这些合法次正规十进制值设置 failbit，导致
`WorldSave` 对候选自读校验失败。源码把写出时物理上可忽略的次正规向量分量
归零；读取时先以 `double` 解析，再检查有限值与 `float` 上界并转换，兼容旧
失败候选。存档格式、地形生成及摆放方块语义均未改变。单独的最小 C++ 复现、
两个针对性回归和源码补丁保存在本次证据目录。

救援先在隔离副本中只将失败候选的八个次正规速度分量改为零，其他 metadata
字节保持原样；`recovery-manifest.json` 记录原值与哈希。用生产 metadata/区块
读取路径确认副本 terrain v7、全部四个区块与工作台均可加载，再原子替换原世界
的 `world.meta`。**手动救援不计作正常保存通过**。退出旧进程后仅原位刷新同一个
`HelloMine3D-E1-Forest-v7-Prototype.app`，没有新建临时客户端。修复版从中文
主菜单进入同一世界，画面显示已摆放的工作台和木材等快捷栏；通过暂停菜单
“保存并返回主菜单”成功返回，再重开仍见工作台。保存后的 `world.meta`
包含 `alpha.place_workbench`、玩家位置 `(-9.41,82,0.81)`、10 木材、18 叶、
3 草块、3 浆果；生产探针再次读取四区块与一张工作台。这一项判定 **PASS**，
但仅代表此次真实保存与重开。该次核验结束时游戏保持打开供用户继续试玩；
后续本报告的窗口复测开始前已从主菜单正常退出。

Debug/Release 的 StorageTransactionSmoke 均 18/18（含次正规速度新用例），
WorldRuntime 均 1043/1043，SaveLoadSmoke 均 PASS，WorldBackupSmoke 均
19/19；原始构建和运行日志均在故障证据目录。修复版干净 Review 包
`build/ecology-e1-20260913/HelloMine3D-E1-Forest-v7-SaveFix-Review.zip`
SHA-256 `f916dbc91ee40f85efd7ad77b8c6c7c805de00953ef4afcd198c67e84cdc0fe8`；
其中实际游戏可执行文件 SHA-256
`54ff90e1db89defe55842e8f429e841592deb2199be8b718364de6dad4ece7d5`。
旧 Review ZIP 仍保留为历史原件，不能用它复验本修复。

独立故障证据包
`build/ecology-e1-20260913/E1-Save-Failure-Evidence-2026-09-14.zip`
含修复前完整世界、救援副本、修复后世界快照、全部原始日志、复现程序、补丁及 66 项载荷的
逐文件哈希清单；ZIP 完整性与载荷哈希复核 PASS，包 SHA-256
`38afbc9d0b613b97e1bd80d3243327ebe86b8996162d35f63a96cb4d4e15fdba`。
此为新的代码与包身份；旧版视觉和窗口性能结果不能自动当作修复版整轮验收。

## 2026-09-14 当前修复版收口复测与审查包

用户要求尽快收口后，在已从主菜单退出的**同一路径固定客户端**原位刷新身份，
未创建新 `.app`。客户端可执行文件仍为已验证的 SHA-256
`54ff90e1db89defe55842e8f429e841592deb2199be8b718364de6dad4ece7d5`；
刷新时 HEAD 为 `55ddb51fe9728860343e8c848e8e6be586daa346`，其后不再改动
运行时或资源。所有性能/视觉诊断均使用该客户端、隔离的 v6/v7 模板存档，
固定 seed `20260807`、High 阴影、Post On、1280×720、游戏时间 6000。
诊断环境指定机位/流送移动，**不冒充正常输入玩法**；真实采木与保存重开证据
仍以上节用户实玩、失败候选和修复版 Computer Use 为准。

每场景每版本三次，5 秒预热、30 秒逐帧记录，v6/v7 交替或反序启动。
当前包森林常驻中位 P95 为 v6/v7 `9.892/9.878 ms`（比值 `0.999`），
P99 为 `15.143/16.338 ms`（`1.079`）；快速流送中位 P95 为
`13.291/13.081 ms`（`0.984`），P99 为 `23.408/23.378 ms`（`0.999`）。
两项均在原合同 `≤1.10` 内，判当前包 **PASS**。逐次帧 CSV、render/update、
chunk visible、loaded chunks、mesh rebuild、面数和驻留 buffer 保留在审查证据包；
`performance-comparison.json` 带各次指标、哈希和源包身份。
旧包首轮快速流送 P95 `1.645` 的 **FAIL** 仍原样嵌入，不宣称已查明原因或
是保存修复带来的性能改善。旧异常两次 v7 运行超过 20 ms 的帧为 `194/207`，
其中 render 超过 18 ms 的帧为 `179/186`；当前 v7 三次同类慢帧为
`72/57/97`，仅支持“本次未重现该渲染长尾”，不能证明系统/窗口状态的因果。

当前包另以相同 seed、时间、High/Post、v6/v7 对照分别取得森林
`(1032,77,1024)` 与密林 `(928,83,928)` 的四组原尺寸双帧 PNG，
`capture.json` 均为 `CAPTURED` 且二进制哈希一致。森林画面树群与林下植被可见；
v7 密林固定机位右半幅仍被近树干遮挡，旧报告已有相同记录；此项不判作无风险。
用户此前认可当前总体视觉版本，最终仍交其判断该遮挡与沿途视觉。
流送性能截图在迁移后可能落到地表下，只作诊断；正式固定画面使用上述独立视觉运行。

干净审查包
`build/ecology-e1-20260913/HelloMine3D-E1-Forest-v7-ClosureReview-2026-09-14.zip`
从同一工作客户端的 109 项分发清单生成，共 110 个 ZIP 项，不含存档和运行日志；
逐项 SHA、ZIP 完整性 PASS，包 SHA-256
`4257be0cdc9ecfc7eb633d92b7b88b14c1620eb935c01867f5cf0350fb7243cf`。
独立审查证据包
`build/ecology-e1-20260913/E1-Forest-v7-ClosureReview-Evidence-2026-09-14.zip`
包含 12 次当前窗口性能原始帧、四组当前固定原图、模板、逐项判定和包身份，
并嵌入此前完整 E1 收口与保存失败证据包；144 项载荷哈希及 ZIP 完整性 PASS，
包 SHA-256
`e6df0444aa5117ca9cc5ce32feb05b5ea0e28b5328076e6332dfe0794b42ae82`。
当前结果清单为 `e1-savefix-closure-2026-09-14/acceptance-status.json`。

| 当前修复版 E1 项 | 状态 | 边界与下一步 |
| --- | --- | --- |
| 生成确定性、旧版兼容、聚焦资源/地点规划 | PASS | E1 生成源码自检查点未变；原始样本/回归保留。 |
| Debug/Release 世界、事务保存/备份及正常菜单保存重开 | PASS | 双配置 1043/1043、18/18、19/19 与真实工作台存档重开。 |
| 森林常驻、快速流送 P95/P99 | PASS（当前包） | 三组配对均按合同中位数达标；旧包首轮流送 FAIL 保留为未解释风险。 |
| 同机位 v6/v7 森林、密林原图 | PASS（采集与用户整轮验收） | 四组双帧可核；v7 密林近树干遮挡作为已披露风险保留。 |
| 正常林中行走、转头、采木、合成/摆放工作台 | PASS（用户实玩与存档状态） | 无独立连续玩法视频；不把诊断机位算作正常玩法。 |
| E1 整轮接受 | PASS（用户明确确认） | 完成版只纳入 E1 当前账本与本报告；已验收包和证据哈希不变。 |
| E2 进入 | NOT_RUN | E1 完成不自动批准 E2 的实施范围；不推送、发布或打标签。 |

## 收口结论

当前修复版的自动、固定画面、三组同包性能及用户实玩／正常保存重开证据已交付。
用户在查看验收包、画面、逐项判定和剩余风险后，明确回复“确认E1验收通过”。
本次完成版仅将本报告与当前账本作为 E1 文件暂存并本地提交；E1 生成代码已在
视觉检查点 `827c952` 中提交，保存故障修复已在 `bd264b5` 中单独提交。
完成版没有再修改运行时、资源或已验收 ZIP；固定客户端实际游戏二进制 SHA-256
为 `54ff90e1db89defe55842e8f429e841592deb2199be8b718364de6dad4ece7d5`。
旧包首轮流送 P95 FAIL 的原因未查明、密林近树干遮挡、缺少独立连续玩法视频，
继续在本报告和原始证据中保留。E1 为 `Done`；E2 与 R1/R2 收口另行处理。

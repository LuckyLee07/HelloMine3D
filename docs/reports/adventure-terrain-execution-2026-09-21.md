# 冒险地图地形生态执行记录

## 当前恢复状态

- 2026-09-21 用户明确启动完整 Goal；宿主 Goal 为 active，地形生态为最高优先级。
- 起始提交 `5d1945a`，开始时工作区干净。范围及退出条件见[目标](../current/adventure-terrain-goal-prompt-2026-09-21.md)，首批约束见[v16 合同](../contracts/adventure-terrain-v16-contract-v1.md)。
- 已读取本地调研索引，核对区域规划、区域高度混合和多类植被选择机制；详细来源仅在忽略提交的本地笔记。
- 当前批次：B1–B4 已实现并分批提交，v19 默认；v1–v18 各自基线保持。B5 完成 42 站连续诊断观察并修正林床／黏土纹理，正常菜单创建／合成界面／保存重开曾在 B4 版本通过，持续正常步行／采集受限。同列 section 合并已提交 `438b92f`，双配置完整各 1710/1710，当前版本正常菜单重开／合成／保存返回和再载入通过。三组正式配对共 72 次、364146 帧全 PASS；海中陆块逐米闭合、双配置一致性与六处岸线实景核对完成，最终客户端正在整理。
- 工作目录统一为 `build/adventure-terrain-20260921/`，复用该目录及少量诊断包；逐步日志、压缩列样本只在本地。基线构建日志 `baseline-build.log`，版本身份在 `baseline/manifest.json`。
- 用户现有工作包 `build/visual-upgrade-20260917/round2-compact-layout-recheck-r3/Diagnostic.app` 及存档受保护；本轮未操作。只读进程核对时未发现活动游戏进程，不能据此将用户包转作诊断包。

## 批次与验收状态

| 批次 | 状态 | 下一步 |
| --- | --- | --- |
| B1 区域骨架与版本化实际地形 | 实现及工程检查通过 | 窗口性能与最终艺术验收随整合版本完成 |
| B2 水系、岸线与差异地貌深化 | 实现、工程与连续水岸观察完成 | 最终性能与客户端随 B5 |
| B3 树型、地被与材质 | 实际生成、材质修正及 42 站连续观察完成 | 保留体素岸坡和少量诊断相机交叉的观察限制 |
| B4 出生、资源和探索路线 | v19 工程与菜单保存重开通过 | 持续输入受工具限制；连续视觉及最终性能在 B5 |
| B5 视觉、性能与客户端交付 | 视觉与必要性能完成，交付整理中 | 打包、精简产物与最终退出审计 |

旧视觉 Goal 的历史结果不作为本次新生成的验收证据。已完成的区域／生态及诊断视觉证据见下文；最新配对窗口性能已通过；完整正常探索仍受持续输入能力限制，也不替用户宣布审美认可。

## B1 实现与工程证据

- terrain v16 接入 `ClassicOverWorldGenerator` 的统一列查询，新世界默认使用独立区域规划。固定九个邻近锚点按连续权重组合七类内陆轮廓，并与陆海场结合；真实地表、水柱、植物和地点消费同一列。原有生成器链保留给 v1–v15。
- 起始生产基线重新构建，v1–v15 各 463,056 列及 32 个区块与候选摘要完全相同。摘要、二进制身份和成本见[精简证据](adventure-terrain-v16-evidence-2026-09-21.json)，原始压缩列在本轮工作目录。后续修复仅涉及新 v16 疏林分支和测试夹具，不改变旧生成路径。
- 纯规划 Debug/Release 各 12/12；八 seed 各有七类内陆地区，每类均有 128 米连片核心。采样高度 28..162、相邻格差最大 4、宏观坡差不超过 2 的比例为 99.9632%。核心平均高度依次为草甸 77.28、林地 80.83、高地 110.67、沙丘 82.57、峡谷 108.28、湿地 64.82、高山 126.23；湿地同时含水下地表与干地。
- `ADVENTURE` 生产检查 33/33：三 seed × 九类选点的四区块投影、反向加载、27,494 列实际表面与 5,863 个真实水柱、植被支持和默认 v16 修改保存重开通过。位点在首次截图前从最大核心连片确定，见 `tools/fixtures/terrain/adventure-regions-v16.json`。
- macOS gmake / Apple clang / x86_64 Debug 与 Release 客户端完成构建，首方代码无编译警告；两配置完整 WorldRuntime 各 **1482/1482**。Windows 和 Xcode 本轮未执行，不混同平台结果。
- 单次 headless 八 seed 中位数：列调查 v15 317.74 ms、v16 417.05 ms；四区块生成 v15 12.90 ms、v16 10.93 ms。该样本不是同机交替三轮窗口性能，查询开销上升仍须纳入后续性能核查。

## 失败与修复

1. 首次完整 Release 为 1443 项、17 个失败。v16 未生成疏林分支，已补入林地内部的疏密区域；旧湿地模型夹具默认读新版本，现显式冻结三个 v15 场景并追加三个 v16 场景，不删除原断言。
2. 起始旧二进制完整回归也出现 N7A 一项失败（1409 项、1 失败）：上一轮罗盘新增三条文本，但断言仍为 478 条。已更新为实际 481 条并检查新增三方向的中英文本，没有放宽语义一致性检查。
3. 首次图形运行在受限环境无法建立 OpenGL 3.0 上下文；按正常授权路径运行独立隐藏包后成功。第二次采集因误设 2× 像素比例失败；原生日志和帧均为 1280×720、1×，后续按实际比例重采。失败记录原样保留，不将失败标为通过。

## 初步画面与后续事项

独立工作包为 `build/adventure-terrain-20260921/Working.app`，Release 二进制 SHA-256 为 `c5b5fe5b78bdcf00c5c8f3e8a211ae049bb8159eefe7668341b8f69dab9a6ab6`。使用冻结位置、时刻 6000、用户当前 FOV 120、1280×720 点及像素，保持隐藏且不激活窗口；用户包未操作。岩台、沙丘、高山各两帧成功采集，实际检查首帧；这是诊断相机观察，不是正常移动。

- 沙丘出现连续沙脊、谷间通路及地标；补充阴影关闭对照后，沿坡面的细小尖点仍存在，不能归因于方向阴影，后续检查地形网格接缝与材质表现。
- 岩台存在大片坡积土，缺少岩壁、坡脚与稀疏植物的层次。
- 高山轮廓已抬升，但当前仍是现有石材，规则材质条带明显；雪线和高地针叶植物尚未接入。
- seed 42 的冻结湿地点位落在浅池内部，正常物理步进后镜头进入水下；该图验证实际浅水与池床，不能作为站在岸上的湿地全景。后续以事先确定的干岸补充路线检查植被层次。
- 下一批优先水系与岸线组织，其后完成配套生态、像素材质及上述画面问题。最终仍需普通出生/探索路线、资源与地标接近、配对窗口性能和正式客户端交付。

## B2 水系与岸坡投影

- terrain v17 追加有向河谷图，512 米节点按较低势能连接，低地节点先规划湖盆水头以协调入／出水河床。高处为干谷，低处河床和湖盆复用水位 64；实际块、公开查询、小地图生态说明及后续地点规划使用同一列。图节点严格下降不代表完成全球水文物理验证；局部终端湖保留。
- 河湖规划固定范围、线程本地派生缓存固定 64 项且不超过 256 KiB，无世界或邻区块加载。原海洋列不修改，新增 River/Lake 生态只追加；v1–v16 每版 463,056 列及 32 区块与原基线完全相同。
- 纯规划 Debug/Release 各 7/7：八 seed 133,128 采样列中改动 7,123，河道 2,113、湖盆 86、干谷 4,905；605 条有向连接下降、43 个局部终端；边界及宏观采样相邻差最大 5，99.9151% 宏观坡差不超过 2。测试包含缓存驱逐、换 seed、线程及坐标极值。
- `ADVENTURE_WATER` 22/22：八 seed 各一个河／湖位点，16,287 个实际表面、9,571 个连续水柱、64 区块反向生成、陆生植物不占水体和默认 v17 水面编辑保存重开。97 列地标覆盖单独排除。位点及干岸机位在截图前冻结于 `tools/fixtures/terrain/adventure-water-v17.json`。
- 生产检查首次发现 seed 20260809 湖岸 4 列被叶块覆盖。原因是旧树冠投影无条件替换陡坡地表；v17 树木、棕榈及仙人掌改为只替换空气或轻型植被，保护地形、水体及已生成树干；v1–v16 保留原投影规则。修复后原 4 列及全部生产检查通过。
- macOS gmake / Apple clang / x86_64 两配置客户端构建及完整 WorldRuntime 各 **1505/1505**；Xcode、Windows 未执行。初次水系纯测试湖盆样本不足、首次生产叶块覆盖失败，以及补充投影策略时缺少版本头文件的编译失败均保留在本地逐步日志，最终版本已修复。
- 当前独立工作包 Release SHA-256 为 `68d409cf145050d80216e42bd9dee469581f99b7a3e29aef75586b9d69f19094`。隐藏窗口、FOV 120、1280×720、medium 阴影、时刻 6000；三 seed 各两帧，均实际检查 5 秒图。草甸湖湾及缓坡岸线可见向外延伸的水道，林地区域形成陡岸海湾／湖盆。此为诊断观察，不是正常键鼠行走或用户审美认可。
- 同轮单次 headless 中位数 v16/v17：列查询 416.60/425.33 ms，四区块生成 10.55/10.13 ms；同时存在后台构建／测试，不作为配对性能结论。最终三轮窗口性能仍待整合版本检查。
- 新增湖盆轮廓仍偏规整；岸坡三角尖点在多个区域清楚可见，B1 关闭方向阴影仍存在，须继续检查 AO／网格与材质。植被仍以现有树种及草花为主，雪线、针叶高地、湿地水岸组合和裸岩坡脚均未完成。
- 精简摘要、二进制与帧哈希见[水系证据](adventure-water-v17-evidence-2026-09-21.json)。当前无运行游戏窗口；工作包诊断配置已清除，用户客户端和存档未操作。下一步 B3，先定位坡岸尖点，再按区域接入树型、地被和像素材质；最终出生路线、资源和配对性能继续挂账。

## B3 前置：岸坡尖点排查

- 复用 B2 Release 工作包，固定 seed 20260807、位置 `1237 68 -345`、旋转 `10 57.53 0`、FOV 120、1280×720、时刻 6000，关闭方向阴影并通过已有 `HELLOMINE3D_DISABLE_VERTEX_AO=1` 关闭 AO；首帧实际检查仍存在尖点。输出在本地 `bank-ao-off-r1`，环境变量通过 direct 启动继承，原 `capture.json` 只记录显式工具环境，因此本段补记该继承变量。
- 新 `BANK_MESH` 用正负坐标斜向沙台阶检查每个三角形的轴向平面、朝外法线、内侧真实方块及总暴露面积；AO 开／关各覆盖 612 平方米且无缺面。另取实际调查点附近四个区块的岸线网格，1,082 个沙面三角形均轴向且有实体支持，合计 1,596 平方米与实际暴露面完全相符。
- 这些检查未发现倾斜三角网格、半面丢失或 AO 引起的尖峰；画面更符合斜向体素台阶的透视遮挡和明暗对比，不能写成修复了不存在的几何漏洞。该视觉重复仍交给后续坡岸材料、坡脚和植被层次改善，保持体素轮廓。
- Debug/Release 新聚焦各 12/12。此批只新增诊断检查，没有改运行时地形或渲染；B2 的双配置完整 1505/1505 保持其原版本归属，新增主入口总数为 1517，未把聚焦结果宣称为重新跑过完整回归。
- B2 已本地提交 `0441789`。B3 接下来补成套树型／地表材料和生态摆放；内置图像工具正在制作一张统一像素尺度的材质源图，接受与否以接入后的实机场景为准。

## B3 资源准备：统一像素生态材质

- 新增 12 个图集语义（第 8 行列 0..11）：针叶／浅色阔叶树皮、断面、叶片，雪、砾石、黏土、林床、苔石、淤泥。由内置图像工具制作源图，首版白孔及 alpha 不合预期后再次编辑；最终源图、完整提示词、编译参数见 `docs/art-sources/adventure-ecology-20260921/`，废弃首版只留本地工作产物。
- 兼容图集 16×16 及标准数组的 32×32 像素簇均通过可复现编译；语义 132、空槽 124，alpha、图标不变、五项破坏性负例通过；标准数组 7 级 mip、叶片透明覆盖率和原叶材来源检查通过。
- 新图块使用原空槽，旧图集所有其他像素及数组其余 244 层的全部 mip 字节相同。数组仍为 5,592,100 字节，没有扩大运行时纹理容量。对照在本地 `adventure-art-old-slots.json`。
- 资源包当前源码重新构建检查 114/114。此步仅准备并接入资源表，未增加方块／物品或切换生成版本；不能宣称新树型已出现在世界，也未用资源测试代替实机美术评审。
- 岸坡诊断已另提交 `1012152`。当前继续按[v18 生态合同](../contracts/adventure-ecology-v18-contract-v1.md)落实树种 metadata、真实地表材料、区域摆放和保存／采集检查。

## B3 基础材料与树种持久化

- 追加六个方块及物品：雪、砾石、黏土、林床、苔石和淤泥；已接入工具／掉落、真实采集放置、图标、双语文本、资源清单、小地图及保存。原 ID 不移动。木材仍使用原存档字符串，界面统一说明为原木。
- 树干／叶块 metadata 0 保持橡树，1／2 分别选择针叶及浅色阔叶树材质，真正进入区块网格与合并键。真实树型和区域生成还未启用，默认 terrain 仍为 v17；挖掘轮廓及碎片反馈的 metadata 传递将在新树进入世界前补齐。
- 新 ADVENTURE_MATERIAL 22/22，覆盖六材料实际采集放置、工具和掉落、全部 12 材质进入真实网格、保存重开与树种数据。首轮出现 3 个失败：测试未选中新采物品导致背包满，以及普通立方体网格入口遗漏 metadata；两者定位修复，保留失败日志。首次编译还发现复用的计数辅助函数不在当前作用域，已改为正式玩家查询接口。
- Debug／Release 客户端均完成构建，完整 WorldRuntime 各 1539/1539、ResourcePack 各 114/114；Release Recipe 126/126。本轮没有运行 Windows／Xcode。详细身份及日志摘要见[材料证据](adventure-material-evidence-2026-09-21.json)。
- 独立 Release 工作包 SHA-256 为 `430e48ccc84b5ad10a64abf0ca3cfe9cac9626f808134cd0e30a070ac30ffbe8`。标准与兼容路径的固定材料展示区首帧均实际查看，三类木叶和六类地表可以区分，叶孔透明；兼容日志明确为 `standard=0`。这是人工搭建夹具的诊断观察，不构成天然生态、正常行走或用户审美验收。
- 用户既有客户端 SHA-256 仍为 `1f9de91945538ec1d363ede56b9784a01aebf5e43e6e8e235312c81e7604f83a`，未操作其客户端或存档；诊断进程已退出并清除工作包临时配置。下一步接入 v18 成套生态、树型和实际地表，再进行多 seed、旧版本与实机路线检查。

## B3 v18 成套生态实际生成

- 新世界默认 v18，旧 v1–v17 保留。`AdventureEcologyPlanner` 直接复用 v17 水系采样所带的原区域权重，避免重新采样宏观场；地表高度／水位不变。树群按 96/28 米斑块、7 米格扰动锚点组织，初选后才执行四次坡度检查；相邻地区通过 37 米生态斑块混合。
- 针叶树为 9–12 格高、逐层收窄的锥形树冠；浅色阔叶树为 7–10 格高的偏心轻冠；湿地树有低垂边缘和横枝。真实原木／叶块带树种数据。林床、砾滩、黏土、泥岸、苔石和波动雪线进入实际地表；雪／苔石下接岩石，黏土有六格以内的浅层厚度。
- 草 metadata 2/3 对应低蕨／芦苇，使用现有像素植被调色与真实可采草块，保留成熟草经济。网格与选中面共用几何；树种数据经选择、破坏事件、挖掘粒子、碎片快照及渲染传递，避免新木材的反馈退回旧外观。
- 纯规划双配置各 14/14：八 seed、172,872 点，全部新地表有覆盖，所有 seed 均有橡／浅色阔叶／针叶／湿地树规划；地被采样覆盖 1,671 点，其余保留裸地或开阔地。高度／水系不变、跨线程／换 seed／坐标极值稳定。
- 生产 `ADVENTURE_ECOLOGY` 最终 88/88：预冻结八 seed × 九地区、288 实际区块及反向生成；73,509 实际表面、12,650 水柱，地标覆盖 219 列及洞口 8 列单独核对。雪／砾石／黏土／林床／苔石／淤泥计数为 5,513／6,365／2,459／4,660／1,089／7,792；三类树干 542／344／221，成熟草／蕨／芦苇 131／445／347。独立树型轮廓、植物支持、碎片 metadata、默认 v18 保存重开通过。
- v1–v17 每版 463,056 列与 32 区块生产摘要完全保持；v18 新摘要另留。旧水系测试现在显式建立 v17 存档，仍检查该身份保存重开。
- 首轮生产 88 项有 1 失败：测试误认为一次 `update(1)` 能清空旧粒子，而正式时间线将每步限制为 .25 秒；改为三个正常步长后新挖掘粒子的树种验证通过。首轮完整 Release 1628 项有 2 失败，均为 FS1 统计将 ChunkBlock 与旧 metadata 0 木材直接比较；改为按真实木材 ID 统计后完整通过，没有放松出生必须有可采木材的要求。出生地表识别同时补入六个新自然材料。
- Debug／Release 完整各 1628/1628，两配置客户端构建完成且首方代码无警告；精简证据见[生态 v18 摘要](adventure-ecology-v18-evidence-2026-09-21.json)。曾误把 `FS1` 当作已注册 focus，实际执行了完整套件并通过；命令随后又执行的重复轮次也通过，未作为额外覆盖。两份原始日志保留，此后只使用已注册 focus。
- 八个预选核心机位（seed 42，FOV 120、1280×720、1×、medium 阴影、时刻 6000）均已在隐藏独立包渲染并查看首帧；另采上一批岸线原机位与兼容针叶林。它们是带正常重力／生物模拟的强制初始机位，个别画面受敌人接近遮挡，不能冒充正常输入路线或像素级锁定相机对照。
- 混合林能识别浅色树干和林间空隙，针叶林具有暗色分层冠与林床／蕨层，雪线下有针叶树，峡谷出现岩石与暖黏土组合。仍存在明显规则岩层色带、雪坡暗线及林床纹理重复感；湿地首帧以泥岸／芦苇为主，垂冠需补近距离和连续视角。草甸首帧有近身敌人遮挡，不作为独立景观验收通过。
- B3 还需补查自定义 Cross 资源形状的蕨类回退、洞口附近树根／冠避让，以及上述艺术问题；B4 的普通出生路线、资源与地标接近、正常输入、B5 最终配对性能和交付尚未完成。整体 Goal 保持 active。

- 本批工作包 Release SHA-256 `20afebd25ff906437bb3e28a217a800cf265248ca127d6e0c0167e1dc31e15c1`；旧 v1–v17 兼容调查由本批 r3 二进制完成，最终 r4 只调整出生支撑、资源统计和通用名称，没有再改变生成器。标准与兼容生态首帧已复查，兼容日志为 `standard=0`。岸线台阶尖点仍可见，不宣称已消除。无活动诊断窗口，工作包配置已清除；本批 build 总量约 2 GiB，后续继续复用该包。

## B3 岩坡着色复查

- 已提交 v18 实际生态 `32670c8`。随后原峡谷机位确认，旧地质 shader 每约 4.25 米高度重复暖／冷正弦色带，叠加体素台阶后呈现规则横纹；改为连续世界坐标中的低对比、不等间距矿物色斑，普通／阴影 shader 使用同一计算。未改变任何方块、生成版本或存档。
- `validate_block_feedback_shader_macos.cpp` 新增 `--irregular-geology` 模式，与本批冻结的旧条带 shader 比较；历史强条带模式保留给其原资源版本。新要求是削弱重复高对比条纹并保留宽尺度空间变化，不能继续沿用旧的“必须有强周期变化”视觉指标。GPU 当前 180/180：atlas／array、普通／阴影、昼夜、时间不变、区块原点／负坐标连续、关闭回退和非目标材质不变。
- 256×256 合成恒定纹素夹具的相邻像素梯度：atlas 1.33984 → .0287787，array 1.12387 → .0247213；远场分别 .096732／.0862745，均通过原定 .12 上限。旧条带负例被检出。该统计仅描述 GPU 夹具，不冒充实机场景性能或审美评分。
- 双配置资源包各 114/114。复用已验证的 v18 Release 二进制 `20afebd25ff906437bb3e28a217a800cf265248ca127d6e0c0167e1dc31e15c1`，没有改 C++ 或重复完整世界回归。原峡谷位置、FOV、时间、阴影及分辨率的新首帧已查看，规则暖灰横带消退，保留真实台阶和黏土／岩体分区；输出 `geology-visual-r1-canyon`。
- 雪坡另做关闭 AO 及方向阴影的同初始机位诊断，暗线仍存在；该继承环境为 `HELLOMINE3D_DISABLE_VERTEX_AO=1`，不在 capture.json 的显式环境表中，因此在此补记。暂不能称为阴影缺陷；继续结合雪层厚度与裸岩表面检查。
- 岩坡 shader 的三轮配对性能将随 B5 最终版本执行；当前不声明性能门槛通过。接下来 B4 同批处理入口植被避让、地被资源覆盖回退及普通探索路线，完整 Goal 保持 active。

## B3 雪材质裁切修复

- 同机位关闭 AO／阴影及临时加厚三格雪层均未消除暗线；已撤回无效的加厚尝试。实际原因是生成源图第二行在 y=713 结束，旧等分裁切到 y=724，将相邻苔石带入雪的最后一行纹素。
- 保持源图内容和哈希，编译器按实际行界裁切并跳过边界混色行。雪纹素最暗 RGB 从 45 改为标准 195／兼容 201；增加相邻材质污染检查。旧等分裁切负例可检出，原有 244 数组层所有 mip 及图集非新增槽逐字节保持。
- 图集确定性重建及五个故障注入、数组全部 132 槽／7 mip／透明覆盖检查通过。固定 v18 Release、原高山位置、FOV 120、时刻 6000、medium 阴影及 1280×720 的首帧实际复查，绿色暗线消失；仍保留真实雪盖／岩壁边界。此为开发诊断，不是正常行走。
- 精简证据见 `adventure-material-crop-evidence-2026-09-21.json`。B4 v19 洞口和营地整合仍在验证中；该资源修复独立提交，不混同整体 Goal 完成。

## B4 探索整合与正常菜单

- v19 在洞口／通道投影附近拒绝树根，并扩大纯入口规划 halo 覆盖来源树冠。八 seed 各找到一个 v18 实际遮挡树根，v19 均清除且邻区块先后生成不改变结果；旧洞形、雪层和地形高度不变。蕨类仅替换标准 Cross，自定义资源几何回退检查通过。
- 八 seed 正常出生路径都有安全支撑及附近 56–315 个实际原木。陆路调查检查每条四米边的全部中间列，只允许一格坡差；768 米路线范围内连接 2–6 个区域核心、水岸和三类地标附近。首次 seed 0 没有营地，原因是旧候选拒绝森林；v19 允许符合原坡度及入口条件的林间空地，复查营地距离 40–648 米。候选路线不等于已经穿过真实碰撞和最后入口。
- 双配置聚焦各 54/54、完整 WorldRuntime 各 1683/1683，资源包各 114/114，客户端均构建通过，首方无编译警告。旧 v1–v18 各 463,056 列／32 区块摘要与冻结基线完全一致。首次入口测试搜索范围不足的失败保留；扩大有界样本覆盖后原语义断言通过。
- Computer Use 对唯一独立工作包执行正常菜单操作：创建 `Adventure-v19-test`、seed 42、普通难度、terrain v19；进入实际出生 `(40.5,92,56.5)`，打开合成，保存返回主菜单，重新进入同一世界并保存退出，均实见成功。存档最终位置 x=36.3 表明短输入确有位移，但连续 WASD／相对鼠标拖动未表现出可靠持续控制。正常采集、跨区域完整步行为工具能力受限，不能用诊断传送补成 PASS；没有修改键鼠权限或让用户接手。
- 交互截图在本任务 Computer Use 记录；启动日志在 `normal-play-r1/client.log`，独立世界存放于工作包自己的 `bin/saves/`（catalogue 环境仅控制目录管理，未改变该正常存档根）。初次连接工具异常耗时，后续连接、菜单和退出正常。已关闭独立窗口，不影响受保护包。
- 证据与八 seed 路线摘要见 `adventure-exploration-v19-evidence-2026-09-21.json`。当前 Release 二进制 SHA-256 为 `f5e3abea72dc3d8b964188ebf205091183935794d797f0e1820fad3840d72806`。下一步 B5 最终连续视觉、性能及精简交付；整体 Goal 保持 active。

## B5 首轮性能失败与局部同步优化（进行中）

- 首次性能预检要求 1× 像素比，但 Cocoa 实际报告当前 Retina 为 2560×1440／1280×720 点，工具拒绝该次采集。失败保留；完整原坐标配对 r2 固定实际 2×、FOV 120、medium 阴影、标准画质、后处理开、seed 42、时刻 6000、5 秒预热＋30 秒记录，禁用帧回读，交替三轮。
- 优化前 Release `f5e3abea…` 原坐标 24 次全采集：林地静止 P95/P99 比 1.708/1.858，加载 1.717/1.712；岩台静止 1.046/1.138，加载 1.110/1.136。原 1.10 护栏并未通过，不删除样本或增加预热。林地原坐标在旧 v15 是海洋，实体面约 14.6 万→43.4 万；其负载差异和真实新增成本必须一并解释。
- 另冻结同类区域对照：旧 v15 林地／岩台从原生产调查最大连片核心的质心选点，未使用性能数据挑位置；新版本沿用事前冻结位置。首组林地三轮仍失败，P95/P99 比 1.172/1.240，更新阶段偏高。该批在后续当前单次采集完整结束后中止调度，原始 10 次采集全部保留；冻结世界代码对照尚未启动，待优化后继续。
- 实际运行采样揭示坐标字符串索引、全量 mesh snapshot 和锁等待占用；原始 `sample.txt` 在 `residency-profile-r1`。该次有系统采样干扰，明确排除于性能比较。最慢帧集中在预热后周边 256→361 区块的继续生成／上传阶段，不能冒充纯静止 GPU 数据。
- Ogre 局部优化保持第一份实际快照：驻留位置不变时复用索引，变更／卸载时仍清理；没有实际上传则跳过无意义的第二次确认；有上传仍核对当前 revision 与 CpuReady，但只查本帧至多八个位置。没有削减视距、分辨率、树木或面数，不改变任何生成输出。
- Debug／Release 客户端编译通过。石块诊断仅观察到挖掘裂纹／松开恢复，不能声称石块已挖除；花朵诊断的前后帧实见实体消失、碎片及背包获得一朵玫瑰，确认运行时改块上传路径继续生效。这些是 Sandbox 定时输入诊断，不是 Computer Use 正常采集。
- 当前优化候选 Release `f5da20918ca3427bd0e64bd30141a86dc407d4e93c23ffcbf7d2d3a8b0f11ad7`，最终性能尚待。连续路线 33 站、每站四帧已冻结于 `tools/fixtures/terrain/adventure-v19-visual-routes.json`，原等待队列已停止，须在最终性能后重新启动；尚未采集或声明观察通过。

- 随后窗口回读实际变为 1280×720，花朵 r2 因期望 2× 而被工具拒绝；r3 按真实 1× 完整采集通过。不能把不同尺寸试测互比。优化后 r3 单次林地 P95/P99 为 9.667/11.490 ms，仅作试测，不是三轮门禁结论。
- 最终协议重新冻结为两端一致的 1280×720 实像素、FOV 120、medium／standard／post on，候选哈希 `f5da2091…`；三个独立组分别比较同类区域、相同 v15 世界、原坐标。此前原协议／失败／中断样本全部保留。总共 72 次按组顺序运行，期间无其他构建或图形客户端。
- 准备复测同进程菜单重开时，Computer Use 明确返回 Mac 锁定且不能自动解锁。只终止经进程工作目录确认的本任务空闲菜单，未绕过锁屏或操作用户包。优化前正常菜单结果保持原版本归属；最新候选正常菜单复测为 BLOCKED，隐藏诊断继续。

- 同步优化后三轮同类林地实测（实际 1280×720）：静止 P95/P99 中位比 1.005/1.121，快速加载 1.158/1.204，仍未满足两项都 ≤1.10。该批在第 15 次完整采集后停止，所有采集及 `final-optimized-matched-r1/interrupted.json` 保留；其后 control／original 尚未执行。当前没有继续运行的性能或路线调度。
- `residency-flower-visual-r3` 已实际查看 1250、2450、6000 ms 三帧，确认花朵从真实场景消失、碎片结束且物品仍在背包。工具新增最多八个递增采集时刻和直接进程峰值 RSS；重复时刻负例拒绝，默认采集时刻不变。
- 同步优化作为独立批次提交，性能整体仍为待修复。下一步消除相同地表列在不同高度 section 的重复纯查询；不改变地形输出、不延长预热、不降低画质。之后重跑必要配对，再采集已冻结的 33 站连续观察。


## B5 地表查询复用

- `763fafe` 已提交区块驻留同步优化。后续 v17+ 地表列查询增加每线程 8192 项、最多 256 KiB 的固定缓存，命中校验完整 seed／生成版本／有符号坐标；无 World 指针、实际方块或存档状态，替换不改变输出。v1–v16 原查询分支未改，生成默认仍为 v19。
- `ADVENTURE_QUERY` 双配置各 3/3：八 seed、v17–v19 与无缓存规划结果一致，含负坐标和整数边界；32768 个不同列后的替换／交替 seed 与版本、四线程共享生成器均通过。双配置客户端编译通过、无新增编译警告；完整 WorldRuntime 各 1686/1686。
- v17、v18、v19 各 463056 列和 32 个真实区块生产摘要与优化前完全相同，见[缓存精简证据](adventure-query-cache-evidence-2026-09-21.json)。本批未改变生成内容，因此不追加生成版本。
- 新 Release 为 `58dd5a9bd29fb37a164f620c86ce5a010fda063b9697d061aaedf4e231330fdb`。独立 Working 包已刷新到该二进制；林地试测与连续路线正在进行，三轮性能尚未验收。用户客户端只读核对，当前后处理关闭、小地图范围 128 米，追加相同出生路线的该设置检查，不更改用户配置。


## B5 连续观察发现与地表材质修正

- 查询缓存试测的更新阶段降低，但单次林地静止 P95/P99 10.302/13.234 ms、快速加载 10.344/14.563 ms，仍不能声明三轮性能护栏通过。当前完整 Goal 保持 active。
- `final-route-visual-r1` 完成事前冻结 33 站×4 帧及用户后处理 Off／小地图 128 米出生点四帧。已逐站检查三条路线及四个补充区域的四时刻联图：混交林→针叶高地、岩台→沙丘、林地→湿地的组合区别可见，路标／营地／遗迹实际出现在途中。部分线性诊断相机穿越山体、树干和结构，尤其东 06、西 04/09、南 00/01/02/06；这些不能算连续地表可走性 PASS。站点静态视图和无穿模片段只作对应区域观察。
- 大片林床的橙色落叶和黏土自身的斜条纹过强，使用图像生成分别替换为低对比灰褐腐殖土与低饱和斑驳黏土；保留粗像素风格。完整提示词和两张源图保存在本项目美术来源目录，编译仍为标准 32／兼容 16 纹素。
- 只改变两种材料的图块：其他图集像素及 254 个 array layer 的全部 mip 完全相同；不改变实际方块、地表规则或生成版本。图集确定性重建、array 哈希／alpha／mip 校验通过，双配置资源包各 114/114。初次工具补丁未完整应用导致缺少导入，构建失败保留在 r1；补齐后 r2 全通过。
- 按原机位复查林地、岩台、湿地和兼容林地，已实际查看大面积地面近景及末帧，强橙黄落叶和重复斜纹消失，地表层级更稳定。见[材质精简证据](adventure-ground-material-refinement-2026-09-21.json)。这批是已发现问题的修复，完整连续路线和最终性能仍待收尾。


## B5 中间高程路线复查

- 诊断相机可选 2–13 个相对 xyz 中间点，全部位于 48 米界内、折线路程最多 64 米，首点零、末点必须等于原 sweep 位移；仍要求隐藏截图／初始世界，禁止混用性能，普通启动不启用。采集工具明确记录继承的诊断环境，不再依靠事后手填。
- 双配置纯相机检查各 33 项及 12000 连续采样通过；双配置客户端构建通过。实际无 sweep 却提供路径的启动负例按预期返回 1，错误信息准确，原始 capture 为 FAIL 保留；关闭诊断的菜单正常采集。这里不声称正常 OS 输入通过。
- 同样的 33 站保留原位置、端点和画质，改为沿原冻结调查的每 4 米高程；另完整补查原 356 米水岸路线的 9 站，总计 42×4 帧。所有四时刻联图已查看，三处原先穿山的代表段消除地表穿入；区域组合和水岸衔接持续可见。仍有少量相机与遗迹平台、树干交叉，逐站保留限制，不能将无碰撞相机当作行走验收。
- 用户当前 post Off／小地图 128 米的相同出生路线已采集并查看末帧，材质与区域仍可读。客户端为 `db37700981c1fe574f243980ae58f365d2bc6088e045e8bd095df6f48ab51c4f`，逐站哈希与发现见[路线证据](adventure-route-visual-evidence-2026-09-21.json)。当前路线及其后续调度均已结束。
- 下一步正在验证封闭 section 的网格输入跳过优化，再进行无其他构建／截图干扰的最终性能配对。完整 Goal 仍未完成；Mac 锁屏时不绕过正常输入边界。


## B5 封闭区段输入优化

- 两条 mesh 构建路径先读取既有层封闭标志，完全封闭才跳过 18³ 方块／光照与生态数据复制；默认快照仍完整。缺少邻区块、打开表面或其他可见层仍走完整捕获，不改变 mesh 判定、revision、状态机、生成内容或工作预算。
- Debug／Release `MESH_INPUT` 各 21/21，包含默认完整输入、封闭零生态查询、复用输入在开洞后补齐、实际可见面与同步／异步状态；完整 WorldRuntime 各 1689/1689，双配置客户端构建无新增警告。见[封闭输入证据](adventure-enclosed-mesh-evidence-2026-09-21.json)。
- 当前候选 Release `3df386cd1d058e074c301169c5c8418a9e3f0f66e534be1250fd352c8e4fe730`，Working 包已刷新。全部构建、测试和截图结束后，启动 `enclosed-final-pairing-r1.log` 的三轮配对：依次 matched／control／original，任一组护栏失败保留全组并停止后续组，继续修复。当前调度 `/private/tmp/hm3d_enclosed_pairing.py`，输出 `final-enclosed-*-r1`；不与其他游戏窗口或重负载任务并行。
- 比较器同时校验可执行文件和资源清单身份，拒绝混入相机／破坏等诊断环境；匹配身份与错误资源哈希负例已验证。P95/P99 1.10、5 秒预热和 30 秒记录均不改变。


## B5 载入边界光照定位

- 封闭区段优化后的林地三轮仍失败：实际 1280×720，静止 P95/P99 比 1.018/1.210，流送 1.110/1.201；停止后续配对并保留 13 次完整采集（包括已完成的首个旧峡谷），见 `final-enclosed-matched-r1/interrupted.json`。首次停止记录脚本假定只有 12 次导致断言失败，未删除任何数据，随后按实际 13 次补记。
- 新独立系统采样 `enclosed-profile-r1/sample.txt` 排除于性能比较：worker 的载入提交 284 个样本中，283 个落在方块光衔接，主线程存在相应 World 锁等待。原实现对所有高度的两侧边界建立哈希集合，再传播大量零光格；区块增高放大了成本。
- 优化仅选择已重建内部光照且亮度大于一的边界格，四个驻留邻区块只查找一次；保持原传播器、熔炉归一化、锁和通知。Release 聚焦 `LIGHT_BOUNDARY` 49/49，包含九区块完整 12800 格光场在正逆加载、detached commit 后仍满足多光源曼哈顿衰减，并验证不会额外加载区块。双配置完整回归和性能复测继续进行。
- 首次仅优化载入的试测：林地静止 P95/P99 9.255/9.943 ms、update P99 1.681 ms；流送 P99 仍 14.219 ms。独立 `boundary-stream-profile-r1` 进一步定位主线程卸载：卸载函数的热点调用有 436 个样本，集中在光照衔接，主要是空边界哈希构造与查找；两个系统采样都不计入门禁。
- 同批补充卸载优化，四个外侧边界没有坐标重叠，直接收集全部非零移除根（保留亮度一）；原删除/重新传播算法不变。三种载入顺序前，先卸载含四个光源的区块，剩余完整光场均归零，重载后恢复。两配置聚焦各 52/52、完整各 1699/1699，客户端构建无新增警告。最终性能未声明通过。
- 夜间火把独立夹具已采集三帧，并查看 4000/10000 ms 原图：火把和受光地面可见，这两个视角未见边界黑缝；敌人后来遮挡中心，不能当作完整夜间正常探索。显式继承环境在[本批证据](adventure-boundary-light-evidence-2026-09-21.json)补记，性能进程不继承该环境。
- 本批 Release `3bb10ef8a953532abafa1a925481cf49112bf84e9af1e3ce9d5ddd26dacea3a0`。所有构建、测试和截图结束后，`/private/tmp/hm3d_boundary_final.py` 开始最终配对，输出 `boundary-final-pairing-r2.log` / `final-boundary-*-r2`；先原坐标，再同类区域及固定 v15 世界。每场完整六次采集后检查，任一 P95/P99 失败即保留整场、停止后续并继续修复，门槛不变。


## B5 绘制成本后续定位（进行中）

- 光照优化已提交 `85376c1`。原坐标三轮林地/旧海面为 P95/P99 1.352/1.397，完整六次采集保留于 `final-boundary-original-r2`；按协议停止后续场景。载入尖峰已改善，但实际新增场景的绘制成本仍未满足护栏，不能声明性能通过。
- 两种距离排序试验均退回：直接逐对象排序破坏材质分组，静止 P95 22.382 ms；保留分组并组内排序为 10.192 ms，也无收益。四个完整试测保留在 `depth-order-rejected-trials.json`，本项目和引擎排序代码均恢复到 `85376c1` 原状态，未将试验作为优化提交。
- 当前试验只为已封闭的不透明 cube 网格前缀剔除背面，随后用原双面状态绘制叶块/资源形状；共享原 GPU 缓冲和材质，归还原剔除状态与完整索引范围。Release `OPAQUE_PREFIX` 34/34，包括六面向外绕序、混合树叶后缀、复制/接纳/清理；原林地 7000 ms 原图已检查地面、树冠与地标完整。当前试测 `opaque-prefix-pilot-r1.log`，完整双配置/性能尚未验收，该试验尚未提交。
- 背面前缀试测仍较慢：静止 P95/P99 10.475/11.253 ms、流送 10.172/11.669 ms。该方式增加每对象剔除状态切换及混合网格的额外 draw，收益不足，六个相关源码/测试文件全部恢复到 `85376c1`；原始采集和 `opaque-prefix-rejected-trial.json` 保留。没有留下上述两类无收益试验的运行时代码。
- 下一步验证同列相邻 section 的合并绘制，针对驱动调用次数；保留每 section 的 CPU 数据、revision/ack 和卸载语义，透明排序保持独立。最终性能与正式客户端交付仍未完成。


## B5 同列不透明区段合并绘制

- 同 x/z 列内最多四个相邻高度 section 共用 solid／flora GPU 对象。全部顶点、索引、UV、重复坐标及光照保持，仅重基到共同原点；CPU 快照副本按 section 保留，改块、卸载和陈旧确认重建所属组，每帧原八 section 确认预算不变。水和玻璃继续独立排序；混合、不写深度、多 pass 或未知程序回退到原逐 section 路径。没有删树、减少几何量或改变生成版本。
- 新增驻留 renderables 计数，并累计分组与独立对象的真实 GPU 缓冲；该数不是实际 draw 次数。Debug／Release 聚焦各 34/34、完整 WorldRuntime 各 1710/1710，双配置客户端构建无新增警告。具体覆盖及日志身份见[分组绘制证据](adventure-render-batch-evidence-2026-09-21.json)。
- 首个原型单轮 1× 林地静止 P95/P99 为 6.113/6.839 ms、流送 5.646/6.619 ms；静止驻留顶点 1550902、索引 2903688、缓冲 61243616 字节与前版同场单轮完全相同。这是试测，不能当成正式三轮通过。
- 当前窗口恢复实际 Retina 2×。预期 1× 的首轮视觉采集被工具拒绝，保留原始 FAIL；r3 固定 2560×1440 后，六个区域/兼容路线各四帧、花朵四帧、阴影玻璃与夜间各三帧全部采集并逐时刻查看。未见合并导致的缺面，花朵消失且背包获得一朵；夜间光源被敌人遮挡，保留限制。
- Computer Use 在当前 Release 正常启动，重开本任务普通难度 seed 42 存档，打开合成，保存返回主菜单，再次载入并保存退出；地形／HUD 正常恢复。十次 W 输入后存档位置仍为 `(36.2999992,92,56.5)`，持续移动／正常采集未通过，不能将诊断破坏补作正常输入证据。用户客户端未操作。
- 当前 Release SHA-256 `3ffe41d4d7f925d3da1e9e349c62c126edc0b04d2ceea59b30f29b3c316dd315`。全部构建、回归、截图和正常窗口结束后，`/private/tmp/hm3d_render_batch_final.py` 开始原坐标→同类区域→固定 v15 世界三轮配对，记录于 `render-batch-final-pairing-r3.log` / `final-render-batch-*-r3`；均按当前真实 2×，不混用旧 1× 样本，原 1.10 护栏和 5+30 秒时间保持。正式性能仍待完成，完整 Goal 保持 active。

- `438b92f` 已本地提交本批实现、聚焦／完整回归和实机证据，未推送。当前原坐标林地两场各三轮已完成：conifer-steady（frame_p95_ms 7.725→6.599 ms，比 0.8542；frame_p99_ms 8.609→8.237 ms，比 0.9568，PASS）。conifer-streaming（frame_p95_ms 8.643→6.955 ms，比 0.8047；frame_p99_ms 10.268→8.233 ms，比 0.8018，PASS）。岩台及后续 matched/control 继续由同一调度执行；这 12 次结果不构成整个性能协议通过。正常窗口已保存退出，当前无构建／完整回归／截图任务。交付摘要本地草稿位于 `build/adventure-terrain-20260921/delivery-draft.md`，最终客户端和产物精简待完整性能后完成。

- 原坐标组 24 次采集全部完成，四场三轮均 PASS：conifer-steady P95/P99 比 0.8542/0.9568；conifer-streaming P95/P99 比 0.8047/0.8018；canyon-steady P95/P99 比 0.6029/0.6439；canyon-streaming P95/P99 比 0.6280/0.5850；原始 frames.csv、完整摘要及 `performance-render-batch-original-r3.json` 保留。同类区域组已自动开始；固定 v15 世界组仍待执行，不能提前宣称三组全部通过。分发清单全部 119 文件（包括图像元信息）与本目标 16 次提交中的 249 个改动文件版本已通过参考标识排除及包哈希核查；此前共享历史不在此次扫描范围，无推送。

- 最终区域覆盖审查追加海中陆块证据：当前 v19 冻结生产宏观列中，按真实填充语义把高度 64 也计为干地，两个完全处于调查内部、海洋边界的粗网格候选为 seed 0 `(-32,67,1824)` 与 seed 20260807 `(1792,66,1280)`。粗样本不证明没有窄陆桥，保留 `island-coarse-inventory-r1/r2.json`；新增 `tools/survey_adventure_islands.cpp` 准备用同一生态列规划逐米细查八邻接、海洋边界和人工陆桥分析负例。源文件已写，构建／执行与实景均排在性能采集结束后，避免负载干扰；尚未把该项写作通过。

- 同类区域组也完成全部 24 次采集，四场三轮全 PASS：conifer-steady P95/P99 比 0.7306/0.7737；conifer-streaming P95/P99 比 0.6634/0.6282；canyon-steady P95/P99 比 0.7056/0.7144；canyon-streaming P95/P99 比 0.5656/0.5509；`performance-render-batch-matched-r3.json` 与全部原始采集保留。当前同一实时调度已进入 control，未重启或并行运行其他性能进程。最终仍需 control 全组、海中陆块细查／实景及精简客户端交付；不得在这些项完成前关闭 Goal。


## B5 最终性能与海岛审查

- 当前运行时代码 `438b92f`，Release `3ffe41d4…6dd315`，实际帧缓冲 2560×1440；与旧版统一 FOV 120、标准画质、中阴影、后处理开启、时刻 6000、视距 8。三类对照各四场、每场三轮交错新旧，共 72 次、364146 帧，P95/P99 中位比均 ≤1.10。原坐标最差比值分别 0.8542/0.9568，同类区域 0.7306/0.7737，固定 v15 世界身份 0.7010/0.6526；完整逐轮结果和成本见[最终性能证据](adventure-final-performance-evidence-2026-09-21.json)。
- 原坐标旧林地位置实际是海洋，新林地有更多几何和内存成本；同类区域也不是完全相同几何。固定 v15 对照使用相同元数据模板及独立新存档，并非整份已生成世界复制。未删除慢样本、未混合旧 1× 与当前 2× 样本；历史失败保留。驻留 renderable 数是 GPU 对象数，不冒充 draw call。
- 当前 Release 世界程序再做三轮 v15/v19 交错生产调查：每轮八 seed、每 seed 四真实区块，所有列／区块摘要与各自冻结基线一致。四区块生成时间的全部 seed/轮次中位数 v15 13.2993 ms、v19 7.7911 ms；不含调查写盘、区块分配、摘要扫描，也不等同窗口载入延迟。
- 对冻结宏观调查中的两处候选海中陆块作 1 m 加密：seed 0 共 30102 个干地格、闭合海水边界 1002 格；seed 20260807 共 6656 个干地格、边界 388 格。以 height≥64 为干地，八邻域包含对角陆桥；向分析副本注入海平面陆桥的负例均能检出。Debug／Release 全部 CSV 字节一致。工具与结果见[海岛证据](adventure-island-evidence-2026-09-21.json)。
- 在出图前冻结六个岸边机位，全部 12 帧已观察，另复看两个原尺寸场景。大岛有棕榈、草地、沙岸与砾岸，小岛是草地及低矮裸岸；水面与地图一致。部分近景敌人遮挡及斜向体素台阶保留，未挑图替代完整记录。海岛围合来自完整网格，不能只由有限视距画面证明；这不是正常玩家环岛步行。
- 本小批新增调查工具／夹具及证据，不修改运行时，无须重复已通过的双配置 1710 项世界回归。剩余为干净客户端、产物精简与最终文档交付。

# 冒险世界体验升级执行记录

## 恢复区

- 范围／退出：[完整规划](../current/adventure-experience-plan-2026-09-22.md) B0–B10 全部必做，
  [Goal](../current/adventure-experience-goal-prompt-2026-09-22.md) 已由用户明确启动，宿主 active；无 token 上限。
- 起始 HEAD：`90951ee3ae7e767f9b2a8a3c6db6635565e5d3ed`；起始工作区干净。
  复用最近运行时 `ee42d30`，save v12／terrain v19；未重新宣称上轮正常采集或持续输入通过。
- 当前：B1a 已提交 `8bfa33d`（视觉: 完善像素日月与云层遮挡），B1b 云形实现／验证通过，准备本地提交。
  后续 B1c 区域光色和阴影复查，再进入 B2；所有后段批次仍为必做。
- 唯一可刷新的工作包：`build/adventure-experience-20260922/Working.app`，
  bundle `local.hellomine3d.adventure-experience-work`；不操作历史客户端。
- 起始程序 SHA-256：`5ce36f2af39fbace92d42fc25feff53c175c4da8623a2cc449db206b9b2eb598`。
  直接采用已验证 Release 程序，119 文件重新打包；记录当前文档提交身份，不冒充重新构建 C++。
- 原包与普通存档保护清单：`build/adventure-experience-20260922/protected-before.json`，49 项哈希。
  启动时没有真实游戏／构建进程；`ps` 在默认沙箱拒绝，获自动审查允许的只读查询后确认，未调整系统权限。
- B1a 性能 session `53575` 已正常结束，18 次／138,338 帧 PASS，记录在 `b1a-performance-r2/`。
- 当前无运行进程。最终 B1b 性能 session `1219` 已正常结束，18 次／143,294 帧 PASS。
  最终画面 `33661`、资源 `13753`、GPU `92878` 均完成，原始结果留在本轮 build 目录。
- 下一步：本地提交 B1b 后接入 B1c 区域氛围与当前八生态／阴影复查。
  `Baseline.app` 保持 B0 原包，`SkyBaseline.app` 为 B1a 冻结资源；两包均不原位刷新。
- 授权：当前完整 Goal、常规实施、独立客户端／正常测试世界、必要验证和中文本地提交；不推送。

## B0 冻结范围与验证表

普通创建种子固定为 **42、20260807、239701883**，普通难度；轻松难度补 seed 42。
前二十分钟按普通输入记录，至少一条完整外出／收获／返家／保存重开回路；已有保存世界仅作为保护对象，
不向其注入物品或修改元数据。新普通测试世界在本轮工作包独立 catalogue 中创建。

八类生态观察点复用 `tools/fixtures/terrain/adventure-ecology-v18.json` 的 seed 42、region 0–7：
草甸 `(1032,74,640)`、林地 `(52,84,-1152)`、高地 `(752,101,-32)`、沙丘 `(780,78,-1012)`、
峡谷 `(1396,110,-1684)`、湿地 `(1620,65,-956)`、雪山 `(1648,127,668)`、海岸 `(-1656,67,24)`。
坐标 Y 为冻结地面高度而非普通玩家传送承诺；诊断相机依据该地面抬高，接近路线与正常玩家移动分开。
连续对照复用 `tools/fixtures/terrain/adventure-v19-waypoint-routes.json`；海岛补充沿用现有岛岸 fixture。
洞穴具体入口在 B7 修改生成器之前从三个冻结种子的生产查询确定并登记，不事后挑选表现好的入口。

| 批次 | 必须核对的范围 | 当前结果 |
| --- | --- | --- |
| B0 | 身份、保护清单、种子／机位、预算、画面与性能基线 | 已冻结；朝日／背日画面及三档各三次帧耗有本轮基线；普通三种子路线待 B2 实走 |
| B1 | 日月／遮挡、云形／动态、八生态、林地海岸雪山四时段、两 FOV、档位回退、阴影 | B1a 实现／聚焦验证完成；B1b 实现／聚焦验证完成；B1c 与普通动态 NOT_RUN |
| B2 | 食物前置、保存目标移出主线、事件引导、三个种子早期节奏、目标／奖励迁移 | NOT_RUN |
| B3 | 三动物、自然栖息、行为与贴地、生命周期、实体成本 | NOT_RUN |
| B4 | 三类地点各两布局、接近线索、三套建造组合、资源／奖励守恒、旧生成保持 | NOT_RUN |
| B5 | 探索持久化、个人标记、返家／任务导航、两视图、未知信息、存储／失效 | NOT_RUN |
| B6 | 世界／机器／设置／任务／地图页面、双语三字号两尺寸、错误与输入反馈 | NOT_RUN |
| B7 | 三地下空间、两目的地、三个种子入口／返程、水体接缝／版本／流送 | NOT_RUN |
| B8 | 第一人称使用、全身角色、基础动作、第三人称遮挡／切换、权威行为保持 | NOT_RUN |
| B9 | 三环境层、河海区分、动物声、四材质脚步、实际听感与暂停／设置 | NOT_RUN |
| B10 | 普通完整回路、地下／视角／声音、当前回归／累计性能／干净包 | NOT_RUN |

画面条件：1280×720 点，当前解锁 Retina 对照为 2560×1440 像素；FOV 90／120，RD8、中文、
UI 1.0、standard 材质。画面时刻 1000／6000／11500／18000，天体定位补 3000／15000；
Off/Off、Medium/Off、High/On（阴影／后处理）分别比较。窗口 framebuffer 不符合预期时记录失败，
不得把 1×／2× 或前后台混成同组。

性能：既有 capture 工具，5 s 预热＋30 s 采样、不截图；每场同条件三对交替，P95/P99 中位数比 ≤1.10。
天空首批固定 seed 42、草甸 `(1024,80,576)` 向天空 `(-44,100.2,0)`，另检查高地常驻／流送
`(752,200,-32)`／`(10,35,0)`；包内原版资源在改动前保持，可交替恢复为同一程序的基线。
后续实体／地图／地下场景各在该批实现前冻结，最终同时对 B0 与上一批评估累计帧耗、RSS、GPU 和生成成本。

## B0 新增系统预算

以下为本 Goal 新能力的上限，进入对应实现前在合同明确统计与边界测试，不替代旧系统已有上限。

| 系统 | 上限／约束 |
| --- | --- |
| B1 天空 | 复用现有天空绘制，不新增 draw call；不新增纹理、RTT、世界查询或逐帧 CPU 噪声；shader 循环须固定有界 |
| 新动物 | 每世界最多 24 个活跃自然动物，玩家 64 m 内最多 12 个；不占用或暗改敌人难度名额 |
| 动物决策 | 每只最多 5 Hz 决策，新增地面／避障查询全局每 fixed tick 最多 48 次，轮转避免饥饿；禁止无界路径搜索 |
| 动物／玩家几何 | 每动物最多 8 个盒体部件（96 三角形），玩家最多 12 部件；动物总新增顶点不超过 10,000，不增加逐实例纹理 |
| 地图 | 继承局部 65×65、195 列／更新和 30 Hz 预算；持久地图运行缓存 ≤16 MiB，磁盘每世界 ≤64 MiB，个人标记 ≤64；满额有提示／明确降采样策略 |
| 声音 | 保留现有全局 16 声部、32 样本、4 MiB 解码样本预算；环境层最多 3 个同时发声，不能饿死交互／危险提示 |
| 相机／地区氛围 | 只查询驻留／纯派生值，不同步加载未知区块；插值与遮挡检测次数在各批合同固定 |

帧时间与内存实际基线保存在三档性能原始记录；上述预算在效果和性能实测前冻结，不因超限结果自动上调。

## 执行与失败记录

### 2026-09-22：启动与 B0 初始核对

- 完整阅读规划、工作流、相关架构、大气／阴影合同与验证矩阵，启动宿主完整 Goal。
- 新工作包程序与 HUD 交付包哈希相同，历史三个用户包程序仍与先前记录一致。
- `b0-sky-morning` 两张 2560×1440 隐藏截图成功，首帧实见云团边缘模糊、云体明暗层次偏弱。
  最初朝向 `(-40,280,0)` 为背太阳方向；保留结果作为云形基线，另按真实相机矩阵在修改前补
  `(-44,100.2,0)` 的朝日基线，不将未朝向太阳的图判为“太阳缺失”。
- B1a 拟仅修改生产天空 shader：默认路径增强像素日月并正确合成遮挡，FS2 回退保持像素一致。
  云形重构、区域氛围和阴影动态仍是后续 B1 范围，不因 B1a 通过而关闭整个 B1。

### B1a：日月与遮挡实施中

- 启动与冻结文档已本地提交 `680f8a5`。新增[天空合同](../contracts/adventure-sky-contract-v1.md)，
  只改生产天空 fragment，不改 C++ 运行时／uniform／存档。太阳采用暖色边缘和内核，
  月亮采用冷色像素坑纹；所有天体与光晕在云之前合成。实际太阳方向基线已查看，旧版为白色圆斑。
- 离屏生产 GPU 36/36 PASS，Apple M1 Pro；工具 `validate_sky_shader_macos.cpp --celestial`。
  两类天体在冻结云柱中的最小透射比为 0.140104／0.137826，原版及删除云混合的反例均检出；
  FS2 白天／夜晚及直视日月逐像素相同，云层上下边界、漂移和天体正午连续通过。
  输出 `b1a-gpu-r1`；该证据是诊断 GPU，不是普通输入。
- 强制重建并运行 Debug／Release ResourcePackSmoke，各 114/114 PASS，
  构建和运行日志为 `b1a-resource-{debug,release}*.log`；没有重复无关完整世界回归。
- 当前工作包已接入候选资源，程序哈希保持起始 Release；`b1a-sunward-fov90`、
  `b1a-sunward-fov120`、`b1a-moonward-fov90` 各两张 2560×1440，已逐组查看首帧。
  FOV 90／120 太阳轮廓清楚，夜间月亮坑纹可辨；尚未以两张帧宣称普通持续移动或整个 B1 完成。
- 为原版／候选交替性能保留第二个独立工作包 `Baseline.app`，不同 bundle ID，
  从修改前已验证工作包仅复制 119 清单文件并复核；它没有用户存档，冻结原版程序和资源。
  同一 Goal 仅复用 Baseline／Working 两包，不为每轮采样新增应用副本。
- 后续 B9 调查补充：当前 `AudioRuntime::platformBackend` 仅 Windows 返回真实后端，macOS 返回空后降级。
  因此本机实际声音能力需要在 B9 内补齐或取得适用平台的真实声音证据，不能仅增加 WAV 和字幕就声称有声。
- 首轮 Off 三对的 P95／P99 中位数比为 **1.4934／1.2512，FAIL**，不放宽 1.10 护栏；
  同组加载 361 区块、地形 GPU 29,405,120 字节一致。当前实现对整片天空计算两套日月投影和坑纹，
  优化为先计算连续像素导数，再以光晕角范围提前返回；避免在非一致分支中求导，保持效果与遮挡。
  此为性能修复原因判断，不将一次性能差异直接证明为唯一原因；首轮其余档位均运行完原候选，未混入优化资源。
- 首轮全部 18 次／150,075 帧保存在 `b1a-performance/`，`comparison.json` 结果 FAIL。
  Off 的原版／候选 P95 为 2.728／4.074 ms，P99 为 4.268／5.340 ms；
  Medium 为 5.531／5.023、6.634／6.446 ms；High 为 6.767／6.778、7.408／7.427 ms。
  原始 CSV、失败记录及候选 shader 均保留，未删除慢样本。
- 优化源码 SHA-256 `6162f41acbecc1fdc8cf3e58de7bea7dc13d1dbf416d2e8950688403b101a568`，
  与当前 Working 资源一致。GPU 第二轮 36/36 PASS（`b1a-gpu-r2.log`），
  Debug／Release 资源各 114/114 PASS（`b1a-resource-r2-{Debug,Release}.log`）；
  复用对应配置对象重新链接资源检查程序，未冒充整个游戏重建。
- 优化后 `b1a-r2-sun90`、`b1a-r2-moon120`、`b1a-r2-fallback` 各两帧，已查看首帧；
  默认像素日月与旧大气圆形回退分别成立。普通输入和 B1 全范围动态仍待后续检查。
- 第二轮全部 18 次／138,338 帧通过，按原 CSV 重算分位数并核对包、配置、framebuffer、实际模拟与采样条件。
  三档同轮原版→候选 P95／P99 中位数如下（单位 ms）：

  | 阴影／后处理 | P95 原版→候选 | 比值 | P99 原版→候选 | 比值 |
  | --- | --- | --- | --- | --- |
  | Off／Off | 4.687 → 4.655 | 0.99317 | 5.668 → 5.617 | 0.99100 |
  | Medium／Off | 5.731 → 5.747 | 1.00279 | 6.592 → 6.552 | 0.99393 |
  | High／On | 6.696 → 6.747 | 1.00762 | 7.596 → 7.506 | 0.98815 |

  不混合两轮绝对耗时；当前结果仅代表同轮交替测量，不把性能变化归因为已证明的单一硬件或 shader 原因。
- 每次 361 驻留区块、4 个自然敌人，地形 GPU buffer 29,397,984–29,405,120 字节；没有新云纹理、RTT、实体或缓存。
  三档峰值 RSS 的三次中位数原版→候选为 339.45→342.19、310.70→324.05、314.09→315.46 MiB。
  网格构建平均 1.037–1.049 ms／section；一次保存 1.904–2.476 ms；原始每次数据保留。
  这些是进程 RSS 与已统计地形 GPU buffer，非整个驱动 GPU 显存测量。
- 已额外查看优化后朝日／朝月第二帧，云与天体持续位移，没有在这两次固定视角中发现跳变；
  不代替转头／步行／完整阴影检查。49 项用户包、配置、存档元数据哈希仍一致。
- B1a 无持久语义迁移：save v12、terrain v19、objective v3 保持。B1b 云形与 B1c 地区、八生态和动态验收继续必做。


### B1b：云团实施与验证

- B1a 提交 `8bfa33d` 后才接入本批。新增临时性能基线 `SkyBaseline.app`，
  bundle `local.hellomine3d.adventure-sky-baseline`，119 清单文件、约 51 MiB，无用户存档；
  复用同一 Working 包，不为每张截图复制应用。B1 关闭后整理阶段基线。
- 两个采样使用同一世界格子场，约 7.67 m 的块状轮廓与连续倒角、深底面和明侧面；
  保留太阳／月亮遮挡及 FS2 完整回退，未增加 shader 接口、纹理或持久状态。
- 首版生产 GPU 41/41、双配置资源各 114/114 PASS；首版 `b1b-{sun90,sun120,moon120,forest,coast,alpine}`
  各两帧、`b1b-sweep` 八帧。已看六组静态首帧；固定海岸机位朝向内陆，不能将其当作水面远景检查。
  雪山及大 FOV 中发现局部淡薄方片，收窄密度过渡范围 0.018→0.006；保留首版，不因自动检查通过忽略实景问题。
- 修正后 GPU 41/41 PASS（`b1b-gpu-refined.log`），包括 60 Hz 昼夜漂移、正负坐标风移不变性、
  日月遮挡与错误反例、上下边界和原版回退。冻结云柱画面中，过渡像素比例从旧版 0.01953 降至 0.00954，
  有云覆盖比例 0.10416，仍保留留白；该数值只为诊断云柱，不冒充全世界云量。
- 当前生产云 shader SHA-256 `e3c28f2a2e8e8c0ad9d774c44f3d5f561cf6ebdc271603a1554815499c89e83e`。
- 修正后双配置资源各 114/114 PASS，日志 `b1b-resource-r2-{Debug,Release}.log`；
  `b1b-r2-*` 五组静态各两帧、一组诊断转头八帧全部 CAPTURED。已看朝日／朝月／雪山首帧，
  以及 sweep 的 5／8／11／15 秒；其余帧在本批继续审阅。没有把诊断相机写成玩家正常输入。
- B1c 准备采用当前生成器的纯 biome／高度查询，世界 64 m 网格四角缓存，单次最多 8 次纯查询，
  每帧只插值；缓存不超过 128 字节，切世界清空。候选实现与检查暂存忽略目录，尚未进入生产路径，
  不在 B1b 性能采样期间构建或刷新包。
- 继续查看 `b1b-r2-coast` 第二帧与八帧 sweep 时发现孤立细线：连续坐标变形再采样噪声可在格子过渡处
  形成窄小的非实体云片。B1b 暂不提交；当前性能按原修正版跑完并保留，下一原型改为世界格子中心的实体覆盖，
  继续守住现有连续漂移检查，不以放宽门槛消除失败。
- 前一修正版性能完成 18 次／137,691 帧，三档 P95 比 0.99851／0.98602／1.00480，
  P99 比 1.01919／1.02165／1.01213，性能 PASS；但孤立细线导致本版视觉未验收，不将性能通过写成批次完成。
- 直接采用实体格子中心的原型被 `day-continuous-drift` 检出 FAIL（`b1b-gpu-cells.log`），未接入生产包。
  最终修复先对细格四角确定实体覆盖，再连续混合覆盖；四角共用同一粗格的四个噪声值，
  每条射线两次层采样共八个 hash，较旧有六个噪声 octave 的 24 个 hash 减少。
- 最终离屏 `b1b-gpu-filtered-crisp.log` 41/41 PASS；60 Hz 昼／夜最大平均通道差为 0.003681／0.001404，
  沿用原 0.05 护栏；云边过渡像素比例为 0.008987（B0 0.019531），云覆盖 0.135132。
  日月云后透射仍为 0.140104／0.137826；正负坐标风移及错误反例通过。
- B1c 忽略目录的区域气候缓存原型已在 O0／O3 各通过 22 个纯逻辑检查，88 字节缓存，
  包括采样上限、重复查询、正负／极值坐标、连续边界、切世界重置、昼夜与水介质保持；生产路径尚未接入。
- 最终 B1b shader SHA-256 `38906d570cff12c267f7b8c24811c12e1edb1c8ebc33cf9fa5fe3c1ce4279005`，之前的候选哈希仅属历史记录。
- 最终 `b1b-r4-sun120`／`b1b-r4-coast` 第二帧已查看，原孤立细线消失；云团保留块状边界、明暗厚度与天空留白。
  最终资源双配置各 114/114 PASS（`b1b-resource-r4-{Debug,Release}.log`）。
  与 B1a 的最终性能正在重新采集，不能沿用前候选的 PASS。
- 最终 `b1b-r4-sweep` 八帧现已全部查看（5–15 秒），`b1b-r4-alpine` 第二帧也已查看；
  未再见孤立细线或整片云跳变。此为限定诊断连续视角，不替代普通行进。
- 用户保护清单复核 49/49 一致（`b1b-protected-check.json`）。一次核验脚本错误地重复拼接相对路径，
  已按清单原路径修正；该次脚本失败不代表用户文件发生变化。
- 最终 B1b 对 B1a 三档各三对、18 次／143,294 帧 **PASS**（`b1b-performance-r2/comparison.json`）。
  同轮原始 CSV 重算分位数，核对程序、设置、真实 framebuffer、预热与采样；后台 2560×1440，没有截图编码。

  | 阴影／后处理 | P95 原版→候选 ms | 比值 | P99 原版→候选 ms | 比值 |
  | --- | --- | --- | --- | --- |
  | off／off | 4.727 → 4.615 | 0.97631 | 5.747 → 5.703 | 0.99234 |
  | medium／off | 5.683 → 5.712 | 1.00510 | 6.524 → 6.611 | 1.01334 |
  | high／on | 5.035 → 5.270 | 1.04667 | 6.037 → 6.323 | 1.04737 |

- 三档峰值 RSS 的三次中位数原版→候选为 340.39→342.03、313.26→315.36、315.65→311.35 MiB。网格平均构建 1.031–1.046 ms；单次保存 1.853–2.337 ms；
  原始驻留／GPU buffer 指标保留在各次 summary；新天空没有新增纹理、RTT、实体或 CPU 缓存。
- B1b 没有持久化迁移，save v12／terrain v19／objective v3 保持；B1c 与后段仍继续实施。

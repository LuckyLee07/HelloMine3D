# 冒险世界体验升级执行记录

## 恢复区

- **2026-09-25 UI 定稿实施中**：用户认可紧凑 HUD 及任务／地图／暂停设计稿，要求尽量一致落地。
  起点 `2cd457f`，工作区干净；本轮先统一常驻 HUD，再任务／暂停，最后两类地图与标记侧栏。
  save v12、terrain v22、exploration.hmap v4 保持。只读授权进程核对无游戏实例，保护用户包和存档；
  旧立体地图在制补丁继续保留，未混入本批。当前显示器查询只有 GPU，视觉及三对性能尚未运行。
  设计与验证产物仅本地 `build/ui-upgrade-20260925/`，设计 PNG 保留原本地目录，不提交。
  B6a：独立快捷槽、细生命条、两行任务卡、低干扰按键提示和描边玩家箭头已实现；
  Debug／Release 客户端编译成功，HUD 输入各 42／地图几何各 26 通过，资产 89 通过、双语 632 keys 对齐。
  B6a 已提交 `fc2982f`。B6b 任务／暂停已完成双配置编译，资源 114、输入／设置 99 通过；
  本批构建 handle 均已结束；下一步两类地图与标记侧栏。当前工程证据不代表设计一致性已实机通过。

- **2026-09-24 最新恢复点**：本轮七项审查修复已完成代码与无窗口工程验证，动物 `f112f52`、
  任务绑定 `e211652`，地图编辑／指针／状态反馈 `a8bcb44`。当前 save v12、terrain v22、
  exploration.hmap v4；旧 v1–v3 可读。完整 B0–B10 Goal 尚未完成。
  本批构建／检查 handles 均已结束；包内无窗口验证 `88706` 正常退出，图形窗口启动 0 次。只读进程查询未发现游戏实例，
  `system_profiler SPDisplaysDataType` 仍仅列 GPU、未列活动显示器，未重复尝试已失败的图形启动。
  普通输入、视觉、双语尺寸矩阵与同机三对性能保持 `NOT_RUN`；不将无窗口 PASS 当作实机验收。
  原未完成立体地图工作完整保留于忽略目录 `build/review-fixes-20260924/relief-in-progress.patch`，
  当前修复客户端不包含该在制补丁；后续继续 B5 两视图整合。未动用户 `Working.app` 或其存档。

- 范围／退出：[完整规划](../current/adventure-experience-plan-2026-09-22.md) B0–B10 全部必做，
  [Goal](../current/adventure-experience-goal-prompt-2026-09-22.md) 已由用户明确启动，宿主最近查询为 usageLimited；无 token 上限，未把阶段交付标为总 Goal 完成。
- 起始 HEAD：`90951ee3ae7e767f9b2a8a3c6db6635565e5d3ed`；起始工作区干净。
  复用最近运行时 `ee42d30`，save v12／terrain v19；未重新宣称上轮正常采集或持续输入通过。
- 当前：B1a 已提交 `8bfa33d`，B1b 已提交 `c1a3b72`，B1c 区域实现已提交 `2095c73`、实景／性能仍待集中验收；B1e 云层工程候选已提交 `174bc39`，实景／动态／性能未通过最终验收。B2a 已本地提交 `8b0987d`，B2b 已提交 `006b4ce`，B2c 已提交 `e6b70bd`。B4a 已提交 `f540b00`，B4b 已提交 `a2d905a`，B4c 已提交 `08da00f`，B4d 已提交 `9c66774`；B4 普通探索与性能待集中验收。
  用户明确反馈日月／云形过方、观感不自然，B1d 圆滑云形也被用户否定，当前优先 B1e 云层重做；各批工程 PASS 不代表美术获认可。
  用户要求降低客户端启动频率；B1 剩余完整场景／性能集中至阶段整合，域逻辑和 shader 先做无窗口检查，稳定候选再一次会话集中观察。B2／B3／B4 普通输入、画面与性能仍待办。所有验收与后段批次仍为必做。
  B5a 观察归档、B5b 文件／备份／真实采样保存及 B5c 标记值层／文件 v4／World 命令已通过双配置无窗口检查。平面图、个人标记管理、北向追踪提示、4–64 m 每格的真实记录缩放总览和已知界石任务地点已接入；真实放置／拆除、远区重开及备份的工程回归通过。画面、输入、保存重开界面回路与普通玩家验收仍待完成，不把编译等同于界面验收。
  前一 `9c493c1` 候选运行截图尝试一次后因 OpenGL 3 上下文不可用退出，无新图；当前更晚 UI 版本尚未尝试窗口；本地已整理四张明确标为历史版本的真实截图及失败记录。恢复图形环境后再补当前图，不重复盲启。
- `Working.app` 已由用户实际操作，当前属于保护客户端，不关闭、不刷新。PID `80477` 的 cwd
  已核对至此包，普通世界 `world-9e3125aa7ef33202ab236751e6a02499`；程序／配置／元数据保护快照
  `build/adventure-experience-20260922/protected-user-b1c.json`。用户游戏中的保存变化不回滚。
- 新天空候选改用独立 `SkyWorkbench.app`，后续只复用这个工作包；不操作用户或历史客户端。
- 起始程序 SHA-256：`5ce36f2af39fbace92d42fc25feff53c175c4da8623a2cc449db206b9b2eb598`。
  直接采用已验证 Release 程序，119 文件重新打包；记录当前文档提交身份，不冒充重新构建 C++。
- 原包与普通存档保护清单：`build/adventure-experience-20260922/protected-before.json`，49 项哈希。
  启动时没有真实游戏／构建进程；`ps` 在默认沙箱拒绝，获自动审查允许的只读查询后确认，未调整系统权限。
- B1a 性能 session `53575` 已正常结束，18 次／138,338 帧 PASS，记录在 `b1a-performance-r2/`。
- B1c 画面 session `29338`（候选 30 组）、`43676`（基线八生态）均已结束；双配置客户端 `38538`
  与 V10C `49961` 均完成，后者各 28/28。旧 handle 不再视作运行中。B1c 性能尚未开始。
- B1d 34 组画面 session `15042` 已全部完成。GPU 45/45、双配置资源 114/114 完成。
- B1d 常驻性能 session `67084` 已结束，18 次／90,124 帧，Off P95 比 1.11164，FAIL；未运行该失败候选流送。当前无性能采样。
- B1e 画面 session `48769` 已按用户反馈停止（runner PID `84167`、SIGTERM、exit 143）；当前采样自然结束。已核对无诊断或游戏进程，未关闭用户客户端。
- 当前运行：无本批构建／GPU handle；B1e Debug `71156`、Release `87737`、聚焦 `94514`、新增净空检查 `56378`／`64092`／`77576` 均已结束。B4d Release 完整 WorldRuntime handle `38666` 已结束，**1974/1974 PASS**。B1e 双配置 V10C 各 **29/29**、资源各 **114/114**、客户端编译和资源清单通过；其当时 Release 程序 SHA-256 `32f8f26a6e177f232f896ef3c810268509a6fa4459b398b3aa4af11df10ee7f9`。默认沙箱只读 `ps` 本轮遭系统拒绝，未据此断言最新进程；最近一次获准核对没有游戏进程。独立 SkyWorkbench 仍为旧 r7 shader／旧云高，**不含当前代码**，不可宣称当前实景。B1e GPU r4 47/47 通过，本轮游戏窗口启动 **0 次**；B2 普通输入与 B3／B4 画面／性能仍待集中实机。禁止恢复逐场景冷启动。
  B5b Release 客户端编译 SHA-256 `da316a4209af7dba2c6f865ac0b5dd580f1132335b6ba8721ef540368408e374`；B5b 运行接线候选双配置编译后 Release 程序 SHA-256 `faa7763b6d487221534d6a935bef9e041a8c4d693664365fe4803de39de86ce9`。均只代表二进制构建；没有刷新或运行工作包。
  B5c 文件 v2 当前 Release 程序 SHA-256 `43e17b4ad4ea5a81b425a5f569dbc1d330c28b476863807fee2e39a4a9e5caa4`，只编译、未打包或启动。
  B5c World 标记命令当前 Release 程序 SHA-256 `05af03df1c5cace7cd4cd48e85fc55ecac7a86b7e090cb6d488cf2d308e6019a`，只编译、未打包或启动。
  B5c 平面地图首批 Release 程序 SHA-256 `7e658d97cebc23bc6cc2e1ab91b994e013754c947fb5c75466f05af687e66799`，只编译、未打包或启动。
  B5c 标记页面／追踪提示候选 Release 程序 SHA-256 `d69fc7397078a09b0f475e9cd4db1aba07b68b94b05e890ac1b43e82c43b65c2`，只编译、未打包或启动。
  B5c 缩放总览候选 Release 程序 SHA-256 `f0d6f32aa06455ab129d6fd5a17f903834f08d858f11883b55f29b2af6b62793`，只编译、未打包或启动。
  B5c 已知任务地点候选 Release 程序 SHA-256 `a76dc0bbb64bdd0834b859621742f4491af652def0c27b76a1d6a53e59eb4bcc`，只编译、未打包或启动。
  B5c 任务地点持久化候选 Release 程序 SHA-256 `24dfce81ab167cd5b160de690c41984c059a715c3af2f2d3ee65a16c7a716207`，只编译、未打包或启动。
  `RegionBaseline.app` 冻结 B1b，`Baseline.app` 保持 B0；不原位刷新冻结包。
- 授权：当前完整 Goal、常规实施、独立客户端／正常测试世界、必要验证和中文本地提交；不推送。

## B0 冻结范围与验证表

普通创建种子固定为 **42、20260807、239701883**，普通难度；轻松难度补 seed 42。
前二十分钟按普通输入记录，至少一条完整外出／收获／返家／保存重开回路；已有保存世界仅作为保护对象，
不向其注入物品或修改元数据。新普通测试世界在本轮工作包独立 catalogue 中创建。

八类生态观察点复用 `tools/fixtures/terrain/adventure-ecology-v18.json` 的 seed 42、region 0–7：
草甸 `(1032,74,640)`、林地 `(52,84,-1152)`、高地 `(752,101,-32)`、沙丘 `(780,78,-1012)`、
峡谷 `(1396,110,-1684)`、湿地 `(1620,65,-956)`、雪山 `(1648,127,668)`、海岸 `(-1656,67,24)`。
坐标 Y 为冻结地面高度而非普通玩家传送承诺；诊断相机依据该地面抬高，接近路线与正常玩家移动分开。
连续对照复用 `tools/fixtures/terrain/adventure-v19-waypoint-routes.json`；海岛补充沿用现有岛岸 fixture。
洞穴具体入口在 B7 修改生成器之前从三个冻结种子的生产查询确定并登记，不事后挑选表现好的入口。

| 批次 | 必须核对的范围 | 当前结果 |
| --- | --- | --- |
| B0 | 身份、保护清单、种子／机位、预算、画面与性能基线 | 已冻结；朝日／背日画面及三档各三次帧耗有本轮基线；普通三种子路线待 B2 实走 |
| B1 | 日月／遮挡、云形／动态、八生态、林地海岸雪山四时段、两 FOV、档位回退、阴影 | B1a/b/c 及 B1e 工程候选已提交；新候选实景、动态、性能与最终区域整合待集中验收 |
| B2 | 食物前置、保存目标移出主线、事件引导、三个种子早期节奏、目标／奖励迁移 | B2a／B2b／B2c 已提交，前期任务、状态提示、危险／种植工程完成；普通路线及阶段 UI／性能待验 |
| B3 | 三动物、自然栖息、行为与贴地、生命周期、实体成本 | B3a／B3b 领域、模型与材质工程 PASS；普通画面、密集流送及同机性能仍 NOT_RUN |
| B4 | 三类地点各两布局、接近线索、三套建造组合、资源／奖励守恒、旧生成保持 | B4a terrain v20 六布局、B4b 三套材料配方、B4c terrain v21 接近线索工程 PASS；B4d terrain v22 三类工位双配置聚焦与 Release 完整回归 PASS；普通探索／画面／性能仍 NOT_RUN |
| B5 | 探索持久化、个人标记、返家／任务导航、两视图、未知信息、存储／失效 | B5a/B5b 工程层及 B5c 格式 v4／World 命令通过；平面图、4–64 m 缩放、标记入口、追踪提示及已知界石地点已编译但未实机验；单一任务地点跨重启工程证据通过，两视图整合、普通回路与性能仍 NOT_RUN |
| B6 | 世界／机器／设置／任务／地图页面、双语三字号两尺寸、错误与输入反馈 | NOT_RUN |
| B7 | 三地下空间、两目的地、三个种子入口／返程、水体接缝／版本／流送 | NOT_RUN |
| B8 | 第一人称使用、全身角色、基础动作、第三人称遮挡／切换、权威行为保持 | NOT_RUN |
| B9 | 三环境层、河海区分、动物声、四材质脚步、实际听感与暂停／设置 | NOT_RUN |
| B10 | 普通完整回路、地下／视角／声音、当前回归／累计性能／干净包 | NOT_RUN |

画面条件：1280×720 点，当前解锁 Retina 对照为 2560×1440 像素；FOV 90／120，RD8、中文、
UI 1.0、standard 材质。画面时刻 1000／6000／11500／18000，天体定位补 3000／15000；
Off/Off、Medium/Off、High/On（阴影／后处理）分别比较。窗口 framebuffer 不符合预期时记录失败，
不得把 1×／2× 或前后台混成同组。

性能：既有 capture 工具，5 s 预热＋30 s 采样、不截图；每场同条件三对交替，P95/P99 中位数比 ≤1.10。
天空首批固定 seed 42、草甸 `(1024,80,576)` 向天空 `(-44,100.2,0)`，另检查高地常驻／流送
`(752,200,-32)`／`(10,35,0)`；包内原版资源在改动前保持，可交替恢复为同一程序的基线。
后续实体／地图／地下场景各在该批实现前冻结，最终同时对 B0 与上一批评估累计帧耗、RSS、GPU 和生成成本。

## B0 新增系统预算

以下为本 Goal 新能力的上限，进入对应实现前在合同明确统计与边界测试，不替代旧系统已有上限。

| 系统 | 上限／约束 |
| --- | --- |
| B1 天空 | 复用现有天空绘制，不新增 draw call；不新增纹理、RTT、世界查询或逐帧 CPU 噪声；shader 循环须固定有界 |
| 新动物 | 每世界最多 24 个活跃自然动物，玩家 64 m 内最多 12 个；不占用或暗改敌人难度名额 |
| 动物决策 | 每只最多 5 Hz 决策，新增地面／避障查询全局每 fixed tick 最多 48 次，轮转避免饥饿；禁止无界路径搜索 |
| 动物／玩家几何 | 每动物最多 8 个盒体部件（96 三角形），玩家最多 12 部件；动物总新增顶点不超过 10,000，不增加逐实例纹理 |
| 地图 | 继承局部 65×65、195 列／更新和 30 Hz 预算；持久地图运行缓存 ≤16 MiB，磁盘每世界 ≤64 MiB，个人标记 ≤64；满额有提示／明确降采样策略 |
| 声音 | 保留现有全局 16 声部、32 样本、4 MiB 解码样本预算；环境层最多 3 个同时发声，不能饿死交互／危险提示 |
| 相机／地区氛围 | 只查询驻留／纯派生值，不同步加载未知区块；插值与遮挡检测次数在各批合同固定 |

帧时间与内存实际基线保存在三档性能原始记录；上述预算在效果和性能实测前冻结，不因超限结果自动上调。

## 执行与失败记录

### 2026-09-22：启动与 B0 初始核对

- 完整阅读规划、工作流、相关架构、大气／阴影合同与验证矩阵，启动宿主完整 Goal。
- 新工作包程序与 HUD 交付包哈希相同，历史三个用户包程序仍与先前记录一致。
- `b0-sky-morning` 两张 2560×1440 隐藏截图成功，首帧实见云团边缘模糊、云体明暗层次偏弱。
  最初朝向 `(-40,280,0)` 为背太阳方向；保留结果作为云形基线，另按真实相机矩阵在修改前补
  `(-44,100.2,0)` 的朝日基线，不将未朝向太阳的图判为“太阳缺失”。
- B1a 拟仅修改生产天空 shader：默认路径增强像素日月并正确合成遮挡，FS2 回退保持像素一致。
  云形重构、区域氛围和阴影动态仍是后续 B1 范围，不因 B1a 通过而关闭整个 B1。

### B1a：日月与遮挡实施中

- 启动与冻结文档已本地提交 `680f8a5`。新增[天空合同](../contracts/adventure-sky-contract-v1.md)，
  只改生产天空 fragment，不改 C++ 运行时／uniform／存档。太阳采用暖色边缘和内核，
  月亮采用冷色像素坑纹；所有天体与光晕在云之前合成。实际太阳方向基线已查看，旧版为白色圆斑。
- 离屏生产 GPU 36/36 PASS，Apple M1 Pro；工具 `validate_sky_shader_macos.cpp --celestial`。
  两类天体在冻结云柱中的最小透射比为 0.140104／0.137826，原版及删除云混合的反例均检出；
  FS2 白天／夜晚及直视日月逐像素相同，云层上下边界、漂移和天体正午连续通过。
  输出 `b1a-gpu-r1`；该证据是诊断 GPU，不是普通输入。
- 强制重建并运行 Debug／Release ResourcePackSmoke，各 114/114 PASS，
  构建和运行日志为 `b1a-resource-{debug,release}*.log`；没有重复无关完整世界回归。
- 当前工作包已接入候选资源，程序哈希保持起始 Release；`b1a-sunward-fov90`、
  `b1a-sunward-fov120`、`b1a-moonward-fov90` 各两张 2560×1440，已逐组查看首帧。
  FOV 90／120 太阳轮廓清楚，夜间月亮坑纹可辨；尚未以两张帧宣称普通持续移动或整个 B1 完成。
- 为原版／候选交替性能保留第二个独立工作包 `Baseline.app`，不同 bundle ID，
  从修改前已验证工作包仅复制 119 清单文件并复核；它没有用户存档，冻结原版程序和资源。
  同一 Goal 仅复用 Baseline／Working 两包，不为每轮采样新增应用副本。
- 后续 B9 调查补充：当前 `AudioRuntime::platformBackend` 仅 Windows 返回真实后端，macOS 返回空后降级。
  因此本机实际声音能力需要在 B9 内补齐或取得适用平台的真实声音证据，不能仅增加 WAV 和字幕就声称有声。
- 首轮 Off 三对的 P95／P99 中位数比为 **1.4934／1.2512，FAIL**，不放宽 1.10 护栏；
  同组加载 361 区块、地形 GPU 29,405,120 字节一致。当前实现对整片天空计算两套日月投影和坑纹，
  优化为先计算连续像素导数，再以光晕角范围提前返回；避免在非一致分支中求导，保持效果与遮挡。
  此为性能修复原因判断，不将一次性能差异直接证明为唯一原因；首轮其余档位均运行完原候选，未混入优化资源。
- 首轮全部 18 次／150,075 帧保存在 `b1a-performance/`，`comparison.json` 结果 FAIL。
  Off 的原版／候选 P95 为 2.728／4.074 ms，P99 为 4.268／5.340 ms；
  Medium 为 5.531／5.023、6.634／6.446 ms；High 为 6.767／6.778、7.408／7.427 ms。
  原始 CSV、失败记录及候选 shader 均保留，未删除慢样本。
- 优化源码 SHA-256 `6162f41acbecc1fdc8cf3e58de7bea7dc13d1dbf416d2e8950688403b101a568`，
  与当前 Working 资源一致。GPU 第二轮 36/36 PASS（`b1a-gpu-r2.log`），
  Debug／Release 资源各 114/114 PASS（`b1a-resource-r2-{Debug,Release}.log`）；
  复用对应配置对象重新链接资源检查程序，未冒充整个游戏重建。
- 优化后 `b1a-r2-sun90`、`b1a-r2-moon120`、`b1a-r2-fallback` 各两帧，已查看首帧；
  默认像素日月与旧大气圆形回退分别成立。普通输入和 B1 全范围动态仍待后续检查。
- 第二轮全部 18 次／138,338 帧通过，按原 CSV 重算分位数并核对包、配置、framebuffer、实际模拟与采样条件。
  三档同轮原版→候选 P95／P99 中位数如下（单位 ms）：

  | 阴影／后处理 | P95 原版→候选 | 比值 | P99 原版→候选 | 比值 |
  | --- | --- | --- | --- | --- |
  | Off／Off | 4.687 → 4.655 | 0.99317 | 5.668 → 5.617 | 0.99100 |
  | Medium／Off | 5.731 → 5.747 | 1.00279 | 6.592 → 6.552 | 0.99393 |
  | High／On | 6.696 → 6.747 | 1.00762 | 7.596 → 7.506 | 0.98815 |

  不混合两轮绝对耗时；当前结果仅代表同轮交替测量，不把性能变化归因为已证明的单一硬件或 shader 原因。
- 每次 361 驻留区块、4 个自然敌人，地形 GPU buffer 29,397,984–29,405,120 字节；没有新云纹理、RTT、实体或缓存。
  三档峰值 RSS 的三次中位数原版→候选为 339.45→342.19、310.70→324.05、314.09→315.46 MiB。
  网格构建平均 1.037–1.049 ms／section；一次保存 1.904–2.476 ms；原始每次数据保留。
  这些是进程 RSS 与已统计地形 GPU buffer，非整个驱动 GPU 显存测量。
- 已额外查看优化后朝日／朝月第二帧，云与天体持续位移，没有在这两次固定视角中发现跳变；
  不代替转头／步行／完整阴影检查。49 项用户包、配置、存档元数据哈希仍一致。
- B1a 无持久语义迁移：save v12、terrain v19、objective v3 保持。B1b 云形与 B1c 地区、八生态和动态验收继续必做。


### B1b：云团实施与验证

- B1a 提交 `8bfa33d` 后才接入本批。新增临时性能基线 `SkyBaseline.app`，
  bundle `local.hellomine3d.adventure-sky-baseline`，119 清单文件、约 51 MiB，无用户存档；
  复用同一 Working 包，不为每张截图复制应用。B1 关闭后整理阶段基线。
- 两个采样使用同一世界格子场，约 7.67 m 的块状轮廓与连续倒角、深底面和明侧面；
  保留太阳／月亮遮挡及 FS2 完整回退，未增加 shader 接口、纹理或持久状态。
- 首版生产 GPU 41/41、双配置资源各 114/114 PASS；首版 `b1b-{sun90,sun120,moon120,forest,coast,alpine}`
  各两帧、`b1b-sweep` 八帧。已看六组静态首帧；固定海岸机位朝向内陆，不能将其当作水面远景检查。
  雪山及大 FOV 中发现局部淡薄方片，收窄密度过渡范围 0.018→0.006；保留首版，不因自动检查通过忽略实景问题。
- 修正后 GPU 41/41 PASS（`b1b-gpu-refined.log`），包括 60 Hz 昼夜漂移、正负坐标风移不变性、
  日月遮挡与错误反例、上下边界和原版回退。冻结云柱画面中，过渡像素比例从旧版 0.01953 降至 0.00954，
  有云覆盖比例 0.10416，仍保留留白；该数值只为诊断云柱，不冒充全世界云量。
- 当前生产云 shader SHA-256 `e3c28f2a2e8e8c0ad9d774c44f3d5f561cf6ebdc271603a1554815499c89e83e`。
- 修正后双配置资源各 114/114 PASS，日志 `b1b-resource-r2-{Debug,Release}.log`；
  `b1b-r2-*` 五组静态各两帧、一组诊断转头八帧全部 CAPTURED。已看朝日／朝月／雪山首帧，
  以及 sweep 的 5／8／11／15 秒；其余帧在本批继续审阅。没有把诊断相机写成玩家正常输入。
- B1c 准备采用当前生成器的纯 biome／高度查询，世界 64 m 网格四角缓存，单次最多 8 次纯查询，
  每帧只插值；缓存不超过 128 字节，切世界清空。候选实现与检查暂存忽略目录，尚未进入生产路径，
  不在 B1b 性能采样期间构建或刷新包。
- 继续查看 `b1b-r2-coast` 第二帧与八帧 sweep 时发现孤立细线：连续坐标变形再采样噪声可在格子过渡处
  形成窄小的非实体云片。B1b 暂不提交；当前性能按原修正版跑完并保留，下一原型改为世界格子中心的实体覆盖，
  继续守住现有连续漂移检查，不以放宽门槛消除失败。
- 前一修正版性能完成 18 次／137,691 帧，三档 P95 比 0.99851／0.98602／1.00480，
  P99 比 1.01919／1.02165／1.01213，性能 PASS；但孤立细线导致本版视觉未验收，不将性能通过写成批次完成。
- 直接采用实体格子中心的原型被 `day-continuous-drift` 检出 FAIL（`b1b-gpu-cells.log`），未接入生产包。
  最终修复先对细格四角确定实体覆盖，再连续混合覆盖；四角共用同一粗格的四个噪声值，
  每条射线两次层采样共八个 hash，较旧有六个噪声 octave 的 24 个 hash 减少。
- 最终离屏 `b1b-gpu-filtered-crisp.log` 41/41 PASS；60 Hz 昼／夜最大平均通道差为 0.003681／0.001404，
  沿用原 0.05 护栏；云边过渡像素比例为 0.008987（B0 0.019531），云覆盖 0.135132。
  日月云后透射仍为 0.140104／0.137826；正负坐标风移及错误反例通过。
- B1c 忽略目录的区域气候缓存原型已在 O0／O3 各通过 22 个纯逻辑检查，88 字节缓存，
  包括采样上限、重复查询、正负／极值坐标、连续边界、切世界重置、昼夜与水介质保持；生产路径尚未接入。
- 最终 B1b shader SHA-256 `38906d570cff12c267f7b8c24811c12e1edb1c8ebc33cf9fa5fe3c1ce4279005`，之前的候选哈希仅属历史记录。
- 最终 `b1b-r4-sun120`／`b1b-r4-coast` 第二帧已查看，原孤立细线消失；云团保留块状边界、明暗厚度与天空留白。
  最终资源双配置各 114/114 PASS（`b1b-resource-r4-{Debug,Release}.log`）。
  与 B1a 的最终性能正在重新采集，不能沿用前候选的 PASS。
- 最终 `b1b-r4-sweep` 八帧现已全部查看（5–15 秒），`b1b-r4-alpine` 第二帧也已查看；
  未再见孤立细线或整片云跳变。此为限定诊断连续视角，不替代普通行进。
- 用户保护清单复核 49/49 一致（`b1b-protected-check.json`）。一次核验脚本错误地重复拼接相对路径，
  已按清单原路径修正；该次脚本失败不代表用户文件发生变化。
- 最终 B1b 对 B1a 三档各三对、18 次／143,294 帧 **PASS**（`b1b-performance-r2/comparison.json`）。
  同轮原始 CSV 重算分位数，核对程序、设置、真实 framebuffer、预热与采样；后台 2560×1440，没有截图编码。

  | 阴影／后处理 | P95 原版→候选 ms | 比值 | P99 原版→候选 ms | 比值 |
  | --- | --- | --- | --- | --- |
  | off／off | 4.727 → 4.615 | 0.97631 | 5.747 → 5.703 | 0.99234 |
  | medium／off | 5.683 → 5.712 | 1.00510 | 6.524 → 6.611 | 1.01334 |
  | high／on | 5.035 → 5.270 | 1.04667 | 6.037 → 6.323 | 1.04737 |

- 三档峰值 RSS 的三次中位数原版→候选为 340.39→342.03、313.26→315.36、315.65→311.35 MiB。网格平均构建 1.031–1.046 ms；单次保存 1.853–2.337 ms；
  原始驻留／GPU buffer 指标保留在各次 summary；新天空没有新增纹理、RTT、实体或 CPU 缓存。
- B1b 没有持久化迁移，save v12／terrain v19／objective v3 保持；B1c 与后段仍继续实施。

### B1c：区域氛围与整合检查

- 云团已按小批次提交 `c1a3b72` 后接入本批。新增 `RegionBaseline.app` 冻结 B1b 程序与 119 项资源，
  独立 bundle `local.hellomine3d.adventure-region-baseline`，约 51 MiB，无用户存档。
  旧阶段基线在本批收口后整理；Working 仍是唯一可刷新的工作包。
- 生产 `OgreBootstrap::syncEnvironment` 接入 64 m 四角区域缓存，切世界三条路径重置。
  单元最多八次 biome／自然高度纯查询、固定 88 字节，无逐帧重复生成查询；预算先于实机验证冻结。
- 对照机位冻结于 `run_b1c_visual.py`：八生态按 B0 坐标，海岸改朝 270° 观察海侧，在候选接入前确定；
  原朝内陆机位仍保留历史归属。候选 30 组覆盖八生态双 FOV、三重点生态四时段、昼夜关闭与三个连续场景。
- 后续 B2 调查已确认食物铁剑前置、保存重开主线、定义 v3 和 Alpha 十个稳定 ID；尚未修改任务运行时。
- 首次单目标 Debug 构建失败：此前清理已移除 `libtracy.a` 等 Debug 依赖库，非 C++ 编译错误。
  原失败日志 `b1c-client-Debug.log` 保留；改用生成 workspace 的 `HelloMine3D` 依赖目标补建，
  不修改第三方源码或生成工程。新增依赖产物复用于后续批次，交付时清理冗余副本。
- 补建后 Debug／Release 客户端均通过；纯区域检查各 22/22 PASS，生产阴影滤波 GPU PASS。
  Debug 程序 SHA-256 `6452bdfe1cb9d32782e376988bb35cb3ff18c712780ed5d8912f86a6034d2118`，
  Release `6be67d09bc27b756778b269a93753ee42e6d8d131a349b8e769d3ddde032a43c`；均为当前 C++ 构建，
  新 Release 已刷新 Working 的 119 项清单；用户包仍保持原程序。构建与源码身份在 `b1c-builds.json`／包收据。
- 候选已查看草甸 FOV 120、林地／高地／沙丘／峡谷／湿地／海岸 FOV 90 的第二帧，
  以及林地夜间 FOV 120；近处地面、敌人和树干可辨，没有因区域着色出现黑面或整屏色偏。
  高地冻结机位位于树冠下，此图覆盖林内氛围，不单独宣称高地远景验收。

### B1d：按用户反馈修正天空轮廓

- 用户通过当前海岸实景反馈日月／云过方、观感不自然。本批改为圆形日月和不规则云团，
  保留像素内纹、轻量配色、世界坐标连续运动与遮挡；不继续将规则方牌、云板作为美术验收目标。
- CUA 曾准备关闭工作包，但收到“用户改变应用，请重新查询”提示后停止；重新读画面确认用户正在普通世界中操作，
  未重试关闭。此为窗口所有权冲突，不是系统权限拒绝；另建后台工作包继续已授权工作。
- 首个忽略目录原型 `b1d-natural-prototype.frag` 日月圆形、云缘使用连续密度团块。
  `b1d-gpu-natural-r1.log` 的遮挡／回退／连续漂移均通过，但边缘清晰度 FAIL：
  过渡像素 0.021118 高于原版 0.019531，不能用旧门槛通过来掩盖模糊。继续收窄云边，保留失败记录。
- 第二版 GPU 41/41，通过边缘／遮挡／连续性，但海岸实景仍有上下两片分离的薄饼感，视觉 FAIL。
  下一原型改为固定六次体密度采样填充厚度，每次四个 hash，总 24（等同 B0 原云噪声预算）；
  不新增资源、draw call、CPU 世界查询。先冻结预算，再实测三对性能，不因美术方向改变放宽 1.10。
- 第三至第五原型保留 GPU 边缘 FAIL（过渡比例依次 0.041245／0.017929／0.016006），
  第五版实景已消除上下端面分离；继续收窄积分后的覆盖边缘，不修改检查门槛。
- 最终候选 `b1d-natural-r6.frag`／生产 shader SHA-256 `35e79bd2e829d267d468d52f0f214c54392e58ab761311d2f9c82cfe885d2ffb`。
  生产 GPU `b1d-gpu-natural-r6.log` **45/45 PASS**，包括新增圆日／圆月和错误方牌负例；
  60 Hz 昼夜最大平均通道差 0.008991／0.002743（护栏 0.05），日月最小透射 0.060618／0.059856；
  云覆盖 0.282516、过渡比例 0.015167（旧版 0.019531），留白／清晰度、风移和 FS2 均通过。
- 当前 Debug／Release ResourcePackSmoke 各 114/114 PASS，`b1d-resource-{Debug,Release}.log`。
  C++ 复用本轮已构建的 B1c Release，不将 shader 修正冒充新的 C++ 构建。
- 独立 `SkyWorkbench.app` 已创建，119 个清单文件，程序仍为 `6be67d09…`；用户 Working 未刷新。
  当前八生态／昼夜／FOV／回退／动态及日月共 34 组画面 session `15042`，
  输出 `b1d-visual-candidate/`，是诊断机位证据，非普通输入。

### B1d 收尾与 B1e 方向修正

- 用户再次明确云形不正确，B1d r6 美术结果记为 **FAIL**，不再称为最终候选。
  本轮已看 31 组静态第二帧、林地昼／昏各八帧；区域过渡查看 5／8／11／15 秒，未查看其余四帧。
  不再为被否定的外观补齐剩余美术采样。月夜雪地近景的棕色矩形另记待查，早期敌人威胁留给 B2 核查。
- B1d 三档三对常驻全部保留，Off P95/P99 为 9.584→10.654 / 11.877→12.624 ms，
  比值 1.11164 / 1.06289；Medium 8.595→8.603 / 9.528→9.606，1.00093 / 1.00819；
  High 7.325→7.611 / 8.513→8.642，1.03904 / 1.01515。整体 **FAIL**，不删除慢样本，不启动该候选流送。
- 调查发现 r6 单频密度场、固定厚度、硬覆盖阈值及梯度法线叠加，形成圆滑石块感。
  “边缘过渡比旧版更窄”是先前造型假设，不能用它代替云形审美；保留旧模式及失败结果供追溯。
- B1e 改为轻薄大气云层：多尺度覆盖与受光分开，保留薄缘、浓淡、云群留白，取消硬挤出形态和实体式法线。
  首个原型每像素四次 value noise／16 hash，无新增纹理／RTT／CPU 查询／draw call；
  日月、风移、遮挡、时刻权威和旧模式回退保持。先做固定海岸／林地／雪山整屏检查，再跑适用全面检查。
- 参考源码路径和工程标识仅记在 Git 忽略笔记，未复制源码或素材入正式资源。

- B1e r1 海岸实景过淡／模糊，r2 仍偏平；记录两版视觉缺陷，未为它们启动全套性能。r3 增加第四层疏碎密度与浓密区域明暗，合计五次 value noise／20 hash；固定三场景 session `87613` 已结束，三组第二帧已查看；近缘柔和度与大 FOV 云量继续整体复核。

- B1e r3 生产 shader `cb5b0b3860c6ccdbb85d404ac9e58eb40e18d7b353340b46bc2e1e77c5bc1326`，GPU 47/47 PASS（`b1e-gpu-r3.log`），双配置资源各 114/114 PASS；GPU session `50722`／资源 session `9151` 均已结束。新轮廓的完整实景与性能待执行，尚未提交或宣称 B1 达标。

### 检验节奏调整（用户反馈）

- 上一目标轮为进展：实现 B1e 薄云、47 项 GPU 正反例和双配置资源验证；用户随后指出客户端启动过频繁。
- 确认问题在逐场景冷启动，不是缺少更多截图。已停止 34 场景驱动，实际 26 组采样收据保存在 `interrupted-batch.json`，未执行项目保留 NOT_RUN。
- 日常渲染修改以一次 GPU 进程内多条件检查和少量代表实景为主；已有当前版本证据不重采。
  完整八生态／昼夜／两 FOV、设置／生命周期与必要三对性能集中阶段验收，优先复用工作客户端；不放宽质量和性能护栏。
- 新方案未有充分工程／画面证据前，不再启动整套性能；下一次图形启动前明确剩余问题与能复用的当前证据。

### B1e 云层投影离屏修正（2026-09-23）

- 最近 r3 雪山实景顶部出现大片灰云。r4 调高覆盖阈值，但一次独立隐藏客户端采集因预期
  2× framebuffer 与实际 1× 不符而失败；保存原图和错误收据，不与 2× 基线混算。
  r5 缩小水平尺度时 GPU `cloud-bottom-crossing-continuity` 失败；补交界渐隐后 r6 恢复 47/47。
  r7 去掉一层高频密度，改为三层密度加一层受光，GPU 47/47；第二次独立隐藏客户端
  以实际 1× 条件观察，顶部大块与地平线条纹仍明显，外观未达标。
- 上述两次均使用独立 SkyWorkbench 隐藏不激活客户端，用户 Working 未触碰；
  `--reuse-app` 只复用包路径，两次均为真实冷启动。用户再次指出启动过密后，
  本次改为 **0 次客户端启动**：先抬云层至 308–332 m，再调整云群尺度及低仰角淡出，
  使用同一离屏 GPU 进程对日月、遮挡、世界风移、上下穿越、FS2 和错误负例验 47 项。
  当前 `b1e-gpu-height320-r4` **47/47 PASS**，`cloud-filled=0.242096`；
  r2 曾因验证器仍把 210 m 当作云上晴空，遮挡归一化误报 FAIL，修正云上参考高度后
  保留原 0.45 遮挡阈值和 0–2048 m 采样范围通过。失败目录留存。
- 同一 GPU 进程额外输出低地 70 m、雪山 129 m、近峰顶 240 m 三张天空探针；
  低地与雪山云群比 r7 的顶部厚板更分散，近峰顶仍可见较大顶端云面，需实景进一步判断。
  当前离屏图仅为合成天空探针，不能证明林地／雪山实景好看、云速自然或帧耗达标。
  下一次只在资源／运行时和形态候选收稳、并核对真实 framebuffer 后，安排同一身份包的
  集中会话覆盖关键机位和动态；正常输入、前后台性能与美术认可分开记录。
- Debug／Release 无窗口构建日志 `b1e-build-{Debug,Release}.log`；追加雪山视角到云底
  至少 128 m 净空检查，重编后 V10C 各 29/29（`b1e-v10c-{Debug,Release}-r2.log`）、ResourcePack 各 114/114
  （`b1e-resource-{Debug,Release}.log`），`b1e-check-assets.log` 退出 0。
  代码／合同本地提交 `174bc39 视觉: 调整圆形日月与高空薄云投影`；提交不代表实景或性能 PASS。

### B2a：解除前期目标前置与定义迁移

- 目标定义 v4 保留 34 个稳定 ID 和注册顺序；可见必做 32，保存重开改为可见可选历史项。
  熔炉改由拾取战利品开启，面包由放置工作台开启；战斗三次仍须先完成铁剑，不因食物提前立即催促战斗。
- Alpha 十步门面独立按旧十位完成标志投影，保持重开标志／历史，不因其他并行机会被错误投影为 Complete。
- save v12／terrain v19 不变；定义 v1／v2／v3 显式归一 v4，未知目标、配方发现、部分进度、玩家状态与旧标志镜像保持。
- 本批没有新增逐帧排序、渲染、缓存、模拟字段或生成查询；预算保持既有目标 128、保存集合各 256 项。
  状态相关提示／机会排序、早期危险与普通三种子回路属于 B2 后续必要项，当前未声明完成。

- B2a Debug 聚焦 `ADVENTURE_PROGRESS` **77/77 PASS**（`b2a-focused-Debug.log`），其中新增 14 项覆盖前置、旧门面、三个定义版本迁移；食物、结局、N1/P11C 现有检查均在本轮执行。未启动图形客户端。

- Release 聚焦同为 **77/77 PASS**，完整世界 **1730/1730 PASS**（`b2a-focused-Release.log`／`b2a-world-full-Release.log`），覆盖原 G6 真实领域路径、奖励／胜利、保存事务与生命周期；此为无窗口工程证据，不冒充普通输入。
- Debug／Release 客户端构建通过，资源各 **114/114 PASS**；本轮没有新增图形客户端启动。
  当前工作区 Release SHA-256 `7ab7e7b8ab0135055128b4d5caff179ed326144700e830db3c26fcf134e1830b`，
  含待提交 B1c 区域代码，作为工作构建保存在 `HelloMine3D-B2a-Release`，未刷新用户或冻结包。
- 原保护清单 **49/49 一致**；用户当前工作包另查程序／配置／存档存在与哈希，结果 `b2a-user-working-check.json`，不回滚用户保存。
- 下一批 B2b：让前置提前后的食物真正进入近期机会，依据库存／生命／照明状态提供提示；另修查自然敌人出生环与追击半径重叠的早期压力，普通三种子路线仍为必要验收。

- B2a 本地提交 **`8b0987d` `玩法: 提前食物引导并解除重开前置`**，只暂存本批核心／资源／文档；天空与区域修改仍独立未提交。没有推送，没有更新宿主 Goal 为完成。

### B2b：真实状态指导与近期机会

- 先修前置后，确认面包仍可能被 HUD 注册序前三项挤出。本批以固定三槽选择替代简单截取，
  保持稳定 ID／同优先级注册顺序；工作台后展示食物，受伤且具备面包时提升恢复，入夜提升照明和落脚点。
- 只读 World→Alpha→Objective 传入实际生命、冷却、每日模拟时钟；一次库存扫描选择材料步骤。
  HUD／暂停／任务详情共用指导文本与改键，手动追踪保持，状态变化重新展开简短说明。
- 新增 18 项双语 key，仍是 34 目标／32 可见必做、definition v4、save v12／terrain v19。
  不创建新持久状态、不改变事件完成／库存／奖励，不新增实体、地形查询或缓存。
- 首轮聚焦保留 `b2b-focused-Debug.log`：98 项中 3 FAIL。原因是旧双语数量断言未追加 18 项，
  新测试保存夹具漏设 `hasPlayerState` 导致按未进入世界处理；修正夹具并清除诊断时刻后重验。
  不放宽运行时语义或性能阈值。编辑脚本一次断言失败发生在测试文件落盘前，未算成构建／运行时 PASS。
- 本批继续无图形客户端启动。UI 三字号／紧凑窗口、正常食物获取与三种子、阶段三对性能保留 NOT_RUN；
  当前图形包尚未刷新，不能将新核心与文本说成已经在用户客户端生效。

- Debug 首次客户端编译抓到暂停页局部变量与新辅助函数同名；改为独立函数名后重编，原失败 `b2b-client-Debug.log` 保留。核心修正夹具后聚焦 **98/98 PASS**（`b2b-focused-Debug-r2.log`）。

- B2b 最终 macOS／gmake／Apple clang／x86_64：Debug、Release 聚焦各 **100/100 PASS**；
  相比 B2a 新增 23 项。Release 完整世界 **1753/1753 PASS**，保留全部原用例；双配置资源各 **114/114 PASS**。
  日志 `b2b-focused-Debug-final.log`／`b2b-focused-Release.log`／`b2b-world-full-Release.log`／
  `b2b-resource-Debug-final.log`／`b2b-resource-Release.log`；全部进程 exit 0。
- 双配置真实客户端构建通过。Debug SHA-256 `763da43b830b94dc981f2a1019aeaa468b5c8961f534fc094a8b04ef0516df68`；
  Release `924a954715b8f9fc3b080596a3f71224b7e37396e58f0711983a0f90a999006e`，
  文件 `HelloMine3D-B2b-Release` 13,016,184 字节，含待提交 B1c 区域 C++，属于工作区构建。
  包未刷新；本批图形客户端启动 **0 次**，没有将编译、无窗口测试或二进制备份误记为 GUI 启动。
- 保护检查 **49/49 + 用户 Working 3/3 一致**，进程只读查询未见游戏／诊断残留；没有关闭用户窗口。
  最终 Debug session `8271`、Release 世界 `99182`、Release 客户端 `25836` 均已结束；无正在运行的工具 handle。
- 提交范围只含 B2b 核心／UI／文本与本合同，架构和矩阵仅暂存 B2b 对应行；B1c／B1e 继续独立未提交。
  UI 全尺寸实机、普通采集恢复／三种子与性能仍 NOT_RUN，作为 B2 阶段整合和完整 Goal 的必需项。

- B2b 本地提交 **`006b4ce` `玩法: 根据生存状态组织近期任务`**，16 个本批文件；无推送。
  B2c 调查确认自然生成最近候选 8 m，小于三类敌人 10／14／18 m 追击距离；尚未调整。
  下一步修正出生缓冲与白天／新手危险节奏，核查不依赖战斗的小麦获取，遵循难度／生成兼容边界。

### B2c：早期危险、照明与土层种植（跨日继续于 2026-09-23）

- 核查发现旧 8 m 内圈与 10／14／18 m 追击范围重叠，白天每秒刷怪且不看照明。新地表自然生成
  在每日 12000 tick 起运行，候选 24–30 m，最终按实际追击半径 + 4 m 过滤；方块光 >= 8 和树冠／
  树干顶拒绝。旧敌人日出不删除，显式路标守卫不受新自然筛选影响。
- 难度档案、攻击／掉落、自然 cap 与尝试预算保持，地形 v19／save v12／objective v4 保持；
  新规则只改变后续运行时自然生成，不编辑地形或已有 Actor。小麦新增林床／淤泥种植，与草地／泥土并行。
- Debug 首轮 `ADVENTURE_SURVIVAL` **110/110 PASS**（`b2c-focused-Debug.log`），包括新的 35 项、D3／D5／D6／G6、难度与食物原用例。
  原人口夹具使用明确夜间时刻及覆盖新距离的加载范围，保留真实生成／cap／保存／卸载／战斗断言。
  D6／G6 既有夹具包含预置地面及位置安排，继续仅作工程路径，不称为普通输入。
- 三个冻结种子正常初始化后，32 m 有界调查分别取得 64／54／41 个成熟高草与适种土层，最近距
  1.87／3.94／7.91 m；领域采集／种植／随机生长各取得 3 小麦，无物品注入、无敌人战斗。
  这是生成资源和领域流程证据，不能替代正常移动、采集或前二十分钟的真人／Computer Use 体验。
- 首次 CSV 将选取的 3 个位置误记为总适种数量，已改为保留完整数量；原日志中的 64／54／41 正确。
  加强加载守卫为前后实际 existing／loaded 数相等，继续保留原 100,100 未加载断言；尚待最终重验。
- B2 性能需要在同等实体负荷下复核；不以白天减少敌人数量造成的更简单场景换取 PASS。
  本批仍未启动图形客户端，UI／普通路线／当前性能保持 NOT_RUN。下一步最终双配置、Release 完整世界和中文本地提交。

- Release 首轮聚焦 110/110 PASS；完整世界 1788 项中 1 FAIL：旧 AL-A5 阶段指标在白天断言必须生成，
  `cycle_attempts=0 budget=16`。保留 `b2c-world-full-Release.log`，补独立白天零工作／预算身份断言，
  原有“生成周期有工作、下一 tick 清零”改在夜间检验，未删原断言；纳入本批聚焦后重跑。

- B2c 最终 macOS／gmake／Apple clang／x86_64：Debug、Release 定向均 **119/119 PASS**，
  Release 完整世界 **1789/1789 PASS**（相比 B2b 增加 36 项），双配置资源各 **114/114 PASS**。
  最终日志 `b2c-focused-Debug-final.log`／`b2c-focused-Release-r2.log`／`b2c-world-full-Release-r2.log`，
  修正后的 AL-A5 实际夜间尝试 16／预算 16，白天及非周期均零，未放宽护栏。
- Debug／Release 客户端构建均通过。Debug SHA-256 `bde56ba5c59923aa258cbceaa7f1e6091693eac6ab78256b07ededf225decfa2`；
  Release `ea8588168dc1134bfeaea81e8d5db899a0ed39b923c2b182081b5b45f2a999b5`，
  工作构建 `HelloMine3D-B2c-Release` 13,016,184 字节，含待提交 B1c 区域代码，不冒充独立干净包。
- 最终保护检查 **49/49 + 用户 Working 3/3 一致**。B2c 图形启动 **0 次**；Release 世界 `18188`、
  Release 客户端 `55047` 和最终 Debug `79001` 均 exit 0，无运行中验证 handle。
- 清理两份已被本轮候选替代的 B2a／B2b 单独程序副本，释放 26,027,984 字节；历史日志／SHA／源码
  保留，历史报告中的这两个独立程序路径现已清理。当前 B2c、所有用户／冻结包及 B1 对照未动。
  三种子资源 CSV 已收进本批忽略产物目录，避免后续回归覆盖唯一调查摘要。
- 本批不声明 B2 普通二十分钟路线或 B1–B10 总 Goal 完成；当前包刷新、正常输入、整套 UI 与
  同负荷性能仍须阶段验收，后续三动物／地下／玩家模型／声音范围保持。

- B2c 本地提交 **`e6b70bd` `玩法: 留出探索准备时间并完善种植条件`**，12 个本批文件，无推送。
  下一次实机使用已停止的独立 SkyWorkbench 工作包，明确刷新至当前代码和资源后只启动一次并复用；
  用户 Working 与冻结基线继续保护。B2 普通输入／多尺寸状态提示未验完前不能称 B2 全部完成；
  受限时继续 B3 独立实现，不重跑已经通过的 B2 核心回归。

### 集中实机工作会话与窗口归属（2026-09-23）

- 在确认工作包已停止后原位刷新 SkyWorkbench，119 文件，程序 SHA-256
  `ea8588168dc1134bfeaea81e8d5db899a0ed39b923c2b182081b5b45f2a999b5`，
  资源清单 `9c4bbd7eb7a0d7419e33c23e4aa0bb7a7926424280a35cbd3c2189a5b72e40cd`。
  程序含待提交 B1c 区域实现，仍是候选工作包，非最终干净交付；用户 Working 和冻结包未刷新。
- 本轮仅一次 `cua.getApp` 打开既有工作包。工具报告等待 14,290 秒，后续实际进程
  `99539 ./HelloMine3D` 的 elapsed 为约 78 秒，不能把工具等待时间当作游戏冷启动成本。
  没有因工具迟缓另启第二实例；原进程查询漏掉相对路径 `./HelloMine3D`，已补查实际命令。
- CUA 可读取窗口及 HUD，实际画面是高处俯瞰；未记录从普通菜单创建世界的过程，不算普通
  出生／移动证据。两次 Escape 均被工具的“用户已改变应用，重新查询状态”保护拦下，
  中间已重读；第二次后停止操作，未换工具发送输入、未关闭窗口或刷新其资源。
  这是并发窗口操作限制，不是权限拒绝，也不是游戏暂停功能 FAIL。
- 本次 `AI_INTERACTIVE` 的菜单、普通输入和多尺寸任务提示仍为 NOT_RUN；现有截图不用于
  关闭 B1 动态、B2 普通路线或人类审美。工作流已明确包复用与进程复用的区别，保留阶段门禁。
- 独立 B3 进入调查已核对 Actor／LivingActor、固定 tick 的 64 项轮转、自然人口、快照渲染、
  world.meta 的 Actor kind 0–2 与备份路径。三动物尚未实现；下一批落实独立动物行为和生命周期，
  不把敌人换色当动物，也不把本次调查标成 B3 完成。

### B3a：三生态动物的权威行为与生命周期（2026-09-23）

- 草地绵羊、林地兔子与湿地／河湖岸鸟作为独立 LivingActor 从真实 biome 与已加载安全地面
  在白昼自然出现，和夜间敌人不共用类型、cap 或难度数量。三种动物各自具备停留、觅食、
  游荡、玩家靠近逃离的节奏；无敌人追击与攻击，无掉落，击杀不推进敌人目标。
- 领域预算：80 fixed tick／最多 8 个生成候选；世界 24、64 m 内 12；每动物新决策
  最多 5 Hz。地面／避障上限 48 次每 fixed tick，生成候选预留 7 次、单步预留
  18 次，拒绝后延后当前动作；沿用现有 64 项 Actor 调度，不同步加载未知区块。
  区块卸载移除动物；自然种群不写入 world.meta，重开后在已加载生态重新出现。
  world save v12、terrain v19 与目标 definition v4 不变。详情见[动物合同](../contracts/adventure-wildlife-contract-v1.md)。
- 真实运行路径聚焦在 seed 42 的草地、林地、湿地冻结坐标分别取得 12／12／12 只
  目标物种；200 连续 fixed tick 看到三行为和位置变化，玩家靠近后逃离；加载数未增加，
  查询预算未超限，击杀无掉落；卸载／保存／重开无重复并自然再生，原 D3 敌人保留。
  这是预置位置的领域测试，不是普通路线遇见证据，也不是模型画面结论。
- 初次测试 `b3a-focused-Debug.log` 35/35，通过；增加活动、目标过滤与卸载／重开、
  水体与头部障碍等断言后，最终 Debug／Release 聚焦 `b3a-focused-Debug-r4.log`／
  `b3a-focused-Release-r2.log` 均 **107/107 PASS**。Release 完整世界
  `b3a-world-full-Release-r2.log` **1883/1883 PASS**；Debug／Release 世界测试与客户端
  均实际编译。Debug 首轮测试编译变量重名失败，已修复，保留 `b3a-build-debug-r2.log`。
  当前 Release 客户端 SHA-256 `6236a9348b9a21226501f9e2d1fab8a9d958af46255392faa98edaf5fb84091c`，
  含未提交 B1c 候选，不是干净交付包。
- 本批游戏窗口启动 **0 次**。B3 独立体素模型、动作画面、三生态普通遇见、密集流送、
  同机三对性能和最终可运行包仍为 NOT_RUN；B1／B2 未验范围不因这批代码通过而关闭。

### B3b：三类模型与像素材质的无窗口验证（2026-09-23）

- 绵羊 8 盒、兔子 7 盒、湿地鸟 8 盒；各自有口鼻／长耳／长颈与翼等不同轮廓。
  真实活动快照驱动步幅、兔子轻跳／耳朵、鸟翼和觅食低头；零动画强度回到静止。
  视觉变换只在 Ogre 侧，地面接触基于 Actor 脚底位置，不改权威碰撞与 save v12。
  三种低饱和无发光材质进入普通／方向阴影、区域光色与雾的同一参数路径。
- 先用无窗口手段验证：Debug／Release `ADVENTURE_WILDLIFE` 各 **110/110 PASS**，
  增加模型盒数／角色、逃离与觅食动作断言；生产普通／阴影 GLSL GPU **136/136 PASS**，
  覆盖新旧角色一致、关闭回退、局部像素、雾遮挡与投射物回归；Debug／Release 资源
  各 **114/114 PASS**，客户端双配置均编译。对应日志：`b3b-focused-Debug.log`、
  `b3b-focused-Release.log`、`b3b-actor-gpu-r2.log`、`b3b-resource-Debug.log`、
  `b3b-resource-Release.log`、`b3b-build-Debug-final.log`、`b3b-build-Release-final.log`。
  默认沙箱 OpenGL 上下文失败已保留在 `b3b-actor-gpu.log`；同一无窗口工具在
  获自动审查允许的 GPU 驱动上下文中通过，没有因此启动图形游戏客户端。
- 本批游戏窗口启动 **0 次**；`SkyWorkbench.app` 未刷新。三生态普通遇见、模型大小／
  动作的连续实景、密集流送与同机三对性能仍为 `NOT_RUN`，与 B1／B2 剩余项目一起
  放入有明确问题清单的集中实机会话，不把 GPU 通过当成美术验收。
- 本地提交 `cfafad2 生态: 完善三类动物模型与像素材质`；B1 天空候选改动仍在工作区，
  未混入本批暂存。

### B4a：目的地两布局与新生成身份（2026-09-23）

- 新 terrain v20 对营地、遗迹、路标各按原地点稳定哈希选两种真实方块空间。
  新营地有步行阶梯与开放瞭望台，遗迹有侧室及庭院，路标有四角石柱与后横梁；
  旧式布局保留并修通路标核心前的立足点。候选位置、占地、箱子／核心坐标和奖励哈希
  与 v19 对齐，矿物数量不增加；旧 v1–v19 世界继续走原蓝图与生成规则，save v12 不变。
- Debug 聚焦首轮 **94/94 PASS**，六布局在实际 seed 42 中均被选择并实际投影，
  检查入口、核心／箱子／矿物、箱子 payload、正逆区块及 v14 E8 回归。
  增补建筑连通、瞭望台楼梯与 v20 编辑保存重开后，Debug 聚焦
  `b4a-focused-Debug-r2.log` **102/102 PASS**。
- 初次 Debug 完整世界 `b4a-world-full-Debug-r1.log` **1916 项中 2 FAIL**：
  `ADVENTURE_EXPLORE/appended-v19` 和 `default-v19-save-reopen` 仍把旧默认版本硬编码为 19。
  已把这两项改为显式验证 v19 旧世界身份及重开，并由新 B4 用例验证 v20 默认新世界。
  不删除失败证据。修订后 Debug／Release `ADVENTURE_LANDMARK` 聚焦均
  **102/102 PASS**（`b4a-focused-Debug-r2.log`、`b4a-focused-Release.log`），
  完整世界均 **1924/1924 PASS**（`b4a-world-full-Debug-r2.log`、
  `b4a-world-full-Release.log`）。双配置客户端编译通过，构建日志
  `b4a-build-Debug-r1.log`、`b4a-build-Debug-r2.log`、`b4a-build-Release.log`；Release 二进制 SHA-256
  `55c7a5e66236cd9776fc94b0440ebd48299e7a454bcb05d906e66f8244083c5e`。
  该程序还包含未提交的 B1 天空候选，不是干净交付包。
- 本批图形客户端启动 **0 次**。普通玩家进入六布局、林／河／山地接近线索、三套可采
  可制作的建造搭配、同机三对性能及干净交付仍为 `NOT_RUN`，不以蓝图测试代替体验。
- 本地提交 `f540b00 探索: 为三类目的地加入双布局`，仅含 B4a；B1 区域／云层候选未混入。

### B1c 当前版本复核与独立收口（2026-09-23）

- 在当前 terrain v20、save v12、目标 definition v4 的工作代码上，重编并运行
  `RegionalAtmosphere` Debug／Release 纯检查各 **22/22 PASS**，日志
  `b1c-regional-Debug-current.log`／`b1c-regional-Release-current.log`；
  缓存 88 字节，覆盖四角八次查询、负坐标／极值、跨区域连续、时刻权威及水介质。
  Release `V10C` 聚焦 **28/28 PASS**（`b1c-v10c-Release-current.log`）。
  当前 Debug／Release 客户端在 B4a 双配置构建中均编译通过，程序同时含未提交
  B1e 云层候选，不能把该混合程序当作 B1c 干净交付。
- 本次额外游戏窗口启动 **0 次**。现有 B1c 地区画面证据属于早期 v19 候选；
  当前 v20 八生态、林地／海岸／雪山四时段、两 FOV、功能关闭、动态和同机三对性能
  仍待集中实机确认。工程验证通过不宣布 B1 或审美完成。
- 下一次图形检查先冻结同一工作包身份，列出 B1 云形／区域、B2 早期引导、B3 动物与 B4 目的地的
  当前版本最小问题清单；同一进程按正常输入及场景切换集中检查，逐项记录画面和启动次数。
  截图脚本的 `--reuse-app` 不视为进程复用；不再按场景循环冷启动。性能另按固定配对条件采样，
  不能由这次观察代替。当前客户端身份尚未冻结，本阶段不为补截图启动游戏。

### B4b：三套区域材料进入真实建造路径（2026-09-23）

- 正式配方加入林地苔石／木板／林地土制箱子、河岸黏土／砾石／淤泥制熔炉、
  高地砾石／石头／木板／铁锭制手摇破碎机；旧配方、物品／方块 ID 与存档格式不变。
  `ADVENTURE_MATERIAL` 通过实际工作台预览／提交、材料扣除、玩家放置、真实容器／机器打开，
  重开后核查三类块实体。自然生成的地区采集、地点中的建筑示例与普通返回建造尚未验收。
- Debug 聚焦首次 **33/33 PASS**（`b4b-material-focused-Debug-r1.log`）；强化机器／容器实际打开后
  最终 Debug／Release 聚焦各 **36/36 PASS**（`b4b-material-focused-Debug-r2.log`、
  `b4b-material-focused-Release.log`）。Debug 完整世界在强化用例前 **1935/1935 PASS**；
  最终 Release 完整世界 **1938/1938 PASS**（`b4b-world-full-Release.log`）。
- 首次 Debug `HelloMine3DRecipeSmoke` 为 **125/126，FAIL**，原因是 N10 将旧资源总数固定为 25；
  检查改为原有核心配方和三条新增 ID 同在、总数 28，Debug／Release 各 **126/126 PASS**
  （`b4b-recipe-Debug-r2.log`、`b4b-recipe-Release.log`），首次失败日志 `b4b-recipe-Debug.log` 保留。
  双配置资源包各 **114/114 PASS**，`check_assets.sh` **89 项 PASS**。
- 本批图形客户端启动 **0 次**。配方数据不引入渲染代码；B4 总体验、普通采集和
  同机三对性能仍保留 `NOT_RUN`，待地点接近线索与场景建筑示例整合后集中验收。

### B4c：真实区域地点的接近线索（2026-09-23）

- 新世界 terrain v21 在三类地点建筑前方铺设最多 6×3 格有界地表线索；实际入口前的
  林地、河湖湿地和高地分别采用区域材料，树木来源避让通路。规划和投影跳过水面、
  过陡地面、其他建筑和不可清理障碍；不新增地点、奖励或保存字段。旧 v1–v20 保持原规则，
  新世界 save format 仍是 v12；[v21 合同](../contracts/adventure-landmark-approach-v21-contract-v1.md)。
- Debug／Release `ADVENTURE_APPROACH` 各 **15/15 PASS**，冻结种子的实际林地、河岸、
  高地各找到一个地点；各样本中心 6 格均有地面、净空和侧边标记，旧 v20 建筑逐格一致，
  v21 正反区块顺序一致，新建世界改块保存重开通过。Debug／Release 原 `ADVENTURE_LANDMARK`
  各 **102/102 PASS**，其中旧 v20 世界使用显式身份创建后编辑、保存和重开。
- 双配置无窗口客户端编译通过；Release 完整 WorldRuntime **1953/1953 PASS**
  （`b4c-world-full-Release-r1.log`）。验证程序真实执行了 v21 当前生成与显式 v20 保存回归。
  这批游戏窗口启动 **0 次**，用户 `Working.app`、存档和冻结包未刷新。用户再次指出频繁启动
  会干扰工作：后续纯逻辑、迁移、资源与编译继续无窗口；B4 建筑示例与 B1 云层稳定后，
  统一在身份冻结的独立工作包中进行一次连续画面／正常行走会话。性能按固定配对另计
  必要启动次数，不能把多次冷启动称为同一会话，普通体验和审美在此之前保留 `NOT_RUN`。

### B4d：区域建筑示例与真实工位（2026-09-23）

- terrain v22 在实际林地、河岸和高地地点路径侧选择干燥、单区块、九列坡差不超过一格的
  3×3 小工位。林地苔石／林地土／橡木组成空箱子储物角；河岸砾石／淤泥／黏土组成
  空熔炉台；高地石头／砾石／橡木组成空手摇破碎机台。三个设备均是原有可打开的
  方块实体，不新增补给或配方；玩家拆除后保存重开不重生。地点原建筑、奖励箱、
  矿物、核心与奖励哈希沿用 v21；旧 v1–v21 按旧生成版本保留。
- Debug／Release `ADVENTURE_WORKSHOP` 各 **21/21 PASS**：三个实际地区、空间及材料组合、
  空方块实体、真实打开、保存重开、拆除不重生、默认 v22 身份和正反生成均覆盖。
  旧 `ADVENTURE_APPROACH` 双配置各 **15/15 PASS**，旧 `ADVENTURE_LANDMARK`
  双配置各 **102/102 PASS**。双配置客户端仅编译、不打开窗口。
- Release 完整 WorldRuntime **1974/1974 PASS**（`b4d-world-full-Release-r1.log`）。
  B4 的普通自然采集、真正步行进入、带回基地建造、画面审美和同机三对性能仍是
  `NOT_RUN`；本批客户端启动 **0 次**，不触碰用户的工作包和存档。

### B5a：已观察地表的有界数据层（2026-09-23）

- 现有小地图／区域立体图只保存附近 65×65 的临时驻留表面快照，切世界清空；
  当前备份只收录 `world.meta` 和区块。B5 不可把这个临时缓存或生成器预测当作探索历史。
  本批新增 `ExplorationAtlas`：只收显式真实观察，4 m 单元、32×32 单元页、最多 4096 页，
  已知空列与未知区分，重复观察可刷新编辑后的地表，满额拒绝新页但保留旧历史。
  负坐标按数学下取整，整数极值不溢出；内存保守上界低于 16 MiB。
- `bash scripts/verify_exploration_atlas.sh` 使用 macOS x86_64 Clang Debug／Release
  `-Wall -Wextra -Werror` 编译与纯测试各 **8/8 PASS**，日志 `b5a-atlas.log`；
  覆盖未知、空列、跨零边界、刷新、非法值、最大容量和清空；当前生产世界还未接线，
  客户端启动 **0 次**，不能宣称跨重启或地图 UI 完成。
- B5b 将独立地图文件纳入事务和备份；下一步在真实已加载表面采样后写入，
  文件版本／世界身份／大小／损坏回退及满额反馈按[探索地图合同](../contracts/adventure-exploration-map-contract-v1.md)
  验证，然后 B5c 接平面总览、标记、已知任务与返回路线。

### B5b：独立地图文件与世界备份工程层（2026-09-23）

- `ExplorationMapStore` 新增世界目录 `exploration.hmap` 格式 v1，记录世界 ID、种子、地形版本、
  有序页、已知位与地表值；64 MiB 文件上界、完整长度与 FNV 校验。旧世界缺文件时返回空地图，
  文件损坏或身份不符时只清空地图内存并报告，不修改 `world.meta` 或区块。
  `StorageTransaction` 对候选文件完整解析后原子发布；标记计数在此格式固定为 0，个人标记仍待后续版本。
- `WorldBackup` 将可选地图作为同一代备份文件：创建、清单验证、恢复、回滚均保留它，
  并核对地图身份与 `world.meta`；旧备份没有地图时恢复会移除较新地图。默认文件数上限
  从 4097 调为 4098；原总字节和单文件上限仍生效。损坏或错身份地图拒绝备份，
  游戏运行时接线前须做好损坏文件隔离，避免阻碍正常保存。
- `bash scripts/verify_exploration_atlas.sh` Debug／Release 各归档 **8/8**、文件 **8/8**，
  涵盖未知／已知空列、重启载入、外来身份、候选替换故障、损坏、重复页。
  `HelloMine3DWorldBackupSmoke` 双配置 **23/23**，世界目录 Release **60/60**，
  Debug 表面地图 **10/10**，Release 完整世界 **1975/1975**；双配置客户端编译通过。
  日志 `b5b-atlas-store.log`、`b5b-build-{Debug,Release}.log`、
  `b5b-backup-{Debug,Release}.log`、`b5b-catalogue-Release.log`、
  `b5b-world-focused-Debug.log`、`b5b-world-full-Release.log`。
- 本批游戏客户端启动 **0 次**。后续接真实驻留地表采样、世界保存／退出，再集中安排一个
  独立工作客户端会话验证地图界面和普通探索；不把 `--reuse-app` 当作进程复用。

### B5b 运行接线：真实观察、保存与损坏隔离（2026-09-23）

- `World` 在固定 tick 每 10 次最多从玩家周围 81 个 4 m 地表列取真实驻留样本；
  不调用生成器或远区加载，锁忙就延后。内存上限仍 4096 页／16 MiB；更新已有列可反映编辑，
  满额保留旧历史。观察改变才写独立地图文件，世界保存按区块／元数据→地图→备份；退出补写。
- 载入旧世界缺地图时归档为空。损坏或错身份文件被保存在 `.corrupt.failed`，不覆盖已有隔离槽；
  世界可进入，元数据和区块不受地图损坏影响。若隔离槽被占用或路径异常，地图文件不被静默替换，
  保存与备份明确失败。跨世界实例各自拥有归档，旧视图不能泄漏到新世界。
- `bash scripts/verify_exploration_atlas.sh` 双配置归档 **8/8**、文件 **12/12**；
  `EXPLORATION_MAP` 双配置各 **6/6**，实测普通 World 构造、驻留采样、地图及备份发布、
  重开保留和损坏隔离后继续保存；备份双配置 **23/23**，Release 完整世界 **1981/1981**。
  双配置客户端只编译。最初 Debug 构建中测试夹具调用了不存在的环境清理函数，修正后
  双配置构建通过；原失败保留于 `b5c-build-Debug.log`。最终日志为 `b5c-atlas-store-final.log`、
  `b5c-build-Debug-r3.log`、`b5c-build-Release-r2.log`、`b5c-map-focused-{Debug,Release}-r3.log`、
  `b5c-backup-{Debug,Release}.log`、`b5c-world-full-Release-r2.log`。
- 本批图形客户端启动 **0 次**，地图 UI、正常探索标记、同机三对性能和界面可理解性
  仍 `NOT_RUN`。下一步 B5c 将归档显示、个人标记、返家与目的地导航接入地图详情；
  与 B1–B4 当前画面集中安排一次独立客户端会话，不逐场景冷启动。

### B5c 首批：个人标记值语义（2026-09-23）

- 已新增 `ExplorationMarkers`：最多 64 个个人标记，稳定递增 ID；创建、重命名、移动、
  删除、唯一基地和唯一追踪状态明确。名字限定 64 字节／24 个 Unicode 标量，拒绝
  破损 UTF-8、控制符与两端空格；满额拒绝新增而不淘汰历史。删除追踪项会清追踪，
  切世界清空该值层。此批尚未接存档或 UI，不能声明玩家可见或跨重启。
- `bash scripts/verify_exploration_atlas.sh` 的 Debug／Release 聚焦各为归档 **8/8**、
  标记 **8/8**、文件 **12/12**，日志 `b5c-marker-core.log`。纯值层无客户端变更，
  游戏窗口启动 **0 次**。下一步将 sidecar 升级到兼容旧版的标记格式，并接 World 命令。

### B5c 第二批：标记文件 v2 与备份兼容（2026-09-23）

- 地图文件格式从 v1 升到 v2，保持相同世界身份与容量边界；旧 v1 仍可读，标记默认空，
  下次实际发布才升级。v2 保存每个标记的稳定 ID、坐标、类型、UTF-8 名称，以及下一个 ID
  和当前追踪 ID。解析拒绝重复／乱序 ID、多个基地、未知地表坐标、非法名称与篡改记录。
  `World` 显式携带标记集合读写，存储写入接口不再允许遗漏标记参数，避免地表刷新抹去标记。
- 双配置 `verify_exploration_atlas.sh` 各归档 **8/8**、标记 **8/8**、文件 **15/15**；
  WorldBackup 双配置 **23/23**，其中 v2 备份恢复同时核对地表、基地名和追踪 ID；
  WorldCatalogue 双配置 **60/60**；`EXPLORATION_MAP` 双配置 **6/6**；
  Release 完整 WorldRuntime **1981/1981**；Debug／Release 客户端仅编译。
  日志 `b5c-map-v2-pure-final.log`、`b5c-map-v2-backup-Release.log`、
  `b5c-map-v2-backup-Debug-verified.log`、`b5c-map-v2-focused-Release.log`、
  `b5c-map-v2-focused-Debug-verified.log`、`b5c-map-v2-world-full-Release.log`。
- 两配置 Smoke 共用 `bin` 输出路径。Release 后第一次切回 Debug 没有重链，
  那三份极短运行未计 Debug 证据；删掉仅三个测试可执行文件后按 Debug 目标重链，
  日志 `b5c-map-v2-smoke-build-Debug-relink.log` 显示三次 Linking，再执行上述 Debug 检查。
  图形客户端启动 **0 次**，玩家标记入口／地图总览与性能仍 `NOT_RUN`。

### B5c 第三批：World 标记命令（2026-09-23）

- `World` 现提供创建、重命名、移动、设基地、追踪、删除及有界值快照。创建／移动先核对
  已探索的 4 m 单元；缺资料时只试查一个驻留列，远区未知位置不会因此生成或载入。
  成功变更令地图文件变脏，World 保存和退出沿既有事务写入，重开保持稳定 ID 和追踪。
- `EXPLORATION_MAP` Debug／Release 各 **9/9**：覆盖远区拒绝不增区块、已知列建基地与追踪、
  中文重命名、保存重开及损坏隔离；Release 完整世界 **1984/1984**，双配置客户端编译通过。
  日志 `b5c-world-marker-focused-{Debug,Release}.log`、
  `b5c-world-marker-full-Release.log`、`b5c-world-marker-build-{Debug,Release}.log`。
  本批客户端启动 **0 次**；真实地图页面、返回方向距离及普通输入仍 `NOT_RUN`。

### B5c 第四批：归档地表平面图首批（2026-09-23）

- 地图页默认展示已真实观察的 65×65 个 4 m 地表单元，可点选材料／坐标、按键或
  右键拖动平移并切换既有局部立体图。平面图只读 `World::exploredSurfaceAt`，缓存最多
  4225 个值样本、静止时每秒刷新一次、平移后即时重采样，不触发远区区块载入；
  未知保持空白，切世界清缓存。平移时的查询成本仍需在同机性能检查中核对。
  两语文本已对应改为探索地图。当前没有大范围缩放、标记编辑和目的地导航；立体图
  仍使用驻留临时样本，不能称为 B5 整体完成。
- Debug／Release 客户端编译通过；两配置资源包各 **114/114**，资源清单
  **89/89**。日志 `b5c-overview-final-build-{Debug,Release}.log`、
  `b5c-overview-resource-{Debug,Release}.log`、`b5c-overview-assets-final.log`。
  玩家操作、640×480／1280×720 三字号、真实画面和同机性能均 `NOT_RUN`。
  本批客户端启动 **0 次**；后续在平面图、标记和导航稳定后复用一次独立工作会话
  集中验收，保护用户 `Working.app` 与存档。

### 本地运行截图保留（2026-09-23）

- 用户要求保留运行截图且不提交媒体。独立 `CaptureWorkbench.app` 使用当前 Release 程序
  `7e658d97…` 和 119 项正式资源打包，未复制或改动 `Working.app`。尝试一次隐藏窗口
  同进程定时抓取场景与地图，客户端在 OpenGL 初始化阶段退出：`OpenGL 3.0 is not
  supported`，未生成当前版本 PNG。失败日志、包身份和参数留在 Git 忽略的
  `build/adventure-experience-20260922/runtime-shots-20260923-coast-map/`。当时 Cocoa
  报告 1280×720、1x backing；`system_profiler SPDisplaysDataType` 只列 GPU、未列活动显示器，
  Computer Use 状态查询超时。尚不能断言唯一根因，不把失败记为画面验收。
- 已把四张先前成功采集的真实客户端 PNG 以硬链接保存在忽略目录
  `build/adventure-experience-20260922/runtime-screenshots-local-20260923/`，分别为雪山、
  林地、海岸和旧局部地图。`manifest.json` 列出每张的原始采集记录、程序哈希与图片哈希；
  `README.md` 明确它们都不是当前 `9c493c1` 的截图。这样避免与最新版混淆，也避免
  重复占用约 6 MiB。当前地图页画面与交互仍 `NOT_RUN`，图形环境可用后再补一轮同进程截图。

### B5c 第五批：标记导航方位值层（2026-09-23）

- 新增北向八方位和水平米数计算，只接受已经由标记／发现提供的坐标；先扩宽 32 位
  坐标再相减，极端旧世界位置仍返回有限距离。该层不查询地形，也不自动揭示任务地点。
- `bash scripts/verify_exploration_atlas.sh` 双配置中导航各 **10/10**，同时原有
  归档／标记／文件检查继续通过；日志 `b5c-navigation-pure.log`。这是独立值层，
  尚未声称 HUD／地图方向显示、普通输入或当前图形窗口通过。

### B5c 第六批：个人标记页面与追踪提示（2026-09-23）

- 在平面图已观察单元创建普通标记或基地，地图页可列出最多 64 个稳定 ID 的标记，
  改名、设基地、追踪／取消、移动到选中单元并确认删除。名称和目标位置最终由 World
  验证，未知远区不可从界面生成地形。平面图标出基地／普通／追踪点，详情显示北向方位
  与水平距离；小地图对当前追踪点显示边缘方向提示与方向／距离文字。切世界清理编辑
  状态，实际持久记录仍由 World 保存事务负责。
- Debug／Release 客户端编译通过，Release 资源包 **114/114**，资源清单 **89/89**；
  两语当前地图键引用均已定义。日志 `b5c-marker-ui-build-{Debug,Release}.log`、
  `b5c-marker-ui-resource-Release.log`、`b5c-marker-ui-assets.log`。本批游戏窗口启动
  **0 次**；由于当前图形环境尚无可用 OpenGL 3 上下文，鼠标操作、640×480／1280×720
  三字号、实际标记可读性、保存重开 UI 回路与性能仍 `NOT_RUN`。不沿用旧地图截图冒充
  这一批画面；继续实现已知目的地及较大范围地图后，再集中实机。

### B5c 第七批：归档总览缩放（2026-09-24）

- 平面图支持 4／8／16／32／64 m 每格、按钮及鼠标滚轮缩放，65×65 格最大覆盖
  4160 m；仍只读已经保存的真实 4 m 地表记录。每个显示格有任一已观察单元才着色，
  以离格中心最近的真实样本作为材料／高度与点选坐标。缩放后创建／移动标记使用这个
  记录坐标，避免对未观察的显示格锚点提交标记。页面每次仅构造有界临时值快照，
  地图文件、存档版本和区块载入语义不变。
- `bash scripts/verify_exploration_atlas.sh` 的 Debug／Release 各为归档 **13/13**、
  标记 **8/8**、方位 **10/10**、文件 **15/15**，增加稀疏路线、代表坐标、页接缝、
  极端坐标及非法范围检查。Debug 客户端和 `EXPLORATION_MAP` **10/10**；Release
  客户端、资源包 **114/114**、资产清单 **89/89**；Release 完整 WorldRuntime
  **1985/1985**。`World` 聚焦例含真实驻留样本的缩放总览，未发现远区生成。
  首次完整回归因前一批双语地图键由 582 增至 619 而旧断言未更新，保留失败日志
  `b5c-overview-zoom-world-full-Release.log`；同步键数后重跑通过，日志
  `b5c-overview-zoom-world-full-Release-r2.log`。
- 当前 Release 程序 SHA-256 见恢复区；本批图形客户端启动 **0 次**。同机三对帧耗、
  滚轮与点击手感、缩放地图在 640×480／1280×720 及三字号的实际可读性均
  `NOT_RUN`。当前图形环境先前缺 OpenGL 3 上下文，没有新证据表明已恢复；
  不反复冷启动。下一批继续接真实已知任务目的地和立体视图的持久记录，稳定后
  在独立工作包一次会话内补齐画面与操作验证。

### B5c 第八批：有权威锚点的任务导航（2026-09-24）

- 平面图读取当前 World 的界石事件锚点，只对与该锚点有关的仪式／防守任务绘制
  任务符号及北向方位、水平距离。若锚点尚未由玩家实际使用建立，或任务仅要求寻找／
  玩家自行选择地点，则不预测生成器中的结构位置；个人追踪标记仍独立显示。
  地图遵从任务日志当前追踪项，未追踪时取主线当前项。此批不扩展目标或地图存储格式。
- `bash scripts/verify_exploration_atlas.sh` 双配置归档 **13/13**、标记 **8/8**、
  导航 **12/12**、文件 **15/15**；新增无锚点、无固定坐标任务和已知界石阶段的
  门槛检查。Debug／Release 客户端编译，资产清单 **89/89**，Release 完整
  WorldRuntime **1985/1985**，日志 `b5c-task-navigation-world-full-Release.log`。
  两语键总数同步为 620，完整世界检查验证键集合一致。
- 这是运行时已知地点提示，**未完成跨区域／跨重启的任务地点持久发现**：界石锚点
  只由实际交互和当前可恢复的已载入事件提供，重启后远离结构可能暂时未知。
  目的地显示、任务追踪选择及紧凑窗口实际输入仍 `NOT_RUN`；本批图形客户端启动
  **0 次**。后续应将真实发现事件纳入有界持久地图状态，再补普通返程验证。

### B5c 第九批：任务地点随地图持久化（2026-09-24）

- 独立 `exploration.hmap` 从 v2 追加到 v3：保留页、稳定标记 ID、基地和追踪状态，
  新增至多一个已知界石位置。v1/v2 读取时该位置为空，不依据任务进度或生成器补造；
  后续真实写入才升级。校验位置标志／高度、长度、校验和世界身份；候选文件仍用现有
  事务验证、原子发布、备份／恢复和损坏隔离。世界主存档 v12、terrain v22 不变。
- `World` 只在玩家真实放置界石、实际激活／继续界石事件时记录；拆除同一界石清除。
  地图页面优先消费该持久坐标，旧存档运行中已知的事件锚点仍可作为回退。
  一个世界仅保留最近一次已知界石地点，不声称记录了所有历史界石或营地。
- 纯数据 Debug／Release 各为归档 **13/13**、标记 **8/8**、导航 **12/12**、
  地图文件 **23/23**，覆盖 v1/v2→v3、标记保留、地点往返、错世界隔离、非法字段与
  原文件保护。`EXPLORATION_MAP` Debug／Release 各 **15/15**：真实放置、远处强制测试位置重开
  时原区块未驻留但位置保留、再拆除和重开清除；最初使用 y=100 的测试夹具碰到
  该种子现有地表，导致放置失败，改为空气位置 y=200 后同一路径通过。
  `HelloMine3DWorldBackupSmoke` Debug／Release **23/23**，v3 备份恢复同时核对标记与
  地点；Release 完整 WorldRuntime **1990/1990**，日志
  `b5c-known-site-backup-Release.log`、`b5c-known-site-world-full-Release.log`。
  Debug／Release 客户端编译、资产清单 **89/89**；本批图形客户端启动 **0 次**。
- 这些是工程／诊断证据，强制测试位置不构成普通步行回路。地图实际点击、已知任务
  指示是否清楚、老世界普通重启、两窗口和三字号、同机三对性能仍 `NOT_RUN`。
  下一步整合局部立体图与已探索资料，再集中进行独立工作包实机与普通返程验证。

### 2026-09-24：未推送提交审查修复（进行中）

- 用户授权处理七项审查问题：动物预算饥饿、运动跳步与悬空，界石任务位置被新放置覆盖，
  地图标记编辑名称串用、玩家朝向固定向北、地图损坏／容量状态无产品反馈。
- 开始于 `7e36029`。既有未完成立体地图改动先完整保存至 Git 忽略的
  `build/review-fixes-20260924/relief-in-progress.patch` 和原文件副本，未混入本次修复。
  后续立体视图整合仍待办；本次以已提交 UI 基线编译。
- 动物已改为独立轮转获取预算、低频决策与连续移动分离、局部身体碰撞／脚下重力，
  表现层插值权威快照。Debug 聚焦 **119/119**，包含 12／24 只公平服务、逐 tick 位移、
  树冠下通行、双向台阶、斜向障碍、挖空后落地及插值暂停／跳变；首次聚焦 117/117
  后补台阶与角落回归再通过。日志位于上述忽略目录 `animals-*`。
- Debug／Release 聚焦均 **119/119**，两配置客户端编译通过；Release 完整世界回归
  **1999/1999**。未启动游戏窗口，普通动物观感及同机三对性能仍 `NOT_RUN`。
  本批没有改变主存档、动物临时种群语义或自然生成上限。用户包未刷新。

### 审查修复第二批：任务地点绑定（2026-09-24）

- 第一批动物修复已本地提交 `f112f52`。本批将最近发现与任务绑定分开；普通放置不改变
  已激活地点，分别拆除分别清除。真实交互建立绑定，远区重开仍保存；v1–v3 地图可读，
  旧发现不猜测活动任务位置，下一次真实保存追加到地图 v4。主存档 v12、terrain v22 不变。
- 双配置纯数据：归档 **13/13**、标记 **8/8**、方位 **12/12**、文件 **28/28**；
  包含 v3→v4、独立记录、错世界清空、非法绑定及原文件保护。双配置地图 World 聚焦
  **20/20**、备份恢复 **23/23**、客户端编译通过；Release 完整回归 **2004/2004**。
  日志为忽略目录 `build/review-fixes-20260924/map-binding-*`。
- 图形启动 **0 次**，正常步行、地图视觉／输入和当前性能仍 `NOT_RUN`。没有改用户存档
  或刷新客户端；下一批处理地图编辑状态、指针方向及异常反馈。

### 审查修复第三批：地图交互与健康反馈（2026-09-24）

- 标记画布、列表、新增入口统一更新 ID 与名称缓冲，跨标记不串用未保存名称；删除／
  切世界清理。平面图和小地图共用玩家 yaw 计算，底图保持北向。
- 新增每世界瞬时地图健康快照：损坏与错身份重置、隔离失败、保存失败、容量满额
  可区分。小地图出现注意标识，详情页说明真实状态；成功重试清除保存错误，失败不会
  覆盖原异常文件。备份帮助先说明整世界回滚范围，再按正常保存关闭流程进入对应世界
  的现有备份列表；保存失败保留当前会话，恢复继续使用原确认／事务路径。
- Debug／Release HUD **42/42**、地图几何 **26/26**，导航 **21/21**、归档 **13/13**、
  标记 **8/8**、文件 **28/28**；两配置地图 World **26/26**、客户端编译通过。
  Release 完整 WorldRuntime **2010/2010**，资源包 **114/114**、资产清单 **89/89**。
  中英键集合各 **631**，当前地图引用无缺键。证据为 `build/review-fixes-20260924/map-ui-*`
  与 `hud-*`，所有日志留本地。
- 首轮 Debug 地图测试 **25/26**：保存失败夹具在地表未变化时没有脏记录，未触发地图
  写入（备份阶段拒绝了其异常路径）。改为先用真实 World 命令创建待保存标记，再制造
  独立夹具路径故障；重试验证保存错误出现／清除、原文件保护均通过，首次日志保留。
- 更新 World 责任表及公开面 SHA-256，补齐这次触及的动物／地图接口。宿主无 PowerShell，
  原 PowerShell 门禁 `NOT_RUN`；按同脚本的声明、重复／缺漏和 hash 规则做 Python 镜像核对，
  **94 个方法**一致，日志 `world-api-map-audit.log`。未声称 Windows 门禁通过。
- 当前画面、正常鼠标备份入口、紧凑窗口／三字号和性能未运行；隔离夹具不算普通玩法。
  下一步更新独立工作包、核对资源身份，并保留原 Goal 的剩余批次和集中实机任务。

### 审查修复交付（2026-09-24）

三批代码本地提交，均未推送：

| 提交 | 审查项 | 已证实范围 |
| --- | --- | --- |
| `f112f52` 修复: 改善动物调度与连续贴地运动 | 1／3／4 | 公平预算、逐 tick 运动、局部支撑／重力、插值及既有生命周期 |
| `e211652` 修复: 分离界石发现与任务导航绑定 | 2 | 多界石实际交互、独立持久绑定、旧图迁移、远区重开／拆除／备份 |
| `a8bcb44` 修复: 统一地图标记编辑与异常恢复提示 | 5／6／7 | 跨入口编辑状态、实际 yaw、地图健康反馈；备份入口已接线，正常点击待验 |

- 已复用并刷新 `build/adventure-experience-20260922/SkyWorkbench.app`，保留其独立配置与
  存档，不新增重复工作包。刷新前只读核对无游戏实例；用户 `Working.app`、冻结 Baseline
  与 RegionBaseline 未改。普通包启动器仍走主菜单，没有写入自动进入世界的环境参数。
- 包代码身份 `a8bcb44a2bf34c4d7377a26aea0a5372cd59317e`，源码清单与当前提交一致、
  打包时无未提交 diff；macOS Release x86_64 程序 SHA-256：
  `2ced115f6942a41bf9363157f979c12a7f77616358dcf5184b2ef5400d0a73fe`。
  资源清单 SHA-256：`9c4bbd7eb7a0d7419e33c23e4aa0bb7a7926424280a35cbd3c2189a5b72e40cd`；
  源码清单 SHA-256：`32939f2b9d11a13341fb00412b3015ab6d700becaf9781d12949d088f9144864`。
- 发布清单 **119/119** 文件哈希匹配。使用既有 macOS 验证条件（seed 20260809、位置
  88.5／67／104.5、`HELLOMINE3D_VALIDATE_ONLY=1`），直接执行包启动器一次，独立存档
  在本批忽略目录；正常退出 0，地形／HUD／Actor 配置有效，两语言各 631 键。
  此模式没有建立图形窗口、没有实际鼠标或声音验收，不等于普通创建／探索回路。
  日志 `package.log`、`package-audit.log`、`package-headless.log`，本批证据约 2 MiB。
- 当前工程修复交付完毕；普通画面／动态、标记点击与备份入口、双语三字号两尺寸、
  同机三对帧耗仍 `NOT_RUN`，包 acceptance 保持 `NOT_RUN`。没有宣称整项体验 Goal 达标。
  后续恢复已保存的立体地图在制补丁并完成 B5，再继续规划 B6–B10；系统图形环境可用时
  合并为同一会话补实机与性能，避免逐项频繁冷启动。

## B6 UI 定稿落地 · 2026-09-25

用户已认可本地 E HUD、F 任务日志、G/H 地图与 I 暂停稿，授权完整实现、验证及中文本地提交。
共同约束为深灰半透明底、细边框、暖白文字、琥珀焦点与像素物品图标；正文和实时数据由代码绘制，
不把整张设计图用作背景伪装交互。地图仅用真实已知样本，任务与标记复用原服务，暂停与保存操作语义保持。

B6a 工程验证：`build/ui-upgrade-20260925/b1-{debug,release}-build.log`、
`hud-{debug,release}/test.log`、`hud-{debug,release}/map-test.log`、`b1-assets.log`。
两种配置客户端退出 0；输入 42、地图 26、资源文件 89 通过。双语目录无重复且 632 keys 完全对齐。
窗口视觉、交互与当前三对性能仍为 NOT_RUN，计划在稳定候选单个工作会话内集中检查。

B6b：任务日志采用可滚动列表／说明区与固定追踪入口，小窗口垂直分区；暂停菜单将旅程与难度折叠，
保留原难度应用和保存命令，保存失败以可见通知报告。新增界面图标为整数像素绘制，物品使用正式图集与几何。
`b2-{debug,release}-build.log` 双配置成功；资源 `b2-resources.log` 114/114。
`b2-input-settings.log` 首轮 98/99：新增文本导致旧 631-key 精确断言过时；两语实际均为 635，
更新冻结数量并检查新语义文案，`b2-input-settings-r2.log` 99/99。保留首轮失败，不改低任何门槛。
CUA 只读枚举确认工作游戏未运行；浏览器 provider 请求失败不等同于 native 窗口输入失败。

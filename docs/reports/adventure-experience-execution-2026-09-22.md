# 冒险世界体验升级执行记录

## 恢复区

- 范围／退出：[完整规划](../current/adventure-experience-plan-2026-09-22.md) B0–B10 全部必做，
  [Goal](../current/adventure-experience-goal-prompt-2026-09-22.md) 已由用户明确启动，宿主 active；无 token 上限。
- 起始 HEAD：`90951ee3ae7e767f9b2a8a3c6db6635565e5d3ed`；起始工作区干净。
  复用最近运行时 `ee42d30`，save v12／terrain v19；未重新宣称上轮正常采集或持续输入通过。
- 当前：B1a 已提交 `8bfa33d`，B1b 已提交 `c1a3b72`；B1c 区域实现与工程检查已完成，尚未提交。B2a 已本地提交 `8b0987d`，B2b 已提交 `006b4ce`，B2c 已提交 `e6b70bd`。
  用户明确反馈日月／云形过方、观感不自然，B1d 圆滑云形也被用户否定，当前优先 B1e 云层重做；各批工程 PASS 不代表美术获认可。
  用户要求降低客户端启动频率；B1 剩余完整场景／性能集中至阶段整合，B2a–c 已连续完成无窗口验证，当前进入 B2 集中实机检查与 B3 进入调查。所有验收与后段批次仍为必做。
- `Working.app` 已由用户实际操作，当前属于保护客户端，不关闭、不刷新。PID `80477` 的 cwd
  已核对至此包，普通世界 `world-9e3125aa7ef33202ab236751e6a02499`；程序／配置／元数据保护快照
  `build/adventure-experience-20260922/protected-user-b1c.json`。用户游戏中的保存变化不回滚。
- 新天空候选改用独立 `SkyWorkbench.app`，后续只复用这个工作包；不操作用户或历史客户端。
- 起始程序 SHA-256：`5ce36f2af39fbace92d42fc25feff53c175c4da8623a2cc449db206b9b2eb598`。
  直接采用已验证 Release 程序，119 文件重新打包；记录当前文档提交身份，不冒充重新构建 C++。
- 原包与普通存档保护清单：`build/adventure-experience-20260922/protected-before.json`，49 项哈希。
  启动时没有真实游戏／构建进程；`ps` 在默认沙箱拒绝，获自动审查允许的只读查询后确认，未调整系统权限。
- B1a 性能 session `53575` 已正常结束，18 次／138,338 帧 PASS，记录在 `b1a-performance-r2/`。
- B1c 画面 session `29338`（候选 30 组）、`43676`（基线八生态）均已结束；双配置客户端 `38538`
  与 V10C `49961` 均完成，后者各 28/28。旧 handle 不再视作运行中。B1c 性能尚未开始。
- B1d 34 组画面 session `15042` 已全部完成。GPU 45/45、双配置资源 114/114 完成。
- B1d 常驻性能 session `67084` 已结束，18 次／90,124 帧，Off P95 比 1.11164，FAIL；未运行该失败候选流送。当前无性能采样。
- B1e 画面 session `48769` 已按用户反馈停止（runner PID `84167`、SIGTERM、exit 143）；当前采样自然结束。已核对无诊断或游戏进程，未关闭用户客户端。
- 当前运行：无运行中构建／验证 runner。独立 SkyWorkbench 已刷新到 B2c Release `ea858816…`（119 文件），本轮仅发起一次 CUA 启动；实际进程 `99539`。窗口发生持续外部操作，CUA 两次状态变更保护均未执行 Escape，已停止竞争输入；保留现有窗口，不再次启动或刷新。B2 集中普通输入尚未完成，后续先核对进程与窗口归属。B2c 已本地提交 `e6b70bd`，聚焦 119/119、Release 世界 1789/1789、资源 114/114 与双配置客户端通过。下一独立实施为 B3 三动物。B1 已采原图先复用，余项集中验证，禁止直接恢复逐场景批量启动。
  `RegionBaseline.app` 冻结 B1b，`Baseline.app` 保持 B0；不原位刷新冻结包。
- 授权：当前完整 Goal、常规实施、独立客户端／正常测试世界、必要验证和中文本地提交；不推送。

## B0 冻结范围与验证表

普通创建种子固定为 **42、20260807、239701883**，普通难度；轻松难度补 seed 42。
前二十分钟按普通输入记录，至少一条完整外出／收获／返家／保存重开回路；已有保存世界仅作为保护对象，
不向其注入物品或修改元数据。新普通测试世界在本轮工作包独立 catalogue 中创建。

八类生态观察点复用 `tools/fixtures/terrain/adventure-ecology-v18.json` 的 seed 42、region 0–7：
草甸 `(1032,74,640)`、林地 `(52,84,-1152)`、高地 `(752,101,-32)`、沙丘 `(780,78,-1012)`、
峡谷 `(1396,110,-1684)`、湿地 `(1620,65,-956)`、雪山 `(1648,127,668)`、海岸 `(-1656,67,24)`。
坐标 Y 为冻结地面高度而非普通玩家传送承诺；诊断相机依据该地面抬高，接近路线与正常玩家移动分开。
连续对照复用 `tools/fixtures/terrain/adventure-v19-waypoint-routes.json`；海岛补充沿用现有岛岸 fixture。
洞穴具体入口在 B7 修改生成器之前从三个冻结种子的生产查询确定并登记，不事后挑选表现好的入口。

| 批次 | 必须核对的范围 | 当前结果 |
| --- | --- | --- |
| B0 | 身份、保护清单、种子／机位、预算、画面与性能基线 | 已冻结；朝日／背日画面及三档各三次帧耗有本轮基线；普通三种子路线待 B2 实走 |
| B1 | 日月／遮挡、云形／动态、八生态、林地海岸雪山四时段、两 FOV、档位回退、阴影 | B1a/b 已提交；外观按用户反馈重做为 B1e，GPU／资源与部分实景已有证据；新候选性能和最终动态待集中验收 |
| B2 | 食物前置、保存目标移出主线、事件引导、三个种子早期节奏、目标／奖励迁移 | B2a／B2b／B2c 已提交，前期任务、状态提示、危险／种植工程完成；普通路线及阶段 UI／性能待验 |
| B3 | 三动物、自然栖息、行为与贴地、生命周期、实体成本 | NOT_RUN |
| B4 | 三类地点各两布局、接近线索、三套建造组合、资源／奖励守恒、旧生成保持 | NOT_RUN |
| B5 | 探索持久化、个人标记、返家／任务导航、两视图、未知信息、存储／失效 | NOT_RUN |
| B6 | 世界／机器／设置／任务／地图页面、双语三字号两尺寸、错误与输入反馈 | NOT_RUN |
| B7 | 三地下空间、两目的地、三个种子入口／返程、水体接缝／版本／流送 | NOT_RUN |
| B8 | 第一人称使用、全身角色、基础动作、第三人称遮挡／切换、权威行为保持 | NOT_RUN |
| B9 | 三环境层、河海区分、动物声、四材质脚步、实际听感与暂停／设置 | NOT_RUN |
| B10 | 普通完整回路、地下／视角／声音、当前回归／累计性能／干净包 | NOT_RUN |

画面条件：1280×720 点，当前解锁 Retina 对照为 2560×1440 像素；FOV 90／120，RD8、中文、
UI 1.0、standard 材质。画面时刻 1000／6000／11500／18000，天体定位补 3000／15000；
Off/Off、Medium/Off、High/On（阴影／后处理）分别比较。窗口 framebuffer 不符合预期时记录失败，
不得把 1×／2× 或前后台混成同组。

性能：既有 capture 工具，5 s 预热＋30 s 采样、不截图；每场同条件三对交替，P95/P99 中位数比 ≤1.10。
天空首批固定 seed 42、草甸 `(1024,80,576)` 向天空 `(-44,100.2,0)`，另检查高地常驻／流送
`(752,200,-32)`／`(10,35,0)`；包内原版资源在改动前保持，可交替恢复为同一程序的基线。
后续实体／地图／地下场景各在该批实现前冻结，最终同时对 B0 与上一批评估累计帧耗、RSS、GPU 和生成成本。

## B0 新增系统预算

以下为本 Goal 新能力的上限，进入对应实现前在合同明确统计与边界测试，不替代旧系统已有上限。

| 系统 | 上限／约束 |
| --- | --- |
| B1 天空 | 复用现有天空绘制，不新增 draw call；不新增纹理、RTT、世界查询或逐帧 CPU 噪声；shader 循环须固定有界 |
| 新动物 | 每世界最多 24 个活跃自然动物，玩家 64 m 内最多 12 个；不占用或暗改敌人难度名额 |
| 动物决策 | 每只最多 5 Hz 决策，新增地面／避障查询全局每 fixed tick 最多 48 次，轮转避免饥饿；禁止无界路径搜索 |
| 动物／玩家几何 | 每动物最多 8 个盒体部件（96 三角形），玩家最多 12 部件；动物总新增顶点不超过 10,000，不增加逐实例纹理 |
| 地图 | 继承局部 65×65、195 列／更新和 30 Hz 预算；持久地图运行缓存 ≤16 MiB，磁盘每世界 ≤64 MiB，个人标记 ≤64；满额有提示／明确降采样策略 |
| 声音 | 保留现有全局 16 声部、32 样本、4 MiB 解码样本预算；环境层最多 3 个同时发声，不能饿死交互／危险提示 |
| 相机／地区氛围 | 只查询驻留／纯派生值，不同步加载未知区块；插值与遮挡检测次数在各批合同固定 |

帧时间与内存实际基线保存在三档性能原始记录；上述预算在效果和性能实测前冻结，不因超限结果自动上调。

## 执行与失败记录

### 2026-09-22：启动与 B0 初始核对

- 完整阅读规划、工作流、相关架构、大气／阴影合同与验证矩阵，启动宿主完整 Goal。
- 新工作包程序与 HUD 交付包哈希相同，历史三个用户包程序仍与先前记录一致。
- `b0-sky-morning` 两张 2560×1440 隐藏截图成功，首帧实见云团边缘模糊、云体明暗层次偏弱。
  最初朝向 `(-40,280,0)` 为背太阳方向；保留结果作为云形基线，另按真实相机矩阵在修改前补
  `(-44,100.2,0)` 的朝日基线，不将未朝向太阳的图判为“太阳缺失”。
- B1a 拟仅修改生产天空 shader：默认路径增强像素日月并正确合成遮挡，FS2 回退保持像素一致。
  云形重构、区域氛围和阴影动态仍是后续 B1 范围，不因 B1a 通过而关闭整个 B1。

### B1a：日月与遮挡实施中

- 启动与冻结文档已本地提交 `680f8a5`。新增[天空合同](../contracts/adventure-sky-contract-v1.md)，
  只改生产天空 fragment，不改 C++ 运行时／uniform／存档。太阳采用暖色边缘和内核，
  月亮采用冷色像素坑纹；所有天体与光晕在云之前合成。实际太阳方向基线已查看，旧版为白色圆斑。
- 离屏生产 GPU 36/36 PASS，Apple M1 Pro；工具 `validate_sky_shader_macos.cpp --celestial`。
  两类天体在冻结云柱中的最小透射比为 0.140104／0.137826，原版及删除云混合的反例均检出；
  FS2 白天／夜晚及直视日月逐像素相同，云层上下边界、漂移和天体正午连续通过。
  输出 `b1a-gpu-r1`；该证据是诊断 GPU，不是普通输入。
- 强制重建并运行 Debug／Release ResourcePackSmoke，各 114/114 PASS，
  构建和运行日志为 `b1a-resource-{debug,release}*.log`；没有重复无关完整世界回归。
- 当前工作包已接入候选资源，程序哈希保持起始 Release；`b1a-sunward-fov90`、
  `b1a-sunward-fov120`、`b1a-moonward-fov90` 各两张 2560×1440，已逐组查看首帧。
  FOV 90／120 太阳轮廓清楚，夜间月亮坑纹可辨；尚未以两张帧宣称普通持续移动或整个 B1 完成。
- 为原版／候选交替性能保留第二个独立工作包 `Baseline.app`，不同 bundle ID，
  从修改前已验证工作包仅复制 119 清单文件并复核；它没有用户存档，冻结原版程序和资源。
  同一 Goal 仅复用 Baseline／Working 两包，不为每轮采样新增应用副本。
- 后续 B9 调查补充：当前 `AudioRuntime::platformBackend` 仅 Windows 返回真实后端，macOS 返回空后降级。
  因此本机实际声音能力需要在 B9 内补齐或取得适用平台的真实声音证据，不能仅增加 WAV 和字幕就声称有声。
- 首轮 Off 三对的 P95／P99 中位数比为 **1.4934／1.2512，FAIL**，不放宽 1.10 护栏；
  同组加载 361 区块、地形 GPU 29,405,120 字节一致。当前实现对整片天空计算两套日月投影和坑纹，
  优化为先计算连续像素导数，再以光晕角范围提前返回；避免在非一致分支中求导，保持效果与遮挡。
  此为性能修复原因判断，不将一次性能差异直接证明为唯一原因；首轮其余档位均运行完原候选，未混入优化资源。
- 首轮全部 18 次／150,075 帧保存在 `b1a-performance/`，`comparison.json` 结果 FAIL。
  Off 的原版／候选 P95 为 2.728／4.074 ms，P99 为 4.268／5.340 ms；
  Medium 为 5.531／5.023、6.634／6.446 ms；High 为 6.767／6.778、7.408／7.427 ms。
  原始 CSV、失败记录及候选 shader 均保留，未删除慢样本。
- 优化源码 SHA-256 `6162f41acbecc1fdc8cf3e58de7bea7dc13d1dbf416d2e8950688403b101a568`，
  与当前 Working 资源一致。GPU 第二轮 36/36 PASS（`b1a-gpu-r2.log`），
  Debug／Release 资源各 114/114 PASS（`b1a-resource-r2-{Debug,Release}.log`）；
  复用对应配置对象重新链接资源检查程序，未冒充整个游戏重建。
- 优化后 `b1a-r2-sun90`、`b1a-r2-moon120`、`b1a-r2-fallback` 各两帧，已查看首帧；
  默认像素日月与旧大气圆形回退分别成立。普通输入和 B1 全范围动态仍待后续检查。
- 第二轮全部 18 次／138,338 帧通过，按原 CSV 重算分位数并核对包、配置、framebuffer、实际模拟与采样条件。
  三档同轮原版→候选 P95／P99 中位数如下（单位 ms）：

  | 阴影／后处理 | P95 原版→候选 | 比值 | P99 原版→候选 | 比值 |
  | --- | --- | --- | --- | --- |
  | Off／Off | 4.687 → 4.655 | 0.99317 | 5.668 → 5.617 | 0.99100 |
  | Medium／Off | 5.731 → 5.747 | 1.00279 | 6.592 → 6.552 | 0.99393 |
  | High／On | 6.696 → 6.747 | 1.00762 | 7.596 → 7.506 | 0.98815 |

  不混合两轮绝对耗时；当前结果仅代表同轮交替测量，不把性能变化归因为已证明的单一硬件或 shader 原因。
- 每次 361 驻留区块、4 个自然敌人，地形 GPU buffer 29,397,984–29,405,120 字节；没有新云纹理、RTT、实体或缓存。
  三档峰值 RSS 的三次中位数原版→候选为 339.45→342.19、310.70→324.05、314.09→315.46 MiB。
  网格构建平均 1.037–1.049 ms／section；一次保存 1.904–2.476 ms；原始每次数据保留。
  这些是进程 RSS 与已统计地形 GPU buffer，非整个驱动 GPU 显存测量。
- 已额外查看优化后朝日／朝月第二帧，云与天体持续位移，没有在这两次固定视角中发现跳变；
  不代替转头／步行／完整阴影检查。49 项用户包、配置、存档元数据哈希仍一致。
- B1a 无持久语义迁移：save v12、terrain v19、objective v3 保持。B1b 云形与 B1c 地区、八生态和动态验收继续必做。


### B1b：云团实施与验证

- B1a 提交 `8bfa33d` 后才接入本批。新增临时性能基线 `SkyBaseline.app`，
  bundle `local.hellomine3d.adventure-sky-baseline`，119 清单文件、约 51 MiB，无用户存档；
  复用同一 Working 包，不为每张截图复制应用。B1 关闭后整理阶段基线。
- 两个采样使用同一世界格子场，约 7.67 m 的块状轮廓与连续倒角、深底面和明侧面；
  保留太阳／月亮遮挡及 FS2 完整回退，未增加 shader 接口、纹理或持久状态。
- 首版生产 GPU 41/41、双配置资源各 114/114 PASS；首版 `b1b-{sun90,sun120,moon120,forest,coast,alpine}`
  各两帧、`b1b-sweep` 八帧。已看六组静态首帧；固定海岸机位朝向内陆，不能将其当作水面远景检查。
  雪山及大 FOV 中发现局部淡薄方片，收窄密度过渡范围 0.018→0.006；保留首版，不因自动检查通过忽略实景问题。
- 修正后 GPU 41/41 PASS（`b1b-gpu-refined.log`），包括 60 Hz 昼夜漂移、正负坐标风移不变性、
  日月遮挡与错误反例、上下边界和原版回退。冻结云柱画面中，过渡像素比例从旧版 0.01953 降至 0.00954，
  有云覆盖比例 0.10416，仍保留留白；该数值只为诊断云柱，不冒充全世界云量。
- 当前生产云 shader SHA-256 `e3c28f2a2e8e8c0ad9d774c44f3d5f561cf6ebdc271603a1554815499c89e83e`。
- 修正后双配置资源各 114/114 PASS，日志 `b1b-resource-r2-{Debug,Release}.log`；
  `b1b-r2-*` 五组静态各两帧、一组诊断转头八帧全部 CAPTURED。已看朝日／朝月／雪山首帧，
  以及 sweep 的 5／8／11／15 秒；其余帧在本批继续审阅。没有把诊断相机写成玩家正常输入。
- B1c 准备采用当前生成器的纯 biome／高度查询，世界 64 m 网格四角缓存，单次最多 8 次纯查询，
  每帧只插值；缓存不超过 128 字节，切世界清空。候选实现与检查暂存忽略目录，尚未进入生产路径，
  不在 B1b 性能采样期间构建或刷新包。
- 继续查看 `b1b-r2-coast` 第二帧与八帧 sweep 时发现孤立细线：连续坐标变形再采样噪声可在格子过渡处
  形成窄小的非实体云片。B1b 暂不提交；当前性能按原修正版跑完并保留，下一原型改为世界格子中心的实体覆盖，
  继续守住现有连续漂移检查，不以放宽门槛消除失败。
- 前一修正版性能完成 18 次／137,691 帧，三档 P95 比 0.99851／0.98602／1.00480，
  P99 比 1.01919／1.02165／1.01213，性能 PASS；但孤立细线导致本版视觉未验收，不将性能通过写成批次完成。
- 直接采用实体格子中心的原型被 `day-continuous-drift` 检出 FAIL（`b1b-gpu-cells.log`），未接入生产包。
  最终修复先对细格四角确定实体覆盖，再连续混合覆盖；四角共用同一粗格的四个噪声值，
  每条射线两次层采样共八个 hash，较旧有六个噪声 octave 的 24 个 hash 减少。
- 最终离屏 `b1b-gpu-filtered-crisp.log` 41/41 PASS；60 Hz 昼／夜最大平均通道差为 0.003681／0.001404，
  沿用原 0.05 护栏；云边过渡像素比例为 0.008987（B0 0.019531），云覆盖 0.135132。
  日月云后透射仍为 0.140104／0.137826；正负坐标风移及错误反例通过。
- B1c 忽略目录的区域气候缓存原型已在 O0／O3 各通过 22 个纯逻辑检查，88 字节缓存，
  包括采样上限、重复查询、正负／极值坐标、连续边界、切世界重置、昼夜与水介质保持；生产路径尚未接入。
- 最终 B1b shader SHA-256 `38906d570cff12c267f7b8c24811c12e1edb1c8ebc33cf9fa5fe3c1ce4279005`，之前的候选哈希仅属历史记录。
- 最终 `b1b-r4-sun120`／`b1b-r4-coast` 第二帧已查看，原孤立细线消失；云团保留块状边界、明暗厚度与天空留白。
  最终资源双配置各 114/114 PASS（`b1b-resource-r4-{Debug,Release}.log`）。
  与 B1a 的最终性能正在重新采集，不能沿用前候选的 PASS。
- 最终 `b1b-r4-sweep` 八帧现已全部查看（5–15 秒），`b1b-r4-alpine` 第二帧也已查看；
  未再见孤立细线或整片云跳变。此为限定诊断连续视角，不替代普通行进。
- 用户保护清单复核 49/49 一致（`b1b-protected-check.json`）。一次核验脚本错误地重复拼接相对路径，
  已按清单原路径修正；该次脚本失败不代表用户文件发生变化。
- 最终 B1b 对 B1a 三档各三对、18 次／143,294 帧 **PASS**（`b1b-performance-r2/comparison.json`）。
  同轮原始 CSV 重算分位数，核对程序、设置、真实 framebuffer、预热与采样；后台 2560×1440，没有截图编码。

  | 阴影／后处理 | P95 原版→候选 ms | 比值 | P99 原版→候选 ms | 比值 |
  | --- | --- | --- | --- | --- |
  | off／off | 4.727 → 4.615 | 0.97631 | 5.747 → 5.703 | 0.99234 |
  | medium／off | 5.683 → 5.712 | 1.00510 | 6.524 → 6.611 | 1.01334 |
  | high／on | 5.035 → 5.270 | 1.04667 | 6.037 → 6.323 | 1.04737 |

- 三档峰值 RSS 的三次中位数原版→候选为 340.39→342.03、313.26→315.36、315.65→311.35 MiB。网格平均构建 1.031–1.046 ms；单次保存 1.853–2.337 ms；
  原始驻留／GPU buffer 指标保留在各次 summary；新天空没有新增纹理、RTT、实体或 CPU 缓存。
- B1b 没有持久化迁移，save v12／terrain v19／objective v3 保持；B1c 与后段仍继续实施。

### B1c：区域氛围与整合检查

- 云团已按小批次提交 `c1a3b72` 后接入本批。新增 `RegionBaseline.app` 冻结 B1b 程序与 119 项资源，
  独立 bundle `local.hellomine3d.adventure-region-baseline`，约 51 MiB，无用户存档。
  旧阶段基线在本批收口后整理；Working 仍是唯一可刷新的工作包。
- 生产 `OgreBootstrap::syncEnvironment` 接入 64 m 四角区域缓存，切世界三条路径重置。
  单元最多八次 biome／自然高度纯查询、固定 88 字节，无逐帧重复生成查询；预算先于实机验证冻结。
- 对照机位冻结于 `run_b1c_visual.py`：八生态按 B0 坐标，海岸改朝 270° 观察海侧，在候选接入前确定；
  原朝内陆机位仍保留历史归属。候选 30 组覆盖八生态双 FOV、三重点生态四时段、昼夜关闭与三个连续场景。
- 后续 B2 调查已确认食物铁剑前置、保存重开主线、定义 v3 和 Alpha 十个稳定 ID；尚未修改任务运行时。
- 首次单目标 Debug 构建失败：此前清理已移除 `libtracy.a` 等 Debug 依赖库，非 C++ 编译错误。
  原失败日志 `b1c-client-Debug.log` 保留；改用生成 workspace 的 `HelloMine3D` 依赖目标补建，
  不修改第三方源码或生成工程。新增依赖产物复用于后续批次，交付时清理冗余副本。
- 补建后 Debug／Release 客户端均通过；纯区域检查各 22/22 PASS，生产阴影滤波 GPU PASS。
  Debug 程序 SHA-256 `6452bdfe1cb9d32782e376988bb35cb3ff18c712780ed5d8912f86a6034d2118`，
  Release `6be67d09bc27b756778b269a93753ee42e6d8d131a349b8e769d3ddde032a43c`；均为当前 C++ 构建，
  新 Release 已刷新 Working 的 119 项清单；用户包仍保持原程序。构建与源码身份在 `b1c-builds.json`／包收据。
- 候选已查看草甸 FOV 120、林地／高地／沙丘／峡谷／湿地／海岸 FOV 90 的第二帧，
  以及林地夜间 FOV 120；近处地面、敌人和树干可辨，没有因区域着色出现黑面或整屏色偏。
  高地冻结机位位于树冠下，此图覆盖林内氛围，不单独宣称高地远景验收。

### B1d：按用户反馈修正天空轮廓

- 用户通过当前海岸实景反馈日月／云过方、观感不自然。本批改为圆形日月和不规则云团，
  保留像素内纹、轻量配色、世界坐标连续运动与遮挡；不继续将规则方牌、云板作为美术验收目标。
- CUA 曾准备关闭工作包，但收到“用户改变应用，请重新查询”提示后停止；重新读画面确认用户正在普通世界中操作，
  未重试关闭。此为窗口所有权冲突，不是系统权限拒绝；另建后台工作包继续已授权工作。
- 首个忽略目录原型 `b1d-natural-prototype.frag` 日月圆形、云缘使用连续密度团块。
  `b1d-gpu-natural-r1.log` 的遮挡／回退／连续漂移均通过，但边缘清晰度 FAIL：
  过渡像素 0.021118 高于原版 0.019531，不能用旧门槛通过来掩盖模糊。继续收窄云边，保留失败记录。
- 第二版 GPU 41/41，通过边缘／遮挡／连续性，但海岸实景仍有上下两片分离的薄饼感，视觉 FAIL。
  下一原型改为固定六次体密度采样填充厚度，每次四个 hash，总 24（等同 B0 原云噪声预算）；
  不新增资源、draw call、CPU 世界查询。先冻结预算，再实测三对性能，不因美术方向改变放宽 1.10。
- 第三至第五原型保留 GPU 边缘 FAIL（过渡比例依次 0.041245／0.017929／0.016006），
  第五版实景已消除上下端面分离；继续收窄积分后的覆盖边缘，不修改检查门槛。
- 最终候选 `b1d-natural-r6.frag`／生产 shader SHA-256 `35e79bd2e829d267d468d52f0f214c54392e58ab761311d2f9c82cfe885d2ffb`。
  生产 GPU `b1d-gpu-natural-r6.log` **45/45 PASS**，包括新增圆日／圆月和错误方牌负例；
  60 Hz 昼夜最大平均通道差 0.008991／0.002743（护栏 0.05），日月最小透射 0.060618／0.059856；
  云覆盖 0.282516、过渡比例 0.015167（旧版 0.019531），留白／清晰度、风移和 FS2 均通过。
- 当前 Debug／Release ResourcePackSmoke 各 114/114 PASS，`b1d-resource-{Debug,Release}.log`。
  C++ 复用本轮已构建的 B1c Release，不将 shader 修正冒充新的 C++ 构建。
- 独立 `SkyWorkbench.app` 已创建，119 个清单文件，程序仍为 `6be67d09…`；用户 Working 未刷新。
  当前八生态／昼夜／FOV／回退／动态及日月共 34 组画面 session `15042`，
  输出 `b1d-visual-candidate/`，是诊断机位证据，非普通输入。

### B1d 收尾与 B1e 方向修正

- 用户再次明确云形不正确，B1d r6 美术结果记为 **FAIL**，不再称为最终候选。
  本轮已看 31 组静态第二帧、林地昼／昏各八帧；区域过渡查看 5／8／11／15 秒，未查看其余四帧。
  不再为被否定的外观补齐剩余美术采样。月夜雪地近景的棕色矩形另记待查，早期敌人威胁留给 B2 核查。
- B1d 三档三对常驻全部保留，Off P95/P99 为 9.584→10.654 / 11.877→12.624 ms，
  比值 1.11164 / 1.06289；Medium 8.595→8.603 / 9.528→9.606，1.00093 / 1.00819；
  High 7.325→7.611 / 8.513→8.642，1.03904 / 1.01515。整体 **FAIL**，不删除慢样本，不启动该候选流送。
- 调查发现 r6 单频密度场、固定厚度、硬覆盖阈值及梯度法线叠加，形成圆滑石块感。
  “边缘过渡比旧版更窄”是先前造型假设，不能用它代替云形审美；保留旧模式及失败结果供追溯。
- B1e 改为轻薄大气云层：多尺度覆盖与受光分开，保留薄缘、浓淡、云群留白，取消硬挤出形态和实体式法线。
  首个原型每像素四次 value noise／16 hash，无新增纹理／RTT／CPU 查询／draw call；
  日月、风移、遮挡、时刻权威和旧模式回退保持。先做固定海岸／林地／雪山整屏检查，再跑适用全面检查。
- 参考源码路径和工程标识仅记在 Git 忽略笔记，未复制源码或素材入正式资源。

- B1e r1 海岸实景过淡／模糊，r2 仍偏平；记录两版视觉缺陷，未为它们启动全套性能。r3 增加第四层疏碎密度与浓密区域明暗，合计五次 value noise／20 hash；固定三场景 session `87613` 已结束，三组第二帧已查看；近缘柔和度与大 FOV 云量继续整体复核。

- B1e r3 生产 shader `cb5b0b3860c6ccdbb85d404ac9e58eb40e18d7b353340b46bc2e1e77c5bc1326`，GPU 47/47 PASS（`b1e-gpu-r3.log`），双配置资源各 114/114 PASS；GPU session `50722`／资源 session `9151` 均已结束。新轮廓的完整实景与性能待执行，尚未提交或宣称 B1 达标。

### 检验节奏调整（用户反馈）

- 上一目标轮为进展：实现 B1e 薄云、47 项 GPU 正反例和双配置资源验证；用户随后指出客户端启动过频繁。
- 确认问题在逐场景冷启动，不是缺少更多截图。已停止 34 场景驱动，实际 26 组采样收据保存在 `interrupted-batch.json`，未执行项目保留 NOT_RUN。
- 日常渲染修改以一次 GPU 进程内多条件检查和少量代表实景为主；已有当前版本证据不重采。
  完整八生态／昼夜／两 FOV、设置／生命周期与必要三对性能集中阶段验收，优先复用工作客户端；不放宽质量和性能护栏。
- 新方案未有充分工程／画面证据前，不再启动整套性能；下一次图形启动前明确剩余问题与能复用的当前证据。

### B2a：解除前期目标前置与定义迁移

- 目标定义 v4 保留 34 个稳定 ID 和注册顺序；可见必做 32，保存重开改为可见可选历史项。
  熔炉改由拾取战利品开启，面包由放置工作台开启；战斗三次仍须先完成铁剑，不因食物提前立即催促战斗。
- Alpha 十步门面独立按旧十位完成标志投影，保持重开标志／历史，不因其他并行机会被错误投影为 Complete。
- save v12／terrain v19 不变；定义 v1／v2／v3 显式归一 v4，未知目标、配方发现、部分进度、玩家状态与旧标志镜像保持。
- 本批没有新增逐帧排序、渲染、缓存、模拟字段或生成查询；预算保持既有目标 128、保存集合各 256 项。
  状态相关提示／机会排序、早期危险与普通三种子回路属于 B2 后续必要项，当前未声明完成。

- B2a Debug 聚焦 `ADVENTURE_PROGRESS` **77/77 PASS**（`b2a-focused-Debug.log`），其中新增 14 项覆盖前置、旧门面、三个定义版本迁移；食物、结局、N1/P11C 现有检查均在本轮执行。未启动图形客户端。

- Release 聚焦同为 **77/77 PASS**，完整世界 **1730/1730 PASS**（`b2a-focused-Release.log`／`b2a-world-full-Release.log`），覆盖原 G6 真实领域路径、奖励／胜利、保存事务与生命周期；此为无窗口工程证据，不冒充普通输入。
- Debug／Release 客户端构建通过，资源各 **114/114 PASS**；本轮没有新增图形客户端启动。
  当前工作区 Release SHA-256 `7ab7e7b8ab0135055128b4d5caff179ed326144700e830db3c26fcf134e1830b`，
  含待提交 B1c 区域代码，作为工作构建保存在 `HelloMine3D-B2a-Release`，未刷新用户或冻结包。
- 原保护清单 **49/49 一致**；用户当前工作包另查程序／配置／存档存在与哈希，结果 `b2a-user-working-check.json`，不回滚用户保存。
- 下一批 B2b：让前置提前后的食物真正进入近期机会，依据库存／生命／照明状态提供提示；另修查自然敌人出生环与追击半径重叠的早期压力，普通三种子路线仍为必要验收。

- B2a 本地提交 **`8b0987d` `玩法: 提前食物引导并解除重开前置`**，只暂存本批核心／资源／文档；天空与区域修改仍独立未提交。没有推送，没有更新宿主 Goal 为完成。

### B2b：真实状态指导与近期机会

- 先修前置后，确认面包仍可能被 HUD 注册序前三项挤出。本批以固定三槽选择替代简单截取，
  保持稳定 ID／同优先级注册顺序；工作台后展示食物，受伤且具备面包时提升恢复，入夜提升照明和落脚点。
- 只读 World→Alpha→Objective 传入实际生命、冷却、每日模拟时钟；一次库存扫描选择材料步骤。
  HUD／暂停／任务详情共用指导文本与改键，手动追踪保持，状态变化重新展开简短说明。
- 新增 18 项双语 key，仍是 34 目标／32 可见必做、definition v4、save v12／terrain v19。
  不创建新持久状态、不改变事件完成／库存／奖励，不新增实体、地形查询或缓存。
- 首轮聚焦保留 `b2b-focused-Debug.log`：98 项中 3 FAIL。原因是旧双语数量断言未追加 18 项，
  新测试保存夹具漏设 `hasPlayerState` 导致按未进入世界处理；修正夹具并清除诊断时刻后重验。
  不放宽运行时语义或性能阈值。编辑脚本一次断言失败发生在测试文件落盘前，未算成构建／运行时 PASS。
- 本批继续无图形客户端启动。UI 三字号／紧凑窗口、正常食物获取与三种子、阶段三对性能保留 NOT_RUN；
  当前图形包尚未刷新，不能将新核心与文本说成已经在用户客户端生效。

- Debug 首次客户端编译抓到暂停页局部变量与新辅助函数同名；改为独立函数名后重编，原失败 `b2b-client-Debug.log` 保留。核心修正夹具后聚焦 **98/98 PASS**（`b2b-focused-Debug-r2.log`）。

- B2b 最终 macOS／gmake／Apple clang／x86_64：Debug、Release 聚焦各 **100/100 PASS**；
  相比 B2a 新增 23 项。Release 完整世界 **1753/1753 PASS**，保留全部原用例；双配置资源各 **114/114 PASS**。
  日志 `b2b-focused-Debug-final.log`／`b2b-focused-Release.log`／`b2b-world-full-Release.log`／
  `b2b-resource-Debug-final.log`／`b2b-resource-Release.log`；全部进程 exit 0。
- 双配置真实客户端构建通过。Debug SHA-256 `763da43b830b94dc981f2a1019aeaa468b5c8961f534fc094a8b04ef0516df68`；
  Release `924a954715b8f9fc3b080596a3f71224b7e37396e58f0711983a0f90a999006e`，
  文件 `HelloMine3D-B2b-Release` 13,016,184 字节，含待提交 B1c 区域 C++，属于工作区构建。
  包未刷新；本批图形客户端启动 **0 次**，没有将编译、无窗口测试或二进制备份误记为 GUI 启动。
- 保护检查 **49/49 + 用户 Working 3/3 一致**，进程只读查询未见游戏／诊断残留；没有关闭用户窗口。
  最终 Debug session `8271`、Release 世界 `99182`、Release 客户端 `25836` 均已结束；无正在运行的工具 handle。
- 提交范围只含 B2b 核心／UI／文本与本合同，架构和矩阵仅暂存 B2b 对应行；B1c／B1e 继续独立未提交。
  UI 全尺寸实机、普通采集恢复／三种子与性能仍 NOT_RUN，作为 B2 阶段整合和完整 Goal 的必需项。

- B2b 本地提交 **`006b4ce` `玩法: 根据生存状态组织近期任务`**，16 个本批文件；无推送。
  B2c 调查确认自然生成最近候选 8 m，小于三类敌人 10／14／18 m 追击距离；尚未调整。
  下一步修正出生缓冲与白天／新手危险节奏，核查不依赖战斗的小麦获取，遵循难度／生成兼容边界。

### B2c：早期危险、照明与土层种植（跨日继续于 2026-09-23）

- 核查发现旧 8 m 内圈与 10／14／18 m 追击范围重叠，白天每秒刷怪且不看照明。新地表自然生成
  在每日 12000 tick 起运行，候选 24–30 m，最终按实际追击半径 + 4 m 过滤；方块光 >= 8 和树冠／
  树干顶拒绝。旧敌人日出不删除，显式路标守卫不受新自然筛选影响。
- 难度档案、攻击／掉落、自然 cap 与尝试预算保持，地形 v19／save v12／objective v4 保持；
  新规则只改变后续运行时自然生成，不编辑地形或已有 Actor。小麦新增林床／淤泥种植，与草地／泥土并行。
- Debug 首轮 `ADVENTURE_SURVIVAL` **110/110 PASS**（`b2c-focused-Debug.log`），包括新的 35 项、D3／D5／D6／G6、难度与食物原用例。
  原人口夹具使用明确夜间时刻及覆盖新距离的加载范围，保留真实生成／cap／保存／卸载／战斗断言。
  D6／G6 既有夹具包含预置地面及位置安排，继续仅作工程路径，不称为普通输入。
- 三个冻结种子正常初始化后，32 m 有界调查分别取得 64／54／41 个成熟高草与适种土层，最近距
  1.87／3.94／7.91 m；领域采集／种植／随机生长各取得 3 小麦，无物品注入、无敌人战斗。
  这是生成资源和领域流程证据，不能替代正常移动、采集或前二十分钟的真人／Computer Use 体验。
- 首次 CSV 将选取的 3 个位置误记为总适种数量，已改为保留完整数量；原日志中的 64／54／41 正确。
  加强加载守卫为前后实际 existing／loaded 数相等，继续保留原 100,100 未加载断言；尚待最终重验。
- B2 性能需要在同等实体负荷下复核；不以白天减少敌人数量造成的更简单场景换取 PASS。
  本批仍未启动图形客户端，UI／普通路线／当前性能保持 NOT_RUN。下一步最终双配置、Release 完整世界和中文本地提交。

- Release 首轮聚焦 110/110 PASS；完整世界 1788 项中 1 FAIL：旧 AL-A5 阶段指标在白天断言必须生成，
  `cycle_attempts=0 budget=16`。保留 `b2c-world-full-Release.log`，补独立白天零工作／预算身份断言，
  原有“生成周期有工作、下一 tick 清零”改在夜间检验，未删原断言；纳入本批聚焦后重跑。

- B2c 最终 macOS／gmake／Apple clang／x86_64：Debug、Release 定向均 **119/119 PASS**，
  Release 完整世界 **1789/1789 PASS**（相比 B2b 增加 36 项），双配置资源各 **114/114 PASS**。
  最终日志 `b2c-focused-Debug-final.log`／`b2c-focused-Release-r2.log`／`b2c-world-full-Release-r2.log`，
  修正后的 AL-A5 实际夜间尝试 16／预算 16，白天及非周期均零，未放宽护栏。
- Debug／Release 客户端构建均通过。Debug SHA-256 `bde56ba5c59923aa258cbceaa7f1e6091693eac6ab78256b07ededf225decfa2`；
  Release `ea8588168dc1134bfeaea81e8d5db899a0ed39b923c2b182081b5b45f2a999b5`，
  工作构建 `HelloMine3D-B2c-Release` 13,016,184 字节，含待提交 B1c 区域代码，不冒充独立干净包。
- 最终保护检查 **49/49 + 用户 Working 3/3 一致**。B2c 图形启动 **0 次**；Release 世界 `18188`、
  Release 客户端 `55047` 和最终 Debug `79001` 均 exit 0，无运行中验证 handle。
- 清理两份已被本轮候选替代的 B2a／B2b 单独程序副本，释放 26,027,984 字节；历史日志／SHA／源码
  保留，历史报告中的这两个独立程序路径现已清理。当前 B2c、所有用户／冻结包及 B1 对照未动。
  三种子资源 CSV 已收进本批忽略产物目录，避免后续回归覆盖唯一调查摘要。
- 本批不声明 B2 普通二十分钟路线或 B1–B10 总 Goal 完成；当前包刷新、正常输入、整套 UI 与
  同负荷性能仍须阶段验收，后续三动物／地下／玩家模型／声音范围保持。

- B2c 本地提交 **`e6b70bd` `玩法: 留出探索准备时间并完善种植条件`**，12 个本批文件，无推送。
  下一次实机使用已停止的独立 SkyWorkbench 工作包，明确刷新至当前代码和资源后只启动一次并复用；
  用户 Working 与冻结基线继续保护。B2 普通输入／多尺寸状态提示未验完前不能称 B2 全部完成；
  受限时继续 B3 独立实现，不重跑已经通过的 B2 核心回归。

### 集中实机工作会话与窗口归属（2026-09-23）

- 在确认工作包已停止后原位刷新 SkyWorkbench，119 文件，程序 SHA-256
  `ea8588168dc1134bfeaea81e8d5db899a0ed39b923c2b182081b5b45f2a999b5`，
  资源清单 `9c4bbd7eb7a0d7419e33c23e4aa0bb7a7926424280a35cbd3c2189a5b72e40cd`。
  程序含待提交 B1c 区域实现，仍是候选工作包，非最终干净交付；用户 Working 和冻结包未刷新。
- 本轮仅一次 `cua.getApp` 打开既有工作包。工具报告等待 14,290 秒，后续实际进程
  `99539 ./HelloMine3D` 的 elapsed 为约 78 秒，不能把工具等待时间当作游戏冷启动成本。
  没有因工具迟缓另启第二实例；原进程查询漏掉相对路径 `./HelloMine3D`，已补查实际命令。
- CUA 可读取窗口及 HUD，实际画面是高处俯瞰；未记录从普通菜单创建世界的过程，不算普通
  出生／移动证据。两次 Escape 均被工具的“用户已改变应用，重新查询状态”保护拦下，
  中间已重读；第二次后停止操作，未换工具发送输入、未关闭窗口或刷新其资源。
  这是并发窗口操作限制，不是权限拒绝，也不是游戏暂停功能 FAIL。
- 本次 `AI_INTERACTIVE` 的菜单、普通输入和多尺寸任务提示仍为 NOT_RUN；现有截图不用于
  关闭 B1 动态、B2 普通路线或人类审美。工作流已明确包复用与进程复用的区别，保留阶段门禁。
- 独立 B3 进入调查已核对 Actor／LivingActor、固定 tick 的 64 项轮转、自然人口、快照渲染、
  world.meta 的 Actor kind 0–2 与备份路径。三动物尚未实现；下一批落实独立动物行为和生命周期，
  不把敌人换色当动物，也不把本次调查标成 B3 完成。

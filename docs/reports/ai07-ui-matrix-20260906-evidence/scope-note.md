# Scope visibility in independent UI acceptance

The owner already authorized normal gameplay acceptance in the persistent Goal.
The fresh recovery agent was intentionally started with no inherited history and
a narrow window-control request. Its automatic approval reviewer therefore did
not accept parent-forwarded quotations of the earlier owner authorization for
world creation. One ordinary retry after adding that evidence was rejected;
no further retry or alternate execution of the rejected action occurred.

Package and binary hashes match; the form is filled, but no new world exists.
Six UI combinations remain NOT_RUN. The proposed recovery is an explicitly
owner-authorized visible independent acceptance task whose initial user message
contains the real validation scope. No security settings or thresholds change.
This is an approval-context limitation, not a game defect or a withdrawal of the
owner's original Goal. Existing recovered menu control and saved images remain
valid within their bounded scope. AI-06 package-only isolation remains separate.

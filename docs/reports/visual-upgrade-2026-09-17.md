# 视觉升级执行记录 2026-09-17

状态：**已实现并完成本轮自动回归；实机视觉、交互与性能验收待图形会话恢复，整体仍 Doing**。
用户已授权体验、制定方案并直接实施，随后要求离开电脑期间自主推进，不要求其在线申请权限；见[升级方案](../current/visual-upgrade-plan-2026-09-17.md)。

## 实际改动

| 范围 | 已落地的行为 |
| --- | --- |
| 字体 | 由原 Noto 可变字体生成 OFL 静态 500 字重 HelloMine UI，保留全部 cmap 与原字体/许可证。实际客户端改用该文件，正文基准从 18 调至 20，告别默认 100 极细字重。 |
| 界面 | 深炭蓝面板、暖白文字、沙金选择色、青绿生命与珊瑚红危险色；任务标题放大并突出，数量与进度并排。 |
| 快捷栏 | 独立深色五槽、数字角标、细亮边与选中底标；保留图标、堆叠数量及耐久。生命改为十段显示并保留精确数值；手持名称有独立底色与固定行高。 |
| 操作提示/制作 | 操作键帽与动作文字分离；制作与箱子使用真实物品图标、名称、数量及选中态，完整名称可通过悬停阅读；仍使用 Button，保留键盘焦点和激活。移除制作预览中的内部 recipe id。 |
| 小地图 | 65×65、两米采样的真实驻留地表，显示树冠、水岸、沙土石和建筑；跳过细草花，邻列高度产生坡面明暗。常态不画区块网格，保留北向、朝向和 32 m 比例尺。原地改块也刷新，未知区暗色。 |
| 世界表现 | 草地细节反差和黄绿色调收敛，连续世界坐标产生较大色块层次。标准图集、纹理数组、阴影接收路径共用相同着色逻辑；关闭大气表现时回退，石材不受该调整影响。没有改动方块轮廓、植被根部、风摆或存档。 |
| 交付工具 | macOS 打包支持显式独立 binary；新增源码文件哈希收据，避免 git diff 不包含新文件造成身份遗漏。 |

## 可复核证据

所有相对产物路径以下列本地目录为根：
`/Users/lizi/Desktop/Workspace/HelloMine3D/build/visual-upgrade-20260917/`。
截图和字体样张属于本地附件，不提交 Git。

| 检查 | 结果 | 证据及范围 |
| --- | --- | --- |
| 隔离源码 | PASS | `checkout/` 基于 dcfd4d0，只纳入本轮视觉改动，地形维持 v10；未纳入并行 E5 的未完成生成器。 |
| 客户端 gmake / Apple clang / x86_64 | Debug、Release 均编译链接 PASS | `isolated-client-{debug,release}-final.log`。第三方静态库复用既有对应配置，第一方对象与可执行文件独立生成。 |
| 完整 WorldRuntime | Debug **1109/1109**、Release **1109/1109 PASS** | `world-full-debug.log`、`world-full-release.log`，包含现有输入、设置、存档、世界/地形等回归。不是正常输入玩法录像。 |
| 小地图聚焦 | **10/10 PASS** | `map-release-r2.log`；未知区不加载、负坐标、花草过滤、透明树冠、建筑覆盖、原地编辑、水、快照稳定、不写 dirty、256 列上限及卸载。完整两配置均再次覆盖。 |
| 字体构建 | PASS | 两次 `python3 tools/build_ui_font.py` 生成字节相同，SHA-256 `63ddd636cd3dc7c70b2a6ca1184d68544ff55e940424157237fe116efc69379a`；保留原始 cmap、静态字重 500、无 fvar。 |
| 资源包 | **105/105 PASS** | `resources-release.log`，资源数据检查。图集/纹理数组检查也通过，原图集和数组像素未修改。 |
| 分发资源 | **80 项 PASS** | `assets-final.log`。 |
| 花草 GPU 回归 | **10/10 PASS** | `flora-gpu-r2.log`，Apple M1 Pro 离屏 OpenGL，961 时间样本，根部固定、连续性、阴影运动一致。 |
| 地形/反馈 GPU | **20/20 PASS** | `ground-gpu.log`；实际生产 GLSL 与图集，验证草地改色、世界坐标变化、阴影关闭一致、石材不受影响及原有十阶段裂纹/遮罩/遮挡。 |
| 干净工作包 | **111 文件逐项哈希 PASS** | `package-final.log`；包内 `distribution-sha256.txt`、`build-identity.json`、314 个第一方源文件的 `source-tree-sha256.txt`。 |
| 升级后真实窗口/双语多字号/操作 | **BLOCKED：电脑锁屏** | CUA 明确返回会话锁定。尚无升级后完整游戏窗口图，不能由字体样张、离屏小块材质图代替。 |
| 同条件三轮窗口性能 | **NOT_RUN / 基线启动未完成** | `baseline-perf-off-r1/`、`baseline-perf-off-r1-r2/`、`baseline-hidden-r1/` 保留失败。没有新的 P95/P99 PASS，也不覆盖 E2 的 1.134× 失败。 |

汇总哈希见 `evidence-summary.json`。`font-specimen.png` 仅为同文本的真实字体字重样张；
`ground-gpu/*-ground-{before,after}.png` 仅为实际 shader 的离屏材质检查，不是整机位游戏画面。

## 可运行包身份

视觉批次自动证据对应的 Release 可执行文件 SHA-256 为
`789c0d52a8b03c614b2dd9b1866b9ee1f156929a68f999b3e2c94584dbde67de`，源码收据 SHA-256 为
`bdb7dad55575a6a1ef36d06dc09afa51908ee51c9cbeec6e3c7227426a552f90`。冻结副本为
`candidate-package-789c0d52/`，原版本副本为 `baseline-package/`；两者都没有被后续刷新覆盖。

复用客户端 `build/visual-upgrade-20260917/HelloMine3D-Visual-Upgrade.app` 随后在同一路径原位
合入 E5 提交 `db22168`，当前可执行文件 SHA-256 为
`d78fbb8bf417aa27ad81d9df7f9d61d3e3301d4ee354746cff4e7e67e9a816e5`。该身份只说明当前可运行
整合包，不追溯替换上面的视觉批次冻结证据。

隔离证据包由隔离检出的代码和资源生成，未覆盖其他任务的工作客户端或主工作区 bin。
这两个目录是供复核/恢复的包内容副本，不额外启动新应用。后续继续复用唯一视觉工作 `.app`。

## 失败记录与处理

- 初次沙箱窗口启动返回 LaunchServices -10827。通过正常的自动权限审核后可启动，但电脑随后锁屏，真实窗口采集超时，没有取得帧。
- CUA 确认 Mac 已锁定，自动解锁也不可用；未绕过系统锁屏，也未要求离开的用户在线处理。
- 隐藏客户端同样未取得帧；确认独立诊断包路径后结束超时进程，保留捕获记录。后续不反复重试同一无变化的锁屏条件。
- 离屏 GPU 首次在沙箱内无法取得 core pixel format；经正常自动审核使用图形上下文后，花草及地形/反馈检查均通过。这是图形 API 自动检查，不是绕过锁屏或模拟输入。
- 首版地图测试夹具直接将 Absent 切到 Resident，产生 7 项失败；按既有 Requested→Loading→Resident 生命周期建立夹具后 10 项通过，没有修改生产生命周期或放宽要求。
- 物品卡初次编译将 Material::ID 写成 int，类型检查报错；恢复强类型签名后两配置通过。

## 恢复操作

1. 先核对本报告、主工作区与隔离检出；不要把 E5 生成器修改或其他任务的包混进此版本身份。
2. 图形会话仍锁定时，保留待验项，不重复启动窗口。会话可用后，通过 CUA 正常进入视觉工作包；正常菜单、建档、移动/观察、制作、保存重开与诊断分开记录。
3. 冻结新包副本已存在。性能基线需要将 `baseline-package/` 的已验证分发文件恢复到同一工作 `.app`，采集完再恢复新包；保留当前用户数据及全部失败目录。
4. 使用 `tools/capture_visual_macos.py --reuse-app --save-template build/visual-upgrade-20260917/v10-template`；固定 seed 20260807、terrain v10、1280×720、时间 6000、HUD fixture。性能点 `(1152,150,64)`、旋转 `(0,0,0)`，Off/Off、Medium/Off、High/On 各三轮静态，加 High/On 三轮快速移动。每轮新目录，首次失败不覆盖；已有 `run_baseline.py` 是原计划参考，恢复时需给失败轮次新输出名。
5. 新旧版本还需相同林地/海岸/草甸/山麓画面对照；双语三字号、夜间/关闭回退、空满快捷栏、制作/箱子检查。发现截断、位置错误或地图性能退化继续修正。
6. 同夹具 P95/P99 三轮中位数均 ≤1.10 才关闭本轮性能；若未通过，报告真实退化并继续定位。当前不声明整体视觉升级完成，也未本地提交、推送或发布。

## 恢复区

- 起始 HEAD：7b2432d（E4 窗口结果）。并行任务已提交 E5 基线 dcfd4d0，并正在修改生成器；本任务不覆盖其文件。
- 体验前客户端：build/ecology-e2-20260914/HelloMine3D-E2-Prototype.app，实际菜单显示新世界 terrain v10。当前视觉工作包改用上方独立固定路径，避免与 E5 争用。
- 正常输入已执行：主菜单→世界列表→新建 Visual Review 0917（seed 20260807）→进入→打开制作。未动用户已有世界。实际发现正文细弱、目标层级弱、制作以文字按钮为主、地图没有树冠。
- 字体根因：media/fonts/NotoSansSC-VF.ttf 的 wght 轴 min/default/max 为 100/100/900，现有 stb 字体入口未选字重。
- 下一步：图形会话恢复后，完成升级后的实机布局、地图与场景检查及同条件性能，按发现继续迭代。
- 窗口由本任务操作。截图与录像保留本地 build/visual-upgrade-20260917，不提交 Git。
- 权限状态：初期 CUA 正常，随后电脑锁屏；离屏 GPU 和工程验证已完成，真实窗口仍待恢复。没有待用户在线批准的问题，也没有自动审批拒绝后绕过限制。

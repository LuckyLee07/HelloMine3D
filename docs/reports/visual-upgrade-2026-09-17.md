# 视觉升级执行记录 2026-09-17

状态：**r27 投射物修复已提交，随后以最高视觉优先级实施 r28 地貌多样化。terrain v13 已通过双配置完整世界回归、旧版兼容、多 seed 限定视觉复查、36 次定向性能及正常新建/保存重开，干净候选已交付。综合视觉 Goal 继续，正常持续输入按用户要求暂缓，最终完整体验尚未验收。** 历史失败、各批次性能证据及其版本/分辨率保留。
用户已授权体验、制定方案并直接实施，随后要求离开电脑期间自主推进，不要求其在线申请权限；见[升级方案](../current/visual-upgrade-plan-2026-09-17.md)。

## 综合 Goal 本轮进展（后续记录优先于下方历史恢复说明）

无预算限制的七阶段 Goal 持续执行，范围见[完整目标](../current/visual-upgrade-goal-prompt-2026-09-17.md)。
第一阶段不是完成条件。已核对 E6 terrain v12 提交及同窗口批量验证；原 E6 的 ARM64 包冻结在
`integrated-e6-arm64-31345fc6/`。本轮使用 `round2-checkout/`（起点 dc5c4bb，另包含后续 OgreBootstrap
诊断修复与字体 manifest 排序）独立构建 x86_64 Debug/Release；从未改写主工作区第三方静态库。
后续 E2 文档/证据提交保留（9e7f64d）。本轮已分为三个本地提交：打包修复 `bc18429`、世界/水体表现 `75aac13`、物品/UI/导航/敌人表现 `a3f25bb`；均未推送。

### 实现及实际检查

- **阶段 1 / HUD 与窗口**：进度计数与细条同行，三列状态区域固定占位；图标与立体手持物改用 nearest
  sampler，字体仍使用 linear。长按键提示换行。箱子、机器和背包卡片按实际内容宽度排布；制作采用紧凑
  方形网格与独立滚动区，结果/提交按钮固定可见。设置长标签与长选项分行。窗口化模式正确应用非显示器
  全屏模式列表中的尺寸，实际 900×600 原图已取得。
- **阶段 2 / E6 与材质**：整合 terrain v12。草顶/草边/高草/叶/花茎共享世界坐标低频色场，收敛细纹理反差，
  花、土、木、石协调饱和度；不改纹理文件或 Alpha。atlas/array、普通/阴影 shader 同步。
- **阶段 3 / 水深与岸线**：有界 8 格驻留水柱快照，角点共享真实深度/实体岸线；水床编辑使受影响上方水段
  失效重建，深度控制吸收与透明度，岸边克制涟漪、连续时间与关闭回退。复用水面 repeat UV，stride 不变。
  [数据合同](../contracts/visual-water-depth-v1.md)。r10 已补入贴水陆地最多 18% 的明暗过渡，并检查昼夜、低档和兼容路径；
  水面细纹按岸线与风向漂移，不表示水文流向。正常改岸、进出水面仍待验。
- **阶段 4 / 工具与掉落物**：方块使用真实三分面，工具按图集 Alpha 挤出轮廓厚度；共享有界缓存。
  第一人称包含移动/待机/开采/接触/回收姿态，复用命中事实与反馈强度。掉落物复制真实材质/数量/年龄，
  渲染实体几何并轻微转动/起伏；库存、碰撞与保存语义不变。[表现合同](../contracts/visual-item-volume-v1.md)。
- **阶段 5 / 天空与远景**：两层云密度由取最大值改为混合，减少叠满天空的云片，收窄边缘过渡并提亮云底。
  日间雾密度由 0.0015 调为 0.0019，夜间仍 0.006。GPU 昼夜运动/连续性/平移视差、原 legacy 回退全部通过。
  昼/夜/暮色、High+post 与关闭回退的 5/10 秒实际客户端诊断图已取得；随后已逐图复核近地树根、墙角、台阶与林下昼夜。
- **阶段 6 / 导航与辨识**：设置增加 64/128/256 m 三档；仍 65×65 采样、30 Hz 每次 195 列，切档丢弃不匹配缓存。
  显示六类当前区域名称。只记录实际使用过的容器、工作台和激活路标，单次世界访问记忆最多 32 个，圆图最多 8 个，
  避开玩家箭头、圆边及相邻图标；换世界清空，不查询未知结构，不显示敌人。设置 v1–v9 迁移至 v10 默认 128 m。
  地标记忆当前仅本次探索，设置说明中明确；不是跨存档探索数据库。敌人新增面向眼眉、躯干/四肢明暗及蓄力眼色，
  复用已有剪影和真实状态 tick。路标仅青色内芯保持夜间色彩，石框仍接受阴影/AO。
- **阶段 7 / 最终整合**：r11 森林/水岸完整对照 72/72 次、12/12 组及掉落物专项 24/24 次、4/4 组均通过独立审计，实际 1280×720，关闭截图读回。r9 受系统睡眠中断的失败保留。交付画面检查发现合成网格遮挡，r12 已修复并完成双配置构建与十组布局检查。干净候选包与四组对照已生成，完整正常输入玩法尚未验收。

### 已完成的本轮自动证据

产物均在 `build/visual-upgrade-20260917/`；每个目录/日志保持原始身份。

| 检查 | 已确认结果 | 路径 |
| --- | --- | --- |
| 前四阶段完整 WorldRuntime | Debug/Release 各 1170/1170 PASS | `world-full-round2-{release-r2,debug-r1}.log` |
| 前四阶段资源 | 两配置各 108/108 PASS | `resources-round2-{release,debug}.log` |
| 水深/物品几何聚焦 | 水深 13/13、物品 8/8 PASS；完整回归再覆盖 | `round2-checkout` 测试及上述全量日志 |
| MeshDirty | PASS | `meshdirty-round2-release.log` |
| 水面生产 GPU | PASS：210 对边界零偏差、深度/视距独立、岸缘/水下/关闭检查 | `water-depth-gpu-r1.log` |
| 地形/材质/反馈 GPU | 96/96 PASS（含新路标内芯、两套纹理及两条 pass） | `waystone-palette-gpu-r1.log` |
| 天空生产 GPU | 10/10 PASS | `sky-gpu-r1.log` |
| 敌人生产 GPU | 18/18 PASS（含夜间眼部亮度） | `actor-gpu-r3.log` |
| 导航/设置/本地化聚焦 | 65/65 PASS | `navigation-release-r1.log` |
| 环境聚焦 | 24/24 PASS，两配置（水下介质新增 3 项） | `round2-underwater-focus-{release,debug}.log` |
| 阶段 6 完整 WorldRuntime | Debug/Release 各 1182/1182 PASS | `world-full-round2-stage6-{release,debug}-r1.log` |
| 天空/UI 诊断矩阵 | 13/13 启动和采集成功，人工逐图检查后继续修正制作布局 | `round2-sky-ui-matrix-r2/` |
| 地图/UI 诊断矩阵 | 9/9 采集成功；三档地图、双语制作/设置、900×600 原图 | `round2-navigation-matrix-r1/` |

诊断图统一 `DEVELOPER_DIAGNOSTIC / normal_input=false`，静态姿态不证明真实连续战斗。
初次 UI 矩阵的 settings 参数未匹配现有 `HELLOMINE3D_V10E_SETTINGS_FIXTURE`；compact 请求当时实际仍为
1280×720。已修正入口及生产窗口尺寸，并让采集工具验证 PNG IHDR 尺寸。两类旧图只保留为未覆盖证据。
新窗口尺寸代码初次插在窗口创建前导致空指针启动失败；`round2-sky-ui-matrix-r1/` 与重试目录保留，
实际改到创建成功后，两配置重建和随后 13 组启动已通过。
敌人整合首图因 terrain loop 错设 actor shader 参数被 Ogre 明确拒绝，`round2-actor-matrix-r1/` 保留；
已修正为仅在 actor loop 设置参数，随后 8 项敌人/UI 整机采集成功；夜间眼部增加有界亮度下限后又完成近地场景复核。

### 近地与动态补充（r2 历史记录）

- `round2-near-scenes-r1/` 的 13 组与 `round2-near-scenes-r2/` 的 6 组诊断均成功采集。已逐图检查林下昼夜、High 阴影树根、台阶、草甸、浅滩、路标内芯、遗迹和敌人眼色。营地前两次机位因自然下落位于土台下；`round2-camp-north-r1/` 改用北侧实际坡地后已看到木围栏、入口和内部顶棚的完整轮廓。
- 首次“水下”坐标在沙层内，保留为不合适机位；第二次真实水下暴露远处水底像空气一样清晰。补入相机介质雾后，`round2-underwater-fixed-r1/` 显示水下视距和颜色衰减。只读取最多两个驻留块，新增 3 项环境测试，两配置 24/24 PASS，Debug/Release 客户端重建 PASS。
- 15 项 macOS 启动负例全部通过：`round2-startup-negatives-r1/`；最后敌人 shader 改动后资源再次两配置 110/110 PASS。
- `round2-hand-motion-{full,off}-r1/` 各 60 原始帧已取得，但旧开采夹具使用空库存，不证明手持工具动作；补入既有 HUD 库存夹具后，`round2-hand-motion-{full,off}-r2/` 各 60 帧和 6 秒视频采集成功，`round2-hand-motion-reduced-r1/` 追加 60 帧 Reduced 档，确认 Full 有挥动姿态变化、Off 保持静态。此处使用石剑进行开采诊断；不是正常工具制作/装备验收。系统编码器普通沙箱报告不可用，按授权图形执行环境编码成功，失败文件保留。
- 最新冻结候选 `round2-final-candidate-r2/`：Release x86_64 SHA-256 `5b39ddd5844969c8f3abba6c35bda7ab396d0456ed89b548c5f8b1267c1a6204`（r1 原件保留），321 个第一方源码文件。该目录不是另一个运行 .app；正式性能始终复用同一工作客户端。

性能 r1 已完成森林 Off 常驻三轮，P95/P99 中位数为 `0.985×/0.994×`；随后在代码复查发现容器/工作台记忆钩子位于被面板跳过的地图绘制函数中。已将钩子移到 HUD 提前返回前。旧 r1 在已验证归属的流送候选进程处主动终止，采集记录明确 `SIGTERM / FAIL`，runner 已恢复候选；旧静态 PASS 不作为新候选最终结论。修复后以同一基线/条件重新执行 r2。

### 本轮正式性能与小窗口补验

`run_round2_performance_r2.py` 完成 72/72 次：森林/水岸 × Off/Medium/High（High+post）× 常驻/快速流送 × 三轮 × 两版本，A/B、B/A、A/B 顺序，每次预热 5 秒、采集 30 秒。基线为同架构 v12 `round2-stage1-reference/`（Release SHA-256 `d3975ba24edcf19ad23f4f239de4149e43641b4d4bfdf9b5aca85f4ad0b2e142`），候选为 r2 `5b39ddd5844969c8f3abba6c35bda7ab396d0456ed89b548c5f8b1267c1a6204`。基线已包含第二轮阶段 1，不能解释为相对最初用户截图的总增量，也不替代历史 E2 结果。

| 场景/画质/运动 | P95 三轮中位数比值 | P99 三轮中位数比值 | 结果 |
| --- | --- | --- | --- |
| forest-off-steady | 0.9837× | 0.9934× | PASS |
| forest-off-streaming | 1.0005× | 0.9696× | PASS |
| forest-medium-steady | 1.0149× | 1.0043× | PASS |
| forest-medium-streaming | 1.0216× | 1.0093× | PASS |
| forest-high-steady | 1.0272× | 1.0269× | PASS |
| forest-high-streaming | 1.0129× | 0.9877× | PASS |
| shore-off-steady | 1.0209× | 0.9966× | PASS |
| shore-off-streaming | 1.0139× | 1.0096× | PASS |
| shore-medium-steady | 0.9965× | 1.0362× | PASS |
| shore-medium-streaming | 0.9820× | 1.0169× | PASS |
| shore-high-steady | 1.0173× | 1.0309× | PASS |
| shore-high-streaming | 1.0045× | 1.0005× | PASS |

`round2-performance-r2/audit.json` 从原始 CSV 重算分位数，复核全部采集包、源码、机位与 1280×720 原图，PASS；12 组最大 P95/P99 为 1.0273×/1.0363×（上取四位），门槛保持 1.10。最初审计对最终 tick 要求完全相等，在一组发现 6699/6700 差别，失败日志 `round2-performance-audit-r2.log` 保留。全部从 tick 6000 启动、CSV 从 6100 开始，30 秒墙钟结束落在 20 Hz tick 边界；审计显式接受该单 tick 差异后 `round2-performance-audit-r3.log` 通过，未删轮次或改变性能门槛。敌人继续运行，全部自然受伤和状态差异保留。上述为诊断运动，不是正常输入玩法。

- `round2-final-ui-matrix-r1/` 完成中英文、0.85/1.0/1.25、HUD/制作/容器/设置的 24 项 900×600 采集，已逐图检查。发现 0.85 容器关闭按钮下沿被裁切，以及 1.25 小窗口完整格挡提示接近/覆盖生命区；分别提高容器最小高度、在空间不足时使用短格挡动作名称。
- 两处修正后 Debug/Release 客户端构建通过；当前 r3 可执行文件为 `80fc7208b7054cced2d8878d2d85a6ba5e3b1d1244887f04e7bc42c86e2ce751`。`round2-candidate-consistency-r3.json` 确认 321 源文件与主工作区一致，相对 r2 仅 `OgreUserInterface.cpp` 变化。性能矩阵使用 1280×720、字号 1.0、关闭容器，不触发这两处小窗口路径；性能证据仍保留原 r2 身份，不能改标成 r3 实测。
- 解锁后桌面恢复 Retina 2×，首次补验 PNG 实际 1800×1200，而旧采集工具将 900×600 窗口点误当成像素，故 `round2-compact-layout-recheck-r1/` 如实保留 FAIL。采集工具增加显式像素比后，r2 四项补采全部通过严格 1800×1200 尺寸检查（工具提交 `ac1e941`），旧失败不覆盖。逐图发现英文 `Guard` 仍被拆词，已继续修正提示栏的键帽/间隔/面板边距宽度计算；该 r2 英文图保留为视觉 FAIL。
- Reduced 首次工具序列 `round2-hand-motion-reduced-r1/` 中有一段看似停住，不能判定连续动作通过。相同候选重采 `round2-hand-motion-reduced-r2/` 60 帧，3.7/4.6 秒实际剑刃姿态不同；灰色剑刃像素方向代理跨度约 12.5°。重采不解释首次现象，正常 Full/Reduced/Off 连续开采仍待验证。Full/Off r2 的静态/运动差别已实际检查，诊断不替代正常制作与装备。

### 小窗口修正最终补验

新增键帽宽度修正后，Debug/Release 构建通过：`round2-hint-width-release-r2.log`、`round2-hint-width-debug-r1.log`。Release r4 SHA-256 为 `4c284054803c1895f8f7d8c58e4e4cc68de4393f5b33d547c2c08bc942a25f05`，`round2-candidate-consistency-r4.json` 核对 321 个源码文件与主工作区相同，相对性能 r2 仍只有 UI 文件变化。r4 未重跑 72 次性能；保留 r2 性能身份与 UI 局部变更边界。

`round2-compact-layout-recheck-r3/` 完成 4/4 采集并逐图复核：中英文 1.25 HUD 的三个提示行清晰、英文 Guard 不拆词、提示栏与生命区分离；中英文 0.85 容器的关闭按钮完整可见。均为 900×600 窗口点、1800×1200 实际像素；原图哈希和判断在 `visual-review.json`。这是静态局部布局 PASS，不代替制作/容器交互或滚动验收。

CUA 主会话同样无法连接正在运行的 r3。停止该进程以刷新包的请求被自动审批拒绝，原因为可能丢失未保存状态；没有终止或覆盖它。改用 `round2-compact-layout-recheck-r3/Diagnostic.app`（bundle `local.hellomine3d.visual-layout-check`）生成和验证 r4，运行使用单独诊断存档，最后恢复正常配置。原工作包、进程和已有存档保持。此临时隔离包是针对无法安全原位刷新的替代检查，不宣称原工作窗口已经运行 r4。

补审 `round2-landscape-comparison-r1/` 草甸和山脚四张 10 秒原图：各对 seed、terrain v12、最终位置/朝向/tick 一致；可见工具体积、地图区域文字和云层变化，地形轮廓保持。草甸前景近身敌人遮挡保留，不作正常战斗验收；记录见目录内 `visual-review.json`。

### 水面顺岸漂移工程批次

在 r4 基础上复用实际驻留岸线，将微风投影到岸线切向并按水覆盖衰减，补充有方向的水面细纹。
使用水材质闲置 UV0，不增加顶点 stride、查询、区块加载或存档字段；双相位细纹循环连续，亮度调制最多 3.5%。
这表达风驱表面运动，E5 等高水体仍没有水文下泄模拟；见更新后的[水面合同](../contracts/visual-water-depth-v1.md)。

- Apple clang / x86_64 客户端 Debug/Release 构建通过，最新日志 `round2-water-drift-client-{debug,release}-r2.log`。
- 水深聚焦两配置各 **18/18 PASS**，完整 WorldRuntime 各 **1190/1190 PASS**；分别见 `round2-water-drift-focus-*-r1.log`、`round2-water-drift-world-full-*-r1.log`。
- 资源接口两配置各 **112/112 PASS**，包括缺失水面漂移顶点/片元声明的负例：`round2-water-drift-resources-*-r4.log`。各配置使用独立工作目录和显式 `HELLOMINE3D_ROOT`。
- Apple M1 Pro 生产 GLSL：210 对边界位置/法线零偏差，漂移运动、相位连续、关闭回退通过，见 `water-drift-gpu-r1.log`。
- 专用 Diagnostic.app 经独立验收者保存并正常退出后原位刷新，111 个托管文件校验通过；321 个第一方源码与主工作区一致。r5 Release SHA-256 为 `9cf3726c4c1356efce77341be90bfde72e8f48d4738b60361d12b580c1d2f02b`，见 `round2-water-drift-package-r5.log`、`round2-water-drift-prepackage-r5.json`。原 r3 进程未操作。
- `round2-water-drift-window-r1/` 的 Medium 与大气关闭两项窗口冒烟成功，5/10 秒四张原图逐图检查通过：水面正常着色、浅水沙底及深水层次可见，无缺失材质或明显破面。窗口 1280×720 点、实际 2560×1440 像素。原图哈希及判断见 `visual-review.json`；静态图不证明漂移动态品质或性能。检查后恢复验收者保存的正常设置。

保留资源执行失败：r1 误用了重链接前的旧测试程序且两配置共享 fixture 目录，Release 因文件竞争异常退出；
r2 分离目录后遗漏资源根，均出现贴图读取失败；r3 补齐资源根后 110/110 通过，再增加两项漂移接口负例得到 r4 的 112/112。
这些日志均保留，不把旧程序的 108 项检查当作当前证据。刷新前只读 `ps` 在普通沙箱被拒，脚本仍继续执行了打包；
此前已有独立验收者正常退出的记录，随后获准的只读核查再次确认专用诊断包无运行进程，未停止任何游戏进程。

本次按用户要求提交工程小批次。**新水面完整动态窗口观察及三轮静态/流送性能仍待执行**；旧 r2 的 72 次结果保持原版本身份，
不作为 r5 新 shader 的性能结论。整体 Goal 保持进行中。

### 正常输入补验 r2

独立验收者已通过 CUA 连接专用 Diagnostic.app 的 r4 包（SHA-256 `4c284054803c1895f8f7d8c58e4e4cc68de4393f5b33d547c2c08bc942a25f05`），
完成菜单、设置及有限正常玩法验证；旧 `normal-input-round2-r1/` 的原包连接失败继续保留。
本次原始九张 PNG、窗口身份/哈希及操作记录位于 `normal-input-round2-r2/`，结论见 `acceptance-record.md`、索引见 `capture-index.tsv`。

| 项目 | 结果与边界 |
| --- | --- |
| 建档、暂停、制作面板 | PASS：正常创建并进入 A/B 两世界，Escape/E 及关闭按钮有效；没有取得材料或实际制作物品。 |
| 紧凑界面 | PASS：900×600 英文 1.25 HUD、制作与设置滚动/固定按钮；中文 0.85、英文 1.0 制作。未取得箱子，容器交互不在本次 PASS 范围。 |
| 设置与地图 | PASS：Full/Reduced/Off 应用与保持、64/128/256 m 范围切换；林地 A → 沙岸 B 显示不同地形。没有已发现地标，不证明标记隔离。 |
| 保存与重开 | PASS：A 显式保存退出、正常重启并载入，seed、旅程、空库存及最后观察的 6/20 生命恢复；最终 A/B 均保留并正常退出。 |
| 受伤反馈 | 有限 PASS：正常敌袭字幕、方向提示、红边和生命下降可见；没有完成攻击、格挡或死亡重生。 |
| 移动、采集、工具、容器、地标、连续动作 | BLOCKED：W、连续 30 次 W、L 后重试及鼠标拖动均无可见移动/转向；接口无文档化持续按键/按住鼠标能力。未用替代注入伪造正常输入。 |

独立上下文与严格 AI-06 隔离不同；本次仓库可读，隔离为 PARTIAL，不声明无上下文盲玩或完整可玩性。
未建立产品 FAIL。验收者将设置恢复为中文、1.0、Medium、Full、128 m、下次启动 1280×720，保存 B 返回菜单后正常退出，已释放窗口。
这些是 r4 的实际证据；之后 r5 只改水面表现，但仍保留各自版本身份，不改称 r5 正常玩法实测。

### 不抢占窗口的后台启动修复

用户要求启动客户端不得打断正在使用的窗口。前台性能 runner 在正常采集边界停止，
恢复候选和正常配置后退出（session 73123 已结束、exit 1 表示主动停止）。r5 完成 18/72 次，
森林 Off 常驻、Off 流送、Medium 常驻三组 P95/P99 中位数比值分别为
0.9968×/0.9934×、0.9977×/1.0216×、0.9355×/0.9451×，均通过；其余 54 次未运行，不能宣称完整性能通过。
原始记录与停止标记保留在 `round2-performance-r5/`。

修复 Cocoa 窗口层：此前 `hidden` 在 NSWindow 创建前执行，后续创建/尺寸更新仍无条件置前，
且 `noActivate` 没有被读取。现在隐藏窗口不显示，不激活应用；显式不激活的可见窗口置于后方。
同时补齐显式窗口路径缺少的 GL context profile 及 Retina/画质默认参数，保留调用者的显式选项。
采集工具默认隐藏，open 路径追加 `-g -j`；打包器从实际二进制记录能力标记，
缺少标记的旧包在启动前拒绝，不通过前台回退绕开用户约定。

| 验证 | 结果与证据 |
| --- | --- |
| macOS x86_64 Debug/Release | 原生窗口库及客户端构建 PASS；最终日志 `round2-background-*-engine-r2.log`、`round2-background-*-client-r3.log`。 |
| Cocoa 输入回归 | 两配置 `verify_cocoa_input.sh` PASS；`round2-background-*-input-r1.log`。非可见自动回归，不是正常 OS 输入验收。 |
| direct 隐藏采集 | PASS：361 次观测中 355 次识别到目标进程，前台激活 0、可见窗口 0；取得 5/10 秒两张 2560×1440 原图。 |
| open 隐藏采集 | PASS：358 次观测中 349 次识别到目标进程，前台激活 0、可见窗口 0；同样取得两张原图。 |
| 启动保护与观察器负例 | 旧 r5 包被拒且未创建输出目录；未启动目标进程的观察器返回 FAIL，避免把“没启动”误判为成功。 |
| 静态画面与恢复 | 四张原图逐图检查，水岸、HUD、地图正常渲染；正常配置恢复，321 个第一方源码和三个修改后的原生窗口源码与构建检出匹配。 |

上述采集/进程观察在 `round2-background-window-r3/`，只读观察器不发送输入、不改变焦点。
循环间休眠 20 ms，加上读取开销，不能排除采样间隔内的瞬时事件；已同时复核原生窗口的置前/激活保护。
画面记录见该目录 `visual-review.json`。能力标记证明包含此实现，不代替每个平台的行为验证。
最新 r6 Release SHA-256：`3f6631dcd0726b3780b1b7babe59baa8829f8cd43a1ee934165b3d0201711f51`，
包与原生源码身份见 `round2-background-consistency-r6-r2.json`，最终配置/哈希核对见 `round2-background-final-check-r1.json`。

失败不覆盖：r1 隐藏路径暴露缺少 contextProfile 导致的启动异常，修复后重建；
r2 已成功渲染，但 NSWorkspace 没有列出隐藏 direct 进程，观察器因目标未出现而 FAIL；
改为 libproc 精确匹配可执行路径后，r3 两路通过。r2/r3 使用相同最终生产二进制。
后续仅使用已验证的后台路径；旧性能基线需以相同窗口修复重建后另开后台对照，不能混用前台/后台口径。

### 手持动作计时精度修复与后台逐帧检查

在 r6 上的临时跟踪没有复现旧 Reduced 停摆：`held-pose-trace-r2/` 三轮各 60 帧，
分别记录 253/247/254 个开采活动帧、220/217/218 个不同挥动值，最大帧间隔 46/52/48 ms。
原始 Reduced r1 的计时值没有记录，**历史停摆根因仍未证实**，不能用新一轮正常结果覆盖它。

另以旧算法重现了确定的精度问题：过大的时间增量使单精度累加器丢失后续帧增量；
连续运行一周的数值量级也无法保留高帧率动画相位。现将纯表现时钟和地图刷新期限改为 double，
拒绝非有限/非正增量，单次最多推进 0.25 秒；长停顿后继续动画，不补播停顿期间的动作。
只影响手持姿态、HUD 呼吸和地图采样节奏；玩法、开采/攻击判定、冷却、字幕及性能统计仍使用原路径。

- Debug/Release 客户端构建 PASS：`presentation-clock-client-*-r1.log`；最终世界测试目标构建 `presentation-clock-world-*-r2.log`。
- 最终计时聚焦各 5/5 PASS：`presentation-clock-focus-*-r2/`；物品表现各 8/8、地表地图各 10/10 PASS：`presentation-clock-focus-*-r1-{item_visual,surface_map}/`。本批未重复完整世界门禁。
- 独立数值回归 `presentation-clock-regression-r2/`：同一组检查旧算法 4 项失败、新算法 5/5 通过，包含非法增量、大时间跳变后恢复、地图期限、长会话精度及普通帧率一致性。它证明这类精度缺陷，不能反推历史 r1 必然遭遇时间跳变。
- 正式候选 Full/Reduced/Off 各 32 张原始 BMP，1280×720 点、2560×1440 像素，位于 `presentation-clock-motion-r1/`。剑刃像素方向代理在所选活动区间分别变化约 37.38°/15.57°/0°；Off 全部 32 帧剑刃掩模一致，Full/Reduced 选取的 20 帧均有变化。代理角度不是物理关节角度或人类手感评分。
- 已查看两帧 Full、两帧 Reduced 和两帧 Off 无损 PNG 副本，手持工具完整、侧面厚度可见、未盖住准星与快捷栏。三档后台启动观测 PASS：631 次采样、598 次识别目标，前台和可见窗口计数均为 0；无正常输入操作。

保留诊断失败：`held-pose-trace-r1/` 使用 PNG 编码产生最高约 3.63 秒帧间隔，只有极少活动帧，
不用于连续动作判断；同轮观察器传入相对路径未匹配到进程而 FAIL。r2 改用 BMP 和绝对目标路径后正常采集。
这些临时跟踪只修改隔离检出，已恢复；最终客户端不含跟踪输出。查看工具不能直接读取 BMP，故离线转换无损 PNG 查看，原件保留。

上一候选 r7 Release SHA-256 为 `d7a011aea7bc4f12d1a0bbe6dc534e99e25a7f474ba37aff7b1df74b4efab242`；
源码/资源/配置恢复及当前包身份见 `presentation-clock-final-check-r1.json`。画面采集后只补强测试断言并刷新来源收据，生产二进制不变。
当前证据是后台夹具动作验证，正常采集、制作、装备和连续手持操作仍待验；完整后台性能对照与水面动态继续推进。

### 水面连续帧与后台性能基线

r7 完成 `water-motion-r7-r1/`：同一水岸开启/关闭大气表现及水下视角，各 64 帧，
从 4 秒到 11.875 秒每 125 ms 一帧，共 192 张 2560×1440 原始 BMP。采集最大迟到分别为 8/7/6 ms。
开启时浅水/深水区域 64 帧均有变化，相邻帧平均绝对 RGB 变化约 0.160/0.181（0–255 色阶）；
关闭时同一区域全部像素一致，符合静止回退。已查看七张无损 PNG 副本，岸线、浅水沙底和水下界面正常，
所查帧未见明显破面。分析及范围见 `temporal-analysis-r1.json`、`visual-review-r1.json`。
这证明后台实际着色路径持续更新及关闭回退，不代替正常游泳/岸边编辑，也不声称已由人类认可流向与舒适度。
后台观测 1278 次，其中 1230 次识别到客户端，前台激活和可见窗口计数均为 0，正常配置已恢复。

后台性能基线保留原阶段 1 视觉范围。原收据的 315 个第一方文件中，只有当时未提交的 UI 无法直接从基准 commit 取得；
按本任务原始两份 UI 补丁恢复后，完整文件 SHA-256 精确匹配 `c1f76ce9e5e628b96b847c43bb5922e8a6d0c0f42fd022bee5ff8164bb10faa5`。
其余 314 个文件均与 `dc5c4bb` 匹配。只加入 `9dedc7e` 的三个原生隐藏窗口文件并重新编译，未替换视觉基线。
重建日志 `background-baseline-client-r2.log` PASS；首次准备因 Python 3.9 不支持 tar 的新参数而失败，尚未解包，失败记录保留；
重试显式检查可信 git archive 的每个路径后解包，未覆盖主源码。

- 后台基线 `background-stage1-reference-r1/`：Release x86_64 SHA-256 `2e84923395dad03ff8215d13ae848d580ab81f19b122978336067374952e2907`。
- 候选 `background-final-candidate-r7/`：仍为 `d7a011aea7bc4f12d1a0bbe6dc534e99e25a7f474ba37aff7b1df74b4efab242`。
- 基线全部视觉资源、315 文件收据与原冻结包一致，只更新可执行文件、构建身份与相应清单哈希；原生源码和依赖库哈希见 `background-performance-freeze-r1.json`。
- 基线隐藏启动 `background-baseline-check-r1/` PASS：两张 2560×1440 图已查看，390 次观测中 372 次识别进程，前台/可见窗口 0；随后恢复 r7 与正常配置。

旧采集工具会在性能计时期间编码两张 PNG。本轮将性能模式改为关闭 GPU 截图读回，
由原生 Cocoa 创建日志严格验证窗口点和 framebuffer 像素，视觉检查独立执行。
实际尺寸缺失/歧义、不匹配及意外开启读回均拒绝；`readback-free-capture-validation-r1.log` 七项正反例通过。
新后台性能结果独立记录，不能与含截图的历史前台结果混算，也不修改 1.10 门槛。
新旧各一次 30 秒采集预检通过：`background-performance-preflight-r1-{baseline,candidate}/`，
实际 2560×1440、无读回，分别 4297/4197 帧；观察器确认 2182 次采样中前台/可见窗口均为 0。
预检仅证明采集路径有效，不能替代三轮正式性能；正常包和配置已恢复。

### 后台正式对照与收口检查

`run_background_performance_r1.py` 已正常退出，完成 72/72 次、12/12 组，全部通过。
原始数据在本地 `background-performance-r1/`，独立审计 `audit-r2.json` 的证据完整性与性能均为 PASS。
矩阵为森林/水岸 × Off/Medium/High（High+post）× 静止/快速流送 × 三轮 × 两版本，
顺序 A/B、B/A、A/B，每次预热 5 秒、采集 30 秒；P95/P99 三轮中位数门槛仍为 1.10。
两版均隐藏、不激活、关闭截图读回，实际 2560×1440；后台结果不与旧前台或带 PNG 编码的记录混算。
快速流送通过四次跨区块传送施压，不构成连续行走或转向的视觉证据。

| 场景 / 设置 / 采样 | P95 比值 | P99 比值 |
| --- | ---: | ---: |
| forest-off-steady | 0.9561× | 0.9406× |
| forest-off-streaming | 0.9781× | 0.9259× |
| forest-medium-steady | 1.0158× | 1.0122× |
| forest-medium-streaming | 1.0263× | 1.0201× |
| forest-high-steady | 0.9784× | 0.9226× |
| forest-high-streaming | 1.0188× | 1.0255× |
| shore-off-steady | 1.0931× | 1.0513× |
| shore-off-streaming | 0.9306× | 0.9185× |
| shore-medium-steady | 0.9811× | 0.9696× |
| shore-medium-streaming | 0.9893× | 0.9747× |
| shore-high-steady | 0.9380× | 0.8698× |
| shore-high-streaming | 1.0220× | 1.0152× |

最大 P95/P99 比值分别为 1.0931×/1.0513×，均来自水岸 Off 静止组。
审计重算全部原始 CSV 分位数，核对交替顺序、机位与设置、原生像素尺寸、截图读回为零，
并核对两版各 111 个包内文件、315/323 个第一方源码、三个原生窗口源码和 17 个依赖归档。
候选 r7 与正常配置均已恢复；配置 SHA-256 为
`21a5024dcd2566664f1c5ed450a733434f333fa0ef7bd771850b9138b5b0f3ff`。

保留首次审计 `audit-r1.json` 的失败：审计器错误地要求最终世界 tick 只能为 6699/6700。
源码中采集计时器在首帧渲染结束才启动，此前已执行该帧模拟；因此首帧 tick 与边界会产生偏移。
修订审计改为检查完整 30 秒墙钟区间、逐帧世界时间与模拟 tick 精确对应、恒定白昼及保存结果一致；
72 次实际测量均为 600/601 tick。CSV 六位有效数字在 30 秒附近保留最大 0.05 ms 舍入界限。
原审计程序及失败记录保留，没有剔除采样、重跑取优或改变性能门槛。

掉落物后台采集 `drop-motion-r7-r1/` 完成时记录为 `CAPTURED; VISUAL_REVIEW_PENDING`：
Full/Reduced/Off 与旧图集 Full 四组，共 128 张 2560×1440 BMP。随后审阅发现薄物品侧转消失，
该 r7 视觉失败保留；下文 r8 修复及同倍率对照是当前结论，不替代正常掉落/拾取及战斗交互。

| 必要收口项 | 当前证据与剩余动作 |
| --- | --- |
| 当前候选性能 | r11 森林/水岸 72 次及掉落物 24 次、r14 水线 24 次、r15 介质 36 次、r16 回退 12 次均按各自身份审计 PASS。最新 r17 已补合成面板静止/流送 12 次、57,709 帧，P95/P99 比分别为 0.988568/0.993128、0.982460/0.992308，满足 1.10 门槛；对应 `notification-performance-r17-r1/audit-r1.json`。各批增量证据独立保留，不将历史全场景数据改标为当前版本。 |
| 行进中的场景可读性 | r13 有界机位已补 11 段、1,067 帧及 11 段视频，包含草甸向林缘连续接近；只控制渲染相机，不证明正常行走或地图位置刷新。两段穿叶路径保留为诊断限制，不能当作游戏闪烁。 |
| 水岸交互 | r14 已修复近裁剪横带；r15 修正版工程、限定范围连续画面、36 次定向性能及审计通过。r16 回退亮带修复通过构建/画面检查，后续 12 次性能复验及审计通过，首次睡眠边界失败保留；正常编辑岸床、游泳及对应刷新仍待验。 |
| 工具与掉落物交互 | r7 Full/Reduced/Off 共 96 帧已检查；r7 掉落物四组 128 帧审阅发现侧转轮廓缺陷，r8 已修复，并完成同倍率 192 帧前后对照及限定范围视觉检查。正常制作、装备、持续采集、掉落和拾取仍未完成。旧 Reduced 停摆原始计时缺失，根因未证实；当前精度回归与多帧结果分别记录。 |
| 导航、容器与敌人 | 三档范围、区域差异和静态姿态已有证据；r17 正常 A→B→A 切换已观察到正确地表图刷新。正常使用地点后记忆标记、原地编辑、切世界标记清理、容器转移及攻击预备到接触的完整交互未验。 |
| 最终包与保存重开 | 最新候选与字幕正常原图对照位于 `delivery-r17/`，并链接 r16 八组历史对照；111 项文件、325 份源码和无窗口启动校验通过。同运行代码的工作包完成正常建档/设置/空库存保存重开及 A→B→A 地表图刷新的有限自测；独立验收及完整正常玩法仍待验。 |

2026-09-19 用户明确允许可视化窗口，已恢复 CUA 正常自测；自动诊断仍隐藏/noActivate。持续按住键鼠与相对转向受现有公开接口能力限制，不把诊断姿态记成正常玩法。
上述清单收拢既有 Goal 的退出项，不新增玩法或扩大视觉范围。

### 掉落物侧转可读性修复（2026-09-18，r8）

审阅 `drop-motion-r7-r1/` 后确认：剑、锭和面包的挤出模型在整周旋转时会露出极窄侧面，
石剑几乎融入叶丛。原始 128 帧、`visual-review-r1.json` 的 FAIL_READABILITY 和像素分析保留。
这不是只根据源码推测的问题；同倍率复现时，标准/兼容档石剑 ROI 亮色像素最低分别仅 1/0 个。

修复在纯表现 `ItemVisualPose` 中实现。方块保持原有整周旋转；挤出图标朝向观察者，局部偏转
限制在 -4° 至 36°，保留真实侧壁，正面朝向投影至少 80%。Reduced 摆幅为 Full 的 35%，
Off 关闭随时间的摆动和悬浮，观察位置变化仍维持可读朝向。Actor、碰撞、拾取、库存、年龄和存档未改。
模型仍使用原网格与有界缓存，分类仅在创建模型时读取，不增加世界查询或材质/纹理资源。

- macOS x86_64 Debug/Release 客户端与 WorldRuntimeSmoke 构建通过；两配置 ITEM_VISUAL 各 13/13。
  新检查覆盖 300 秒生命周期、12 个方位、五种仰俯角和三个反馈档位共 432180 个姿态，
  检查正面投影、悬浮边界、Off 时间稳定及方块四分之一圈旋转。未重复无关的全量世界回归。
- 首次 `drop-motion-r8-r1/` 保留为 FAIL：屏幕当前变为 1×，实际 1280×720，旧脚本仍要求
  2560×1440；原生日志明确 backing scale 为 1.0。该轮正常退出并恢复配置，没有将其改标通过。
- 新对照 `drop-readability-comparison-r8-r2/` 对两版统一使用 1280×720 点/实际像素、terrain v12、
  seed 20260807、相同机位/时间/设置。r7 标准与兼容 Full 两组，r8 标准 Full/Reduced/Off 和
  兼容 Full 四组，每组 32 帧，合计 192 帧；逐帧哈希及尺寸均核对，10 张原图已实际查看。
- r8 标准/兼容 Full 石剑 ROI 亮色像素最低分别为 111/139；标准 Full 范围 111–146，
  Reduced 为 123–141。Off 的 32 个石剑轮廓掩码相同、均 130 像素。该灰色阈值只是本场景
  轮廓辅助指标，不是通用物品检测器；世界背景和光照仍更新，不声称整幅画面像素不变。
- 图中剑、锭、面包的主要轮廓持续可见，方块有分面与转动；新标准/兼容画面检查为
  `PASS_SCOPED`，只覆盖上述五件物品与固定诊断机位。正常掉落、拾取、连续转向和战斗仍待验。
- 六组观察器共 1593 次识别到客户端，前台/可见窗口采样均为 0；20 ms 采样不能排除更短瞬态。
  专用工作包及正常配置恢复成功，未发送正常输入，未启动前台窗口。

r8 Release SHA-256：`78dd417323a6a8e4bd03bba323056f88a78e252b4b952c788e95e817971fd1ec`。
包内 111 项及 324 个第一方源码身份匹配当前源码和构建检出；冻结副本为
`background-readable-candidate-r8/`。本批性能增量尚待验证；r7 的正式性能 PASS 不自动转移给 r8。
正常玩法、地景连续观察及最终整合交付仍按既有 Goal 继续。

### 无截图读回的物品诊断入口（r9）

r8 性能补验首轮 `drop-performance-r8-r1/` 在创建窗口前退出：物品展示入口只接受
`HELLO_RENDER_CAPTURE`，与性能采样禁用截图读回的设置冲突，实际帧数为 0。
失败原始日志和零帧记录保留；已恢复工作包与配置，不将其当作性能退化。

入口现与已有方块反馈诊断一致：明确启用画面采集或 `RuntimePerformanceCapture` 之一即可使用；
合法姿态限制保留。未启用诊断、或姿态非法时仍拒绝，不影响正常玩法路径。
Debug/Release 客户端构建通过，四个启动负例均在创建窗口前按预期退出；Release 新旧两版
已完成开启性能、关闭画面采集的正例，原生日志、CSV 和零截图输出均符合预期。
记录为 `actor-capture-entry-validation-r9.json`；此 PASS 只针对入口，不代表三轮性能已完成。

r9 候选二进制为 `c7182b4fc56ec27f2ab76fe053d7312103cb138a4925ac22f678a6a95ee33530`，
视觉源码与 r8 相同，仅增加上述入口修复。对照基线为
`6e0a6c89e87695cd2a95b2db0fd4e446dca107209efaeecfb80694a901063cf7`，第一方源码对应
`3a73058` 的 r7 视觉加同一份入口修复；对照包与 r7 的源码清单差异严格限制为 `OgreBootstrap.cpp`。
324/323 个源码及共同原生窗口/依赖身份分开留存于 `drop-performance-packages-r9.json` 和冻结记录。

`drop-performance-r9-r1/` 已采集 r7 → r8 掉落物变化的 24 次专项对照：固定五个掉落物和四个敌人姿态，
Off/High+post × 静止/快速流送 × 三轮 × 两版，共 24 次、四组；同为实际 1280×720、隐藏、静音、
不读回截图。三轮顺序 A/B、B/A、A/B，预热 5 秒后采集 30 秒，P95/P99 门槛仍为 1.10。
这是掉落物变化的增量检查，不替代最终整体性能或正常玩法；快速流送仍是四次跨区块传送。
独立审计保留 `audit-r1.json` 的首个时长失败；`audit-r2.json` 完成全部数据和身份核对，
结果为 `evidence_integrity=PASS`、`timing_protocol=FAIL`、`performance_status=NOT_CLAIMED_PROTOCOL_FAIL`。
虽然四组原始 P95/P99 中位数比值均不超过 1.10，但 High 静态 r2 基线的最后一帧渲染约 25.279 秒，
实测结束于 48.032 秒、累计 456 tick；High 静态 r3 候选最后一帧渲染约 925.423 秒，
结束于 926.865 秒、累计 29 tick。两者不满足等时长模拟协议，不能用百分位汇总覆盖停顿。
Off 静态 r3 候选还在第 2900 帧记录 `dt_ms=1.84467e+19`（该帧耗时 5.7895 ms），
其计时异常另列待查；不把这些现象归因于掉落物或操作系统，根因尚无证据。
所有 24 次原件保留，不删异常、不调整门槛、不以挑选重采取得 PASS。

原 exec handle 16079 已失效，工具没有返回其最终退出码；进程核查确认 runner 与专用客户端均已退出，
因此退出码记 `UNAVAILABLE`。审计独立核对全部 CSV、固定协议、324/323 份源码、各 111 项包文件、
原生窗口与依赖身份；确认专用包已恢复 r9，正常配置 SHA-256 为
`21a5024dcd2566664f1c5ed450a733434f333fa0ef7bd771850b9138b5b0f3ff`。下一步定位长停顿与 delta 异常，
之后补齐受影响的完整三轮对照；原 r7 完整矩阵保持独立，不把本轮失败改记为完成。

### 陆地岸面湿润过渡（r10）

补齐此前只在水面上表现岸线、陆地没有湿润变化的缺项。实际不透明方块网格按相邻驻留水的覆盖，
对角点派生明暗施加最多 18% 衰减，沿一格岸面过渡回原材质；不新增几何或资源，不改变存储光照、
水体、碰撞及存档。含水标志在原快照复制时计算，17³ 字节缓存限制重复读取；无水快照跳过，
greedy 合并沿用最终明暗重建检查。低档和关闭大气表现时仍保留静态岸面过渡。

macOS Apple clang / gmake / x86_64 的 Debug、Release 客户端及 WorldRuntimeSmoke 构建通过，
水深/岸面定向两配置各 **29/29**、完整世界回归各 **1211/1211** 通过。新增 11 项实际网格检查覆盖
干燥字节一致、真实水邻接、渐变范围、横向/竖向接缝、AO 关闭、光照/驻留不变、移除水恢复、旧快照独立和索引有效性。
shader、贴图及 17 份依赖库未变；资源和 GPU 旧证据仅复用于未改内容，新网格实际路径由本轮客户端画面覆盖。

`shore-r10/captures-r2/` 以同 seed 20260807 / terrain v12、同机位、同时间、实际 1280×720 采集
改动前后白天 High、低档大气回退、夜间和兼容材质四组，共 16 张原图。已查看八张 5000 ms 原图，
沙岸呈克制的暗色带、内陆纹理保留，未见新增接缝或缺材质。固定岸面区域 `[655,430,750,465]` 的 RGB
均值在白天 High 从 199.03 降为 177.25，夜间从 84.91 降为 76.07；内陆区域均值变化小于 0.003，
这些仅是该机位的描述性数据，不是通用审美判定。首轮 `captures-r1/` 已正常采得基线两图，
随后核查脚本误读性能专用字段 `framebuffer_size_pixels` 而失败；修正为图片的实际尺寸字段后重做并保留原件。

身份和恢复核对保存在 `shore-r10/verification-r1.json`，画面记录为 `visual-review-r1.json`。
候选 Release 二进制 SHA-256 为 `c99f2709eb7394daae0e03e1e65108e41e164b1f14ec5c4c76c12c7eb8e18d6f`，
324 份源码与构建检出逐字节相符；包冻结于 `shore-r10/frozen-candidate/`，专用工作包与正常配置已恢复。
本轮仅声明工程和固定机位视觉范围通过；岸面性能、正常输入改岸/游泳及最终整合仍待完成。

r9 的巨大 delta 追查发现当前 Ogre POSIX Timer 使用可回拨的 `gettimeofday`，Root 对无符号毫秒值相减；
回拨会溢出为巨大帧增量。这是下一批应验证修复的明确代码风险，尚未证明它就是本次主机异常的触发原因，
也不能由此解释 25/925 秒的真实渲染停顿。下一步独立复现计时边界，修复后补齐相应性能验证。

### POSIX 经过时间单调性修复（r11）

实际 Ogre Timer 源码的隔离故障注入已证实缺陷：墙钟回拨 1 ms 后，毫秒值从 20 退到 19，
Root 使用的无符号差值变为 `18446744073709551615`。它可以解释巨大 delta 的形成机制，
但没有本次主机墙钟跳变记录，因此不宣称 r9 异常触发原因或 25/925 秒真实渲染停顿已经确定。

`OgrePOSIXTimerImp.h/.cpp` 改用 `std::chrono::steady_clock` 保存起点与计算经过时间，
不改变单位、reset、CPU 计时、fixed tick、存档和墙钟时间戳。测试仅在单独编译的 Timer 对象中把
`gettimeofday` 重定向到假时钟，不调整操作系统时间或注入运行中的游戏。
新入口 `bash scripts/verify_posix_timer.sh Debug|Release` 包含回拨、跨起点、向前跳一小时、
墙钟停住但真实经过时间继续、10,000 次读取、实例 reset、单位和 CPU API 检查。
旧源码两配置各 8 项失败，修复后两配置各 **10/10 PASS**；失败日志完整保存在 `timer-r11/before-*`。

使用顶层 gmake 的 `HelloMine3D` 依赖图重建 Debug/Release Ogre 核心与受影响客户端，两个配置均通过。
324 份第一方源码与 r10 相同，r10 世界回归覆盖未改内容，不重复用无 Ogre 的 WorldRuntime 证明计时器。
Release 17 份依赖库只有 `libogre3d.a` 改变；原生窗口三份源码保持不变。

`timer-r11/runtime-preflight-r1.json` 的真实隐藏客户端各采集 30 秒（预热 5 秒、无截图读回）：
Debug 1312 帧 / 600 tick、最大 delta 53 ms；Release 3734 帧 / 601 tick、最大 delta 13 ms，
两次均未出现巨大增量，结束时长分别 30014.6/30004.5 ms。20 ms 间隔的进程/窗口观察累计识别
2138 个客户端样本，前台与可见窗口样本均为 0；不排除短于观察间隔的瞬态。
这两次仅为计时集成检查，不宣称已完成三轮性能对照或正常玩法。

独立文件核对见 `timer-r11/verification-r1.json`，新 Release SHA-256 为
`c006bece67c99b9ca9aafb0cc87015dc9a150f6c01da987843ad30229b5984c1`，冻结包为
`timer-r11/frozen-release/`。专用工作包现为 r11，正常配置恢复且 SHA-256 未变。
下一步继续定位真实渲染停顿，并以同一单调时钟修复重新构建对照双方，再补掉落物、岸面及最终性能范围。
macOS Apple clang/gmake x86_64 为实际平台；其他 POSIX 平台未运行，Windows Timer 未修改。

后续只读查询 `pmset -g log` 找到两条异常的外部时间证据：01:10:49–01:11:45 的基线采集中，
系统在 01:11:18 进入 Maintenance Sleep，01:11:45 DarkWake，记录睡眠 27 秒；
01:12:23–01:27:57 的候选采集中，系统在 01:12:30 入睡、01:27:57 DarkWake，记录睡眠 927 秒。
两条最长帧分别约 25/925 秒，与睡眠区间相吻合，现分类为 **受系统睡眠中断的性能样本**。
原始日志摘录和关联记录见 `timer-r11/r9-power-transition-query.txt`、`r9-sleep-correlation-r1.json`。
日志只有秒级时间，不能细分 GPU 内部阶段；原记录仍为时长协议 FAIL，不补写 PASS。
此证据不解释此前独立的 Reduced 动画现象，也不证明 r9 巨大 delta 当时确由墙钟回拨触发。
后续矩阵在采样进程存活期间使用系统提供的临时 `caffeinate -i -s` 断言（`-s` 仅 AC 有效），
结束即释放；不使用唤醒显示器或声明用户活动的选项，不修改永久电源设置。保持同一三轮完整协议和 1.10 门槛。


### 同计时器的完整后台对照（r11，PASS）

冻结候选仍为 `timer-r11/frozen-release/`，可执行文件 SHA-256 见上一节。
完整基线 `background-stage1-reference-r11/` 的 315 份第一方源码和运行资源与旧
`background-stage1-reference-r1/` 相同，只以双方共用的 5 份原生窗口/计时源码和 17 份 Release 库重建；
基线 SHA-256 为 `d6e657909dc286cc59402f7cd8c1ab8c8a30a972b8d7554d5c67e3d55c24c232`。
基线包的 `source_commit` 来自所在外层工作区，实际组成由源码清单证明，不能说成从该外层提交构建了全部当前玩法。
首次打包缺少 `bin/Mine.cfg` 的失败保留；补齐旧基线清单内三份配置后重新打包成功，未修改旧冻结包。

`run_background_performance_r11_r1.py` 已完成森林/水岸 × Off/Medium/High × 常驻/快速流送 ×
三轮 × 两版本，共 72 次、12 组；顺序 A/B、B/A、A/B，每次预热 5 秒、测量 30 秒。
当前实际窗口与 framebuffer 均为 1280×720、倍率 1，隐藏且不激活、静音、关闭截图读回，门槛仍为 1.10。
每次检查 AC 电源、计时区间与模拟 tick；绑定进程且限时一小时的 `caffeinate -i -s` 临时阻止系统睡眠。
快速流送沿用四次跨区块跳转，是流送压力场景，不能用来证明连续走动或转头观感。

原 exec session `51407` 已返回退出码 0。`background-performance-r11-r1/terminal.json` 与 runner 日志
记录 72 次完成、候选和正常配置恢复、临时断言释放；独立审计 `audit-r1.json` 从 359,472 行原始帧数据
重算分位数并核对两包各 111 个文件、315/324 份源码、原生依赖、计时和电源记录，全部 PASS。
各次实际结束于 30000.1–30008.0 ms，模拟 600/601 tick，无截图读回，也没有系统睡眠覆盖采样区间。
正常配置 SHA-256 恢复为 `21a5024dcd2566664f1c5ed450a733434f333fa0ef7bd771850b9138b5b0f3ff`。

| 场景/画质/运动 | P95 三轮中位数比值 | P99 三轮中位数比值 | 结果 |
| --- | --- | --- | --- |
| forest-off-steady | 1.0069× | 1.0302× | PASS |
| forest-off-streaming | 1.0329× | 1.0554× | PASS |
| forest-medium-steady | 1.0176× | 1.0153× | PASS |
| forest-medium-streaming | 1.0411× | 0.9798× | PASS |
| forest-high-steady | 1.0183× | 1.0365× | PASS |
| forest-high-streaming | 1.0135× | 1.0096× | PASS |
| shore-off-steady | 0.9579× | 0.9796× | PASS |
| shore-off-streaming | 0.9918× | 1.0086× | PASS |
| shore-medium-steady | 1.0015× | 1.0486× | PASS |
| shore-medium-streaming | 1.0074× | 1.0102× | PASS |
| shore-high-steady | 1.0711× | 1.0506× | PASS |
| shore-high-streaming | 1.0082× | 0.9796× | PASS |

最大 P95/P99 分别为 1.071147/1.055411（上取四位为 1.0712/1.0555），门槛保持 1.10。
这是当前 r11 的隐藏诊断对照；基线已包含阶段 1 与 terrain v12，不解释为相对最初用户截图的全部开销，
也不替代连续走动、正常玩法、历史 E2、前台或 2× 分辨率验收。

### 掉落物同计时器专项（r11，PASS）

完整矩阵结束并审计通过后，`prepare_drop_performance_r11.py` 在独立 `drop-baseline-checkout-r11/`
完成构建和打包。基线与当前候选共用岸线、五份原生修复与 17 份依赖，仅回退 r8 的五个物品姿态相关文件，
运行资源一致；323/324 份源码及明确差异见 `drop-performance-freeze-r11-r1.json`。
基线包 `drop-performance-reference-r11/` 的可执行文件 SHA-256 为
`d52c2a608f027fdcb2424f5997f89f75e7f52b066db94560d729e665c1261468`，候选继续使用同一 r11 冻结包。

`run_drop_performance_r11_r1.py` 已完成 Off/High × 常驻/快速流送 × 三轮 × 两版本共 24 次，
五件复制掉落物和四个待机敌人只用于表现诊断；仍使用隐藏窗口、静音、无读回与临时防系统睡眠。
原 session `89786` 已观察到退出码 0；`drop-performance-r11-r1/terminal.json` 记录候选与正常配置恢复、断言释放。
`audit-r1.json` 独立核对 119,625 行帧数据、完整运行时长、包/源码/依赖身份与电源历史，完整性及性能均 PASS。
四组 P95/P99 中位数比值如下（向上保留四位，门槛均为 1.10）：

| 场景 | P95 比值 | P99 比值 |
| --- | --- | --- |
| Off 常驻 | 0.9873 | 0.9718 |
| Off 快速流送 | 1.0095 | 1.0289 |
| High 常驻 | 0.9915 | 0.9869 |
| High 快速流送 | 1.0062 | 1.0179 |

快速流送仍是四次跨区块跳转，不证明连续行走或正常掉落/拾取。r9 失败记录保持原样。
此前两个入口拒绝提前执行的实际负例保存在 `drop-r11-preparation-checks-r1.json`，不冒充运行验证。

### 合成网格与材料区布局修复（r12）

交付对照 `handoff-captures-r11-r1/` 已采集森林日间、水岸、森林夜间及紧凑合成界面各两版本、两帧，共 16 张图。
已查看八张 5 秒原图：世界表现对照成立，但 900×600、1.25 字号合成界面只露出九宫格前两行。
这些基线已有阶段 1 HUD 和 terrain v12，不能当成最初用户截图的同条件前态。

修复仅位于 `OgreUserInterface::Impl::drawCrafting()`：左侧配方/材料独立滚动，右侧保持完整网格和清空按钮，
底部保留结果与合成/关闭按钮。材料按宽度排成两或三列，格子按剩余宽高缩放；制作、库存、配方和输入处理语义未改。
Debug/Release 客户端构建通过。`ui-r12/captures-r1/` 十组隐藏诊断采集完成，共 20 张 PNG；
实际查看十张 5 秒原图，覆盖中英文、900×600 三档字号、640×480 大字号与 1280×720 默认字号。
九宫格、清空及底部按钮全部可见；最小窗口材料列表可滚动，英文长材料名使用既有省略显示。
未通过正常输入执行滚动、装载配方与制作，不把静态布局检查当作玩法通过。

`ui-r12/verification-r1.json` 核对 324 份第一方源码与构建检出一致，只有上述函数变化；五份原生源文件、
17 份 Release 依赖及运行资源与 r11 一致。两份包各 111 项清单核对通过，正常配置恢复为 `21a5024d…`。
Release SHA-256：`a57269ee3768f49ba6264a2a5abb0d96826ab7f24d0b76c725ef32a8655efbcb`，冻结于 `ui-r12/frozen-release/`。
原采集 handle `9504` 恢复时已不可用；自身终止记录明确退出 0、配置恢复及断言释放，不额外声称观察到原工具退出码。
此次只有面板布局变化，关闭面板的世界渲染路径保持一致，不重复完整世界性能矩阵；r11 两项性能证据仍保留 r11 身份。

### 当前候选交付（r12，正常玩法待验）

合成布局修复已本地提交 `1feb4d5`，随后从干净工作区打包：
`build/visual-upgrade-20260917/delivery-r12/HelloMine3D-Visual-Candidate.app`，
bundle id 为 `local.hellomine3d.visual-candidate`，macOS / x86_64 / Release、本地未签名。
包内 111 项分发清单、324 份源码及正常配置均核对通过；二进制与上述 r12 原图对应，资源、源码清单与冻结包相同。
来源提交为 `1feb4d5`、tracked diff 为空，没有用户世界或诊断存档。

以 `HELLOMINE3D_VALIDATE_ONLY=1` 在隔离数据目录运行，原 session `36933` 观察到退出 0，未创建渲染窗口。
这是资源和启动自检，不冒充正常打开窗口、创建世界或保存重开的验收。
验证日志保存在包外 `delivery-r12/validate-only-r1/`；正常配置不带诊断参数，未自动启动前台客户端。

同目录 `README.md` 是交付说明；`gallery/index.html` 提供森林、水岸、夜间和合成界面的四组对照。
八张 PNG 与原件逐字节一致，同组尺寸相同，来源和二进制身份在 `gallery/evidence.json`；本地链接检查通过，未另做浏览器渲染验收。
前三组保持 r11 的世界表现证据，第四组直接比较 r11 与 r12 合成布局，未混称为同一旧版本。
完整交付核对见 `delivery-verification.json`。当前候选具备可运行文件与明确证据，整体 Goal 仍等待连续场景及正常玩法退出项。

### 隐藏连续机位与新发现（r13）

现有 fast-streaming 只有四次跨区块跳转。新增 `VisualCameraSweep` 与 Ogre 诊断入口：
`HELLOMINE3D_VISUAL_CAMERA_SWEEP="dx dy dz yaw pitch duration"`，4 秒预热后使用首尾速度归零的曲线，
限制 48 m 位移、180°/45°转向及 1–30 秒时长。仅在隐藏截图且存在初始世界时允许，拒绝混用性能和批量场景。
只改变渲染相机与物品面向的观察点，不注入 OS 输入或玩家移动；世界仍模拟，玩家可正常受敌人伤害。
退出世界即停用，未设置开关时沿用原渲染机位。

- `verify_visual_camera_sweep.sh`：Debug/Release 各 21 项、各 12,000 个连续采样点通过（macOS arm64 原生单元程序）。
- x86_64 Debug/Release 客户端构建通过；Release 无截图开关、非法数值、位移超界三个实际启动负例均在创建窗口前拒绝。
- 关闭新开关的原有隐藏截图路径通过，原 session `12681` 退出 0，5 秒原图已检查。
- `camera-r13/captures-r1..r3/` 共 11 段、每段 97 张 1280×720 BMP、间隔 125 ms，共 1,067 张。
  独立核对原始记录中的采集时间互不重复，最大迟到 12 ms、最大相邻间距 135 ms；机位轨迹与玩家坐标分别记录。
  这不是帧性能门槛，也不能排除高于 8 Hz 的视觉问题。
- 实际查看 39 张序列原图和一张关闭路径原图。查看工具不支持 BMP，先无缩放转换为 PNG，逐张核对 RGB 字节与原 BMP 一致。
  `videos-r2/` 另提供 11 段 8 fps 视频与本地 `index.html`；视频生成成功，不声称已通过浏览器连续播放验收。

**新增待修项**：`captures-r2/water-column/frames/capture_08500ms.bmp` 至 `capture_08625ms.bmp`
显示出水瞬间横向深色区域和明显亮度变化；世界 ROI 采样平均 RGB 变化为 60.09/255。
已进一步查看 8.375、8.5、8.625、8.75 秒相邻原图，不能把水上/水下端点清晰当作过渡通过。
下一批定位水面近相机裁剪与介质混合的关系，再做同机位修复前后连续对照；当前不提前指定根因。

林下昼夜、草甸到林缘、岸床、遗迹及路标内芯在抽查帧中可辨。
`forest-edge` 上升进入叶片，已追加 `forest-edge-level` 原高度转向；`camp-mountain` 的 11.875→12 秒大变化也对应穿叶，
保留为路径限制，不标为正常穿行通过。机位移动时玩家仍在原处，因此地图和伤害方向不作为移动玩法证据。

首轮 runner `45977` 退出 1：校验脚本误期待日志 `hidden=1`，真实日志为 `hidden=true noActivate=true`；
客户端本身退出 0 并保留 97 帧，独立分析核对后用于诊断，原 runner FAIL 不改标。
其余八段原 session `61316`、两条补充路径 `97809` 均退出 0；正常配置与临时防睡眠断言均恢复。
系统 ffmpeg 因缺少 libtiff 依赖失败，未修改系统库；已改用项目已有 AVFoundation 编码器，session `28631` 退出 0。
失败包装器经定向停止后退出 143；其 ffmpeg 子进程 `21189` 最后仍在系统 UE 等待状态，已发送 SIGKILL，不能声称已观察到子进程退出。

完整核对见 `camera-r13/verification-r1.json`、`sequence-analysis-r1.json`。
325 份第一方源码与构建检出一致，五份原生修复、17 份 Release 依赖和资源沿用 r12；专用包与冻结包各 111 项清单通过。
Release SHA-256 为 `396bc2c8a080e50ceb37c8308451e9ccae3e02ee0e02abc2ffe8fdd2d629b271`，冻结于 `camera-r13/frozen-release/`。
此批是关闭状态不改变玩法的诊断扩展，不把 r11 性能数据改标为 r13；r12 交付包原件保留。

### r14 水线过渡修正（2026-09-18，定向性能与审计通过）

生产 water fragment 原先在 `camera.y < surface.y + 0.12` 时硬切换深色与 0.90 透明度。
相机近裁剪面切过这层高不透明度水面，形成 r13 的整屏横带；实际 GLSL 反例在相邻 1 mm
高度采样中得到最大 80/255 合成颜色跳变。改为连续颜色混合，近裁剪区淡出表层，
水上 0.35 m、水下 1.0 m 外保持既有覆盖和真实水深吸收；无新增接口、查询、几何或存档变更。

- GPU：Apple M1 Pro、原生 arm64 验证程序；同一程序执行冻结旧 shader 退出 1，当前退出 0。
  浅/深水 × 细节开启/关闭共 8,004 样本最大变化 2/255；210 对世界坐标接缝位置/法线误差仍为零。
  深度吸收、距离独立、深度上限、岸波、漂移及关闭回退通过。见 `waterline-r14/gpu-*-final.log`。
- 同机位出水：seed 20260807、terrain v12、初始位置 `-176 61 280`、朝向 `-15 -60 0`、
  时间 6000、High+post、1280×720，保持 r13 的 12 秒相机路线和 97 帧采样。
  首次修改虽消除横带，最大 ROI 变化仍为 56.33/255，保留为未达预期的版本；最终为 19.19/255，
  原版为 60.09/255。该指标用于定位大幅亮度变化，不证明所有帧无闪烁或人类舒适度。
- 最终八条有效序列共 776 帧：出水 High、Off、关闭大气、夜间；昼夜水岸；岸边入水 High 与关闭大气。
  已实际查看 27 张关键帧，BMP 的 PNG 解码逐一验证 RGB 相同。
  前六条为 1280×720，后两条因显示环境改变为 2560×1440，分辨率分开记录，不混作同像素前后对照。
  首次入水路线预热期间受重力落到水底，后续穿过地形，明确无效；已改用实际岸边锚点
  `-240 66 312`、`40 -3 0` 位移观察入水。2× 首次 wrapper 尺寸检查 FAIL 保留，
  实际客户端退出 0 的 97 帧另按原生尺寸独立核验，未重标原 runner。
- 资源 Debug/Release 各 112/112。初次隔离工作目录未指定资源根，纹理数组检查失败，
  原证据保留；指定 `HELLOMINE3D_ROOT` 后通过，没有修改测试或放宽条件。
- 只改运行时 shader，复用 r13 双配置客户端。Release SHA 仍为 `396bc2c8…`，
  325 份第一方 C++ 源文件、五份原生文件及 17 份库身份保持一致；不声称进行了新的 C++ 构建。
  冻结包 `waterline-r14/frozen-release-r2/` 与 r13 的分发差异仅 water fragment 和构建身份元数据。

检查总表：`waterline-r14/verification-r1.json`。正式性能 Off/High × 静态/快速流送 ×
三组交替前后对照共 24 次、127,773 帧及独立证据审计通过，统一 Retina 2×、关闭截图读回。
P95/P99 最大中位数比值为 1.012151 / 1.017446，门槛保持 1.10。完整 CSV、相机/世界身份、
5 秒预热和 30 秒采样、无睡眠中断、候选包与正常配置恢复均已核对，见
`waterline-performance-r14-r1/audit-r1.json`。原工具句柄 72980 在恢复时已被回收，
不声称直接观察了退出码；runner 终态声明 0，与全部证据及独立审计一致。
水下远端仍可见水平亮边，旧版也存在，下一步单独定位；
本批只关闭近裁剪暗带和局部硬切换缺陷，不宣称整个水面或正常游泳体验已验收。

### r15 水下远景与介质过渡（2026-09-18，修正版工程、定向画面与性能通过）

根因是 `forCameraMedium` 只更新地形雾色，天空地平线仍使用空气色。初版让两者使用同一
水介质色，实际水下白天原图中的远端水平亮带消失，近处台阶和水底仍可辨认。代码探针覆盖
四个时段、404 个浸入量，原版天空/雾色最大差为 0.84574，初版为零；双配置 V10C 各 26/26。

随后对初版四组前后、八段共 776 帧做只读核验：seed 20260807、terrain v12、1280×720 窗口点、
2560×1440 framebuffer，统一 8 Hz、4 秒预热后 12 秒相机路线；昼夜水中出水、大气关闭回退和
干燥水岸的成对设置相同，所有原图哈希、采样时间及隐藏窗口日志通过。实际查看 16 张关键帧，
PNG 与原 BMP 解码后 RGB 相同；来源在 `underwater-horizon-r15/review-before/`、`review-after/`。
但不能据端点图关闭此批：相同归一化世界 ROI 的最大相邻平均 RGB 变化如下，单位为 `/255`。

| 序列 | r14 基线 | r15 初版 | 观察 |
| --- | ---: | ---: | --- |
| 白天出水 | 19.8724 | 48.0530 | 初版动态变化未达预期，需修正。 |
| 夜间出水 | 4.4934 | 4.5662 | 已查看近景与出水关键帧，夜间可读性保留。 |
| 大气关闭 | 19.8314 | 20.0131 | 旧回退表现保留，旧亮带仍存在，不声明此路径已消除亮带。 |
| 干燥水岸 | 2.2109 | 2.1858 | 起点成对原图未发现色板退化；正常 Actor 没有冻结。 |

该 ROI 指标仅用于定位突变和选择检查帧，不是帧性能、闪烁或人类舒适度门槛。
原始记录、失败结论保留于 `underwater-horizon-r15/verification-r1.json` 和 `sequence-analysis-r1.json`。
初版源码与包冻结在 `underwater-horizon-r15/frozen-release/`，Release SHA 为 `613a490c…`。

初版的新天空色沿旧 0.25 m 介质区间恢复，造成出水亮度变化集中。修正版将共同介质量的过渡
扩展至水面下 0.05–1.0 m；在顶层水块底部达到完全浸入，与下一块连续，仍最多两次驻留读取，
无新增加载、生成、存档或时间积累。新增深度中点、表面/块边界和四时段 4,804 个毫米样本检查。
以低优先级完成 macOS x86_64 Debug/Release 客户端与 WorldRuntime 构建，V10C 各 **28/28 PASS**；
命令、325 份源码收据、二进制及日志见 `underwater-horizon-r15/revision2/`，原 runner 63879 已观察退出 0。
修正版 Release SHA 为 `186f388da2127044b2c3eef129272a2b1a01984c1704044112b4bc90ff4b5e11`。
用户随后明确表示只作短暂观察，允许继续自主工作。已通过暂停菜单“保存并退出”正常关闭预览，
日志记录 OGRE Shutdown，预览存档保留；工作包随后刷新至修正版并隐藏运行，无前台自动启动。

修正版四段共 388 帧采集完成，原 runner 84364 实际退出 0；与既有 r14 基线合计 776 帧的
原图哈希、2560×1440 尺寸、时间序列、成对设置和相机轨迹核验通过。实际审阅修正版 18 张
RGB 无损解码原图：白天水下远端亮带消失，近景台阶保留，出水天空与雾色逐步恢复；夜间近景
仍可读，干燥岸边色板和真实岸线小地图未见退化。关闭大气的旧亮带仍存在，不宣称此回退问题已修复。

| 序列 | r14 基线最大变化 | r15 修正版最大变化（/255） |
| --- | ---: | ---: |
| 白天出水 | 19.8724 | 21.3618 |
| 夜间出水 | 4.4934 | 3.3108 |
| 大气关闭 | 19.8314 | 19.9939 |
| 干燥水岸 | 2.2109 | 2.1921 |

白天变化明显低于失败初版的 48.0530，但略高于 r14；没有将该诊断指标改作闪烁或舒适度门槛。
证据在 `underwater-horizon-r15/after-r2/`、`sequence-analysis-r2.json`、`review-after-r2/` 和
`revision2/visual-acceptance.json`。这份限定范围画面记录允许进入性能比较，不代替正常游泳输入。
实际图像分析 runner 38037 和无损解码 runner 37751 均退出 0。采集后正常配置原样恢复。
本批已本地提交 `b1a4a1c`（修复: 协调水下远景与出水颜色过渡）。

修正版性能输入已独立冻结，不操作前台工作包：`horizon-performance-freeze-r15-r1.json` 核对两版各
111 项分发文件、325 份源码，以及共用的 5 份原生源码和 17 份依赖归档。运行资源完全相同，
第一方源码仅 `WorldEnvironment.cpp/.h`、`OgreBootstrap.cpp` 和对应测试不同；新旧源码分别保留在
`underwater-horizon-r15/performance-source-snapshots/`，不再用当前源码冒充旧版源码。
冻结候选 `underwater-horizon-r15/performance-frozen-candidate/Diagnostic.app` 只作为对照输入，未启动。
`run_horizon_performance_r15_r1.py` 与 `audit_horizon_performance_r15_r1.py` 已准备 Off/Medium/High ×
静态/四次传送流送 × 三组交替前后对照，共 36 次、6 组，P95/P99 门槛仍为 1.10。
启动前必须存在与 Release、源码、资源哈希匹配的修正版连续画面验收记录；普通配置在实际采样开始时
保存并在结束后原样恢复。五项准备检查覆盖真实冻结输入、拒绝占用当前 PID 43696、缺少画面验收、
工具身份变更和拒收旧 24 次结果，见 `revision2/performance-preparation-validation-r1.json`。
画面采集与冻结性能候选的完整构建记录因文档差异
而不同，但 Release、325 份源码清单和资源清单 SHA 完全一致；不混称完整包身份相同。

性能现已完成：原 runner **8378 实际退出 0**，36 次、6 组共 **174,097 帧**；独立审计 runner
**38149 实际退出 0**，结果为完整性与性能均 PASS。固定 2560×1440 framebuffer、1280×720 窗口点，
每次 5 秒预热、30 秒无读回采样；三轮次序 AB/BA/AB。P95/P99 三轮中位数比值如下：

| 画质与场景 | P95 比值 | P99 比值 |
| --- | ---: | ---: |
| Off 静态 | 0.985163 | 0.979265 |
| Off 区块切换 | 1.006195 | 0.989092 |
| Medium 静态 | 0.998099 | 0.993306 |
| Medium 区块切换 | 0.976241 | 1.008316 |
| High / 后处理开启 静态 | 1.005642 | 1.001911 |
| High / 后处理开启 区块切换 | 1.006863 | 0.990102 |

六组均满足原门槛 1.10。逐帧时间和模拟 tick、恒定白天、机位、真实窗口倍率、全部 CSV 哈希、
源码/包/共用依赖身份及无睡眠中断已核对；候选包和采样前普通配置原样恢复，临时防睡眠断言释放。
完整结果在 `horizon-performance-r15-r1/audit-r1.json`，原始日志为 `horizon-performance-r15-r1-runner.log`。
这是当前 r14→r15 修正版的后台性能结论，四次传送式流送不等于正常行走或游泳；历史结果保持原范围。

### r15 候选客户端更新

代码提交 `b1a4a1c` 的干净候选包位于 `delivery-r15/HelloMine3D-Visual-Candidate.app`，
未自动打开前台窗口。Release、325 份源码清单和资源清单与上述已测候选一致；111 项分发文件
逐项验证通过，普通设置使用采样前保存的真实配置，未打包用户世界或诊断存档。
`HELLOMINE3D_VALIDATE_ONLY=1` 在独立数据目录实际退出 0（session 59978），未创建渲染窗口。
结果在 `delivery-r15/validate-only-r1/result.json` 与 `delivery-verification.json`。

同目录 `README.md` 列出最新改动、性能和未完成项；`gallery/index.html` 提供六组、十二张
原图对照，新增白天/夜间水下 r14→r15 修正版，前期四组保留 r11/r12 实际身份。
图片复制哈希、成对尺寸和页面链接均已校验；沿用已存在画廊布局，未新增浏览器视觉验收声明。
首次画廊脚本误按通用 capture 的 `package_identity` 读取定向记录，报 KeyError；修正为其实际
`identity` 字段后通过，初始脚本与失败说明保留在交付目录，不改原始采集数据。

本次仅只读重查公开 CUA 文档：仍提供单次 `pressKey`、click、drag，没有持续键/按钮状态或
按住时长接口。未重复已失败的相同输入，也未通过替代注入冒充正常玩法。持续行走、采集制作、
工具/容器/战斗、地图生命周期和当前版保存重开仍待验；关闭大气旧亮带与首次 Reduced 停顿
根因的不确定性继续保留。候选包交付不代表完整 Goal 已完成。

### r16：回退模式保留基础水介质

关闭增强大气时，`OgreBootstrap::syncEnvironment` 的同一条件同时跳过了基础水介质识别，
使 r15 回退画面仍出现空气色远端亮带。本次只移除该识别入口对 `m_v10cAtmosphereEnabled`
的依赖；云层、定向光照、表面/角色着色与水面细节的原有回退分支保留。驻留查询仍最多两次，
空气/未知列仍为零浸水，不加载区块，不改变游泳、碰撞或存档。

隔离检出核对全部 325 份源码，仅同步 `OgreBootstrap.cpp`；Debug/Release 客户端构建通过
（原 session 8068 退出 0）。构建器请求 nice 降低优先级被环境拒绝，但 make 正常完成，
不声明已降优先级。水介质纯函数与定向测试未改变，沿用 r15 双配置 V10C 各 28/28 的对应证据，
本轮没有新增 smoke 执行声明。新 Release SHA-256 为
`bbccd2d4dc6c190f178874022ac73875f4935aea5654e251e7f2ecd6705ee7ec`。

四个前后条件为强制回退水下白天、回退水下夜间、回退干燥岸边、增强大气白天。
全部实际 2560×1440 / 1280×720 点、4 秒预热、12 秒相机序列、8 Hz、每段 97 帧；
新增基线两段与候选四段共 582 帧，复用身份匹配的 r15 白天两段后共 776 帧。
逐帧哈希、时间顺序、稳定玩家位置和成对条件核验通过。实际查看 21 张无损解码原图：
回退水下亮带消除，近处台阶仍可读，出水关键帧未见新增明显色块；夜间远水保持暗色，
干燥岸边与增强大气画面保留原表现。简化云形与静止水面细节仍属回退路径。

| 条件 | 原版 ROI 最大相邻变化 /255 | r16 /255 |
| --- | ---: | ---: |
| 回退白天 | 19.9939 | 22.2621 |
| 回退夜间 | 3.3644 | 4.1808 |
| 回退干燥岸边 | 1.7965 | 1.8717 |
| 增强大气白天 | 21.3618 | 21.6059 |

恢复水雾后白天统计最大值略升；这些值用于定位复核帧，不作为舒适度门槛。
证据为 `fallback-medium-r16/{build-results,sequence-analysis-r1,visual-acceptance}.json`，
原图解码校验在 `review-r1/decode-verification.json`。构建、前后采集、分析、解码原始 session
8068 / 14255 / 80653 / 78875 / 14612 均已实际退出 0。

专项性能输入已冻结到 `fallback-performance-freeze-r16-r1.json`：只比较受影响的强制回退路径，
Off 阴影/关闭后处理，静态与四次传送流送各三轮 AB/BA/AB，共 12 次、2 组。
沿用 5 秒预热、30 秒采样、2×实际尺寸、无截图读回、AC 和临时防系统睡眠，门槛仍为 1.10。
原 runner 94856 已实际退出 0，12 次共 69,354 帧，候选和普通设置已恢复，临时防睡眠断言已释放。
静态 P95/P99 比值为 0.983706/0.978742，流送为 0.947946/0.991948；这些仅为原始统计，
**本轮专项性能尚未通过独立审计**。

第一次审计 `audit-r1.json` 发现审计器一处残留 1,200 秒检查，与已冻结的 3,600 秒协议自相矛盾；
保留原脚本/报告，新 `audit_fallback_performance_r16_r2.py` 只令该值匹配已冻结协议，未改数据或门槛。
修正记录见 `auditor-revision-r2.json`。第二次审计原 session 89388 退出 1：最后一条流送候选的
外部采集结束时间为 2026-09-19 00:08:15.052928，系统在同秒记录 `Clamshell Sleep`。
秒级日志不足以证明睡眠发生于采样结束之后；即使帧增量正常，也保留审计失败，不宣称矩阵有效。
原始数据和两次失败完整保存在 `fallback-performance-r16-r1/`。

随后只读检查确认 `AppleClamshellState=Yes`。新恢复入口
`run_fallback_performance_r16_r2.py` 在预检及每次采样前要求开盖，其他 12 次协议不变；
预检实际退出 2，未启动窗口或覆盖旧样本。输入收据 `fallback-performance-freeze-r16-r2.json`
沿用原冻结二进制、源码、资源与依赖。该入口未启动采样；后续真实图形可用性检查取代过度严格的开盖条件，见下文。
相机序列、传送与隐藏诊断均不替代正常游泳/行走或完整玩法；本地保存这批代码不表示该性能项完成。

### r16 复验与当前版正常自测（2026-09-19）

本轮重新检查实际电源状态：虽然合盖，`System Capabilities=15`，CPU/Graphics 均可用，
`AppleClamshellCausesSleep=false` 且连接 AC。原先仅要求开盖的恢复条件过严，已停用该入口。
先用同版隐藏渲染完成 5 秒预热/30 秒流送探针，5,442 帧、600 tick、最大 delta 80 ms，
没有睡眠覆盖；此探针仅证明可渲染。r3 新门控检查 CPU/Graphics、AC、无强制合盖睡眠，
7 项正反例和真实预检通过。每次正式采集前后记录状态，原严格睡眠审计、性能门槛和冻结输入不变。

`run_fallback_performance_r16_r3.py` 原 session 85622 退出 0；12 次、2 组共 68,855 帧。
`audit_fallback_performance_r16_rerun3.py` 原 session 28192 退出 0，完整性、性能及无睡眠覆盖审计通过。

| 2× 2560×1440 隐藏模式 | P95 候选/基线 | P99 候选/基线 |
| --- | --- | --- |
| 静态 | 1.005021 | 0.997465 |
| 四次传送流送 | 1.006654 | 0.976728 |

两组均满足 1.10 门槛。候选及普通设置已恢复，临时断言已释放；r1 失败和 r2 未启动记录保留。
证据：`fallback-performance-r16-r3/audit-r1.json`、`fallback-medium-r16/verification-r2.json`。

用户明确允许可视化窗口后，主任务使用 CUA 从正常菜单新建 `NormalR16-20260919`，seed 20260807，
terrain v12；保留原 A/B 世界。实际切换英文/1.25 字号，打开并关闭配方书展开的合成界面，网格及底部
按钮完整可见；随后恢复中文/1.0，设置字节哈希与原配置一致。通过菜单保存退出、重新启动进程并打开
同世界，世界名、seed、位置、空库存和旅程 0/33 保持。两次存档世界时间 2166→2524、生命 2→6，
重开后存在正常模拟间隔，不声明精确生命值或完整动态状态往返通过。正常短按/拖动未证明连续移动或
转向；没有注入、传送或编辑存档。这是实现者同上下文的有限正常输入自测，不能替代独立验收或 AI-06。
三张原始窗口图及只读存档摘要位于 `normal-input-r16-r1/`，窗口辅助条导致的两次歧义失败亦保留。

大字号合成原图显示敌人预警字幕被面板覆盖，已定位通知早于面板绘制，列入下一批 r17 修正。
当前仍未证明正常采集、产出合成、装备、容器转移、游泳、完整战斗及地图生命周期。

候选更新到 `delivery-r16/HelloMine3D-Visual-Candidate.app`，代码 `6ae3b5b`，Release
`bbccd2d4dc6c190f178874022ac73875f4935aea5654e251e7f2ecd6705ee7ec`。
111 项分发文件、325 份源码、无窗口启动校验通过；附八组十六张原图，历史图保留实际版本标签。
校验生成的诊断世界移入证据目录，未混入交付包。整理脚本的路径/清单字段假设失败保留于
`delivery-r16/preparation-failures.json`；客户端校验本身退出 0，没有重复采样取优。
此候选并不表示完整 Goal 结束。

### r17：合成时的预警保持可见（2026-09-19）

正常体验原图 `normal-input-r16-r1/02-crafting-en125.png` 中，英文 1.25 字号合成面板把底部
敌人攻击字幕盖住。原因是 `drawHudNotifications()` 早于合成/容器窗口绘制，`NoInputs` 并不
保证显示在最上层。本批只改 `OgreUserInterface.cpp`：通知背景和文字共同放入 ImGui 前景层，
沿用原 HUD/屏幕底部锚点，按窗口宽度换行并约束边界，不取得焦点、不拦截控件输入。
字幕时长、优先级、语言/启用开关和所有玩法状态保持不变。

Debug/Release 客户端均构建通过，原 session 63796 退出 0。Release SHA-256：
`25903a9f9e13e4dde140e91333d8810915aa55e34d73b25aeffa13270fd96ac0`。
在同一正常世界通过 CUA 重开、切换设置并打开合成：中文 1.0 与英文 1.25 的敌人预警可读，
配方书展开后网格和关闭按钮仍可见，实际点击关闭成功；随后恢复中文/1.0并正常保存退出。
原图 `notifications-r17/normal/01-crafting-en125-warning.png` 与原版使用相同窗口/字号，
两次自然世界时间不同，只比较界面遮挡，不冒充场景画质同帧对照。

原 session 97864 退出 0，完成六组双语隐藏布局检查、十二张原图：640×480 点/1.25 字号的
工作台及容器，900×600 点/0.85 字号的工作台，均为实际 2× framebuffer。实际审阅六张
10 秒帧和一张正常窗口原图，通知均清晰可见，合成底部操作未被遮挡。小窗口容器保留已有滚动
正文，本检查不声明容器转移完成。普通设置已按原字节恢复，临时断言已释放。

完整收据在 `notifications-r17/verification-r1.json`。当前正常输入为实现者同上下文自测，
不是独立或盲玩验收；该收据生成时尚未补做通知绘制专项性能，r16 性能证据保留原身份。
已本地提交 `95d57c7`，候选位于 `delivery-r17/HelloMine3D-Visual-Candidate.app`。111 项分发文件、325 份源码与无窗口启动校验通过（原 session 17667 退出 0）；附正常 UI 前后原图页并链接 r16 的八组历史对照。生成的诊断存档留在校验证据目录，没有混入交付包。

### r17 正常切换世界的小地图检查

同一前台进程中，通过正常菜单依次进入 `NormalR2A-20260917`（seed 6123472）、
保存回主菜单、进入 `NormalR2B-20260917`（seed 898910252）、保存并返回 A。
A 地表图显示草地及树群，B 显示海水、阶梯沙岸和内陆树群，与窗口中的世界对应；切回 A 后恢复
原地表图，未残留 B 的海岸色块。初始进入时可见未知区域逐渐补齐，三张完整刷新后的原图均已审阅。
最后通过菜单保存退出；原测试世界保留，时间及战斗生命值自然推进，未修改存档或输入坐标。
证据为 `map-normal-r17/acceptance-record.json`，包含前后存档摘要与精确窗口身份。

本项仅关闭实现者正常输入自测中的**切世界地表图刷新**，不关闭尚未使用过的地标记忆/清理、
移动刷新或改块刷新，也不充当独立验收。现有公开 CUA 仍无持续按住键鼠或相对转向接口；
界面和地表切换已完成可执行项，不反复尝试相同无效输入。当前没有权限、睡眠或窗口占用阻塞。

### r17 通知绘制专项性能补验（2026-09-19）

按通知绘制的实际改动补齐专项对照：基线为 r16 `bbccd2d4…`，候选为 r17 `25903a9f…`，
325 份第一方源码仅 `OgreUserInterface.cpp` 不同，资源、五份原生窗口/计时源码和 17 份依赖一致。
使用林地机位、打开合成面板、中等阴影及关闭后处理；实际 2× 2560×1440 隐藏/noActivate 窗口，
每次预热 5 秒、采集 30 秒，无截图读回。静止及既有四次传送流送各三轮成对运行，顺序 A/B、B/A、A/B。

原 runner session 13294 退出 0，12/12 次共 57,709 帧；单独审计命令退出 0，
完整性、逐帧计时、包/源码身份与系统睡眠覆盖审计均通过。预检拒绝错误版本及未验收画面两种负例；
审计器拒绝面板条件及候选身份不匹配两种负例，原始文件未被改写。

| 合成面板 / Medium / 2× | P95 候选/基线 | P99 候选/基线 |
| --- | --- | --- |
| 静止 | 0.988568 | 0.993128 |
| 四次传送流送 | 0.982460 | 0.992308 |

两组均满足 1.10 门槛；最新工作包、普通设置已恢复，临时睡眠断言已释放。
证据为 `notification-performance-r17-r1/audit-r1.json`、`notifications-r17/verification-r2.json`，
交付目录新增 `delivery-r17/performance-addendum-r1.json` 与 `PERFORMANCE.md`，原包及旧收据保持不变。
声音字幕开启，但逐帧 CSV 不计字幕可见时长；本项不证明正常行进、面板交互、合成产出或完整玩法。
没有重复 r16 水下及历史全场景矩阵，也不将此范围扩展为独立正常输入验收。

交付页实际打开检查未完成：内置浏览器明确以 URL 安全策略拒绝本地 HTML，未使用替代浏览器、
本地转发或其他路径规避。原图审阅与链接校验仍有效，原 `delivery-verification.json` 不改写；
另记 `delivery-r17/gallery-browser-review-r1.json`。这不是客户端启动或游戏画面的失败，也不要求用户留机观察。

### r19 云层形体与边界连续性（2026-09-19）

用户明确暂缓持续键鼠输入相关验收，继续迭代视觉；该项保留为待验，不阻塞本批。
当前阶段聚焦实际地面画面中云层平、灰、缺少受光层次的问题，保留既有体素世界风格与天空留白。

- 两处三频噪声同时求解析斜率，使云团随太阳/月亮方向产生明暗；采样次数仍为最多 24 次 lattice hash，
  没有增加贴图、世界查询或体积步进。收紧边缘、加强云底和薄边的区别，沿用昼夜/黄昏配色。
- 日月受光连续相加；云层入口采样、极薄穿行距离的透明度、内外水平淡出和补光连续过渡。
  新增高度边界检查先暴露旧的不连续，再修正；失败原样保留。
- FS2 回退、地平雾、太阳/月亮圆盘及水介质参数不改；GLSL 接口与客户端 C++ 不变。
  复用 r17 已验证的 Release `25903a9f…` 与 325 份源码，实际变动资源仅 `HelloMine3DSkybox.frag`。
  最终 shader SHA256 为 `8b24bc4b24db4190166b09f474d9c99c1f4ad8a62c490290f902f58c643dd891`。

工程验证：macOS Apple M1 Pro 的生产 GLSL 21/21 通过，包含昼夜回退逐像素一致、定向受光、
无云地平线保持、上下边界、日月交接和各 120 步连续漂移；60 Hz 相邻帧平均最大差异为
昼 `0.00109482`、夜 `0.000572205`（8 bit RGBA）。去掉定向受光的故障负例准确失败。
资源 Debug/Release 各 112/112，资产 80/80 通过。只改 fragment，未冒充 C++ 全量重建。

实机诊断：固定 seed 20260807、terrain v12、Retina 2560×1440 隐藏窗口，最终候选六组各两帧，
覆盖林地白天/夜晚、水岸白天/黄昏、FS2 回退、高阴影加后处理。四组有本批旧版同机位对照。
实现者审阅八张最终客户端原图、四张旧版原图和两张精确云层高度 GPU 原图：白天云底与亮边可辨，
黄昏保留暖灰、夜晚保留深蓝，水天交界未见新增亮带。该模型仍是有界云层近似，不是体积云或天气系统。

证据边界：最初两次 `cloud-inside/cloud-above` 客户端夹具在截图前落回地面，不能算云内/云顶验收；
记录及原图保留。精确高度及上下边界改由生产 fragment 的 GPU 渲染检查覆盖，不宣称正常飞行玩法。
隐藏诊断与实现者自测不替代正常输入、人类舒适度或独立黑盒验收。Windows 新 shader 检查未运行。

性能：三档（Off/Off、Medium/Off、High/On）×静止/四次传送流送×三轮前后对照，共 36 次、
169,165 帧，原进程退出 0，独立逐帧/身份/无睡眠审计通过。全部固定 5 秒预热、30 秒测量、
不做 GPU 回读，P95/P99 中位数比值门槛 1.10。各组结果如下（小于 1 仅表示本次测量更低）：

| 阴影 / 后处理 | 静止 P95 / P99 | 流送 P95 / P99 |
| --- | --- | --- |
| Off / Off | 1.022548 / 1.017039 | 0.989026 / 1.034386 |
| Medium / Off | 1.013721 / 1.005881 | 0.999643 / 0.982401 |
| High / On | 0.985003 / 0.961731 | 0.996314 / 0.987127 |

普通配置已按字节恢复，工作包恢复最终候选。四组图像的实际位置/朝向/设置核对一致；
日间截图最终世界时间相差最多 3 ticks，昼/夜/黄昏类别一致，未宣称截图逐像素同步。

本地产物根为 `build/visual-upgrade-20260917/`：
`cloud-depth-r19/visual-verification.json`、`gpu-final-r2.log`、`negative-final-r2.log`、
`resource-results.json`、`before-*` 与 `after-r3-*` 原图；性能目录 `cloud-performance-r19-r1/`。
交付 `delivery-r19/HelloMine3D-Visual-Candidate.app`：111 项文件、325 份源码、无窗口启动校验通过；
`delivery-r19/COMPARISON.md` 附四组当前原图链接，未改变旧交付包或混入用户存档。
GPU 重现入口是 `tools/validate_sky_shader_macos.cpp` 文件头所示命令，基线为
`cloud-depth-r19/Skybox-before.frag`。性能脚本 `run_cloud_performance_r19_r1.py`、
`audit_cloud_performance_r19_r1.py` 与 `cloud-performance-freeze-r19-r1.json` 冻结此次范围。

用户随后明确视觉范围也包括玩家、动物等模型，下一批优先转向角色表现。
代码核对：已有 Stalker/Brute/Spitter 等敌人的分部模型与姿态，玩家主要呈现手持物；尚未发现
独立动物模型/行为体系。优先完善玩家手臂与持物连接、现有生物的身体/面部层次及动作衔接；
动物需要物种轮廓、四足步态与真实行为路径一并设计，不把敌人换色冒充动物。
草地和树冠的高频反差仍列为后续视觉候选，未计为本批已完成。

### r20 玩家手部与握持连接（2026-09-19）

按用户补充范围，第一人称玩家新增青灰袖子、皮质袖口、手掌、拇指和指节；空槽也显示手臂。
空手、握图标、托方块共三种有界缓存，每种 54 个四边形。手与持物共用现有动作时间线、刚性旋转、
透视、背面剔除与深度排序，不新增输入、Player 字段或世界查询。昼夜曝光使用现有 daylight，
制作、容器、设置/暂停界面隐藏；按 HUD 右侧余量缩放，以兼容 800×600、1.25 UI。

本批只修改 `OgreUserInterface.cpp` 并新增纯 `PlayerHandPresentation.h`；资源与 r19 完全相同。
首轮袖子过长触发既定几何半径检查，缩短后按原门槛通过。r2 窄窗口持方块曾被左侧裁切，
r3 根据可用空间缩放后修正；失败原图和日志均保留，没有用移除检查换 PASS。

工程验证：macOS gmake / Apple clang / x86_64 客户端 Debug、Release 编译链接通过；
手部独立检查使用本机 ARM clang，双配置各 8/8，覆盖几何预算、非退化分面、透视余量、
刚性抓握、关闭静止、减弱幅度、结束动作与非法输入。WorldRuntime 相关源码未改变，复用身份匹配的
r15 测试二进制实际运行 P11A、P11B、ITEM_VISUAL，双配置各 99/99、107/107、13/13；
未重跑或宣称本批完整 WorldRuntime、Windows 验证。验证入口另有 3/3 负例通过：非法构建配置、拒绝覆盖既有证据、非法反馈档位。

最终 r3 的 14 组静态捕获覆盖空手/剑的昼夜、方块/食物/种子、关闭/减弱反馈、窄窗口和三个面板。
另用现有开采诊断驱动生产反馈路径，捕获空手 Full、剑 Full/Reduced、方块 Off 四组连续动作：
每组 40 个不同时间点，最大延迟分别 8/11/39/10 ms。实现者共审阅 12 张最终静态原图、9 张
连续动作关键帧；手与持物连接、三档幅度、夜间明度、窄窗口完整轮廓及界面隐藏通过限定范围复查。
初次 PNG 编码积压只取得每组 6 个不同时间点，判为失败并保留；改用无压缩 BMP 后复查通过，
查看工具不支持 BMP，使用像素逐字节相同的 PNG 审阅。四段视频由现有 AVFoundation 编码器生成，
保留 Homebrew ffmpeg 缺少 libtiff 的失败记录，未改系统库。宽区域颜色阈值曾把少量背景误作袖子，
其 FAIL 保留；固定右下袖口区域后 40 帧轮廓哈希完全相同，作为 Off 纯姿态测试的补充。
这些均为隐藏开发诊断，不能证明正常持续输入或人类舒适度；持续输入仍按用户要求暂缓。

性能：空手/持剑 × 静止/四次传送流送 × 三轮前后对照，共 24 次、101,897 帧，
Medium 阴影、后处理 Off，2× 2560×1440；5 秒预热、30 秒无截图读回测量。
原 runner `55195` 已实际退出 0，逐帧、完整矩阵、包/源码/依赖身份及无睡眠覆盖审计通过；
普通配置按字节恢复，工作包恢复最终候选，临时防睡眠断言释放。P95/P99 中位数比值均满足 1.10 门槛：

| 状态 | 静止 P95 / P99 | 流送 P95 / P99 |
| --- | --- | --- |
| 持剑 | 0.993590 / 1.004345 | 0.993928 / 1.000574 |
| 空手 | 1.007543 / 0.996149 | 0.982887 / 1.015072 |

这些数值只代表本次隐藏诊断；四次传送流送不声明正常持续步行。

当前 Release SHA256：`0aa2b4e52733de1a4f0d0d5260318861fcb3401bf9b8b1473a3135553186b02d`；
326 份源码清单 SHA256：`e826887f473e3d940bebe4ab9ad6e40c29e75ea7653f3aa558cd717684dd6ef5`。
本地产物位于 `build/visual-upgrade-20260917/player-hand-r20/`：`visual-verification.json`、
`r3/build-results.json`、`verify-{Debug,Release}/`、`input-feedback-test-results.json`、
`motion-cadence-audit.json` 和 `motion-bmp-*/review.mp4`。性能入口为
`run_hand_performance_r20_r1.py` 与 `audit_hand_performance_r20_r1.py`，输出 `hand-performance-r20-r1/`。

交付 `delivery-r20/HelloMine3D-Visual-Candidate.app`：111 项分发文件、326 份源码与无窗口启动校验通过，
附前后原图及四段动作视频；普通配置未混入诊断选项，未包含用户存档，旧交付包保持。

本批建立第一人称身体存在感，未实现第三人称全身玩家或独立动物体系。已把角色与动物方向写入
[方案补充](../current/visual-upgrade-plan-2026-09-17.md)。下一批先处理现有生物关节和步态：
旧步态在 135°/315° 行进时因 x+z 抵消而不变；肩髋端点随中心旋转漂移，Spitter 头与口鼻独立旋转。
只读数值核对见 `player-hand-r20/model-followup-audit.json`，尚未把这些后续修复计作完成。

### r21 生物关节连接与斜向步态（2026-09-19）

本批继续角色视觉方向，修复三个可复现问题：旧 `(x + z) * 4.5` 步态沿特定斜向不变化；
四肢绕自身中心摆动导致肩髋端点漂移；Spitter 头与口鼻的独立旋转让接缝拉开。
现在每个渲染实例按实际水平路程累积有界相位，停止和垂直移动不推进，单次超过 2 m 的位移只重设参考点；
相位随实例清理，不进入 Actor 或存档。四肢绕顶部肩髋端点旋转，头口共用颈部变换；
Stalker 臂根内收、腿部略加长以接合躯干，脚底静态高度保持。

运行时代码仅涉及 `EnemyPresentation.h`、`OgreActorRenderer.{h,cpp}`、`OgreBootstrap.cpp`；
不改变碰撞、攻击 tick、伤害、死亡时序或存档，部件预算仍为最多 8。
既有诊断模型展示增加 `walk/cycle`，只构造渲染快照，启动仍要求诊断模式；普通游戏不会创建展示样本。
已有固定 tick 姿态切换节奏保持，这一批不声称完成攻击过渡混合或脚掌贴地。

工程验证：macOS gmake / Apple clang / x86_64 客户端和 WorldRuntimeSmoke 均双配置重新构建；
同源 P11E 双配置各 35/35。新增原生 ARM 关节/步态检查双配置各 14/14，覆盖八方向等路程、
位置平移、停止/恢复、传送、无效坐标、长程有界相位、肩髋端点、头口接缝、旋转顺序和部件预算。
把坐标和步态、中心旋转、独立头口变换三个旧缺陷分别注回测试，均被检出；
Debug/Release 的无诊断 `walk/cycle` 和非法姿态共六项入口负例均在创建窗口前拒绝。
没有把这项定向结果表述为本批完整 WorldRuntime 或 Windows 验证。

画面：基线与候选各捕获站立、蓄力、恢复，候选另有夜间，共七组；实际审阅一张基线蓄力原图、
四张候选静态原图。四类展示模型另捕获斜向步态和姿态循环两段，每段 41 个不同时间点、10 fps，
最大延迟分别 7/9 ms；实际审阅七张关键帧，BMP 与供查看的 PNG 像素逐字节一致。
重型生物从肩部抬臂，追击者臂根与腿部连接躯干，Spitter 头口共同转动，x=-z 路径下腿部姿态发生变化。
原 AVFoundation 编码在沙箱内失败，日志及零字节文件保留；同一编码器经工具审批执行成功，视频使用
`motion-bmp-{walk,cycle}/review-r2.mp4`。这些是开发者诊断，不证明正常战斗、碰撞、脚掌贴地或人类舒适度。

当前 Release SHA256：`56418f7f2e2ef5ea00d314d8786cddbd21cdf6fbee09d781cce1b9995bb92533`；
326 份源码清单 SHA256：`dbb993129e15fa2f39836f712724e152ee76a19fb54651ac83a45f06cd990442`。
本地产物根为 `build/visual-upgrade-20260917/actor-joints-r21/`：`build-r1/build-results.json`、
`verify-{Debug,Release}-r1/`、`negative-tests/results.json`、`entry-guards/summary.json`、
`visual-verification.json`、`motion-cadence-audit.json`。

性能：既有四类模型蓄力展示，Medium 阴影、后处理 Off，2× 2560×1440；
静止/四次传送流送各三轮 AB/BA/AB，共 12 次、43,636 帧，5 秒预热、30 秒无截图读回测量。
原 runner `87240` 已实际退出 0，包/源码/依赖、逐帧时序、完整矩阵和测量期间无睡眠的审计通过；
两个版本资源、五份原生引擎文件与 17 个依赖归档一致。普通配置逐字节恢复，工作包恢复最终候选，
临时防睡眠断言释放。三轮中位数比值均满足 1.10 门槛：

| 场景 | P95 比值 | P99 比值 |
| --- | --- | --- |
| 静止 | 0.883033 | 0.831244 |
| 四次传送流送 | 1.011866 | 1.033812 |

入口 `run_actor_performance_r21_r1.py` 与 `audit_actor_performance_r21_r1.py`，输出
`actor-performance-r21-r1/audit-r1.json`；只代表本次隐藏诊断，不将差值解释为普遍性能提升。
`delivery-r21/HelloMine3D-Visual-Candidate.app` 的 111 项分发文件、326 份源码与无窗口启动校验通过，
附三组前后原图、夜间原图和两段动作视频；普通配置未混入诊断选项，未包含用户存档，旧交付保持。

面部、材质和中远距离类型辨识留待下一批；独立动物体系与第三人称全身玩家未计作完成。
正常持续输入仍按用户要求暂缓，不重复权限排查。

### r22 生物面部与材质分区（2026-09-19）

当前 r21 原图显示模型近看仍以整体平色为主。本批给 Stalker 增加胸前布料、系带、扣件和护腕，
给 Brute 增加分块胸甲、宽下颌和深色手足，给 Spitter 增加背腹与鼻口分区；守护者 crest 增加亮符记和暗边。
由已有 `profile.archetype` 传入 `actorPartData.w`，不靠材质颜色猜类型；图案绑定部件局部坐标，
低反差颗粒按屏幕覆盖渐隐，分区边缘用导数平滑。普通与阴影 fragment 同步；只保留眼睛/符记的有界亮度下限，
其他分区跟随环境曝光与阴影，全部受雾和深度遮挡。不开额外灯光，不新增贴图查询或几何，不改变玩法与存档。

运行时修改 `OgreActorRenderer.cpp`、`OgreBootstrap.cpp` 和两个 Actor fragment shader。
已有诊断展示增加可选 6/12/24 m 距离，只允许合法展示模式；普通入口拒绝。
macOS gmake / Apple clang / x86_64 客户端 Debug、Release 均重新构建。
原生 Apple M1 Pro GPU 程序运行生产 shader，90/90 通过，覆盖相同底色下三原型区别、普通/阴影路径、
局部坐标稳定、关闭回退、夜间朝向、背面无眼色、雾遮挡和非发光胸腹。
同一程序运行冻结 r21 shader 时，在新增原型区分项按预期失败，原日志保留。
复用测试源码未变的已有二进制，对当前资源实际运行双配置 ResourcePack 各 112/112、P11E 各 35/35；
资产 80/80。无展示距离、非法距离和无诊断模式共六项启动负例均在创建窗口前拒绝。
没有将这些定向结果称作本批完整 WorldRuntime 或 Windows 验证。

前后各六组静态捕获：站立、蓄力、夜间、Off 阴影昼夜、增强表现回退；候选另有 12/24 m 昼夜四组，
共 16 组。实际审阅七张候选静态原图、一张身份相同的 r21 基线原图。
近距离姿态循环与 24 m 斜向步态各 41 个不同时间点、10 fps，最大延迟 9/8 ms；实际审阅四张关键帧，
供查看的 PNG 与原 BMP 像素逐字节一致，原图及视频全部保留。
近景分区可读、12 m 仍保留胸腹色块，24 m 主要依靠体型和整体色彩，夜间小细节辨识有限。
连续关键帧中图案跟随部件，未声称完成全面的时间抗锯齿验收。回退画面保持平色轮廓。

本次也发现 r21 的 Spitter 颈部虽在数学上有固定支点，但侧面抬头时实际连接面积太小，前下缘显得悬空。
这是下一批几何修正项，不用材质 PASS 掩盖；正常战斗、脚掌贴地、独立动物体系和全身玩家仍未计作完成。

Release SHA256：`4c545a8a26144c2084515881391b3223bee1e69f0f26c16e8e5ed0ddc55cc8f8`；
326 份源码清单 SHA256：`cee7b5e6746fb9819ac4336c8dbabbf42a85b8789e8793ed6f765e5439218f2c`。
本地产物 `actor-surfaces-r22/` 包含构建、GPU/负例、资源/P11E、入口检查、16 组静态、两组连续帧与视频；
`visual-verification.json` 单独保存两个 shader 的内容哈希，资源路径清单哈希不代替资源内容身份。
性能入口为 `run_actor_surface_performance_r22_r1.py` 和 `audit_actor_surface_performance_r22_r1.py`，
Off/Medium 阴影 × 静止/四次传送流送 × 三轮 AB/BA/AB，共 24 次、109,124 帧，
2× 2560×1440，后处理 Off；每次 5 秒预热、30 秒无截图读回测量。原 runner `77018` 实际退出 0，
逐帧时序、完整矩阵、包/源码/依赖与无睡眠覆盖审计通过，普通配置逐字节恢复，候选包恢复，防睡眠断言释放。
P95/P99 中位数比值均满足 1.10 门槛：

| 阴影 | 静止 P95 / P99 | 流送 P95 / P99 |
| --- | --- | --- |
| Off | 1.006967 / 1.006624 | 1.009307 / 1.032834 |
| Medium | 1.003728 / 0.996331 | 0.966407 / 0.977850 |

这些是限定隐藏诊断结果，不冒充正常移动或普遍性能提升。证据位于 `actor-surface-performance-r22-r1/audit-r1.json`。
`delivery-r22/HelloMine3D-Visual-Candidate.app` 的 111 项分发文件、326 份源码、两个 shader 内容哈希与无窗口启动校验通过；
附五组前后原图、12/24 m 昼夜原图和两段动作视频。保持普通设置，不含用户存档，旧交付与失败证据全部保留。

### r23 四足生物颈部体积连接（2026-09-19）

r22 的斜向连续原图显示 Spitter 抬头时，头身虽共享固定支点，接触部分仍薄得像一道缝。
本批在躯干与头部之间加入固定颈部体积；静止时大部分藏在原轮廓里，抬头时填住连接处。
这是局部几何修正，口鼻前缘仍保留悬挑，不声明整体模型重做或新增动物。

`Neck` 追加到部件角色枚举末尾，既有编号保持；Spitter 由 7 个部件增至 8 个，硬上限仍为 8。
头口共用支点、四肢和攻击角度不变，不改 AABB、伤害时机、自然生成、Actor 或存档。
颈部随既有 visual 生命周期创建与清除。资源和生产 shader 内容与 r22 完全一致。

验证：macOS AppleClang/x86_64 客户端及 P11E 测试目标 Debug/Release 构建通过，
P11E 各 35/35，关节定向各 15/15。新增检查在 Idle/Chase/Windup/Recover 的多个进度下，
检查颈部两端内缩截面分别落在头身内部；不是仅检查一个固定支点。
把颈部高度缩至 0.02 的故障负例按预期只在新检查失败，保留原始日志。
生产 GPU 检查 93/93：新角色编号不误用 crest 自发光，普通/阴影关闭路径相同，关闭增强表现保留平色。
未重新声明全量 WorldRuntime、Windows 或正常战斗通过。

同机位捕获五组静态（静止、蓄力、收势、夜间、关闭阴影），每组 5/10 秒原图；
侧面姿态循环为 4–8 秒、每 100 ms 一帧，共 41 个不同时间点，最大迟到 8 ms。
保留 BMP 原始帧、经像素一致检查的 PNG 和本地视频。
实际审阅四张最终静态原图、三张最终动作关键帧及一张 r22 基线动作原图。
抬头连接缝得到填补，低头与静止轮廓、昼夜曝光保持；上述画面只属开发者诊断。

Release SHA-256：`447e226fcef5f97ecf21cbec9d4d71fd55c5e7d4223a7e4a21977a9f4ab0746e`；
326 份源码 manifest：`9f73f46a6404fb7d8ed2c93ec3710f6f6be7003d2bb413ce20966d39addffef7`。
基线为 r22 已冻结包，源码差异仅生产 profile 和对应 P11E 部件数断言；分发资源、引擎、依赖一致。
证据根 `build/visual-upgrade-20260917/actor-neck-r23/`，包含构建、聚焦检查、GPU、负例、原图、
连续帧及 `visual-verification.json`；视频为 `motion-bmp-cycle/review.mp4`。

性能：首轮 24 次已完成并审计，Off/静止 P95/P99 中位数比值为 1.181232/1.107164，**FAIL 保留**；
其余三组通过。异常组耗时随时间下降，主要差异在渲染阶段，逻辑更新时间接近。
随后只读观察到系统媒体分析与 WindowServer 并行负载，但未确认首次波动的具体原因。
在补采前固定 BA/AB/BA 六次反序复核，包、配置、5 秒预热 + 30 秒测量及 1.10 门槛保持；
复核 P95/P99 为 0.940941/0.899151，PASS。
汇总包含全部同条件轮次，没有剔除首次失败样本；共 30 次、134,881 帧，结果如下。

| 场景 | 次数 | P95 比值 | P99 比值 |
| --- | --- | --- | --- |
| Off / 静止（首轮 + 复核） | 12 | 1.057365 | 0.998994 |
| Off / 流送 | 6 | 0.991689 | 0.970390 |
| Medium / 静止 | 6 | 1.004207 | 0.996244 |
| Medium / 流送 | 6 | 1.008530 | 1.029120 |

首轮 `actor-neck-performance-r23-r1/audit-r1.json` 保持性能 FAIL；反序复核为
`actor-neck-performance-r23-r2/audit-r1.json`，汇总为 `actor-neck-r23/performance-combined.json`。
两轮证据完整性、包/源码/引擎/依赖身份、逐帧时序与无睡眠覆盖审计通过，普通配置逐字节恢复，
工作包恢复候选，临时防睡眠断言已释放。结论仅属本次隐藏诊断，不把差值解释为普遍提速。

`delivery-r23/HelloMine3D-Visual-Candidate.app` 的 111 项分发文件、326 份源码及无窗口启动校验通过，
使用普通中文窗口配置，不含用户存档。原图对照与视频入口为该目录 `COMPARISON.md`。
本轮仍沿用放大的旧展示夹具；下一批按真实 enemy registry 尺寸/攻击时长校正双方夹具后再比较动作，
不把展示尺寸称作正常世界的实际比例。

正常持续输入继续按用户要求暂缓。下一批处理蓄力切到收势时的角度突跳；
姿态混合、脚掌贴地、独立动物体系和第三人称全身玩家不计入本批已完成范围，综合 Goal 保持执行中。

### r24 生物动作过渡与真实展示比例（2026-09-19）

r23 原图中的蓄力和收势会直接跳到下一姿态；原展示夹具也放大了生物，统一用 20 ticks
代替各类型的攻击时长。本批分别修正实际渲染的动作连续性和诊断场景的真实性。
正常世界生物的尺寸、AABB、攻击 tick、伤害、生成及存档语义保持不变。

每个渲染实例保存一份有界姿态历史，以帧时间驱动指数过渡。蓄力、收势、其他状态的时间常数
分别为 45、35、60 ms；只混合关节角度/缩放、身体前倾和垂直偏移，随后重新计算肩髋及头颈支点。
受击翻转、死亡表现和危险眼色仍即时响应。首次出现、死亡边界、超过 2 m 的位移、
非法帧时间或超过 250 ms 的间隔直接重置，暂停零帧时间不推进。缓存随原 visual 生命周期清除。

展示夹具改从实际 enemy registry 读取尺寸和攻击时长，循环按 20 Hz 计算，并在攻击前后各休息一秒。
实测日志中的 Stalker/Brute/Spitter/守护者蓄力与收势时长分别为 6/7、14/12、12/10、6/7 ticks。
为避免把夹具变化混入性能收益，先在 r23 上只修正夹具并冻结对照，再加入动作过渡；两端的
327 份源码仅 4 个运行时文件不同，资源、依赖及引擎一致。没有拿旧放大夹具直接作基线。

验证：macOS AppleClang/x86_64 客户端及 P11E 目标 Debug/Release 构建通过，P11E 各 35/35。
关节、过渡与夹具检查各 28/28，包括不整帧跳变、不越过目标、30/60/120 fps 等时收敛、
暂停/异常间隔/传送重置、即时受击与死亡、混合后肩髋支点和颈部体积连接，以及真实尺寸/时长。
瞬时切换、取消重新固定支点、错误展示尺寸三项故障负例均编译成功并在对应断言失败。
生产 shader 未改，不声称本批重新执行 GPU 93 项；全量 WorldRuntime 和 Windows 未重跑。

八组静态捕获覆盖双方静止/蓄力/收势，以及新版夜间和关闭阴影。首轮 50 fps 连续捕获出现
重复实际时间点，原始失败保留；后续 25 fps 在双方各取得 51 个不同时间点，最大迟到分别
12/9 ms，framebuffer 为 1280×960。首次原生视频编码因受限环境失败并保留，之后获准编码成功。
原始 BMP、经像素一致检查的 PNG 及两段视频均保留，没有重绘或拼接证据。

实际审阅 11 张最终原图与 3 张对照原图。上身从蓄力到收势已有中间姿态，所审画面里的肩颈
连接稳定，静态、夜间和关闭阴影的外观保持。小窗口 HUD 遮住部分脚部，不能据此证明脚掌贴地。
捕获的目标毫秒数也不保证双方处于完全相同的战斗 tick；以上均属开发者诊断，非正常战斗体验。

候选 Release SHA-256：`16788400d3709e1d9d2adac0a1206eda74300e410684d4bae5f2ddc71eb19aa0`；
327 份源码 manifest：`ef12f71b2c6e3e0b7f3e31f17e170437e683a0781096341d8a8643d2288d3434`。
对照 Release：`3dac2ad0e1dafa34bbcdda152aa4c2b3d940aa2575f63aa5f975b5361bc684bd`；
对照源码 manifest：`938ea328644debfbebcdc7bbbe4b858bd4a6da390bde8ab3ed4b41431705df1d`。
证据根 `build/visual-upgrade-20260917/actor-motion-r24/`，含双配置构建、定向检查、三项负例、
`visual-verification.json`、原图及 `before-motion-bmp-cycle-r2/review-r2.mp4`、
`after-motion-bmp-cycle-r2/review-r2.mp4`。

性能对照 24 次完成：真实比例/时长的 cycle 夹具，关闭/中档阴影 × 静止/流送 × 三对；
每组固定 AB/BA/AB、5 秒预热和 30 秒测量，2560×1440 隐藏窗口且无截图读回。
四组 P95/P99 三轮中位数比值均不超过 1.10，结果如下。

| 场景 | 次数 | P95 比值 | P99 比值 |
| --- | --- | --- | --- |
| Off / 静止 | 6 | 0.964105 | 0.967790 |
| Off / 流送 | 6 | 1.005785 | 0.999735 |
| Medium / 静止 | 6 | 0.996892 | 1.003991 |
| Medium / 流送 | 6 | 0.857236 | 0.903409 |

冻结协议为 `actor-motion-performance-freeze-r24-r1.json`，审计为
`actor-motion-performance-r24-r1/audit-r1.json`。原始 runner 退出 0，全部逐帧、包/源码/资源/依赖身份、
时序、无读回及无睡眠覆盖检查通过。工作包恢复候选，普通配置逐字节恢复，临时防睡眠断言释放。
结果限于当前隐藏诊断，不将小幅差值解读为普遍提速。

`delivery-r24/HelloMine3D-Visual-Candidate.app` 的 111 项分发文件、327 份源码及无窗口启动校验通过。
使用普通中文 1280×720 设置，不含用户存档；对照原图和两段动作视频见该目录 `COMPARISON.md`。

七个方向均已有工程实现及分批证据，但完整正常整合体验仍缺持续移动、采集/工具链、
容器转移、岸线编辑/游泳、地标发现生命周期和完整战斗。上述项目依用户要求继续暂缓，
已有历史菜单/设置/重开证据不能代替最终版本的完整旅程。脚掌贴地、独立动物体系和
第三人称全身玩家没有被本批结果计为完成；不为重复检查已通过场景而追加性能矩阵。

### r25：当前候选的整合画面与正常菜单旅程

本轮没有运行时代码改动，全部证据对应 r24 Release `16788400…`、源码清单 `ef12f71b…`。
11 组场景共 22 张原图，实际审阅各组 10 秒原图，覆盖林地昼夜、林缘、草甸、山麓、遗迹、
营地、夜间石碑、沙岸、浅水及水下。初始沙岸机位没有拍到水，单独补拍且未覆盖原件。
首轮包装脚本误读仅性能捕获才有的尺寸字段而失败，已保留；原始两帧正常，修正版完成其余场景。
111 项分发文件、327 份源码及所引用的 14 个环境/界面/资源文件身份核验通过。
本地原图索引为 `integrated-review-r25/GALLERY.md`，审计为该目录 `verification.json`。

随后通过公开 CUA 正常点击、文本与 Escape 完成中文菜单、新建 `NormalR25-20260919`
（seed 20260807）、进入、空手 HUD、暂停、英文设置应用、保存退出、重新启动、进入同一世界、
恢复中文并退出。8 张原生窗口截图与两次测试进程身份已核验，记录在 `normal-r25/verification.json`。
属于实现者有限自测，不是独立黑盒或完整战斗验收；未使用诊断夹具、传送、存档编辑或持续按键。
重开时实际看到首次保存的 6/20 生命与空库存；后续自然交战后的最终保存为 20 生命/出生点，
中间死亡重生未直接观察，不据此声明战斗、任意库存或位置持久化通过。

保留首轮小辅助窗口造成的捕获歧义失败，以及 CUA 在退出后读取状态自动重开菜单造成的首次
进程关闭检查失败。最终在主菜单退出后只用只读原生查询确认该工作包无进程/窗口；普通中文
配置逐字节恢复，所有旧存档和本轮正常测试世界保留。没有重建、重跑性能或冒充完整玩法通过。

正常场景原图 `normal-r25/03-world-empty-hand.png` 新暴露近镜头纯紫色方盒投射物问题，
生产路径确认为 `OgreActorRenderer::updateProjectileVisual` 的拉长立方体；列入后续表现修整。
用户随后反馈阴影持续闪烁、像 BUG，本轮优先诊断并修复阴影稳定性，投射物问题排在其后。
最终完整持续输入旅程仍按用户要求暂缓，Goal 保持执行。

### r26：阴影边缘与时间稳定性

按用户新反馈优先处理阴影闪烁。旧实现使用 512/1024 纹理、无权重 2×2 比较，以及在太阳
接近头顶时会切换参考轴的 Ogre 默认投影。r26 增加归太阳灯所有的稳定相机设置，以世界 Z
为参考并在光源平面对称取整；不修改第三方源码。中/高档改为 1024/2048 纹理、0.004/0.002
bias，九次比较作连续二次权重滤波，最大强度由 0.42 收敛到 0.34；Off 路径保持。

首版只作双线性权重和参考轴修复，边缘柔和但连续帧仍有整格跳动，未作为最终版本验收。
初始正常地形诊断受自然战斗击退干扰，因此另用既有高处障碍物夹具隔离相机；旧画面均保留。
新版在相同地面 ROI 的相邻帧平均 RGB 差值，正午由 1.642 降至 1.031，上午由 0.780 降至 0.373；
超过 10/255 的差值比例分别由 7.27% 降至 3.93%、2.64% 降至 1.33%。这些是运动太阳阴影的
有限场景指标，最大像素变化仍存在，不能推论所有场景零闪烁。没有以降低帧率或关闭阴影掩盖问题。

macOS AppleClang/x86_64 客户端、WorldRuntimeSmoke、ResourcePackSmoke 双配置构建通过；
V10D 各 35/35、资源各 114/114（新增两个旧滤波覆盖拒绝用例）。相机数学各 11/11，恢复旧
参考轴的故障注入被检出；滤波 GPU 40/40、完整生物 shader 93/93、地形/反馈 shader 96/96，
旧无权重滤波在连续性检查失败。资源分发检查通过。

最终候选 Release `fb0f62fda6313dcb0a8a416221aa3bc1b7de34b4f916a6c7c5dc2cd169affce9`。
`shadow-stability-r26/visual-verification.json` 保存 3 段旧版与 7 段最终版的 10 fps 连续捕获，
每段 41 个不同实际时间点；实际审阅 11 张最终原图以及旧版与首版对照。覆盖正午、上午、
移动、中高档、夜间、关闭和林地；小窗口原图 1280×960。10 fps 不能排除未采样的高频闪动。

保留初次旧资源接口要求 `litSamples / 4.0` 导致启动失败；接口改为加权滤波要求并补了旧覆盖
拒绝测试，没有删除门禁。另保留构建包装脚本误预期 V10D 为 21 项（实际 35 项通过）、诊断
包装脚本写错配置键的失败，以及受限只读预检不能调用 ps 的记录；获准的正常检查路径继续运行。

24 次性能和逐帧核验通过：Medium/High × 静止/流送 × 三对，AB/BA/AB，5 秒预热、30 秒采样、
2560×1440 隐藏窗口且无截图读回。原始 runner session 80430 退出 0，候选和普通配置恢复，
临时防睡眠断言释放。协议 `shadow-performance-freeze-r26-r1.json`，最终审计
`shadow-performance-r26-r1/audit-r2.json`。下表为三轮中位数比值，均不超过 1.10。

| 场景 | P95 比值 | P99 比值 |
| --- | --- | --- |
| Medium / 静止 | 0.950307 | 0.997502 |
| Medium / 流送 | 0.976836 | 0.980433 |
| High / 静止 | 0.984873 | 0.983020 |
| High / 流送 | 1.010196 | 0.941047 |

个别单轮波动完整保留，未筛掉慢样本；不把小幅差值解读为普遍提速。原审计的校验逻辑与数据
通过，但返回摘要误保留旧候选 327 文件及 r24 说明；原件保留，r2 仅按已验证 manifest/协议
校正为 327/328 和当前范围，未修改任何运行、指标、护栏或冻结脚本。

`delivery-r26/HelloMine3D-Visual-Candidate.app` 通过 111 项分发文件、328 份源码及无窗口启动
校验，使用普通中文 1280×720 配置，不含用户存档；前后原图见该目录 `COMPARISON.md`。
源码清单 SHA-256 为 `20e1368be47e1359b78e601ec8c269f3ea4d19f4f40a41425b9a4d779d0719b3`。

性能结束后 CUA 正常进入主菜单、世界列表、世界与暂停，原生窗口确认当前 Release 身份。
读取该进程只见 `HELLOMINE3D_ROOT`，没有诊断参数。但 CUA 在进入后报告用户改变应用，随后
设置动作报告用户仍在操作；已让出输入，不重试、不强制退出、不覆盖用户设置。
`normal-r26/verification.json` 为 PARTIAL，阴影切档、缩放、正常退出及位置持久化不计通过。
该窗口当时归用户，后续自动诊断须先核对占用；不能把性能结束时的“配置恢复/无诊断进程”误写成
用户后来打开的客户端已关闭。投射物外观排在本批之后，整体 Goal 尚未完成。

#### r26 正常设置与重开补验

随后只读原生截图确认工作窗口已回到主菜单，CUA 新状态读取和后续操作正常，没有再次报告
用户占用。使用同一个 r26 Release（`fb0f62fd…`），从正常界面建立独立测试世界
`ShadowR26-20260919`（seed 20260807），执行中→高→关闭→中并逐次重新打开设置核对。
高档下保存退出后以 960×600 点窗口重开，实际再次进入原测试世界，场景、HUD 和设置正常。
随后在同一进程返回菜单，新建并进入 `新世界ShadowR26B-20260919`（seed 6123472），
确认不同地形、小地图和场景可正常显示；没有进入原有四个存档。

窗口尺寸设置按产品提示在重启后生效，没有把它写成即时拖动缩放通过。恢复原来的中档和
1280×720 设置后，通过游戏菜单保存退出、重开检查原窗口尺寸，再从主菜单正常退出。
三次退出均用精确包路径的原生查询确认无 PID/窗口；没有强制终止。用户先前调整的 FOV 120
及其他设置保留，最终配置与本次操作前逐字节相同；原有四个存档的 `world.meta` 哈希不变，
两个新增测试存档保留。首个截图调用漏 `.png` 后缀，以及误读 `Ogre.log` 的辅助命令失败
均不计产品失败；使用有效新路径和实际 `MineOgre.log` 后继续，没有覆盖历史证据。

`normal-r26-followup/verification.json` 为 `PASS_SCOPED_NORMAL_SHADOW_LIFECYCLE`，
保存六张窗口原图、包身份、设置和存档核对、退出查询。CUA 实际观察了各步画面，另检查了
小窗口高档设置与第二世界原尺寸截图。这是实现者正常输入自测，不是独立黑盒验收；场景为
自然起始时间，不用于阴影滤波的新旧定量比较，也不声称正常持续移动或全部场景零闪烁。
源码、资源和二进制没有变化，复用上述身份匹配的工程、GPU、连续帧和 24 次性能证据。


### r27：投射物模型与朝向

近镜头紫色长方盒改为封闭分面滴形，修正 X 向飞行时模型朝向反转，增加明暗前后端并保留
昼夜、雾、平色回退和单个不透明 draw section。World 行为和 20 Hz 步进未改变。
独立结果、包身份与真实缺项见[投射物修复记录](projectile-presentation-r27-2026-09-19.md)。
用户已明确本批完成后立即实施地形多样化，不插入其他视觉小批次；v12 八 seed 生产基线已采集，
后续通过新增生成版本接入新世界，保持旧存档。

### r28：新世界地貌多样化

投射物 `2b4cf15` 提交后立即进入本批。terrain v13 增加弯曲沙脊、岩台/切谷、浅水湿地，
并让林地盆地和草甸起伏参与连续高度组合，最后决定表面与生态。新增湿地/岩台区域名称，
接入真实生成、植物、小地图及自然敌人映射；旧世界继续使用原生成版本，保存格式仍为 v12。

双配置完整 WorldRuntime 各 1253/1253、E7 各 38/38、18 片实际密集区域通过；v1–v12
生产调查与修改前逐字节相同。最终 207 帧诊断中审阅 50 张，包含三类地貌各三个 seed、
Off/High 和连续相机；保留遮挡视角及早期坡差/湿地无水/岩台铺沙过宽的失败。
三轮 headless 生成成本比值 1.0482；36 次帧时间对照共 156413 帧全部通过 1.10 门槛，
最大 P95 比值 1.0160。正常菜单创建 seed 42 v13 世界、保存返回、同世界重开与保存退出
完成限定自测；原七个世界和配置保持。`delivery-r28` 的 111 项文件、330 份源码及无窗口
启动校验通过，三组真实沙丘/岩台/湿地前后原图已交付。
详细结果和真实限度见[地貌批次记录](terrain-diversity-v13-2026-09-19.md)。

### 当前恢复入口

- 水面工程批次已本地提交 `864c86e`；前六个本地提交保留，均未推送。每个可独立验证的小批次及时提交，整合性能/玩法待办继续单独跟踪。
- 原工作路径 `HelloMine3D-Visual-Upgrade.app`、bundle id `local.hellomine3d.visual-upgrade` 保留 r3。此前自动审批拒绝终止其进程，理由为可能丢失未保存状态；本轮未停止、刷新或覆盖该运行包。
- 专用工作路径 `round2-compact-layout-recheck-r3/Diagnostic.app`，bundle id `local.hellomine3d.visual-layout-check`，当前为 r28 地貌 R4，Release `1f9e8b3c…` / 330 份源码。36 次后台帧时间及原始数据核验通过，正常新建/保存重开后已退出，无剩余 runner。工作包原 FOV 120 / 中文 / Medium 设置保持，原七个世界保留，新增一个 v13 自测世界。最新交付为 `delivery-r28/`，实际提交和证据状态见本地 `goal-recovery-r7.json`、地貌批次记录及 `normal-r28/verification.json`。自动诊断继续隐藏/noActivate，持续输入暂缓；后续地形美术深化另分批。
- r14 的 24 次性能对照与审计已通过。r15 初版连续画面失败保留，修正版工程、限定范围画面、36 次性能及审计通过，已提交 `b1a4a1c`，候选为 `delivery-r15/`。r16 回退修复构建、画面、12 次性能复验和候选交付已完成。r17 字幕修复构建、限定正常/诊断检查、12 次合成面板性能及候选交付通过，正常切世界地表图刷新已补。剩余持续输入、地标/改块/容器/完整战斗等保持待验；不重复已通过的性能矩阵，也不反复尝试无变化的持续输入接口；完整证据见本地 `goal-recovery-r7.json`。
- 正常菜单、设置、保存/重开已有 r4/r16 历史证据及 r25 当前 r24 包有限自测，持续移动/采集等仍待验。前述窗口占用已由用户释放指令解除；2026-09-19 用户暂缓持续输入相关验收，继续独立视觉迭代，完整 Goal 仍未完成，不再重复权限/输入排查。首次 Reduced 静止现象缺原始计时、根因尚未证实；完整 Goal 范围和恢复步骤全部保留。
- 用户要求沿用中文提交形式，最近 12 条英文提交已仅重写说明和相应父提交 ID；各提交文件树、顺序、作者与日期保持一致。旧证据 SHA 保留，可通过 `refs/rewrites/visual-message-style-20260917` 追溯；新旧映射在本地 `commit-message-style-rewrite-r1.json`。


## 实际改动

| 范围 | 已落地的行为 |
| --- | --- |
| 字体 | 由原 Noto 可变字体生成 OFL 静态 500 字重 HelloMine UI，保留全部 cmap 与原字体/许可证。实际客户端改用该文件，正文基准从 18 调至 20，告别默认 100 极细字重。 |
| 界面 | 深炭蓝面板、暖白文字、沙金选择色、青绿生命与珊瑚红危险色；任务标题放大并突出，数量与进度并排。 |
| 快捷栏 | 独立深色五槽、数字角标、细亮边与选中底标；保留图标、堆叠数量及耐久。生命改为十段显示并保留精确数值；手持名称有独立底色与固定行高。 |
| 操作提示/制作 | 操作键帽与动作文字分离；制作与箱子使用真实物品图标、名称、数量及选中态，完整名称可通过悬停阅读；仍使用 Button，保留键盘焦点和激活。移除制作预览中的内部 recipe id。 |
| 小地图 | 65×65、两米采样的真实驻留地表，显示树冠、水岸、沙土石和建筑；跳过细草花，邻列高度产生坡面明暗。常态不画区块网格，保留北向、朝向和 32 m 比例尺。原地改块也刷新，未知区暗色。 |
| 世界表现 | 草地细节反差和黄绿色调收敛，连续世界坐标产生较大色块层次。标准图集、纹理数组、阴影接收路径共用相同着色逻辑；关闭大气表现时回退，石材不受该调整影响。没有改动方块轮廓、植被根部、风摆或存档。 |
| 交付工具 | macOS 打包支持显式独立 binary；新增源码文件哈希收据，避免 git diff 不包含新文件造成身份遗漏。 |

## 可复核证据

所有相对产物路径以下列本地目录为根：
`/Users/lizi/Desktop/Workspace/HelloMine3D/build/visual-upgrade-20260917/`。
截图和字体样张属于本地附件，不提交 Git。

| 检查 | 结果 | 证据及范围 |
| --- | --- | --- |
| 隔离源码 | PASS | `checkout/` 基于 dcfd4d0，只纳入本轮视觉改动，地形维持 v10；未纳入并行 E5 的未完成生成器。 |
| 客户端 gmake / Apple clang / x86_64 | Debug、Release 均编译链接 PASS | `isolated-client-{debug,release}-final.log`。第三方静态库复用既有对应配置，第一方对象与可执行文件独立生成。 |
| 完整 WorldRuntime | Debug **1109/1109**、Release **1109/1109 PASS** | `world-full-debug.log`、`world-full-release.log`，包含现有输入、设置、存档、世界/地形等回归。不是正常输入玩法录像。 |
| 小地图聚焦 | **10/10 PASS** | `map-release-r2.log`；未知区不加载、负坐标、花草过滤、透明树冠、建筑覆盖、原地编辑、水、快照稳定、不写 dirty、256 列上限及卸载。完整两配置均再次覆盖。 |
| 字体构建 | PASS | 两次 `python3 tools/build_ui_font.py` 生成字节相同，SHA-256 `63ddd636cd3dc7c70b2a6ca1184d68544ff55e940424157237fe116efc69379a`；保留原始 cmap、静态字重 500、无 fvar。 |
| 资源包 | **105/105 PASS** | `resources-release.log`，资源数据检查。图集/纹理数组检查也通过，原图集和数组像素未修改。 |
| 分发资源 | **80 项 PASS** | `assets-final.log`。 |
| 花草 GPU 回归 | **10/10 PASS** | `flora-gpu-r2.log`，Apple M1 Pro 离屏 OpenGL，961 时间样本，根部固定、连续性、阴影运动一致。 |
| 地形/反馈 GPU | **20/20 PASS** | `ground-gpu.log`；实际生产 GLSL 与图集，验证草地改色、世界坐标变化、阴影关闭一致、石材不受影响及原有十阶段裂纹/遮罩/遮挡。 |
| 干净工作包 | **111 文件逐项哈希 PASS** | `package-final.log`；包内 `distribution-sha256.txt`、`build-identity.json`、314 个第一方源文件的 `source-tree-sha256.txt`。 |
| 升级后真实窗口/双语多字号/操作 | **PARTIAL；完整验收待续** | 排除锁屏后发现 manifest 排序导致启动失败，修复后 CUA 已观察主菜单、建档、世界 HUD/真实地图、制作、暂停。多字号、操作全场景与包窗口身份存证未完成；专用截图查询仍无匹配窗口。 |
| 同条件三轮窗口性能 | **NOT_RUN / 基线启动未完成** | `baseline-perf-off-r1/`、`baseline-perf-off-r1-r2/`、`baseline-hidden-r1/` 保留失败。没有新的 P95/P99 PASS，也不覆盖 E2 的 1.134× 失败。 |

汇总哈希见 `evidence-summary.json`。`font-specimen.png` 仅为同文本的真实字体字重样张；
`ground-gpu/*-ground-{before,after}.png` 仅为实际 shader 的离屏材质检查，不是整机位游戏画面。

## 可运行包身份

视觉批次自动证据对应的 Release 可执行文件 SHA-256 为
`789c0d52a8b03c614b2dd9b1866b9ee1f156929a68f999b3e2c94584dbde67de`，源码收据 SHA-256 为
`bdb7dad55575a6a1ef36d06dc09afa51908ee51c9cbeec6e3c7227426a552f90`。冻结副本为
`candidate-package-789c0d52/`，原版本副本为 `baseline-package/`；两者都没有被后续刷新覆盖。

复用客户端 `build/visual-upgrade-20260917/HelloMine3D-Visual-Upgrade.app` 随后在同一路径原位
合入 E5 提交 `db22168`，当时可执行文件 SHA-256 为
`d78fbb8bf417aa27ad81d9df7f9d61d3e3301d4ee354746cff4e7e67e9a816e5`。该身份只说明当前可运行
整合包，不追溯替换上面的视觉批次冻结证据。
该整合包的 111 个分发文件另经逐项哈希核验并冻结在 `integrated-package-d78fbb8b/`，
以便对比完成后恢复当前版本。视觉源码已由并行整合工作纳入本地提交 `fcb7e2d`；
此提交不代表窗口验收完成。

后续启动诊断暂将同一工作 `.app` 刷新为隔离 terrain v10 的 `789c0d52…` 二进制，
并纳入已排序的字体清单；见 `package-manifest-fix.log`。当前不得把此工作包称作最新 E5/E6
整合版本，整合冻结副本保留。最终仍须生成与当前源码对应的整合客户端。

隔离证据包由隔离检出的代码和资源生成，未覆盖其他任务的工作客户端或主工作区 bin。
这两个目录是供复核/恢复的包内容副本，不额外启动新应用。后续继续复用唯一视觉工作 `.app`。

## 失败记录与处理

- 初次沙箱窗口启动返回 LaunchServices -10827。通过正常的自动权限审核后可启动，但电脑随后锁屏，真实窗口采集超时，没有取得帧。
- CUA 确认 Mac 已锁定，自动解锁也不可用；未绕过系统锁屏，也未要求离开的用户在线处理。
- 隐藏客户端同样未取得帧；确认独立诊断包路径后结束超时进程，保留捕获记录。后续不反复重试同一无变化的锁屏条件。
- 离屏 GPU 首次在沙箱内无法取得 core pixel format；经正常自动审核使用图形上下文后，花草及地形/反馈检查均通过。这是图形 API 自动检查，不是绕过锁屏或模拟输入。
- 首版地图测试夹具直接将 Absent 切到 Resident，产生 7 项失败；按既有 Requested→Loading→Resident 生命周期建立夹具后 10 项通过，没有修改生产生命周期或放宽要求。
- 物品卡初次编译将 Material::ID 写成 int，类型检查报错；恢复强类型签名后两配置通过。
- 桌面恢复后直接执行工作包启动入口，日志 `launch-diagnosis-r1.log` 明确报 manifest 第 52 行不满足唯一且有序要求：新增 HelloMine UI 字体被排在 Noto 字体之后。修正实际清单，并给 Python 打包器增加有序、唯一及路径校验；生产 89 条清单通过，倒序和重复负例均被拒绝。此前资源测试和包哈希 PASS 没有证明可启动，保留原记录。
- 修复后 `launch-manifest-fixed.log` 进入 Ogre/GL；CUA 已取得正常世界 HUD，地图可辨树冠、空地、沙岸和水面，并发现任务进度数字换行问题。`round2-paused-before.json` 专用窗口采集仍未匹配，故 CUA 会话观察不冒充已完成的本地包身份截图。

## 恢复操作

1. 先核对本报告、主工作区与隔离检出；不要把 E5 生成器修改或其他任务的包混进此版本身份。
2. 图形会话仍锁定时，保留待验项，不重复启动窗口。会话可用后，通过 CUA 正常进入视觉工作包；正常菜单、建档、移动/观察、制作、保存重开与诊断分开记录。
3. 冻结新包副本已存在。先确认没有其他任务使用工作窗口，并核对当前整合包是否又有更新；如有更新先冻结其分发文件。性能对比将 `baseline-package/` 和 `candidate-package-789c0d52/` 的已验证分发文件分别恢复到同一工作 `.app`，采集完恢复当前整合包（本次冻结为 `integrated-package-d78fbb8b/`）；保留当前用户数据及全部失败目录，不把 v10 对比版本留作最新整合版本。
4. 使用 `tools/capture_visual_macos.py --reuse-app --save-template build/visual-upgrade-20260917/v10-template`；固定 seed 20260807、terrain v10、1280×720、时间 6000、HUD fixture。性能点 `(1152,150,64)`、旋转 `(0,0,0)`，Off/Off、Medium/Off、High/On 各三轮静态，加 High/On 三轮快速移动。每轮新目录，首次失败不覆盖；已有 `run_baseline.py` 是原计划参考，恢复时需给失败轮次新输出名。
5. 新旧版本还需相同林地/海岸/草甸/山麓画面对照；双语三字号、夜间/关闭回退、空满快捷栏、制作/箱子检查。发现截断、位置错误或地图性能退化继续修正。
6. 同夹具 P95/P99 三轮中位数均 ≤1.10 才关闭本轮性能；若未通过，报告真实退化并继续定位。当前不声明整体视觉升级完成，视觉源码已本地提交；没有执行推送或发布。

## 恢复区

- 起始 HEAD：7b2432d（E4 窗口结果）。后续已有 E5 基线 dcfd4d0、E5 生成器 db22168、视觉实现 fcb7e2d；隔离 v10 证据保持原身份，本任务不覆盖其他改动。
- 体验前客户端：build/ecology-e2-20260914/HelloMine3D-E2-Prototype.app，实际菜单显示新世界 terrain v10。当前视觉工作包改用上方独立固定路径，避免与 E5 争用。
- 正常输入已执行：主菜单→世界列表→新建 Visual Review 0917（seed 20260807）→进入→打开制作。未动用户已有世界。实际发现正文细弱、目标层级弱、制作以文字按钮为主、地图没有树冠。
- 字体根因：media/fonts/NotoSansSC-VF.ttf 的 wght 轴 min/default/max 为 100/100/900，现有 stb 字体入口未选字重。
- 下一步：修复任务进度行和状态区布局，核对运行进程与窗口身份，完成后续实机/性能；用户已要求按第二轮分析继续，并要求整理可直接使用的 [Goal 提示词](../current/visual-upgrade-goal-prompt-2026-09-17.md)。
- 自动续跑：已创建本任务 heartbeat「继续 HelloMine3D 视觉升级验收」（automation id `hellomine3d`），每 30 分钟检查并继续。锁屏无变化时保持安静，不反复启动；全部验收完成或用户取消后暂停。恢复时先核对自动任务状态，避免重复创建。
- 窗口由本任务操作。截图与录像保留本地 build/visual-upgrade-20260917，不提交 Git。
- 直接启动诊断 session 72178 / PID 77661 已通过独占本轮日志的文件句柄确认并结束；后续先核对是否还有其他任务的实例，不复用旧 PID，也不同时启动重复客户端。
- 权限状态：初期 CUA 正常，随后电脑锁屏；09:17 左右再次分析时应用清单已可读，锁屏条件有变化，但读取客户端仍超时。正常 open 返回 0，精确窗口检查仍无 PID/窗口（`review-round2-menu-r1.json`）；原因尚未定位，不能据此认定游戏已启动。没有待用户在线批准的问题，也没有自动审批拒绝后绕过限制。
- 用户追加要求分析后续提升方向，见[下一轮分析](visual-next-pass-analysis-2026-09-17.md)。此记录区分当前缺陷、E6 在建和候选项，没有重复改写并行 E6。

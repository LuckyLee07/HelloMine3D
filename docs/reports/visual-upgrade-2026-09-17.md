# 视觉升级执行记录 2026-09-17

状态：**综合视觉 Goal 继续执行；r11 完整后台性能 72/72 次、12/12 组及掉落物专项 24/24 次、4/4 组均通过独立审计。当前 r12 合成布局修复已提交，双配置构建及十组布局原图检查通过；干净候选客户端、说明和四组原图对照已交付，连续行进及完整正常输入仍待最终验收**。历史性能证据保留原版本与分辨率。
用户已授权体验、制定方案并直接实施，随后要求离开电脑期间自主推进，不要求其在线申请权限；见[升级方案](../current/visual-upgrade-plan-2026-09-17.md)。

## 综合 Goal 本轮进展（后续记录优先于下方历史恢复说明）

无预算限制的七阶段 Goal 持续执行，范围见[完整目标](../current/visual-upgrade-goal-prompt-2026-09-17.md)。
第一阶段不是完成条件。已核对 E6 terrain v12 提交及同窗口批量验证；原 E6 的 ARM64 包冻结在
`integrated-e6-arm64-31345fc6/`。本轮使用 `round2-checkout/`（起点 dc5c4bb，另包含后续 OgreBootstrap
诊断修复与字体 manifest 排序）独立构建 x86_64 Debug/Release；从未改写主工作区第三方静态库。
后续 E2 文档/证据提交保留（9e7f64d）。本轮已分为三个本地提交：打包修复 `bc18429`、世界/水体表现 `75aac13`、物品/UI/导航/敌人表现 `a3f25bb`；均未推送。

### 实现及实际检查

- **阶段 1 / HUD 与窗口**：进度计数与细条同行，三列状态区域固定占位；图标与立体手持物改用 nearest
  sampler，字体仍使用 linear。长按键提示换行。箱子、机器和背包卡片按实际内容宽度排布；制作采用紧凑
  方形网格与独立滚动区，结果/提交按钮固定可见。设置长标签与长选项分行。窗口化模式正确应用非显示器
  全屏模式列表中的尺寸，实际 900×600 原图已取得。
- **阶段 2 / E6 与材质**：整合 terrain v12。草顶/草边/高草/叶/花茎共享世界坐标低频色场，收敛细纹理反差，
  花、土、木、石协调饱和度；不改纹理文件或 Alpha。atlas/array、普通/阴影 shader 同步。
- **阶段 3 / 水深与岸线**：有界 8 格驻留水柱快照，角点共享真实深度/实体岸线；水床编辑使受影响上方水段
  失效重建，深度控制吸收与透明度，岸边克制涟漪、连续时间与关闭回退。复用水面 repeat UV，stride 不变。
  [数据合同](../contracts/visual-water-depth-v1.md)。r10 已补入贴水陆地最多 18% 的明暗过渡，并检查昼夜、低档和兼容路径；
  水面细纹按岸线与风向漂移，不表示水文流向。正常改岸、进出水面仍待验。
- **阶段 4 / 工具与掉落物**：方块使用真实三分面，工具按图集 Alpha 挤出轮廓厚度；共享有界缓存。
  第一人称包含移动/待机/开采/接触/回收姿态，复用命中事实与反馈强度。掉落物复制真实材质/数量/年龄，
  渲染实体几何并轻微转动/起伏；库存、碰撞与保存语义不变。[表现合同](../contracts/visual-item-volume-v1.md)。
- **阶段 5 / 天空与远景**：两层云密度由取最大值改为混合，减少叠满天空的云片，收窄边缘过渡并提亮云底。
  日间雾密度由 0.0015 调为 0.0019，夜间仍 0.006。GPU 昼夜运动/连续性/平移视差、原 legacy 回退全部通过。
  昼/夜/暮色、High+post 与关闭回退的 5/10 秒实际客户端诊断图已取得；随后已逐图复核近地树根、墙角、台阶与林下昼夜。
- **阶段 6 / 导航与辨识**：设置增加 64/128/256 m 三档；仍 65×65 采样、30 Hz 每次 195 列，切档丢弃不匹配缓存。
  显示六类当前区域名称。只记录实际使用过的容器、工作台和激活路标，单次世界访问记忆最多 32 个，圆图最多 8 个，
  避开玩家箭头、圆边及相邻图标；换世界清空，不查询未知结构，不显示敌人。设置 v1–v9 迁移至 v10 默认 128 m。
  地标记忆当前仅本次探索，设置说明中明确；不是跨存档探索数据库。敌人新增面向眼眉、躯干/四肢明暗及蓄力眼色，
  复用已有剪影和真实状态 tick。路标仅青色内芯保持夜间色彩，石框仍接受阴影/AO。
- **阶段 7 / 最终整合**：r11 森林/水岸完整对照 72/72 次、12/12 组及掉落物专项 24/24 次、4/4 组均通过独立审计，实际 1280×720，关闭截图读回。r9 受系统睡眠中断的失败保留。交付画面检查发现合成网格遮挡，r12 已修复并完成双配置构建与十组布局检查。干净候选包与四组对照已生成，完整正常输入玩法尚未验收。

### 已完成的本轮自动证据

产物均在 `build/visual-upgrade-20260917/`；每个目录/日志保持原始身份。

| 检查 | 已确认结果 | 路径 |
| --- | --- | --- |
| 前四阶段完整 WorldRuntime | Debug/Release 各 1170/1170 PASS | `world-full-round2-{release-r2,debug-r1}.log` |
| 前四阶段资源 | 两配置各 108/108 PASS | `resources-round2-{release,debug}.log` |
| 水深/物品几何聚焦 | 水深 13/13、物品 8/8 PASS；完整回归再覆盖 | `round2-checkout` 测试及上述全量日志 |
| MeshDirty | PASS | `meshdirty-round2-release.log` |
| 水面生产 GPU | PASS：210 对边界零偏差、深度/视距独立、岸缘/水下/关闭检查 | `water-depth-gpu-r1.log` |
| 地形/材质/反馈 GPU | 96/96 PASS（含新路标内芯、两套纹理及两条 pass） | `waystone-palette-gpu-r1.log` |
| 天空生产 GPU | 10/10 PASS | `sky-gpu-r1.log` |
| 敌人生产 GPU | 18/18 PASS（含夜间眼部亮度） | `actor-gpu-r3.log` |
| 导航/设置/本地化聚焦 | 65/65 PASS | `navigation-release-r1.log` |
| 环境聚焦 | 24/24 PASS，两配置（水下介质新增 3 项） | `round2-underwater-focus-{release,debug}.log` |
| 阶段 6 完整 WorldRuntime | Debug/Release 各 1182/1182 PASS | `world-full-round2-stage6-{release,debug}-r1.log` |
| 天空/UI 诊断矩阵 | 13/13 启动和采集成功，人工逐图检查后继续修正制作布局 | `round2-sky-ui-matrix-r2/` |
| 地图/UI 诊断矩阵 | 9/9 采集成功；三档地图、双语制作/设置、900×600 原图 | `round2-navigation-matrix-r1/` |

诊断图统一 `DEVELOPER_DIAGNOSTIC / normal_input=false`，静态姿态不证明真实连续战斗。
初次 UI 矩阵的 settings 参数未匹配现有 `HELLOMINE3D_V10E_SETTINGS_FIXTURE`；compact 请求当时实际仍为
1280×720。已修正入口及生产窗口尺寸，并让采集工具验证 PNG IHDR 尺寸。两类旧图只保留为未覆盖证据。
新窗口尺寸代码初次插在窗口创建前导致空指针启动失败；`round2-sky-ui-matrix-r1/` 与重试目录保留，
实际改到创建成功后，两配置重建和随后 13 组启动已通过。
敌人整合首图因 terrain loop 错设 actor shader 参数被 Ogre 明确拒绝，`round2-actor-matrix-r1/` 保留；
已修正为仅在 actor loop 设置参数，随后 8 项敌人/UI 整机采集成功；夜间眼部增加有界亮度下限后又完成近地场景复核。

### 近地与动态补充（r2 历史记录）

- `round2-near-scenes-r1/` 的 13 组与 `round2-near-scenes-r2/` 的 6 组诊断均成功采集。已逐图检查林下昼夜、High 阴影树根、台阶、草甸、浅滩、路标内芯、遗迹和敌人眼色。营地前两次机位因自然下落位于土台下；`round2-camp-north-r1/` 改用北侧实际坡地后已看到木围栏、入口和内部顶棚的完整轮廓。
- 首次“水下”坐标在沙层内，保留为不合适机位；第二次真实水下暴露远处水底像空气一样清晰。补入相机介质雾后，`round2-underwater-fixed-r1/` 显示水下视距和颜色衰减。只读取最多两个驻留块，新增 3 项环境测试，两配置 24/24 PASS，Debug/Release 客户端重建 PASS。
- 15 项 macOS 启动负例全部通过：`round2-startup-negatives-r1/`；最后敌人 shader 改动后资源再次两配置 110/110 PASS。
- `round2-hand-motion-{full,off}-r1/` 各 60 原始帧已取得，但旧开采夹具使用空库存，不证明手持工具动作；补入既有 HUD 库存夹具后，`round2-hand-motion-{full,off}-r2/` 各 60 帧和 6 秒视频采集成功，`round2-hand-motion-reduced-r1/` 追加 60 帧 Reduced 档，确认 Full 有挥动姿态变化、Off 保持静态。此处使用石剑进行开采诊断；不是正常工具制作/装备验收。系统编码器普通沙箱报告不可用，按授权图形执行环境编码成功，失败文件保留。
- 最新冻结候选 `round2-final-candidate-r2/`：Release x86_64 SHA-256 `5b39ddd5844969c8f3abba6c35bda7ab396d0456ed89b548c5f8b1267c1a6204`（r1 原件保留），321 个第一方源码文件。该目录不是另一个运行 .app；正式性能始终复用同一工作客户端。

性能 r1 已完成森林 Off 常驻三轮，P95/P99 中位数为 `0.985×/0.994×`；随后在代码复查发现容器/工作台记忆钩子位于被面板跳过的地图绘制函数中。已将钩子移到 HUD 提前返回前。旧 r1 在已验证归属的流送候选进程处主动终止，采集记录明确 `SIGTERM / FAIL`，runner 已恢复候选；旧静态 PASS 不作为新候选最终结论。修复后以同一基线/条件重新执行 r2。

### 本轮正式性能与小窗口补验

`run_round2_performance_r2.py` 完成 72/72 次：森林/水岸 × Off/Medium/High（High+post）× 常驻/快速流送 × 三轮 × 两版本，A/B、B/A、A/B 顺序，每次预热 5 秒、采集 30 秒。基线为同架构 v12 `round2-stage1-reference/`（Release SHA-256 `d3975ba24edcf19ad23f4f239de4149e43641b4d4bfdf9b5aca85f4ad0b2e142`），候选为 r2 `5b39ddd5844969c8f3abba6c35bda7ab396d0456ed89b548c5f8b1267c1a6204`。基线已包含第二轮阶段 1，不能解释为相对最初用户截图的总增量，也不替代历史 E2 结果。

| 场景/画质/运动 | P95 三轮中位数比值 | P99 三轮中位数比值 | 结果 |
| --- | --- | --- | --- |
| forest-off-steady | 0.9837× | 0.9934× | PASS |
| forest-off-streaming | 1.0005× | 0.9696× | PASS |
| forest-medium-steady | 1.0149× | 1.0043× | PASS |
| forest-medium-streaming | 1.0216× | 1.0093× | PASS |
| forest-high-steady | 1.0272× | 1.0269× | PASS |
| forest-high-streaming | 1.0129× | 0.9877× | PASS |
| shore-off-steady | 1.0209× | 0.9966× | PASS |
| shore-off-streaming | 1.0139× | 1.0096× | PASS |
| shore-medium-steady | 0.9965× | 1.0362× | PASS |
| shore-medium-streaming | 0.9820× | 1.0169× | PASS |
| shore-high-steady | 1.0173× | 1.0309× | PASS |
| shore-high-streaming | 1.0045× | 1.0005× | PASS |

`round2-performance-r2/audit.json` 从原始 CSV 重算分位数，复核全部采集包、源码、机位与 1280×720 原图，PASS；12 组最大 P95/P99 为 1.0273×/1.0363×（上取四位），门槛保持 1.10。最初审计对最终 tick 要求完全相等，在一组发现 6699/6700 差别，失败日志 `round2-performance-audit-r2.log` 保留。全部从 tick 6000 启动、CSV 从 6100 开始，30 秒墙钟结束落在 20 Hz tick 边界；审计显式接受该单 tick 差异后 `round2-performance-audit-r3.log` 通过，未删轮次或改变性能门槛。敌人继续运行，全部自然受伤和状态差异保留。上述为诊断运动，不是正常输入玩法。

- `round2-final-ui-matrix-r1/` 完成中英文、0.85/1.0/1.25、HUD/制作/容器/设置的 24 项 900×600 采集，已逐图检查。发现 0.85 容器关闭按钮下沿被裁切，以及 1.25 小窗口完整格挡提示接近/覆盖生命区；分别提高容器最小高度、在空间不足时使用短格挡动作名称。
- 两处修正后 Debug/Release 客户端构建通过；当前 r3 可执行文件为 `80fc7208b7054cced2d8878d2d85a6ba5e3b1d1244887f04e7bc42c86e2ce751`。`round2-candidate-consistency-r3.json` 确认 321 源文件与主工作区一致，相对 r2 仅 `OgreUserInterface.cpp` 变化。性能矩阵使用 1280×720、字号 1.0、关闭容器，不触发这两处小窗口路径；性能证据仍保留原 r2 身份，不能改标成 r3 实测。
- 解锁后桌面恢复 Retina 2×，首次补验 PNG 实际 1800×1200，而旧采集工具将 900×600 窗口点误当成像素，故 `round2-compact-layout-recheck-r1/` 如实保留 FAIL。采集工具增加显式像素比后，r2 四项补采全部通过严格 1800×1200 尺寸检查（工具提交 `ac1e941`），旧失败不覆盖。逐图发现英文 `Guard` 仍被拆词，已继续修正提示栏的键帽/间隔/面板边距宽度计算；该 r2 英文图保留为视觉 FAIL。
- Reduced 首次工具序列 `round2-hand-motion-reduced-r1/` 中有一段看似停住，不能判定连续动作通过。相同候选重采 `round2-hand-motion-reduced-r2/` 60 帧，3.7/4.6 秒实际剑刃姿态不同；灰色剑刃像素方向代理跨度约 12.5°。重采不解释首次现象，正常 Full/Reduced/Off 连续开采仍待验证。Full/Off r2 的静态/运动差别已实际检查，诊断不替代正常制作与装备。

### 小窗口修正最终补验

新增键帽宽度修正后，Debug/Release 构建通过：`round2-hint-width-release-r2.log`、`round2-hint-width-debug-r1.log`。Release r4 SHA-256 为 `4c284054803c1895f8f7d8c58e4e4cc68de4393f5b33d547c2c08bc942a25f05`，`round2-candidate-consistency-r4.json` 核对 321 个源码文件与主工作区相同，相对性能 r2 仍只有 UI 文件变化。r4 未重跑 72 次性能；保留 r2 性能身份与 UI 局部变更边界。

`round2-compact-layout-recheck-r3/` 完成 4/4 采集并逐图复核：中英文 1.25 HUD 的三个提示行清晰、英文 Guard 不拆词、提示栏与生命区分离；中英文 0.85 容器的关闭按钮完整可见。均为 900×600 窗口点、1800×1200 实际像素；原图哈希和判断在 `visual-review.json`。这是静态局部布局 PASS，不代替制作/容器交互或滚动验收。

CUA 主会话同样无法连接正在运行的 r3。停止该进程以刷新包的请求被自动审批拒绝，原因为可能丢失未保存状态；没有终止或覆盖它。改用 `round2-compact-layout-recheck-r3/Diagnostic.app`（bundle `local.hellomine3d.visual-layout-check`）生成和验证 r4，运行使用单独诊断存档，最后恢复正常配置。原工作包、进程和已有存档保持。此临时隔离包是针对无法安全原位刷新的替代检查，不宣称原工作窗口已经运行 r4。

补审 `round2-landscape-comparison-r1/` 草甸和山脚四张 10 秒原图：各对 seed、terrain v12、最终位置/朝向/tick 一致；可见工具体积、地图区域文字和云层变化，地形轮廓保持。草甸前景近身敌人遮挡保留，不作正常战斗验收；记录见目录内 `visual-review.json`。

### 水面顺岸漂移工程批次

在 r4 基础上复用实际驻留岸线，将微风投影到岸线切向并按水覆盖衰减，补充有方向的水面细纹。
使用水材质闲置 UV0，不增加顶点 stride、查询、区块加载或存档字段；双相位细纹循环连续，亮度调制最多 3.5%。
这表达风驱表面运动，E5 等高水体仍没有水文下泄模拟；见更新后的[水面合同](../contracts/visual-water-depth-v1.md)。

- Apple clang / x86_64 客户端 Debug/Release 构建通过，最新日志 `round2-water-drift-client-{debug,release}-r2.log`。
- 水深聚焦两配置各 **18/18 PASS**，完整 WorldRuntime 各 **1190/1190 PASS**；分别见 `round2-water-drift-focus-*-r1.log`、`round2-water-drift-world-full-*-r1.log`。
- 资源接口两配置各 **112/112 PASS**，包括缺失水面漂移顶点/片元声明的负例：`round2-water-drift-resources-*-r4.log`。各配置使用独立工作目录和显式 `HELLOMINE3D_ROOT`。
- Apple M1 Pro 生产 GLSL：210 对边界位置/法线零偏差，漂移运动、相位连续、关闭回退通过，见 `water-drift-gpu-r1.log`。
- 专用 Diagnostic.app 经独立验收者保存并正常退出后原位刷新，111 个托管文件校验通过；321 个第一方源码与主工作区一致。r5 Release SHA-256 为 `9cf3726c4c1356efce77341be90bfde72e8f48d4738b60361d12b580c1d2f02b`，见 `round2-water-drift-package-r5.log`、`round2-water-drift-prepackage-r5.json`。原 r3 进程未操作。
- `round2-water-drift-window-r1/` 的 Medium 与大气关闭两项窗口冒烟成功，5/10 秒四张原图逐图检查通过：水面正常着色、浅水沙底及深水层次可见，无缺失材质或明显破面。窗口 1280×720 点、实际 2560×1440 像素。原图哈希及判断见 `visual-review.json`；静态图不证明漂移动态品质或性能。检查后恢复验收者保存的正常设置。

保留资源执行失败：r1 误用了重链接前的旧测试程序且两配置共享 fixture 目录，Release 因文件竞争异常退出；
r2 分离目录后遗漏资源根，均出现贴图读取失败；r3 补齐资源根后 110/110 通过，再增加两项漂移接口负例得到 r4 的 112/112。
这些日志均保留，不把旧程序的 108 项检查当作当前证据。刷新前只读 `ps` 在普通沙箱被拒，脚本仍继续执行了打包；
此前已有独立验收者正常退出的记录，随后获准的只读核查再次确认专用诊断包无运行进程，未停止任何游戏进程。

本次按用户要求提交工程小批次。**新水面完整动态窗口观察及三轮静态/流送性能仍待执行**；旧 r2 的 72 次结果保持原版本身份，
不作为 r5 新 shader 的性能结论。整体 Goal 保持进行中。

### 正常输入补验 r2

独立验收者已通过 CUA 连接专用 Diagnostic.app 的 r4 包（SHA-256 `4c284054803c1895f8f7d8c58e4e4cc68de4393f5b33d547c2c08bc942a25f05`），
完成菜单、设置及有限正常玩法验证；旧 `normal-input-round2-r1/` 的原包连接失败继续保留。
本次原始九张 PNG、窗口身份/哈希及操作记录位于 `normal-input-round2-r2/`，结论见 `acceptance-record.md`、索引见 `capture-index.tsv`。

| 项目 | 结果与边界 |
| --- | --- |
| 建档、暂停、制作面板 | PASS：正常创建并进入 A/B 两世界，Escape/E 及关闭按钮有效；没有取得材料或实际制作物品。 |
| 紧凑界面 | PASS：900×600 英文 1.25 HUD、制作与设置滚动/固定按钮；中文 0.85、英文 1.0 制作。未取得箱子，容器交互不在本次 PASS 范围。 |
| 设置与地图 | PASS：Full/Reduced/Off 应用与保持、64/128/256 m 范围切换；林地 A → 沙岸 B 显示不同地形。没有已发现地标，不证明标记隔离。 |
| 保存与重开 | PASS：A 显式保存退出、正常重启并载入，seed、旅程、空库存及最后观察的 6/20 生命恢复；最终 A/B 均保留并正常退出。 |
| 受伤反馈 | 有限 PASS：正常敌袭字幕、方向提示、红边和生命下降可见；没有完成攻击、格挡或死亡重生。 |
| 移动、采集、工具、容器、地标、连续动作 | BLOCKED：W、连续 30 次 W、L 后重试及鼠标拖动均无可见移动/转向；接口无文档化持续按键/按住鼠标能力。未用替代注入伪造正常输入。 |

独立上下文与严格 AI-06 隔离不同；本次仓库可读，隔离为 PARTIAL，不声明无上下文盲玩或完整可玩性。
未建立产品 FAIL。验收者将设置恢复为中文、1.0、Medium、Full、128 m、下次启动 1280×720，保存 B 返回菜单后正常退出，已释放窗口。
这些是 r4 的实际证据；之后 r5 只改水面表现，但仍保留各自版本身份，不改称 r5 正常玩法实测。

### 不抢占窗口的后台启动修复

用户要求启动客户端不得打断正在使用的窗口。前台性能 runner 在正常采集边界停止，
恢复候选和正常配置后退出（session 73123 已结束、exit 1 表示主动停止）。r5 完成 18/72 次，
森林 Off 常驻、Off 流送、Medium 常驻三组 P95/P99 中位数比值分别为
0.9968×/0.9934×、0.9977×/1.0216×、0.9355×/0.9451×，均通过；其余 54 次未运行，不能宣称完整性能通过。
原始记录与停止标记保留在 `round2-performance-r5/`。

修复 Cocoa 窗口层：此前 `hidden` 在 NSWindow 创建前执行，后续创建/尺寸更新仍无条件置前，
且 `noActivate` 没有被读取。现在隐藏窗口不显示，不激活应用；显式不激活的可见窗口置于后方。
同时补齐显式窗口路径缺少的 GL context profile 及 Retina/画质默认参数，保留调用者的显式选项。
采集工具默认隐藏，open 路径追加 `-g -j`；打包器从实际二进制记录能力标记，
缺少标记的旧包在启动前拒绝，不通过前台回退绕开用户约定。

| 验证 | 结果与证据 |
| --- | --- |
| macOS x86_64 Debug/Release | 原生窗口库及客户端构建 PASS；最终日志 `round2-background-*-engine-r2.log`、`round2-background-*-client-r3.log`。 |
| Cocoa 输入回归 | 两配置 `verify_cocoa_input.sh` PASS；`round2-background-*-input-r1.log`。非可见自动回归，不是正常 OS 输入验收。 |
| direct 隐藏采集 | PASS：361 次观测中 355 次识别到目标进程，前台激活 0、可见窗口 0；取得 5/10 秒两张 2560×1440 原图。 |
| open 隐藏采集 | PASS：358 次观测中 349 次识别到目标进程，前台激活 0、可见窗口 0；同样取得两张原图。 |
| 启动保护与观察器负例 | 旧 r5 包被拒且未创建输出目录；未启动目标进程的观察器返回 FAIL，避免把“没启动”误判为成功。 |
| 静态画面与恢复 | 四张原图逐图检查，水岸、HUD、地图正常渲染；正常配置恢复，321 个第一方源码和三个修改后的原生窗口源码与构建检出匹配。 |

上述采集/进程观察在 `round2-background-window-r3/`，只读观察器不发送输入、不改变焦点。
循环间休眠 20 ms，加上读取开销，不能排除采样间隔内的瞬时事件；已同时复核原生窗口的置前/激活保护。
画面记录见该目录 `visual-review.json`。能力标记证明包含此实现，不代替每个平台的行为验证。
最新 r6 Release SHA-256：`3f6631dcd0726b3780b1b7babe59baa8829f8cd43a1ee934165b3d0201711f51`，
包与原生源码身份见 `round2-background-consistency-r6-r2.json`，最终配置/哈希核对见 `round2-background-final-check-r1.json`。

失败不覆盖：r1 隐藏路径暴露缺少 contextProfile 导致的启动异常，修复后重建；
r2 已成功渲染，但 NSWorkspace 没有列出隐藏 direct 进程，观察器因目标未出现而 FAIL；
改为 libproc 精确匹配可执行路径后，r3 两路通过。r2/r3 使用相同最终生产二进制。
后续仅使用已验证的后台路径；旧性能基线需以相同窗口修复重建后另开后台对照，不能混用前台/后台口径。

### 手持动作计时精度修复与后台逐帧检查

在 r6 上的临时跟踪没有复现旧 Reduced 停摆：`held-pose-trace-r2/` 三轮各 60 帧，
分别记录 253/247/254 个开采活动帧、220/217/218 个不同挥动值，最大帧间隔 46/52/48 ms。
原始 Reduced r1 的计时值没有记录，**历史停摆根因仍未证实**，不能用新一轮正常结果覆盖它。

另以旧算法重现了确定的精度问题：过大的时间增量使单精度累加器丢失后续帧增量；
连续运行一周的数值量级也无法保留高帧率动画相位。现将纯表现时钟和地图刷新期限改为 double，
拒绝非有限/非正增量，单次最多推进 0.25 秒；长停顿后继续动画，不补播停顿期间的动作。
只影响手持姿态、HUD 呼吸和地图采样节奏；玩法、开采/攻击判定、冷却、字幕及性能统计仍使用原路径。

- Debug/Release 客户端构建 PASS：`presentation-clock-client-*-r1.log`；最终世界测试目标构建 `presentation-clock-world-*-r2.log`。
- 最终计时聚焦各 5/5 PASS：`presentation-clock-focus-*-r2/`；物品表现各 8/8、地表地图各 10/10 PASS：`presentation-clock-focus-*-r1-{item_visual,surface_map}/`。本批未重复完整世界门禁。
- 独立数值回归 `presentation-clock-regression-r2/`：同一组检查旧算法 4 项失败、新算法 5/5 通过，包含非法增量、大时间跳变后恢复、地图期限、长会话精度及普通帧率一致性。它证明这类精度缺陷，不能反推历史 r1 必然遭遇时间跳变。
- 正式候选 Full/Reduced/Off 各 32 张原始 BMP，1280×720 点、2560×1440 像素，位于 `presentation-clock-motion-r1/`。剑刃像素方向代理在所选活动区间分别变化约 37.38°/15.57°/0°；Off 全部 32 帧剑刃掩模一致，Full/Reduced 选取的 20 帧均有变化。代理角度不是物理关节角度或人类手感评分。
- 已查看两帧 Full、两帧 Reduced 和两帧 Off 无损 PNG 副本，手持工具完整、侧面厚度可见、未盖住准星与快捷栏。三档后台启动观测 PASS：631 次采样、598 次识别目标，前台和可见窗口计数均为 0；无正常输入操作。

保留诊断失败：`held-pose-trace-r1/` 使用 PNG 编码产生最高约 3.63 秒帧间隔，只有极少活动帧，
不用于连续动作判断；同轮观察器传入相对路径未匹配到进程而 FAIL。r2 改用 BMP 和绝对目标路径后正常采集。
这些临时跟踪只修改隔离检出，已恢复；最终客户端不含跟踪输出。查看工具不能直接读取 BMP，故离线转换无损 PNG 查看，原件保留。

上一候选 r7 Release SHA-256 为 `d7a011aea7bc4f12d1a0bbe6dc534e99e25a7f474ba37aff7b1df74b4efab242`；
源码/资源/配置恢复及当前包身份见 `presentation-clock-final-check-r1.json`。画面采集后只补强测试断言并刷新来源收据，生产二进制不变。
当前证据是后台夹具动作验证，正常采集、制作、装备和连续手持操作仍待验；完整后台性能对照与水面动态继续推进。

### 水面连续帧与后台性能基线

r7 完成 `water-motion-r7-r1/`：同一水岸开启/关闭大气表现及水下视角，各 64 帧，
从 4 秒到 11.875 秒每 125 ms 一帧，共 192 张 2560×1440 原始 BMP。采集最大迟到分别为 8/7/6 ms。
开启时浅水/深水区域 64 帧均有变化，相邻帧平均绝对 RGB 变化约 0.160/0.181（0–255 色阶）；
关闭时同一区域全部像素一致，符合静止回退。已查看七张无损 PNG 副本，岸线、浅水沙底和水下界面正常，
所查帧未见明显破面。分析及范围见 `temporal-analysis-r1.json`、`visual-review-r1.json`。
这证明后台实际着色路径持续更新及关闭回退，不代替正常游泳/岸边编辑，也不声称已由人类认可流向与舒适度。
后台观测 1278 次，其中 1230 次识别到客户端，前台激活和可见窗口计数均为 0，正常配置已恢复。

后台性能基线保留原阶段 1 视觉范围。原收据的 315 个第一方文件中，只有当时未提交的 UI 无法直接从基准 commit 取得；
按本任务原始两份 UI 补丁恢复后，完整文件 SHA-256 精确匹配 `c1f76ce9e5e628b96b847c43bb5922e8a6d0c0f42fd022bee5ff8164bb10faa5`。
其余 314 个文件均与 `dc5c4bb` 匹配。只加入 `9dedc7e` 的三个原生隐藏窗口文件并重新编译，未替换视觉基线。
重建日志 `background-baseline-client-r2.log` PASS；首次准备因 Python 3.9 不支持 tar 的新参数而失败，尚未解包，失败记录保留；
重试显式检查可信 git archive 的每个路径后解包，未覆盖主源码。

- 后台基线 `background-stage1-reference-r1/`：Release x86_64 SHA-256 `2e84923395dad03ff8215d13ae848d580ab81f19b122978336067374952e2907`。
- 候选 `background-final-candidate-r7/`：仍为 `d7a011aea7bc4f12d1a0bbe6dc534e99e25a7f474ba37aff7b1df74b4efab242`。
- 基线全部视觉资源、315 文件收据与原冻结包一致，只更新可执行文件、构建身份与相应清单哈希；原生源码和依赖库哈希见 `background-performance-freeze-r1.json`。
- 基线隐藏启动 `background-baseline-check-r1/` PASS：两张 2560×1440 图已查看，390 次观测中 372 次识别进程，前台/可见窗口 0；随后恢复 r7 与正常配置。

旧采集工具会在性能计时期间编码两张 PNG。本轮将性能模式改为关闭 GPU 截图读回，
由原生 Cocoa 创建日志严格验证窗口点和 framebuffer 像素，视觉检查独立执行。
实际尺寸缺失/歧义、不匹配及意外开启读回均拒绝；`readback-free-capture-validation-r1.log` 七项正反例通过。
新后台性能结果独立记录，不能与含截图的历史前台结果混算，也不修改 1.10 门槛。
新旧各一次 30 秒采集预检通过：`background-performance-preflight-r1-{baseline,candidate}/`，
实际 2560×1440、无读回，分别 4297/4197 帧；观察器确认 2182 次采样中前台/可见窗口均为 0。
预检仅证明采集路径有效，不能替代三轮正式性能；正常包和配置已恢复。

### 后台正式对照与收口检查

`run_background_performance_r1.py` 已正常退出，完成 72/72 次、12/12 组，全部通过。
原始数据在本地 `background-performance-r1/`，独立审计 `audit-r2.json` 的证据完整性与性能均为 PASS。
矩阵为森林/水岸 × Off/Medium/High（High+post）× 静止/快速流送 × 三轮 × 两版本，
顺序 A/B、B/A、A/B，每次预热 5 秒、采集 30 秒；P95/P99 三轮中位数门槛仍为 1.10。
两版均隐藏、不激活、关闭截图读回，实际 2560×1440；后台结果不与旧前台或带 PNG 编码的记录混算。
快速流送通过四次跨区块传送施压，不构成连续行走或转向的视觉证据。

| 场景 / 设置 / 采样 | P95 比值 | P99 比值 |
| --- | ---: | ---: |
| forest-off-steady | 0.9561× | 0.9406× |
| forest-off-streaming | 0.9781× | 0.9259× |
| forest-medium-steady | 1.0158× | 1.0122× |
| forest-medium-streaming | 1.0263× | 1.0201× |
| forest-high-steady | 0.9784× | 0.9226× |
| forest-high-streaming | 1.0188× | 1.0255× |
| shore-off-steady | 1.0931× | 1.0513× |
| shore-off-streaming | 0.9306× | 0.9185× |
| shore-medium-steady | 0.9811× | 0.9696× |
| shore-medium-streaming | 0.9893× | 0.9747× |
| shore-high-steady | 0.9380× | 0.8698× |
| shore-high-streaming | 1.0220× | 1.0152× |

最大 P95/P99 比值分别为 1.0931×/1.0513×，均来自水岸 Off 静止组。
审计重算全部原始 CSV 分位数，核对交替顺序、机位与设置、原生像素尺寸、截图读回为零，
并核对两版各 111 个包内文件、315/323 个第一方源码、三个原生窗口源码和 17 个依赖归档。
候选 r7 与正常配置均已恢复；配置 SHA-256 为
`21a5024dcd2566664f1c5ed450a733434f333fa0ef7bd771850b9138b5b0f3ff`。

保留首次审计 `audit-r1.json` 的失败：审计器错误地要求最终世界 tick 只能为 6699/6700。
源码中采集计时器在首帧渲染结束才启动，此前已执行该帧模拟；因此首帧 tick 与边界会产生偏移。
修订审计改为检查完整 30 秒墙钟区间、逐帧世界时间与模拟 tick 精确对应、恒定白昼及保存结果一致；
72 次实际测量均为 600/601 tick。CSV 六位有效数字在 30 秒附近保留最大 0.05 ms 舍入界限。
原审计程序及失败记录保留，没有剔除采样、重跑取优或改变性能门槛。

掉落物后台采集 `drop-motion-r7-r1/` 完成时记录为 `CAPTURED; VISUAL_REVIEW_PENDING`：
Full/Reduced/Off 与旧图集 Full 四组，共 128 张 2560×1440 BMP。随后审阅发现薄物品侧转消失，
该 r7 视觉失败保留；下文 r8 修复及同倍率对照是当前结论，不替代正常掉落/拾取及战斗交互。

| 必要收口项 | 当前证据与剩余动作 |
| --- | --- |
| 当前候选性能 | r11 森林/水岸 72 次、12 组及掉落物 24 次、4 组均审计 PASS，P95/P99 最大分别为 1.0712×/1.0555×、1.0095×/1.0289×。r12 仅改打开的合成面板布局，不将 r11 原始性能数据改标为 r12；面板打开时未另测性能。 |
| 行进中的场景可读性 | 已有森林、草甸、水岸、山脚及地点固定图，GPU 连续性已检查；仍需连续行进画面观察遮挡、闪烁与地图刷新。后台诊断可补视觉证据，不能替代正常移动。 |
| 水岸交互 | r7 开启/关闭/水下共 192 帧已检查；r10 陆地湿润过渡双配置世界各 1211 项及四组前后 16 张画面通过。正常编辑岸床、进出水面的刷新与可读性未验。 |
| 工具与掉落物交互 | r7 Full/Reduced/Off 共 96 帧已检查；r7 掉落物四组 128 帧审阅发现侧转轮廓缺陷，r8 已修复，并完成同倍率 192 帧前后对照及限定范围视觉检查。正常制作、装备、持续采集、掉落和拾取仍未完成。旧 Reduced 停摆原始计时缺失，根因未证实；当前精度回归与多帧结果分别记录。 |
| 导航、容器与敌人 | 三档范围、区域差异和静态姿态已有证据；正常使用地点后记忆标记、原地编辑、切世界标记清理、容器转移及攻击预备到接触的完整交互未验。 |
| 最终包与保存重开 | r12 干净候选包、说明、来源清单及四组原图对照位于 `delivery-r12/`，无窗口启动校验通过；当前正常玩法与保存重开待验。r4 历史正常菜单及保存重开证据保留。 |

正常输入项沿用用户当前不抢窗口的约定，保持待验；不发送后台伪输入，不把诊断姿态记成正常玩法。
上述清单收拢既有 Goal 的退出项，不新增玩法或扩大视觉范围。

### 掉落物侧转可读性修复（2026-09-18，r8）

审阅 `drop-motion-r7-r1/` 后确认：剑、锭和面包的挤出模型在整周旋转时会露出极窄侧面，
石剑几乎融入叶丛。原始 128 帧、`visual-review-r1.json` 的 FAIL_READABILITY 和像素分析保留。
这不是只根据源码推测的问题；同倍率复现时，标准/兼容档石剑 ROI 亮色像素最低分别仅 1/0 个。

修复在纯表现 `ItemVisualPose` 中实现。方块保持原有整周旋转；挤出图标朝向观察者，局部偏转
限制在 -4° 至 36°，保留真实侧壁，正面朝向投影至少 80%。Reduced 摆幅为 Full 的 35%，
Off 关闭随时间的摆动和悬浮，观察位置变化仍维持可读朝向。Actor、碰撞、拾取、库存、年龄和存档未改。
模型仍使用原网格与有界缓存，分类仅在创建模型时读取，不增加世界查询或材质/纹理资源。

- macOS x86_64 Debug/Release 客户端与 WorldRuntimeSmoke 构建通过；两配置 ITEM_VISUAL 各 13/13。
  新检查覆盖 300 秒生命周期、12 个方位、五种仰俯角和三个反馈档位共 432180 个姿态，
  检查正面投影、悬浮边界、Off 时间稳定及方块四分之一圈旋转。未重复无关的全量世界回归。
- 首次 `drop-motion-r8-r1/` 保留为 FAIL：屏幕当前变为 1×，实际 1280×720，旧脚本仍要求
  2560×1440；原生日志明确 backing scale 为 1.0。该轮正常退出并恢复配置，没有将其改标通过。
- 新对照 `drop-readability-comparison-r8-r2/` 对两版统一使用 1280×720 点/实际像素、terrain v12、
  seed 20260807、相同机位/时间/设置。r7 标准与兼容 Full 两组，r8 标准 Full/Reduced/Off 和
  兼容 Full 四组，每组 32 帧，合计 192 帧；逐帧哈希及尺寸均核对，10 张原图已实际查看。
- r8 标准/兼容 Full 石剑 ROI 亮色像素最低分别为 111/139；标准 Full 范围 111–146，
  Reduced 为 123–141。Off 的 32 个石剑轮廓掩码相同、均 130 像素。该灰色阈值只是本场景
  轮廓辅助指标，不是通用物品检测器；世界背景和光照仍更新，不声称整幅画面像素不变。
- 图中剑、锭、面包的主要轮廓持续可见，方块有分面与转动；新标准/兼容画面检查为
  `PASS_SCOPED`，只覆盖上述五件物品与固定诊断机位。正常掉落、拾取、连续转向和战斗仍待验。
- 六组观察器共 1593 次识别到客户端，前台/可见窗口采样均为 0；20 ms 采样不能排除更短瞬态。
  专用工作包及正常配置恢复成功，未发送正常输入，未启动前台窗口。

r8 Release SHA-256：`78dd417323a6a8e4bd03bba323056f88a78e252b4b952c788e95e817971fd1ec`。
包内 111 项及 324 个第一方源码身份匹配当前源码和构建检出；冻结副本为
`background-readable-candidate-r8/`。本批性能增量尚待验证；r7 的正式性能 PASS 不自动转移给 r8。
正常玩法、地景连续观察及最终整合交付仍按既有 Goal 继续。

### 无截图读回的物品诊断入口（r9）

r8 性能补验首轮 `drop-performance-r8-r1/` 在创建窗口前退出：物品展示入口只接受
`HELLO_RENDER_CAPTURE`，与性能采样禁用截图读回的设置冲突，实际帧数为 0。
失败原始日志和零帧记录保留；已恢复工作包与配置，不将其当作性能退化。

入口现与已有方块反馈诊断一致：明确启用画面采集或 `RuntimePerformanceCapture` 之一即可使用；
合法姿态限制保留。未启用诊断、或姿态非法时仍拒绝，不影响正常玩法路径。
Debug/Release 客户端构建通过，四个启动负例均在创建窗口前按预期退出；Release 新旧两版
已完成开启性能、关闭画面采集的正例，原生日志、CSV 和零截图输出均符合预期。
记录为 `actor-capture-entry-validation-r9.json`；此 PASS 只针对入口，不代表三轮性能已完成。

r9 候选二进制为 `c7182b4fc56ec27f2ab76fe053d7312103cb138a4925ac22f678a6a95ee33530`，
视觉源码与 r8 相同，仅增加上述入口修复。对照基线为
`6e0a6c89e87695cd2a95b2db0fd4e446dca107209efaeecfb80694a901063cf7`，第一方源码对应
`3a73058` 的 r7 视觉加同一份入口修复；对照包与 r7 的源码清单差异严格限制为 `OgreBootstrap.cpp`。
324/323 个源码及共同原生窗口/依赖身份分开留存于 `drop-performance-packages-r9.json` 和冻结记录。

`drop-performance-r9-r1/` 已采集 r7 → r8 掉落物变化的 24 次专项对照：固定五个掉落物和四个敌人姿态，
Off/High+post × 静止/快速流送 × 三轮 × 两版，共 24 次、四组；同为实际 1280×720、隐藏、静音、
不读回截图。三轮顺序 A/B、B/A、A/B，预热 5 秒后采集 30 秒，P95/P99 门槛仍为 1.10。
这是掉落物变化的增量检查，不替代最终整体性能或正常玩法；快速流送仍是四次跨区块传送。
独立审计保留 `audit-r1.json` 的首个时长失败；`audit-r2.json` 完成全部数据和身份核对，
结果为 `evidence_integrity=PASS`、`timing_protocol=FAIL`、`performance_status=NOT_CLAIMED_PROTOCOL_FAIL`。
虽然四组原始 P95/P99 中位数比值均不超过 1.10，但 High 静态 r2 基线的最后一帧渲染约 25.279 秒，
实测结束于 48.032 秒、累计 456 tick；High 静态 r3 候选最后一帧渲染约 925.423 秒，
结束于 926.865 秒、累计 29 tick。两者不满足等时长模拟协议，不能用百分位汇总覆盖停顿。
Off 静态 r3 候选还在第 2900 帧记录 `dt_ms=1.84467e+19`（该帧耗时 5.7895 ms），
其计时异常另列待查；不把这些现象归因于掉落物或操作系统，根因尚无证据。
所有 24 次原件保留，不删异常、不调整门槛、不以挑选重采取得 PASS。

原 exec handle 16079 已失效，工具没有返回其最终退出码；进程核查确认 runner 与专用客户端均已退出，
因此退出码记 `UNAVAILABLE`。审计独立核对全部 CSV、固定协议、324/323 份源码、各 111 项包文件、
原生窗口与依赖身份；确认专用包已恢复 r9，正常配置 SHA-256 为
`21a5024dcd2566664f1c5ed450a733434f333fa0ef7bd771850b9138b5b0f3ff`。下一步定位长停顿与 delta 异常，
之后补齐受影响的完整三轮对照；原 r7 完整矩阵保持独立，不把本轮失败改记为完成。

### 陆地岸面湿润过渡（r10）

补齐此前只在水面上表现岸线、陆地没有湿润变化的缺项。实际不透明方块网格按相邻驻留水的覆盖，
对角点派生明暗施加最多 18% 衰减，沿一格岸面过渡回原材质；不新增几何或资源，不改变存储光照、
水体、碰撞及存档。含水标志在原快照复制时计算，17³ 字节缓存限制重复读取；无水快照跳过，
greedy 合并沿用最终明暗重建检查。低档和关闭大气表现时仍保留静态岸面过渡。

macOS Apple clang / gmake / x86_64 的 Debug、Release 客户端及 WorldRuntimeSmoke 构建通过，
水深/岸面定向两配置各 **29/29**、完整世界回归各 **1211/1211** 通过。新增 11 项实际网格检查覆盖
干燥字节一致、真实水邻接、渐变范围、横向/竖向接缝、AO 关闭、光照/驻留不变、移除水恢复、旧快照独立和索引有效性。
shader、贴图及 17 份依赖库未变；资源和 GPU 旧证据仅复用于未改内容，新网格实际路径由本轮客户端画面覆盖。

`shore-r10/captures-r2/` 以同 seed 20260807 / terrain v12、同机位、同时间、实际 1280×720 采集
改动前后白天 High、低档大气回退、夜间和兼容材质四组，共 16 张原图。已查看八张 5000 ms 原图，
沙岸呈克制的暗色带、内陆纹理保留，未见新增接缝或缺材质。固定岸面区域 `[655,430,750,465]` 的 RGB
均值在白天 High 从 199.03 降为 177.25，夜间从 84.91 降为 76.07；内陆区域均值变化小于 0.003，
这些仅是该机位的描述性数据，不是通用审美判定。首轮 `captures-r1/` 已正常采得基线两图，
随后核查脚本误读性能专用字段 `framebuffer_size_pixels` 而失败；修正为图片的实际尺寸字段后重做并保留原件。

身份和恢复核对保存在 `shore-r10/verification-r1.json`，画面记录为 `visual-review-r1.json`。
候选 Release 二进制 SHA-256 为 `c99f2709eb7394daae0e03e1e65108e41e164b1f14ec5c4c76c12c7eb8e18d6f`，
324 份源码与构建检出逐字节相符；包冻结于 `shore-r10/frozen-candidate/`，专用工作包与正常配置已恢复。
本轮仅声明工程和固定机位视觉范围通过；岸面性能、正常输入改岸/游泳及最终整合仍待完成。

r9 的巨大 delta 追查发现当前 Ogre POSIX Timer 使用可回拨的 `gettimeofday`，Root 对无符号毫秒值相减；
回拨会溢出为巨大帧增量。这是下一批应验证修复的明确代码风险，尚未证明它就是本次主机异常的触发原因，
也不能由此解释 25/925 秒的真实渲染停顿。下一步独立复现计时边界，修复后补齐相应性能验证。

### POSIX 经过时间单调性修复（r11）

实际 Ogre Timer 源码的隔离故障注入已证实缺陷：墙钟回拨 1 ms 后，毫秒值从 20 退到 19，
Root 使用的无符号差值变为 `18446744073709551615`。它可以解释巨大 delta 的形成机制，
但没有本次主机墙钟跳变记录，因此不宣称 r9 异常触发原因或 25/925 秒真实渲染停顿已经确定。

`OgrePOSIXTimerImp.h/.cpp` 改用 `std::chrono::steady_clock` 保存起点与计算经过时间，
不改变单位、reset、CPU 计时、fixed tick、存档和墙钟时间戳。测试仅在单独编译的 Timer 对象中把
`gettimeofday` 重定向到假时钟，不调整操作系统时间或注入运行中的游戏。
新入口 `bash scripts/verify_posix_timer.sh Debug|Release` 包含回拨、跨起点、向前跳一小时、
墙钟停住但真实经过时间继续、10,000 次读取、实例 reset、单位和 CPU API 检查。
旧源码两配置各 8 项失败，修复后两配置各 **10/10 PASS**；失败日志完整保存在 `timer-r11/before-*`。

使用顶层 gmake 的 `HelloMine3D` 依赖图重建 Debug/Release Ogre 核心与受影响客户端，两个配置均通过。
324 份第一方源码与 r10 相同，r10 世界回归覆盖未改内容，不重复用无 Ogre 的 WorldRuntime 证明计时器。
Release 17 份依赖库只有 `libogre3d.a` 改变；原生窗口三份源码保持不变。

`timer-r11/runtime-preflight-r1.json` 的真实隐藏客户端各采集 30 秒（预热 5 秒、无截图读回）：
Debug 1312 帧 / 600 tick、最大 delta 53 ms；Release 3734 帧 / 601 tick、最大 delta 13 ms，
两次均未出现巨大增量，结束时长分别 30014.6/30004.5 ms。20 ms 间隔的进程/窗口观察累计识别
2138 个客户端样本，前台与可见窗口样本均为 0；不排除短于观察间隔的瞬态。
这两次仅为计时集成检查，不宣称已完成三轮性能对照或正常玩法。

独立文件核对见 `timer-r11/verification-r1.json`，新 Release SHA-256 为
`c006bece67c99b9ca9aafb0cc87015dc9a150f6c01da987843ad30229b5984c1`，冻结包为
`timer-r11/frozen-release/`。专用工作包现为 r11，正常配置恢复且 SHA-256 未变。
下一步继续定位真实渲染停顿，并以同一单调时钟修复重新构建对照双方，再补掉落物、岸面及最终性能范围。
macOS Apple clang/gmake x86_64 为实际平台；其他 POSIX 平台未运行，Windows Timer 未修改。

后续只读查询 `pmset -g log` 找到两条异常的外部时间证据：01:10:49–01:11:45 的基线采集中，
系统在 01:11:18 进入 Maintenance Sleep，01:11:45 DarkWake，记录睡眠 27 秒；
01:12:23–01:27:57 的候选采集中，系统在 01:12:30 入睡、01:27:57 DarkWake，记录睡眠 927 秒。
两条最长帧分别约 25/925 秒，与睡眠区间相吻合，现分类为 **受系统睡眠中断的性能样本**。
原始日志摘录和关联记录见 `timer-r11/r9-power-transition-query.txt`、`r9-sleep-correlation-r1.json`。
日志只有秒级时间，不能细分 GPU 内部阶段；原记录仍为时长协议 FAIL，不补写 PASS。
此证据不解释此前独立的 Reduced 动画现象，也不证明 r9 巨大 delta 当时确由墙钟回拨触发。
后续矩阵在采样进程存活期间使用系统提供的临时 `caffeinate -i -s` 断言（`-s` 仅 AC 有效），
结束即释放；不使用唤醒显示器或声明用户活动的选项，不修改永久电源设置。保持同一三轮完整协议和 1.10 门槛。


### 同计时器的完整后台对照（r11，PASS）

冻结候选仍为 `timer-r11/frozen-release/`，可执行文件 SHA-256 见上一节。
完整基线 `background-stage1-reference-r11/` 的 315 份第一方源码和运行资源与旧
`background-stage1-reference-r1/` 相同，只以双方共用的 5 份原生窗口/计时源码和 17 份 Release 库重建；
基线 SHA-256 为 `d6e657909dc286cc59402f7cd8c1ab8c8a30a972b8d7554d5c67e3d55c24c232`。
基线包的 `source_commit` 来自所在外层工作区，实际组成由源码清单证明，不能说成从该外层提交构建了全部当前玩法。
首次打包缺少 `bin/Mine.cfg` 的失败保留；补齐旧基线清单内三份配置后重新打包成功，未修改旧冻结包。

`run_background_performance_r11_r1.py` 已完成森林/水岸 × Off/Medium/High × 常驻/快速流送 ×
三轮 × 两版本，共 72 次、12 组；顺序 A/B、B/A、A/B，每次预热 5 秒、测量 30 秒。
当前实际窗口与 framebuffer 均为 1280×720、倍率 1，隐藏且不激活、静音、关闭截图读回，门槛仍为 1.10。
每次检查 AC 电源、计时区间与模拟 tick；绑定进程且限时一小时的 `caffeinate -i -s` 临时阻止系统睡眠。
快速流送沿用四次跨区块跳转，是流送压力场景，不能用来证明连续走动或转头观感。

原 exec session `51407` 已返回退出码 0。`background-performance-r11-r1/terminal.json` 与 runner 日志
记录 72 次完成、候选和正常配置恢复、临时断言释放；独立审计 `audit-r1.json` 从 359,472 行原始帧数据
重算分位数并核对两包各 111 个文件、315/324 份源码、原生依赖、计时和电源记录，全部 PASS。
各次实际结束于 30000.1–30008.0 ms，模拟 600/601 tick，无截图读回，也没有系统睡眠覆盖采样区间。
正常配置 SHA-256 恢复为 `21a5024dcd2566664f1c5ed450a733434f333fa0ef7bd771850b9138b5b0f3ff`。

| 场景/画质/运动 | P95 三轮中位数比值 | P99 三轮中位数比值 | 结果 |
| --- | --- | --- | --- |
| forest-off-steady | 1.0069× | 1.0302× | PASS |
| forest-off-streaming | 1.0329× | 1.0554× | PASS |
| forest-medium-steady | 1.0176× | 1.0153× | PASS |
| forest-medium-streaming | 1.0411× | 0.9798× | PASS |
| forest-high-steady | 1.0183× | 1.0365× | PASS |
| forest-high-streaming | 1.0135× | 1.0096× | PASS |
| shore-off-steady | 0.9579× | 0.9796× | PASS |
| shore-off-streaming | 0.9918× | 1.0086× | PASS |
| shore-medium-steady | 1.0015× | 1.0486× | PASS |
| shore-medium-streaming | 1.0074× | 1.0102× | PASS |
| shore-high-steady | 1.0711× | 1.0506× | PASS |
| shore-high-streaming | 1.0082× | 0.9796× | PASS |

最大 P95/P99 分别为 1.071147/1.055411（上取四位为 1.0712/1.0555），门槛保持 1.10。
这是当前 r11 的隐藏诊断对照；基线已包含阶段 1 与 terrain v12，不解释为相对最初用户截图的全部开销，
也不替代连续走动、正常玩法、历史 E2、前台或 2× 分辨率验收。

### 掉落物同计时器专项（r11，PASS）

完整矩阵结束并审计通过后，`prepare_drop_performance_r11.py` 在独立 `drop-baseline-checkout-r11/`
完成构建和打包。基线与当前候选共用岸线、五份原生修复与 17 份依赖，仅回退 r8 的五个物品姿态相关文件，
运行资源一致；323/324 份源码及明确差异见 `drop-performance-freeze-r11-r1.json`。
基线包 `drop-performance-reference-r11/` 的可执行文件 SHA-256 为
`d52c2a608f027fdcb2424f5997f89f75e7f52b066db94560d729e665c1261468`，候选继续使用同一 r11 冻结包。

`run_drop_performance_r11_r1.py` 已完成 Off/High × 常驻/快速流送 × 三轮 × 两版本共 24 次，
五件复制掉落物和四个待机敌人只用于表现诊断；仍使用隐藏窗口、静音、无读回与临时防系统睡眠。
原 session `89786` 已观察到退出码 0；`drop-performance-r11-r1/terminal.json` 记录候选与正常配置恢复、断言释放。
`audit-r1.json` 独立核对 119,625 行帧数据、完整运行时长、包/源码/依赖身份与电源历史，完整性及性能均 PASS。
四组 P95/P99 中位数比值如下（向上保留四位，门槛均为 1.10）：

| 场景 | P95 比值 | P99 比值 |
| --- | --- | --- |
| Off 常驻 | 0.9873 | 0.9718 |
| Off 快速流送 | 1.0095 | 1.0289 |
| High 常驻 | 0.9915 | 0.9869 |
| High 快速流送 | 1.0062 | 1.0179 |

快速流送仍是四次跨区块跳转，不证明连续行走或正常掉落/拾取。r9 失败记录保持原样。
此前两个入口拒绝提前执行的实际负例保存在 `drop-r11-preparation-checks-r1.json`，不冒充运行验证。

### 合成网格与材料区布局修复（r12）

交付对照 `handoff-captures-r11-r1/` 已采集森林日间、水岸、森林夜间及紧凑合成界面各两版本、两帧，共 16 张图。
已查看八张 5 秒原图：世界表现对照成立，但 900×600、1.25 字号合成界面只露出九宫格前两行。
这些基线已有阶段 1 HUD 和 terrain v12，不能当成最初用户截图的同条件前态。

修复仅位于 `OgreUserInterface::Impl::drawCrafting()`：左侧配方/材料独立滚动，右侧保持完整网格和清空按钮，
底部保留结果与合成/关闭按钮。材料按宽度排成两或三列，格子按剩余宽高缩放；制作、库存、配方和输入处理语义未改。
Debug/Release 客户端构建通过。`ui-r12/captures-r1/` 十组隐藏诊断采集完成，共 20 张 PNG；
实际查看十张 5 秒原图，覆盖中英文、900×600 三档字号、640×480 大字号与 1280×720 默认字号。
九宫格、清空及底部按钮全部可见；最小窗口材料列表可滚动，英文长材料名使用既有省略显示。
未通过正常输入执行滚动、装载配方与制作，不把静态布局检查当作玩法通过。

`ui-r12/verification-r1.json` 核对 324 份第一方源码与构建检出一致，只有上述函数变化；五份原生源文件、
17 份 Release 依赖及运行资源与 r11 一致。两份包各 111 项清单核对通过，正常配置恢复为 `21a5024d…`。
Release SHA-256：`a57269ee3768f49ba6264a2a5abb0d96826ab7f24d0b76c725ef32a8655efbcb`，冻结于 `ui-r12/frozen-release/`。
原采集 handle `9504` 恢复时已不可用；自身终止记录明确退出 0、配置恢复及断言释放，不额外声称观察到原工具退出码。
此次只有面板布局变化，关闭面板的世界渲染路径保持一致，不重复完整世界性能矩阵；r11 两项性能证据仍保留 r11 身份。

### 当前候选交付（r12，正常玩法待验）

合成布局修复已本地提交 `1feb4d5`，随后从干净工作区打包：
`build/visual-upgrade-20260917/delivery-r12/HelloMine3D-Visual-Candidate.app`，
bundle id 为 `local.hellomine3d.visual-candidate`，macOS / x86_64 / Release、本地未签名。
包内 111 项分发清单、324 份源码及正常配置均核对通过；二进制与上述 r12 原图对应，资源、源码清单与冻结包相同。
来源提交为 `1feb4d5`、tracked diff 为空，没有用户世界或诊断存档。

以 `HELLOMINE3D_VALIDATE_ONLY=1` 在隔离数据目录运行，原 session `36933` 观察到退出 0，未创建渲染窗口。
这是资源和启动自检，不冒充正常打开窗口、创建世界或保存重开的验收。
验证日志保存在包外 `delivery-r12/validate-only-r1/`；正常配置不带诊断参数，未自动启动前台客户端。

同目录 `README.md` 是交付说明；`gallery/index.html` 提供森林、水岸、夜间和合成界面的四组对照。
八张 PNG 与原件逐字节一致，同组尺寸相同，来源和二进制身份在 `gallery/evidence.json`；本地链接检查通过，未另做浏览器渲染验收。
前三组保持 r11 的世界表现证据，第四组直接比较 r11 与 r12 合成布局，未混称为同一旧版本。
完整交付核对见 `delivery-verification.json`。当前候选具备可运行文件与明确证据，整体 Goal 仍等待连续场景及正常玩法退出项。

### 当前恢复入口

- 水面工程批次已本地提交 `864c86e`；前六个本地提交保留，均未推送。每个可独立验证的小批次及时提交，整合性能/玩法待办继续单独跟踪。
- 原工作路径 `HelloMine3D-Visual-Upgrade.app`、bundle id `local.hellomine3d.visual-upgrade` 保留 r3。此前自动审批拒绝终止其进程，理由为可能丢失未保存状态；本轮未停止、刷新或覆盖该运行包。
- 专用工作路径 `round2-compact-layout-recheck-r3/Diagnostic.app`，bundle id `local.hellomine3d.visual-layout-check`，已恢复为 r12 与正常配置。A/B 正常存档保持原状。
- 当前无活动采样 runner；r11 两项性能审计、r12 布局采集与候选包交付核对均已结束，不重新启动旧 handle。剩余是连续行进与正常玩法验收；沿用不抢窗口约定，不能以诊断图替代。机器可读检查点为本地 `goal-recovery-r7.json`。历史 Reduced 停摆仍未决，旧前台和 2× 数据不改标为当前后台结果。
- 正常菜单、设置、保存/重开已有 r4 证据，持续移动/采集等输入仍缺可用路径；按用户约定不启动前台/CUA，继续不依赖它的工作。首次 Reduced 静止现象尚未解释，不以重采掩盖。Goal 已恢复，尚未完成。
- 用户要求沿用中文提交形式，最近 12 条英文提交已仅重写说明和相应父提交 ID；各提交文件树、顺序、作者与日期保持一致。旧证据 SHA 保留，可通过 `refs/rewrites/visual-message-style-20260917` 追溯；新旧映射在本地 `commit-message-style-rewrite-r1.json`。


## 实际改动

| 范围 | 已落地的行为 |
| --- | --- |
| 字体 | 由原 Noto 可变字体生成 OFL 静态 500 字重 HelloMine UI，保留全部 cmap 与原字体/许可证。实际客户端改用该文件，正文基准从 18 调至 20，告别默认 100 极细字重。 |
| 界面 | 深炭蓝面板、暖白文字、沙金选择色、青绿生命与珊瑚红危险色；任务标题放大并突出，数量与进度并排。 |
| 快捷栏 | 独立深色五槽、数字角标、细亮边与选中底标；保留图标、堆叠数量及耐久。生命改为十段显示并保留精确数值；手持名称有独立底色与固定行高。 |
| 操作提示/制作 | 操作键帽与动作文字分离；制作与箱子使用真实物品图标、名称、数量及选中态，完整名称可通过悬停阅读；仍使用 Button，保留键盘焦点和激活。移除制作预览中的内部 recipe id。 |
| 小地图 | 65×65、两米采样的真实驻留地表，显示树冠、水岸、沙土石和建筑；跳过细草花，邻列高度产生坡面明暗。常态不画区块网格，保留北向、朝向和 32 m 比例尺。原地改块也刷新，未知区暗色。 |
| 世界表现 | 草地细节反差和黄绿色调收敛，连续世界坐标产生较大色块层次。标准图集、纹理数组、阴影接收路径共用相同着色逻辑；关闭大气表现时回退，石材不受该调整影响。没有改动方块轮廓、植被根部、风摆或存档。 |
| 交付工具 | macOS 打包支持显式独立 binary；新增源码文件哈希收据，避免 git diff 不包含新文件造成身份遗漏。 |

## 可复核证据

所有相对产物路径以下列本地目录为根：
`/Users/lizi/Desktop/Workspace/HelloMine3D/build/visual-upgrade-20260917/`。
截图和字体样张属于本地附件，不提交 Git。

| 检查 | 结果 | 证据及范围 |
| --- | --- | --- |
| 隔离源码 | PASS | `checkout/` 基于 dcfd4d0，只纳入本轮视觉改动，地形维持 v10；未纳入并行 E5 的未完成生成器。 |
| 客户端 gmake / Apple clang / x86_64 | Debug、Release 均编译链接 PASS | `isolated-client-{debug,release}-final.log`。第三方静态库复用既有对应配置，第一方对象与可执行文件独立生成。 |
| 完整 WorldRuntime | Debug **1109/1109**、Release **1109/1109 PASS** | `world-full-debug.log`、`world-full-release.log`，包含现有输入、设置、存档、世界/地形等回归。不是正常输入玩法录像。 |
| 小地图聚焦 | **10/10 PASS** | `map-release-r2.log`；未知区不加载、负坐标、花草过滤、透明树冠、建筑覆盖、原地编辑、水、快照稳定、不写 dirty、256 列上限及卸载。完整两配置均再次覆盖。 |
| 字体构建 | PASS | 两次 `python3 tools/build_ui_font.py` 生成字节相同，SHA-256 `63ddd636cd3dc7c70b2a6ca1184d68544ff55e940424157237fe116efc69379a`；保留原始 cmap、静态字重 500、无 fvar。 |
| 资源包 | **105/105 PASS** | `resources-release.log`，资源数据检查。图集/纹理数组检查也通过，原图集和数组像素未修改。 |
| 分发资源 | **80 项 PASS** | `assets-final.log`。 |
| 花草 GPU 回归 | **10/10 PASS** | `flora-gpu-r2.log`，Apple M1 Pro 离屏 OpenGL，961 时间样本，根部固定、连续性、阴影运动一致。 |
| 地形/反馈 GPU | **20/20 PASS** | `ground-gpu.log`；实际生产 GLSL 与图集，验证草地改色、世界坐标变化、阴影关闭一致、石材不受影响及原有十阶段裂纹/遮罩/遮挡。 |
| 干净工作包 | **111 文件逐项哈希 PASS** | `package-final.log`；包内 `distribution-sha256.txt`、`build-identity.json`、314 个第一方源文件的 `source-tree-sha256.txt`。 |
| 升级后真实窗口/双语多字号/操作 | **PARTIAL；完整验收待续** | 排除锁屏后发现 manifest 排序导致启动失败，修复后 CUA 已观察主菜单、建档、世界 HUD/真实地图、制作、暂停。多字号、操作全场景与包窗口身份存证未完成；专用截图查询仍无匹配窗口。 |
| 同条件三轮窗口性能 | **NOT_RUN / 基线启动未完成** | `baseline-perf-off-r1/`、`baseline-perf-off-r1-r2/`、`baseline-hidden-r1/` 保留失败。没有新的 P95/P99 PASS，也不覆盖 E2 的 1.134× 失败。 |

汇总哈希见 `evidence-summary.json`。`font-specimen.png` 仅为同文本的真实字体字重样张；
`ground-gpu/*-ground-{before,after}.png` 仅为实际 shader 的离屏材质检查，不是整机位游戏画面。

## 可运行包身份

视觉批次自动证据对应的 Release 可执行文件 SHA-256 为
`789c0d52a8b03c614b2dd9b1866b9ee1f156929a68f999b3e2c94584dbde67de`，源码收据 SHA-256 为
`bdb7dad55575a6a1ef36d06dc09afa51908ee51c9cbeec6e3c7227426a552f90`。冻结副本为
`candidate-package-789c0d52/`，原版本副本为 `baseline-package/`；两者都没有被后续刷新覆盖。

复用客户端 `build/visual-upgrade-20260917/HelloMine3D-Visual-Upgrade.app` 随后在同一路径原位
合入 E5 提交 `db22168`，当时可执行文件 SHA-256 为
`d78fbb8bf417aa27ad81d9df7f9d61d3e3301d4ee354746cff4e7e67e9a816e5`。该身份只说明当前可运行
整合包，不追溯替换上面的视觉批次冻结证据。
该整合包的 111 个分发文件另经逐项哈希核验并冻结在 `integrated-package-d78fbb8b/`，
以便对比完成后恢复当前版本。视觉源码已由并行整合工作纳入本地提交 `fcb7e2d`；
此提交不代表窗口验收完成。

后续启动诊断暂将同一工作 `.app` 刷新为隔离 terrain v10 的 `789c0d52…` 二进制，
并纳入已排序的字体清单；见 `package-manifest-fix.log`。当前不得把此工作包称作最新 E5/E6
整合版本，整合冻结副本保留。最终仍须生成与当前源码对应的整合客户端。

隔离证据包由隔离检出的代码和资源生成，未覆盖其他任务的工作客户端或主工作区 bin。
这两个目录是供复核/恢复的包内容副本，不额外启动新应用。后续继续复用唯一视觉工作 `.app`。

## 失败记录与处理

- 初次沙箱窗口启动返回 LaunchServices -10827。通过正常的自动权限审核后可启动，但电脑随后锁屏，真实窗口采集超时，没有取得帧。
- CUA 确认 Mac 已锁定，自动解锁也不可用；未绕过系统锁屏，也未要求离开的用户在线处理。
- 隐藏客户端同样未取得帧；确认独立诊断包路径后结束超时进程，保留捕获记录。后续不反复重试同一无变化的锁屏条件。
- 离屏 GPU 首次在沙箱内无法取得 core pixel format；经正常自动审核使用图形上下文后，花草及地形/反馈检查均通过。这是图形 API 自动检查，不是绕过锁屏或模拟输入。
- 首版地图测试夹具直接将 Absent 切到 Resident，产生 7 项失败；按既有 Requested→Loading→Resident 生命周期建立夹具后 10 项通过，没有修改生产生命周期或放宽要求。
- 物品卡初次编译将 Material::ID 写成 int，类型检查报错；恢复强类型签名后两配置通过。
- 桌面恢复后直接执行工作包启动入口，日志 `launch-diagnosis-r1.log` 明确报 manifest 第 52 行不满足唯一且有序要求：新增 HelloMine UI 字体被排在 Noto 字体之后。修正实际清单，并给 Python 打包器增加有序、唯一及路径校验；生产 89 条清单通过，倒序和重复负例均被拒绝。此前资源测试和包哈希 PASS 没有证明可启动，保留原记录。
- 修复后 `launch-manifest-fixed.log` 进入 Ogre/GL；CUA 已取得正常世界 HUD，地图可辨树冠、空地、沙岸和水面，并发现任务进度数字换行问题。`round2-paused-before.json` 专用窗口采集仍未匹配，故 CUA 会话观察不冒充已完成的本地包身份截图。

## 恢复操作

1. 先核对本报告、主工作区与隔离检出；不要把 E5 生成器修改或其他任务的包混进此版本身份。
2. 图形会话仍锁定时，保留待验项，不重复启动窗口。会话可用后，通过 CUA 正常进入视觉工作包；正常菜单、建档、移动/观察、制作、保存重开与诊断分开记录。
3. 冻结新包副本已存在。先确认没有其他任务使用工作窗口，并核对当前整合包是否又有更新；如有更新先冻结其分发文件。性能对比将 `baseline-package/` 和 `candidate-package-789c0d52/` 的已验证分发文件分别恢复到同一工作 `.app`，采集完恢复当前整合包（本次冻结为 `integrated-package-d78fbb8b/`）；保留当前用户数据及全部失败目录，不把 v10 对比版本留作最新整合版本。
4. 使用 `tools/capture_visual_macos.py --reuse-app --save-template build/visual-upgrade-20260917/v10-template`；固定 seed 20260807、terrain v10、1280×720、时间 6000、HUD fixture。性能点 `(1152,150,64)`、旋转 `(0,0,0)`，Off/Off、Medium/Off、High/On 各三轮静态，加 High/On 三轮快速移动。每轮新目录，首次失败不覆盖；已有 `run_baseline.py` 是原计划参考，恢复时需给失败轮次新输出名。
5. 新旧版本还需相同林地/海岸/草甸/山麓画面对照；双语三字号、夜间/关闭回退、空满快捷栏、制作/箱子检查。发现截断、位置错误或地图性能退化继续修正。
6. 同夹具 P95/P99 三轮中位数均 ≤1.10 才关闭本轮性能；若未通过，报告真实退化并继续定位。当前不声明整体视觉升级完成，视觉源码已本地提交；没有执行推送或发布。

## 恢复区

- 起始 HEAD：7b2432d（E4 窗口结果）。后续已有 E5 基线 dcfd4d0、E5 生成器 db22168、视觉实现 fcb7e2d；隔离 v10 证据保持原身份，本任务不覆盖其他改动。
- 体验前客户端：build/ecology-e2-20260914/HelloMine3D-E2-Prototype.app，实际菜单显示新世界 terrain v10。当前视觉工作包改用上方独立固定路径，避免与 E5 争用。
- 正常输入已执行：主菜单→世界列表→新建 Visual Review 0917（seed 20260807）→进入→打开制作。未动用户已有世界。实际发现正文细弱、目标层级弱、制作以文字按钮为主、地图没有树冠。
- 字体根因：media/fonts/NotoSansSC-VF.ttf 的 wght 轴 min/default/max 为 100/100/900，现有 stb 字体入口未选字重。
- 下一步：修复任务进度行和状态区布局，核对运行进程与窗口身份，完成后续实机/性能；用户已要求按第二轮分析继续，并要求整理可直接使用的 [Goal 提示词](../current/visual-upgrade-goal-prompt-2026-09-17.md)。
- 自动续跑：已创建本任务 heartbeat「继续 HelloMine3D 视觉升级验收」（automation id `hellomine3d`），每 30 分钟检查并继续。锁屏无变化时保持安静，不反复启动；全部验收完成或用户取消后暂停。恢复时先核对自动任务状态，避免重复创建。
- 窗口由本任务操作。截图与录像保留本地 build/visual-upgrade-20260917，不提交 Git。
- 直接启动诊断 session 72178 / PID 77661 已通过独占本轮日志的文件句柄确认并结束；后续先核对是否还有其他任务的实例，不复用旧 PID，也不同时启动重复客户端。
- 权限状态：初期 CUA 正常，随后电脑锁屏；09:17 左右再次分析时应用清单已可读，锁屏条件有变化，但读取客户端仍超时。正常 open 返回 0，精确窗口检查仍无 PID/窗口（`review-round2-menu-r1.json`）；原因尚未定位，不能据此认定游戏已启动。没有待用户在线批准的问题，也没有自动审批拒绝后绕过限制。
- 用户追加要求分析后续提升方向，见[下一轮分析](visual-next-pass-analysis-2026-09-17.md)。此记录区分当前缺陷、E6 在建和候选项，没有重复改写并行 E6。
